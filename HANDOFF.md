# Передача работы · Pine-to-Pine (бывш. FLOPS, бывш. «Вуокса-2026 · сборный лист»)

Этот файл — точка входа для новой сессии. Сначала прочитай `README.md` целиком: там архитектура,
модель данных, формулы расчётов, права, Supabase, ограничения. Здесь — только состояние работ.

## 🔬 06.08.2026 — ДЕСЯТАЯ ПОРЦИЯ: финальный прогон Playwright'ом, два дефекта найдены и починены

Коммит `da34ee6`, **выложено и сверено по содержимому**: `/assets/index-DS6jJFI-.js`,
455 530 байт, в нём есть «pine-offline.html», «это не заготовка», «Пустая строка
не сохранилась», «Показать маршрут», «Что добавить». Под картой на боевом —
«Карта Google · сборка da34ee6a».

Снимок боевой строки до работы: **1 123 566 байт**, метка `2026-08-05T21:04:38.391`,
автор «Макс», люди 3 · сборы 96 · закупка 53 · маршрут 8. **На выходе те же
1 123 566 байт и та же метка** — боевая строка этой сессией не писалась ни разу.
Вся работа на `vuoksa2026-test` (залита копией боевой, после работы выхолощена
до 255 байт).

### ⛔ Дефект 1 (крупный): офлайн-копия скачивалась битой

«Забрать офлайн-копию» на боевом отдавала файл 1,53 МБ со ссылками на
`/assets/index-*.js`. Открытая двойным щелчком, она показывала **пустую страницу**
с одной полоской объяснения. Причина: заготовку клал в корень репозитория
`publish-v2.mjs`, а Cloudflare раздаёт **`app/dist`** — запрос попадал в правило
SPA, край отвечал **200** и отдавал `index.html`. Проверка `res.ok` проходила,
тост говорил «Копия у вас. Откроется без интернета». Урок **У-101**.

Починено: заготовку кладёт сама сборка (`npm run build` → `dist/pine-offline.html`,
скрипт `app/scripts/offline-into-dist.mjs`), имя латиницей, а `template()` перед
отдачей проверяет, что приехал самодостаточный файл, а не страница сайта.

**Замер после выкладки:** `/pine-offline.html` — 200, 1 278 017 байт, ссылок
на `/assets/` **0**. Копия, скачанная с боевого: **2 816 492 байта**, ссылок
на `/assets/` 0. Открыта файлом при выключенной сети: все шесть разделов,
**105 строк «Сборов», 61 «Закупки», 10 точек маршрута**, «Общий бюджет 47 149 ₽»,
полоска «Это офлайн-копия листа от 6 августа 2026», горизонтальной прокрутки нет
(390 = 390), ошибок в консоли нет.

### ⛔ Дефект 2 (крупный): новая покупка на телефоне исчезала

Замер: «Добавить покупку» → в базе 53 → 54, поле открылось на правку; вписали
название, Enter → в базе снова **53**, строки нет, тост говорит «Пустая строка
не сохранилась». На 1280 та же правка работала. Причина: в ленте стояло
`onEditEnd={() => onFreshEnd(p.i, !!p.n)}` — проп **того рендера**, где названия
ещё нет. Урок **У-100**.

Починено `app/src/components/buy/BuyStrip.tsx`: признак «успели ввести» —
ссылка `saved`, как давно сделано в матрице (`BuyRow`).

**Замер после правки (390, песочница, сверка с базой):** своей кнопкой раздела
53 → 54 → **54 после ввода**, «Тест own» в базе и на экране; общим «плюсом»
из «Сборов» 55 → 56 → **56**, «Тест plus» в базе. Соседний путь цел: пустая
строка, из которой ушли ничего не вписав, по-прежнему исчезает с тостом
«Пустая строка не сохранилась» (закупок было 56, стало 56). Удаление в ленте:
кнопка 44 × 44, «„Тест own“ удалено · Отменить», в базе 56 → 55.

### Дефект 3 (мелкий): «Скинулись поровну» показывалось как «Ски…»

В липкой колонке «Расчёта дороги» на 390 (176 px) две кнопки действий (90 px)
оставляли строке «Платит» **45 px** при нужных 112 — это У-81. Починено там же
(`RoadCalc.tsx`): строка переносится (`flex-wrap`), тексту дана наименьшая
ширина (`basis-40`). Замер: 390 — текст **159** px (было 65), действия уехали
под текст; 1280 — текст **217** px, действия рядом, вид не изменился.
Ужатых `truncate` на странице: было 2, стало **0**.

### Что прогнано и с какими числами

**Ступень 1:** `tsc` чист · `oxlint` без новых предупреждений (5 старых
`only-export-components`) · контрольные цифры **330 км · 21 385 / 26 005 /
47 390 / 11 848 ₽ · 2 канистры** сходятся · обе сборки прошли.

**Прогон Playwright, песочница-копия боевой** (390 × 844 и 1280 × 900,
светлая и тёмная, три ключа):

| | 390 светлая | 390 тёмная | 1280 светлая | 1280 тёмная |
|---|---|---|---|---|
| обрезанный текст | 0 | 0 | 0 | 0 |
| прокрутка страницы вбок | 390 = 390 | 390 = 390 | 1280 = 1280 | 1280 = 1280 |
| ошибки в консоли | нет | нет | нет | нет |

**Раскрытые подробности позиций** (по три строки в «Сборах», «Закупке»,
«Дороге»): обрезки 0, целей < 44 из наших органов 0, ошибок 0 — под Максом
на 390 и 1280, под Мишей на 390, под Костей на 1280.

**Цели касания.** Меньше 44 остаются только инлайновые правки текста внутри
строк расчёта: «кто платит» 167 × 20 (на 1280 — 225 × 20), «расход» 109,3 × 26,5,
«цена топлива» 67,4 × 26,5. Это ровно У-95, решение оставлено прежним: добор
высоты перенёс бы промах на соседа. Счётчики проверены заново — видимый кружок
28 × 28, зона нажатия `before:-inset-2` = **44 × 44**, `elementFromPoint` и
пробный клик Playwright попадают в сам орган. Пункты меню «⋯» — 216 × 44 все семь.

**Контраст** (переходы сняты по У-55, цвета сырые): светлая — название раздела
13,86 : 1, основной текст 5,66 : 1, подпись 5,33 : 1, мелочь 6,22 : 1; тёмная —
8,91 · 4,63 · 4,67 · 4,63. Порог 4,5 держат все. ⚠️ Проба напечатала 1,1 : 1
у имён «Макс», «Костя», «Миша» — это У-93: имя лежит на фотографии человека,
проба нашла кремовый фон карточки. Проверено снимком экрана — читается.

**Функции.** Excel: 121 282 байта, сигнатура PK, девять листов — и владельцу,
и участнику. Поиск: до запроса объяснение «Всего позиций: 214», после «спальн» —
20 находок группами, поле 52 px. «Мои поездки» открываются. Возврат копии
в онлайн проверен по-настоящему (в файле правилось поле и метка): «Правки
из файла вернулись: новых 0, изменённых 1, отметок 0», в базе поле изменилось.
Вход новичка: голый адрес и неверный ключ дают «Этот лист закрыт» с личной
ссылкой и входом по почте. Права участника: в меню Миши нет «Ссылок команды»
и «Вернуть копию в онлайн» (постулат 6).

**То, чего среда не давала проверить, проверено Playwright'ом:** кнопка
«Показать маршрут» в заглушке карты — 175,3 × 44, при выключенной сети
прокручивает страницу **y 0 → 17 037** (в офлайн-копии — 0 → 17 078), верх
«Дороги» встаёт на 56 px под липкой шапкой. Заглушка показывает число точек,
списка нет (`<li>` = 0). Возврат сети возвращает «Карта Google».

**Боевой домен после выкладки** (три ключа): обрезки 0 · прокрутки вбок нет
(390 = 390, 1280 = 1280) · ошибок консоли нет · «Карта Google · сборка da34ee6a»
у всех троих.

### Ответы заказчика, полученные в этой сессии

* **«Купить / не купить» отдельной колонкой — не нужно.** Ответ: «Оставить
  как есть». Галочка «Берём» остаётся, У-53 закрыт.
* **«Попросить отметить / изменить количество» — функция не нужна**, убрана
  из планов. Поля `Gear.q` и `Buy.qask` в документе не трогаем (постулат 4),
  но органа не будет.

### Осталось незакрытым

* `S.del` копится вечно — 24 метки, 704 байта из 1 123 566 (0,06 %). Решение
  девятой порции в силе: чистка по возрасту сегодня не удалила бы ни одной,
  а слияние ломать нельзя.
* «Меню» на 390 осталось матрицей (лентой переведены «Сборы», «Закупка»,
  «Дорога»). Замер: горизонтальной прокрутки у страницы нет, переполнения
  внутри блока `overflow-x: visible` — обрезки не даёт. Заказчик на «Меню»
  не жаловался, за три дня до выезда не трогаю.
* У кнопки удаления в ленте «Закупки» ярлык «Удалить позицию» без имени
  позиции, а в «Дороге» — «Убрать «Honda Accord»». Разнобой только для чтения
  вслух, глазами не виден.

## ➕ 06.08.2026 — ДЕВЯТАЯ ПОРЦИЯ: общий «плюс» с четырьмя видами и хвосты

Снимок боевой строки до работы: **1 123 566 байт**, метка `2026-08-05T21:04:38.391`,
автор «Макс», люди 3 · сборы 96 · закупка 53 · маршрут 8 · техника 4 · аренда 2.
Вся работа — на `vuoksa2026-test` (залита копией боевой и после работы выхолощена).

### ⛔ Спрошено первым делом и получен ответ

Вопрос был про общий плюс («мешает ли сегодняшний плюс в каждом разделе») и про
повтор четырёх сумм. Ответ заказчика: **«Вопрос за тобой. Главное, чтобы это не
нарушало логику и следовало общепринятым стандартам восприятия UX, типа NN/G»**
и **«Решай сам, как посчитаешь наиболее адекватным»**. Дальше — решения и почему.

### Общий «плюс»: составная кнопка, а не второй орган

Плюс раздела остался и работает как работал: **одно нажатие — своё действие**
(«Добавить вещь», «Добавить покупку», «Добавить день»). Рядом с ним появилась
узкая часть со стрелкой, она раскрывает **под полосой** ряд «Что добавить»:
**Вещь · Покупка · Топливо · Аренда**. Так частое действие осталось в один тап,
а четыре вида доступны из любого раздела — как заказчик и просил.

⛔ Не шторкой и не выпадающим меню (попапы забракованы трижды). Раскрытие на
месте — тем же приёмом, что «знаки» в той же полосе; ряд кнопок — по образцу
`UnitPick` из `gear/GearMatrix.tsx`. Ничего не выдумано.

Постулат 3.5 цел: плюс **ничего не показывает**, он заводит строку в её родном
разделе и уводит туда человека. Разбор — уроки **У-96** и **У-97**.

Право: у участника в ряду только «Вещь» и «Покупка» — расчёты правит редактор,
и лишние кнопки не рисуются вовсе (постулат 6). Проверено под ключом Миши.

### Повтор четырёх сумм: связь вместо удаления

Плитки на обложке и «Итоги поездки» в «Дороге» показывают одни и те же числа.
Убрать нельзя ни то, ни другое: оба места заказчик просил сам (04.08 и 05.08).
Число на плитке стало кнопкой к своей строке расчёта, где написано, из чего оно
сложилось. Урок **У-98**.

### Хвосты пункта 12

* **`PermNotice` при `perms.stale`** — ветка оказалась жива в офлайн-копии, а текст
  в ней был неверен («попросите новую ссылку» при отсутствии сети). Переписан
  под единственный случай, где он показывается. Урок **У-99**.
* **Список точек в заглушке карты** дублировал ленту маршрута — заменён числом
  и кнопкой «Показать маршрут» (постулат 3.5). Замер: `<li>` стало 0, текст
  «их 3 и они с координатами — в разделе „Дорога“», кнопка 174,6 × 44.
* **«не узнаны»** в шапке → «не узнан», и наведением/вслух — что делать
  («Откройте свою личную ссылку целиком, вместе с ключом»).
* **`Gear.q` / `Buy.qask`** («Попросить отметить», «Попросить изменить количество»)
  — ⛔ **не сделано осознанно.** Замер боевой строки: просьб про количество **0**,
  `Buy.qask` **0**, «не могу взять» 1 (эта отметка показывается кружком и работает).
  Скрытых данных нет — значит постулат 4 не нарушен, это отсутствующая функция,
  а не потерянное поле. Новый орган в каждой из 149 строк за четыре дня до поездки
  дороже пользы. Решение заказчика: нужен ли он вообще.
* **`S.del` копится вечно** — ⛔ **не сделано осознанно.** Замер: 24 метки,
  **704 байта из 1 123 566** (0,06 % документа). Чистка по возрасту сегодня
  не удалила бы ни одной метки (все моложе двух дней), а слияние — то, что ломать
  нельзя. Польза нулевая, риск ненулевой.

### Замеры

**Ступень 1:** `tsc` чист · `oxlint` без новых предупреждений · контрольные цифры
**330 км · 21 385 / 26 005 / 47 390 / 11 848 ₽ · 2 канистры** сходятся · обе сборки
прошли.

**Полоса раздела, 390** (У-11, соседи поимённо): заголовку осталось 196 px
в «Сборах» и «Закупке», 302 в «Дороге» и «Команде», 252 в «Меню». Ни один
не обрезан: самый длинный текст — «Старые пидарасы» 214,1 px при доступных 302,
одна строка. Кнопки: свой плюс 50 × 44, «ещё» 44 × 44, «знаки» 44 × 44.
Горизонтальной прокрутки страницы нет: **390 = 390**, на 1280 — **1265 = 1265**.

**Ряд «Что добавить», 390:** одна строка, x от 16 до 310,5, кнопки 57,9…76,7 × 44.
`elementFromPoint` в центре и на ±21 px по вертикали — везде отвечает сам орган
(все четыре кнопки, свой плюс и «ещё»).

**Контраст** (переходы сняты по У-55): светлая — плюс 5,66 : 1, ряд 13,86 : 1;
тёмная — плюс 6,49 : 1, ряд 11,04 : 1. Порог 4,5 держат все.

**Живой прогон нажатиями** (все четыре вида, оба направления):
«Аренда» из «Сборов» → аренд 2 → 3, страница y 0 → 20 374, новая строка видна
(top 263,8) · «Вещь» из «Дороги» → сборов 96 → 97, y 20 374 → 2556, строка
**открыта на правку** (фокус в поле), легла в первый блок `od` · «Покупка»
из «Сборов» → 53 → 54, блок `byt` · «Топливо» из «Закупки» → техники 4 → 5,
новая строка на top 263. Ошибок в консоли нет ни в одном проходе.

**Playwright (он единственный рисует кадры и по-настоящему прокручивает):**
390 и 1280, обе темы. Ряд из 4 кнопок на обеих ширинах, ошибок консоли 0,
горизонтальной прокрутки нет. Прогон «Аренда из Сборов» с настоящей плавной
прокруткой: 390 — y 6288 → 23 186, строка на top 241; 1280 — y 4999 → 21 552,
top 353.

**Плитки денег:** четыре кнопки 126,8 × 44 на одной линии (сетка `subgrid` цела,
У-70 не сломан). Тап по «С каждого» подводит к строке «С каждого · Делим
на 3 человек · 15 716 ₽», top 331,8.

### ⚠️ Чего среда проверить не даёт

`scrollIntoView({behavior:'smooth'})` в браузере среды **не едет вовсе** — замер:
после `smooth` y = 0, после `instant` y = 17 005. Это известная особенность среды
и общий механизм проекта (`scrollToSection` жил до этой правки), поэтому кнопка
«Показать маршрут» в заглушке карты проверена только логикой перехода. Сам
общий плюс от этого не зависит: он доводит до строки мгновенным
`scrollIntoView` и проверен полностью, в том числе Playwright'ом.

## 🛣️ 06.08.2026 — ВОСЬМАЯ ПОРЦИЯ: «Дорога» лентой, последний список ушёл из матрицы

Коммит `a8e4646`, **выложено и сверено по содержимому**. Снимок боевой строки до работы:
**1 123 566 байт** (`data`), метка `2026-08-05T21:04:38.391`, автор «Макс», люди 3 ·
сборы 96 · закупка 53 · маршрут 8 · техника 4. **На выходе — те же 1 123 566 байт
и та же метка:** боевая строка этой сессией не писалась ни разу, вся работа
на `vuoksa2026-test`.

### Что сделано

**`road/RouteStrip.tsx`** — «Маршрут» вертикальной лентой на телефоне. Это был
последний список, остававшийся матрицей на 390: колонка точки шириной 13 rem,
и название вставало в ней по два-три слова.

⛔ Ничего не выдумано. Плашка — `flops/StripRow`, поля подробности — `StripField`,
расстановка по образцу `gear/GearStrip` и `buy/BuyStrip`. Кружок этапа (`Dot`),
отметка «едет» (`Rider`) и строка места (`PlaceRow`) взяты **теми же функциями**
из матрицы: они получили `export` в `RouteTiming.tsx`, а наведение с карты вынесено
в хук `useRouteFocus` — два вида одного раздела обязаны вести себя одинаково.

Полоска: **название слева, время справа**, вторая строка — метка этапа · кто едет ·
примечание. Подробность на месте: название, время, «метка, путь, расстояние»
(та же карточка точки, но цель нажатия 332 × 48 вместо значка), описание, место
с кнопкой «на карте», «пройдено», «кто едет» по строке на человека, удаление.

Переключение вида — в `map/RouteBoard.tsx` по `useIsDesktop()`, ровно как в «Сборах»
и «Закупке». ⛔ Прокрутка блока на `lg:` не тронута: на ней держатся наведение
с карты и липкая шапка (`DataTable.inScroller`, У-91).

**Попутно найден и починен дефект `StripRow`:** `line-clamp-2` не работал **вообще
нигде** — соседняя утилита `block` перебивала `display:-webkit-box`. У 5 подписей
из 157 текст занимал 4 строки вместо двух. Урок **У-94**.

### Замеры (Playwright, песочница-копия боевой, 390 × 844 и 1280 × 900, обе темы)

| что | 390 | 1280 |
|---|---|---|
| вид раздела | лента, 8 строк 356 × **109,8…137,3** | матрица, строки 1230 × 108…193 |
| матрица в разметке | нет | есть |
| прокрутка страницы вбок | 390 = 390 | 1280 = 1280 |
| ошибки в консоли | нет | нет |

Строка ленты до правки клампа была **173 px**, после — **137,3**.
Название точки получило **235,5 px** ширины (было ~150 в колонке 13 rem)
и читается целиком: «Сбор у Кости: Санкт-Петербург, Суздальский пр., 95» — три
строки без обрезки.

**Цели касания в раскрытой точке** (`elementFromPoint` в центре и на ±21 px
во все четыре стороны — везде отвечает сам орган):
«пройдено» 44,0 × 44,0 · «едет» у человека 44,0 × 44,0 · «на карте» 70,6 × 44,0 ·
«метка, путь, расстояние» 332,0 × 48,0 · вся полоска строки 336,0 × 172,0.

**Контраст** (переходы сняты по У-55, цвета сырые): светлая — название
`rgb(38,37,19)` на `rgb(254,250,224)` = **14,72 : 1**, вторая строка **7,51 : 1**;
тёмная — `rgb(249,243,212)` на `rgb(58,71,38)` = **8,91 : 1**, вторая строка
**4,67 : 1**. Порог 4,5 держат все.

**Соседи (У-11), 390, обе темы:** «Сборы» 96 строк 64…108,8 — **до и после правки
клампа одинаково**; «Закупка» 53 строки 64…146,9; подписей с обрезкой 157,
строк в подписи максимум **2** (было 4 у пяти штук).

**Липкая шапка матрицы на 1280 (У-91 цела):** шапка стоит на y = 17921 при верхе
блока 17920 и **не двигается** при `scrollTop = 300`.

**Живой прогон нажатиями (390):** тап по полоске раскрывает (`aria-expanded=true`) ·
«пройдено» переключается (`aria-pressed=true`, название зачёркнуто, справа подпись
«пройдено») · «едет» переключается, вторая строка полоски становится «едут: Макс · …» ·
поле «метка, путь, расстояние» открывает карточку точки. Ошибок в консоли нет.
Записи дошли до песочницы: `r2.done = true`, `r2.o = {"max":1}`.

**Боевой домен после выкладки** (`?cb=`, вердикт по содержимому — У-89):
`/assets/index-0R7zT0Ue.js`, 452 311 байт, `application/javascript`, в нём есть
«Метка, путь, расстояние», «Кто едет», «Название точки», «Маршрута пока нет».
Живьём: 390 под ключом Макса — 8 строк ленты 356 × 137,3, матрицы нет, прокрутки
нет, консоль чиста; 1280 — матрица, ленты нет; 390 под ключом Миши — то же, что
у Макса. Ошибок нет ни в одном проходе.

### ⛔ Решено осознанно и НЕ сделано: цель касания у `InlineText`

Замер на 390: кнопка покоя `InlineText` — это сам текст с отступом 2 px,
**19…34 px** по высоте, и таких целей **91 из 170**. Добирать высоту невидимым
слоем **нельзя**, и это доказано соседом: у «Названия поездки» (34 px) точка
**+21 px по вертикали уже попадает в «Подзаголовок поездки»** — другую правку.
Расширитель накрыл бы соседа целиком, и тап по подписи правил бы название. Оставлено
как есть, высоту до 44 px даёт окружение (полка `StripField`, строка таблицы).
Урок **У-95** с числами.

### ⛔ Что НЕ сделано

* **Один липкий «плюс» с четырьмя видами позиции** — не тронут, упирается
  в постулат 3.5, решения нет.
* **Хвосты пункта 12** (`PermNotice`, «Попросить отметить», дубли точек и плиток,
  «не узнаны», `S.del`) — не тронуты.

### Данные и уборка

Песочница `vuoksa2026-test` заведена копией боевой (1 123 566 байт) и после работы
выхолощена до **4 949 байт**: один человек, ни маршрута, ни сборов, ни закупки,
обложка снята. Удалить строку из среды нельзя — база не разрешает. Уборка по У-69:
сначала уведена вкладка (`about:blank`), потом остановлен сервер, потом вычищена
строка. Портов в `LISTENING` на входе не было.

## 📱 06.08.2026 — СЕДЬМАЯ ПОРЦИЯ: мобильная версия переосмыслена лентой

Коммиты `68b2caf` и `93723b0`. Снимок боевой строки до работы: **1 130 358 байт**,
метка `2026-08-05T21:04:38.391`, автор «Макс», люди 3 · сборы 96 · закупка 53 ·
маршрут 8 · техника 4. Боевая строка этой сессией **не писалась ни разу**: вся работа
на `vuoksa2026-test`.

### ⛔ Первым делом спрошено (У-44) и получен ответ

Вопрос: «все списки раскрыты» и «всё по умолчанию свёрнутое» — что именно?
Ответ заказчика: **разделы раскрыты сразу, подробность позиции свёрнута до тапа**.
Дальше сделано ровно по этому ответу.

### Лента вместо матрицы на телефоне

`flops/StripRow.tsx` — новая общая форма: плашка во всю ширину, название 20 px слева,
главное число справа, подробность раскрывается **на месте** (не шторкой). В шапке файла
записаны внешние источники, по которым она собрана: Material Design 3 (Lists), Apple HIG
(Disclosure controls), Nielsen Norman Group (progressive disclosure), GOV.UK Design System
(summary list), Smashing Magazine (accessible responsive tables). Все пять сходятся
на **одном** уровне вложенности — второго аккордеона внутри строки нет.

`gear/GearStrip.tsx` и `buy/BuyStrip.tsx` — «Сборы» и «Закупка» лентой. На 1280 матрица
осталась: её заказчик сам прислал эталоном (Excel 2024, колонки Макс · Костя · Миша ·
Жека и «Общее количество»). Модель, права, `patch` и органы правки общие; рисуется
ровно один вид (`useIsDesktop`), а не два спрятанных.

**Замеры на 390 (Playwright, песочница):** 98 строк «Сборов» по 357 × 65, раскрытая
строка 590; 53 строки «Закупки» по 357 × 92…109; горизонтальной прокрутки у страницы
нет — 390 = 390. На 1280: 1265 = 1265. Кнопки счётчика 28 × 28 с невидимым
расширителем — `elementFromPoint` на ±21 px во все четыре стороны отвечает самой
кнопкой, то есть цель настоящие 44.

### Остальные правки разбора

* **Кадрирование обложки.** Рамка редактора была квадратной, а плашка — 4 : 3; теперь
  форма одна. Масштаб от 0,2 (был от 1), поля закрашиваются кремом ДО отрисовки —
  иначе JPEG сохранил бы чёрные полосы. Кнопки «Вписать целиком», «Другой снимок»,
  «Убрать снимок». Кнопка обложки открывает редактор на текущем снимке, а не сразу
  выбор файла, — иначе удалить обложку было нечем. Урок **У-92**.
  **Замер:** рамка 274 × 205 (1,33), снимок 273 × 273 → после «Вписать целиком»
  204 × 204, поля по 35 px, ползунок 0,75.
* **Две иконки геолокации → одна.** Немой значок слева стал кнопкой, вторая
  (`MapPinned`) убрана. Пусто — та же кнопка предлагает поставить точку
  (`askPlaceMain` в `lib/mapfocus.ts`). **Замер:** в обложке ровно один `map-pin`,
  44 × 44, подпись «Показать «Вуокса» на карте».
* **Слово «Поездка» и полоса раздела убраны**, обложка поднята. На их месте — строка
  принадлежности «ВУОКСА · 2026 · Вы — Макс, владелец» (пункт 9 разбора).
  ⛔ В полосу верхнего меню она не вернулась (У-11), адрес не переписывается (У-37).
* **Деньги.** Колонка «Сумма» убрана из строки и на телефоне, и на десктопе вместе
  с правкой «сумма ÷ количество». Итоги блока — «Сумма, план» и «Сумма, факт»
  всегда, даже нулями. `sumOf` и `lib/calc.ts` не тронуты, **контрольные цифры
  сходятся: 330 км · 21 385 / 26 005 / 47 390 / 11 848 ₽ · 2 канистры**.
* **Липкая шапка «Логистики».** Причина оказалась не в самой прокрутке, а в поправке
  `top: var(--header-h)` внутри блока со своей прокруткой. Заведён проп
  `DataTable.inScroller`. **Замер:** шапка стоит на y = 239 при верхе блока 239
  и не двигается при `scrollTop = 300` (было бы 64 px ниже края). Урок **У-91**.
* **Возврат офлайн-копии в онлайн** — пункт «Вернуть копию в онлайн» в «⋯», только
  владельцу и только в онлайне. Данные читаются из `__PINE_B64__` и **сливаются**
  `mergeInto`, а не подменяют документ.
* **Заголовки подразделов** больше не обрезаются многоточием, **адрес точки маршрута**
  не встаёт столбиком по одному слову.

### ⛔ «Цель касания 42 px» дефектом не была

Перемер: `min-height` у `[role="menuitem"]` — 44px, а замер даёт 41,8 = 44 × 0,95,
потому что Radix открывает меню с `zoom-in-95`, а анимации в этой среде не доигрывают.
Снятие `animation` вернуло 44,0. Правка не нужна, урок **У-90**.

### Финальный прогон (Playwright, 72 прохода)

390 и 1280 · светлая и тёмная · три ключа · шесть разделов. Скриншоты и `report.json` —
в папке проверки. Горизонтальной прокрутки страницы нет **ни в одном** проходе.
Ошибок в консоли нет, кроме `401` под ключом Кости — это **артефакт песочницы**:
в `vuoksa2026-test` его ключ `fg4qetdoy88v` (остался от проверки смены роли в шестой
порции), в боевой строке ключ Кости `8sqsfh85` цел и работает.

Замечания прогона, разобранные поимённо:
* «контраст 1,0 / 1,1» — **ложные срабатывания пробы**: цвет приходит строкой `oklab(…)`,
  а имя на карточке человека лежит поверх фотографии. Урок **У-93**.
* «цель < 44 по вертикали» у `InlineText` («Название вещи. Изменить», 26 px высотой) —
  это **инлайновая цель внутри текста**, а не орган-иконка; так `InlineText` работал
  всегда. Трогать его за четыре дня до поездки не стал — записано в задание.

### ⛔ Что НЕ сделано и почему

* **«Дорога» лентой.** Раздел остался матрицей на 390: название точки переносится
  по два-три слова в колонке 13 rem. Адрес починен, форма — нет. Это следующая работа.
* **Один липкий «плюс» с четырьмя видами позиции** («расчёт с топливом», «расчёт
  с арендой», «товар купить», «вещь добавить»). Сделан плюс **в каждом списочном
  разделе**, кладущий позицию в тот блок, который человек читает. Общий плюс через все
  разделы упирается в постулат 3.5 («один список — один раздел») и требует решения.
* **Хвосты пункта 12** (`PermNotice`, «Попросить отметить», дубли точек и плиток,
  «не узнаны», `S.del`) — не тронуты.

## ✅✅ 05.08.2026 — ОБА КАБИНЕТА ЗАКРЫТЫ ЗАКАЗЧИКОМ: карта работает, лист закрыт

Заказчик применил обе строки в тот же вечер. Проверено живьём, а не со слов.

### Карта Google — работает у всех троих

Причина была ровно та, что нашлась (У-86), и подтвердилась его скриншотом: в списке
разрешённых стояло `*.pine-to-pine.com/*`, то есть «любой **поддомен**», а сайт живёт
на **голом** домене — под звёздочку с точкой он не подпадает. Добавлена одна строка
`pine-to-pine.com/*`.

**Замер после (Playwright, боевой домен, 390):** у Макса, Кости и Миши подпись под
картой — **«Карта Google · сборка 8b99948d»**, узел `.gm-style` **есть**, Leaflet
выгружен, полосы отказа нет, консоль Google **чиста** (было
`RefererNotAllowedMapError`). Плиток Google в разметке 5, карта 356 × 280 на x = 17,
нажатие в центре доходит. Снимок экрана: плитки, логотип Google, «Картографические
данные ©2026 Google», нитка маршрута с точками 1–3 и метка «Вуокса».

### Чтение листа закрыто от посторонних (`rls-apply-e.sql`)

| проба | ответ |
|---|---|
| посторонний: `GET /rest/v1/trips?id=eq.vuoksa2026&select=data` | **HTTP 200, тело `[]`, 2 байта** — документ не отдан |
| свой: `rpc/trip_read` с ключом Макса | HTTP 200, **1 124 099 байт**, люди 3, сборы 98 |
| чужой ключ `postoronniy1` | **HTTP 401** — отказ |

Живой заход всей командой по личным ссылкам после закрытия: у Макса, Кости и Миши
по **6 разделов** и лист на месте. Ничего не погасло.

### ⛔ Что нашлось сразу после включения карты — и починено

Карта заработала, и под ней стал виден перекос, которого раньше не было видно вовсе.
**Замер на боевом, 390:** кнопка «Разметить маршрут» — **83 × 147**, её подпись
«Точек без места на карте: 6» — **19 × 108**, то есть текст встал в столбик по одной
букве. На 1280 при этом всё ровно (329 × 44 и 265 × 18) — потому и не ловилось.

Причина не в переносе: `flex-wrap` в полосе был. При `flex-1` **без базиса** браузер
предпочитает СЖАТЬ соседа, а не перенести его. Родня У-81 — кнопка «Точка», заведённая
прошлой сессией, съела соседа. Починено `basis-64` в `map/TripMap.tsx`.

**Стало (390):** кнопка **332 × 44**, подпись **268 × 18** в одну строку, «Точка»
(101 × 44) и «Конечная» (131 × 44) переехали строкой ниже. **1280 не тронут:**
329 × 44, подпись 265 × 18, все три органа в одном ряду на y = 428. Нажатия проходят
в центре и на ±21 px, обе темы; скролл страницы 390 = 390 и 1280 = 1280.
Коммит `a55494b`.

## 🔑🚪📥 05.08.2026 — ШЕСТАЯ ПОРЦИЯ: права, вход новичка, копия каждому, диагноз карты

Коммит `0cb18b6`. Снимок боевой строки до работы: **1 124 140 байт**, метка
`2026-08-05T15:44:53.414`, автор «Макс», люди 3 · сборы 98 · закупка 53 · маршрут 8 ·
техника 4. **На выходе — байт в байт та же** (разница 41 знака в замере объясняется
полем `owner_email`, которого не было во втором запросе), метка не сдвинулась,
ключи и роли целы. Песочница `vuoksa2026-test` заведена копией и удалена, в таблице
осталась одна строка.

### ⛔ Причина отказа карты найдена — и она НЕ в коде (урок У-86)

Боевой домен открыт настоящим браузером (Playwright, глобальный 1.58.2). Консоль:

```
Google Maps JavaScript API error: RefererNotAllowedMapError
Your site URL to be authorized: https://pine-to-pine.com/
```

Домена `pine-to-pine.com` **нет в ограничениях ключа** в кабинете Google Cloud.
Три сессии правились сроки ожидания и объяснения отката — причина всё это время была
в одной строке настройки. Замер, почему без браузера это не ловилось: загрузчик
`maps/api/js` с `Referer` боевого домена, `pages.dev` и заведомо чужого
`evil.example.com` отвечает **200 и одинаковые 13 261 байт во всех трёх случаях**
(развитие У-30). Все 20 запросов к `googleapis` на живой странице — тоже 200,
плитки при этом не рисуются, подпись под картой честно говорит
«Карта OpenStreetMap · Google не пускает с этого адреса».

**Что надо сделать в кабинете** (текст для заказчика — в ответе сессии): добавить
`https://pine-to-pine.com/*` и `https://*.pine-to-pine.com/*` в Website restrictions
ключа `AIzaSyDIVMTSa86eoaOYSlkqGP6xEZXEQTtInAY`. Кода это не касается вовсе.

### Пункт 5 брифа — права по образцу Телеграма (ответ заказчика: три роли)

Спрошено первым делом, ответ — **«Три роли, коротко»**, а не галочки полномочий.
Сделано в `crew/PersonSheet.tsx`: список «Что может <роль>» из семи пунктов
(≈344 знака: `permRights` 4 + `CANT` 2 + заголовок + подсказка строки) заменён выбором
из трёх строк с короткой подписью — **139 знаков, высота блока 220 px**. Шторка выбора
(`PickSheet` уровня `perm`) убрана: правка на месте (постулат 2). Орган взят готовый —
`ui/radio-group.tsx` (Radix), своей разметки не сверстано.

`permRights()` заменена на `permShort()` — одна строка на роль: «Всё, включая
удаление» · «Правит списки, деньги, маршрут» · «Отмечает своё».

**Замеры.** 390: строки 358 × 57, 1280: 446 × 57 — цель касания по всей строке,
`elementFromPoint` в центре и на ±21 px отвечает `SPAN`/`LABEL` внутри строки.
Контраст со снятыми переходами (У-55), **по классам, а не по позиции** (У-88):
светлая **14,72** (название) · **7,51** (подпись); тёмная **8,91** · **4,67** при норме
4,5 — одинаково у всех трёх строк, включая выбранную. Скролл страницы 390 = 390
и 1280 = 1280. Живой прогон: Костя «редактор» → «участник» → тост «Костя теперь
участник — старая ссылка больше не действует», ключ сменился (`8sqsfh85` →
`fg4qetdoy88v`), возврат в «редактора» сработал. Под ключом участника строк выбора
роли **ноль**, кнопок «Скопировать его ссылку» и «Убрать из команды» нет вовсе,
стоит одна строка «Права · Редактор — правит списки, деньги, маршрут».

### Человеческий вход для того, кто ещё не зарегистрирован

Заказчик: «надо сделать здесь как-то человеческий вход для человека, который ещё
не зарегистрирован… подходить к ситуации, когда уже можно пользоваться сервисом».

Было: голый адрес → «Этот лист закрыт» и ровно один путь — вставить чужую ссылку.
Вошедший по почте **не член** этой поездки читал «одной почты мало» и упирался
в тупик: список поездок живёт в меню «⋯», а меню — внутри листа, до которого он
не доходит.

Стало (`auth/ClosedList.tsx`): без ключа заголовок **«Сборный лист поездки»** вместо
«Этот лист закрыт» (слово «закрыт» осталось там, где ключ действительно отклонён),
подпись «Заведите свою поездку или откройте чужую по личной ссылке», блок «Своя
поездка» с входом по почте — он же регистрация, — и **кнопка «Мои поездки»** для тех,
кто уже вошёл. У `OwnerLogin` появился проп `hint`: на закрытом экране он говорит
«Заведите свою поездку — письмо и заводит её, и открывает», а не «почтой входит
владелец» (для новичка та фраза читалась как отказ).

**Замеры (Playwright, 390 и 1280, обе темы).** Экран входа: **328 знаков**,
разделов листа **0** (утечки данных нет), кнопки 324 × 52 на 390 и 478 × 52 на 1280,
скролл 390 = 390 и 1280 = 1280.

### ⛔ Попутно закрыта утечка, которую открыла новая дверь (урок У-87)

Через кнопку «Мои поездки» посторонний **без ключа** прочитал в списке
«ВУОКСА · 2026 · правки 5 августа, Макс». Причина не в кнопке: `listTrips()` всегда
показывала «открытую сейчас» поездку — безобидно ровно до тех пор, пока в список
нельзя было попасть, не пройдя ворота. Починено в `lib/trips.ts`: открытая сейчас
поездка попадает в список, только если у браузера **есть ключ**. Замер после: чистая
память + голый адрес → «Поездок пока нет», название и автор не показаны; с ключом
Макса поездка в списке по-прежнему видна.

### Офлайн-копия и Excel — каждому, и по-родному

Заказчик: «имеется эта возможность у каждого из участников команды: что в Excel
выгрузить себе, что офлайн-версию сохранить себе».

* `canSaveFile()` в `lib/perm.ts` — теперь **всем**, было `isChief()`.
* ⛔ **Чужие ключи из копии вычищаются** (`withoutOthersKeys` в `lib/offline.ts`):
  участник получает файл со СВОИМ ключом, у остальных `key: ''`. Иначе он открыл бы
  скачанный файл в блокноте и взял ключ владельца — ровно урок У-65. Владельцу
  документ уезжает целиком: ссылки команды раздаёт он.
* **Нативная отдача файла** — `deliver()`: на телефоне системный лист «Поделиться»
  (`navigator.share` с файлом) — «Сохранить в Файлы», отправить себе, открыть в Excel;
  на компьютере прежнее скачивание.

**Прогон чистки ключей** (через `import()` после `location.reload()`, У-85): копия
Миши — `max:ПУСТО, kos:ПУСТО, mis:prs90sd7`; ключей владельца и Кости в тексте файла
**нет**; исходный документ в памяти не тронут; `gear` цел. Копия владельца — документ
целиком.

**Прогон четырёх веток `deliver`:** устройство умеет делиться → системный лист,
1 файл, имя в заголовке · не умеет → скачивание · человек закрыл лист сам
(`AbortError`) → **ничего не скачивается**, не навязываемся · сломалось
(`NotAllowedError`) → скачивание, человек без файла не остался.

Книга Excel под ключом участника собирается: **121 623 байта**, имя
«ВУОКСА · 2026 · 05.08.2026.xlsx». Пункты меню у участника: «Мои поездки»,
«Забрать таблицу Excel», «Забрать офлайн-копию», «Вход владельца», «О сервисе»;
«Ссылки команды» нет — она осталась владельцу.

### Проверки

`check-sums.mjs` — двенадцать строк сходятся (**330 км · 21 385 / 26 005 / 47 390 /
11 848 ₽ · 2 канистры**). `tsc --noEmit` — без вывода. `oxlint` — 10 прежних
предупреждений `only-export-components`, ошибок **0**. `build` — 454,92 КБ,
`build:offline` — 1 277,10 КБ. Офлайн-заготовка открыта с диска Playwright'ом:
6 разделов, ошибок страницы нет.

### Новые уроки

**У-86** (причина отказа карты — домен вне ограничений ключа; ищется Playwright'ом
за минуту), **У-87** (новая дверь в закрытый раздел — новый путь для постороннего),
**У-88** (замер цвета по позиции узла врёт на выбранном органе Radix).

### Что осталось на заказчике — ровно две строки в двух кабинетах

1. **Google Cloud** — добавить домен в ограничения ключа карты (выше).
2. **Supabase SQL** — `docs/rls-apply-e.sql`; из среды применить нечем (MCP требует
   авторизации, `service_role`-ключа нет). Пока не применён, `SELECT` открыт всем:
   прямой запрос к `/rest/v1/trips` отдаёт документ целиком.

## ✂️ 05.08.2026 — ПЯТАЯ ПОРЦИЯ: взаиморасчёты переехали, текста стало вдвое меньше

### Ответ заказчика 1 — «Взаиморасчёты» переносятся в «Закупку» целиком

Спрошено первым делом, ответ дословно: «Все затраты закупки имеют отношение также
и к затратам, которые связаны с проездом, то есть бензин и так далее. То есть это всё
должно быть в одном разделе. Внизу должны быть итоги… Я думаю, что необходимо перенести
целиком в закупку».

Сделано: `road/Settlement.tsx` → `buy/Settlement.tsx` (переезд файла, `git mv`), блок
стоит последним в «Закупке». ⛔ В «Дороге» второго экземпляра нет — проверено замером:
`h3` с текстом «Взаиморасчёты» на странице **один**, `sec-road.innerText` его не содержит.

**Замеры.** 1280: блок 1217 px — ровно как соседи (итоги закупки 1217, последняя группа
1217); липкая шапка таблицы 137 → **121** (= `--header-h` 64 + `--sec-h` 57) при прокрутке,
`elementFromPoint` в ней отвечает «Участник»; страница 1265 = 1265. 390: блок 358, шапка
155 → **113** (56 + 57), прокрутка внутри блока (сетка 536 при видимых 357), страница
390 = 390. Контраст со снятыми переходами: светлая **14,72** (заголовок) · **7,51**
(подпись) · **14,72** (итог строки); тёмная **8,91** · **4,67** — при норме 4,5.

**Живой прогон пересчёта** (заказчик: «итоги меняются, там перерасчёт происходит»):
снял «не берём» с газового баллона (2 × 900 ₽) → общий счёт 26 005 → **27 805 ₽**,
доля 15 797 → 16 397 ₽, итог Макса −2 312 → −2 912 ₽, Кости 2 318 → 2 618 ₽,
Миши −6 → **+294 ₽**. Вернул обратно — 26 005 ₽ и −2 312 ₽ сошлись.

### Ответ заказчика 2 — пункт 15 «первый заход» это ПРОСЬБА УБРАТЬ ТЕКСТ

Готовилась полоса «С чего начать». Спросил — и оказалось наоборот, дословно:
«без огромного количества инструкций. **У тебя просто гигантское количество текста**,
везде которого можно было бы в теории просто избегать вообще… писать целые статьи
о том, как пользоваться каким-то конкретным элементом, это слишком». Урок **У-84**.

**Мера.** Весь видимый текст, которого нет в документе поездки (то есть текст самого
интерфейса): было **1 439 знаков в 32 узлах**, из них четыре абзаца длиннее 60 знаков;
стало **905 знаков в 31 узле**, длиннее 60 — **ни одного**. Отдельно: примечание
«Выложил деньги вперёд, ему возвращают» стояло у КАЖДОГО имени в КАЖДОЙ строке расчёта —
21 повторение, плюс 7 у «Скинулись поровну»; убрано. Экран отказа: **901 → 377 знаков**.
Подсказка участнику в «Сборах»: три строки → одна (390: 54 → **18 px** высоты).

⛔ Что НЕ трогалось: причины отказов, правила расчёта («в счёт идёт план», «выложенное
минус своя доля»), тексты пустых состояний. Постулат 5 остаётся: молчаливых отказов
не бывает, убиралась только инструкция «как пользоваться».

### Хвосты задачи 5 прежнего задания — три из четырёх закрыты

* **`putNew` больше не врёт «поездка заведена».** `trip_write` с `p_seen = null`
  по существующей строке отвечает 200 и не делает ничего (У-77) — теперь проверяется
  `out.length`. **Проба на песочнице:** HTTP 200, тело `[]`, метка `16:42:36.424`
  до и после — строка не тронута. Человек читает: «Поездка с таким именем строки
  уже есть. Назовите её иначе и повторите». `taken()` оставлен быстрой пробой,
  и в комментарии написано, почему он после `rls-apply-e.sql` бесполезен.
* **Ключ поездки при переходе.** `TripResult.cred` + `openTrip(id, cred)`: новая
  и дублированная поездка открывается ссылкой с ключом. Ключ берётся СВОЙ, если он
  есть в новом документе, и только иначе — владельца (иначе дублировавший вошёл бы
  под чужим именем). Замер: `tripHref('poezdka-ab12', {u:'own',k:'k1k2k3k4'})` →
  `/?sandbox=1&u=own&k=k1k2k3k4&trip=poezdka-ab12`; без `cred` — прежний чистый адрес.
* **Офлайн-копия непервой поездки (У-62).** Закрыто **парой**: `BOOT` читает
  `W.__PINE_KEY__||'flops.doc'`, `docKey()` отдаёт приложению то же имя. Замер:
  на сайте `flops.doc`, при вшитом ключе — `flops.doc.vuoksa2027`.
* **`dative()`.** Добавлено правило беглой гласной в уменьшительных на `-ёк`:
  «Санёк» → **Саньку**, «Витёк» → **Витьку**, «Игорёк» → **Игорьку**. Прежние случаи
  целы: Пётр → Петру, Лев → Льву, Костя → Косте, Игорь и МИШКА — без падежа.

### Проверки

`check-sums.mjs` — двенадцать строк сходятся (**330 км · 21 385 / 26 005 / 47 390 /
11 848 ₽ · 2 канистры**). `tsc --noEmit` — без вывода. `oxlint` — 10 прежних
предупреждений `only-export-components`, ошибок **0**. `build` — 454,77 КБ,
`build:offline` — 1 271,67 КБ.

### Новые уроки

**У-84** (пункт «первый заход» — про меньше текста, а не про подсказки),
**У-85** (`import()` во вкладке отдаёт модуль, загруженный ДО правки; перед прогоном —
`location.reload()`).

### Что из брифа осталось

**Пункт 5 — права по образцу Телеграма.** Ответ заказчика получен, но общий:
«представь себе, что это канал в Телеграме, то есть владелец, условно, модератор
или редактор, которому разрешено вносить изменения функциональные… по подобию сделать.
Конечно, специфика будет отличаться… дизайн должен быть премиальным». Работа не начата
осознанно: до потолка сессии оставалось мало, а это крупная функция. Разбор и план —
в `PROMPT-NEXT.md`.

## ✅ 05.08.2026 — вопрос про Женю ЗАКРЫТ: его убрал заказчик

Спрошено первым делом четвёртой сессии, ответ дословно: «Да, это я убрал Женю».
Ничего не восстанавливалось, `people:zhe` в `S.del` остаётся, людей в листе трое
(`max`, `kos`, `mis`). Снимок боевой строки перед работой: **1 124 081 байт**,
`updated_at` 2026-08-05T14:59:27, автор «Макс».

⚠️ `app/scripts/check-sums.mjs` считает контрольные цифры из `seed-v2.json`, а там
людей по-прежнему четверо — поэтому во взаиморасчётах скрипта Женя есть, и это норма:
образец для проверок и боевой лист живут отдельно (так и задумано, см. задачу 1
прежнего задания).

## 🗺️🔎🛒 05.08.2026 — ЧЕТВЁРТАЯ ПОРЦИЯ ДИЗАЙНА: карта, поиск, посредник

Пункты брифа **11** и **12** закрыты целиком; пункт **16 часть Б** сделана кодом
и проверена разбором, но ждёт выкладки посредника в Supabase (нужен токен доступа —
заказчик его добывает).

### Пункт 11 — добавление точки на карте очевидно и на телефоне

Заказчик: «я вот забил туда условную информацию по разным точкам, но толку я не увидел…
если я хочу добавить новую точку, я её даже должен…». Причина найдена: точка заводилась
**тапом по пустому месту карты**, и тап работал, — но узнать о нём было неоткуда,
а на телефоне нет ни наведения, ни курсора. Урок **У-78**.

Сделано: в полосе под картой появилась кнопка **«Точка»** (заливка, значок `MapPinPlus`)
рядом с прежней «Конечной». Нажатие включает ожидание со строкой «Тапните по карте, где
новая точка маршрута» и кнопкой «Отменить» — ровно так же, как это уже работало
у конечной точки. Прежний тап без режима оставлен как быстрый путь.

**Замеры.** 1280 светлая: «Точка» 101 × 44 на x = 989, «Конечная» 131 × 44 на x = 1098,
обе отзываются по `elementFromPoint` в центре и по краям. 390 тёмная: «Точка» 101 × 44
(контраст надписи **6,49**), «Конечная» 131 × 44 (**12,8**), горизонтального скролла
у страницы нет (390 = 390). Живой прогон на Leaflet (Google плиток в среде не рисует,
У-76): «8 точек · 2 на карте» → нажатие «Точка» → тап по карте → **«9 точек · 3 на карте»**,
появилась «Новая точка», полоса вернулась в обычный вид. «Отменить» гасит ожидание.

### Пункт 12 — поиск целиком

Заказчик: «наверху всё перекошено, дизайн не единообразен, и размеры у тебя все везде
по-разному». Замер показал **две разные беды**, и чинились они в разных файлах (У-79):

* орган приехал из образца shadcn со своими размерами: поле ввода **30 px** высоты
  (цель касания 44!), кегль **14 px** — чужой шкале 12 / 13 / 15,5, заголовки групп 12,
  строки находок 53. Починено в `ui/command.tsx`: поле 52 px во всю ширину со значком
  слева и волосяной линией снизу, кегли `body` / `note` / `micro`, строка ≥ 44 px;
* окно показывало **217 позиций листа сразу**, без запроса и без заголовков. Починено
  в `SearchCommand.tsx`: до первой буквы — «Поиск по всему листу» и число позиций,
  после — находки **группами** «Сборы · Закупка · Дорога · Меню · Команда». Ширина окна
  384 → **560 px**, запрос забывается при закрытии.

**Замеры.** 1280: окно 560 × 209 (было 384 × 477 со свалкой), поле 52 px, отзывается.
390: окно 358 × 560, поле 339 × 52, страница 390 = 390. Контраст светлая **14,72**
(ввод) · **7,51** (заголовок группы) · **12,55** (название) · **6,41** (примечание);
тёмная **8,91** · **4,67** · **12,8** · **6,71** при норме 4,5. Живой прогон: «вода» →
пять групп с заголовками, 68 находок, первая — «Солнцезащитные очки».

⚠️ Замер внутри окна занижен на 5 %: недоигранная анимация `zoom-in-95` держит узел
на `scale(0.95)`. Сверять с `getComputedStyle().height` — урок **У-80**.

### Пункт 16 часть Б — посредник, который читает страницу товара

Написан `supabase/functions/product/` — Edge Function на Deno:

* `parse.ts` — разбор разметки: `schema.org/Product` (название, фото, `offers.price`),
  Open Graph, `itemprop="price"`, `<title>` как последняя надежда;
* `index.ts` — сам посредник: CORS, таймаут 12 с, потолок чтения 1 МБ, разбор кодировки
  из заголовка (windows-1251 у старых магазинов), защита от обращения во внутреннюю сеть
  (SSRF), честное имя робота. ⛔ Заслоны не обходятся: ни капч, ни имитации браузера;
* `test.ts` — 22 проверки разбора, гоняются `node --experimental-strip-types`
  (Node 22+ читает TypeScript сам). Все сошлись. С `--live` дополнительно берёт
  настоящие страницы — это разведка, а не проверка.

Сторона приложения — `app/src/lib/product.ts`. ⛔ Правила, ради которых он отдельный:
цена ложится **только в «Цена, план»** (`Buy.pr`) и **только в пустое место**; «Цена,
факт» не трогается ничем; название заменяется, только если своего у позиции нет (своим
считается всё, кроме пустого и имени сайта); снимаем **один раз** при вставке и потом
по кнопке — фоном никогда. Состояние («читаем…», причина отказа) живёт в памяти вкладки,
а не в документе: «магазин не отдал карточку» — про сегодняшнюю попытку, а не про поездку.

Показ — в `flops/ProductLink.tsx`: кнопка «Проверить цену» (44 × 44) и строка словами
под ссылкой. ⚠️ Кнопка чуть не съела соседа: на 390 имя сайта сжалось до **8 px**
в липкой колонке 184 px. Лечится `flex-wrap` + `basis-24` — урок **У-81**.

**Живой прогон (посредник ещё не выложен, и это главная проверка):** в «Закупке» в поле
названия «Газовый баллон» вставлен адрес alpindustria — ссылка сохранилась и нажимается,
название цело, цена плана 900 и факт 0 **не тронуты**, под ссылкой строка «Сервер
не ответил про карточку — название и цену впишите сами». То же в «Сборах» («Носки»,
kant.ru). Неудача чтения не ломает ничего — ровно как обещано заказчику.

**Посредник ВЫЛОЖЕН** (заказчик дал токен доступа Supabase). Живая проверка с боевого
адреса функции, четыре настоящие карточки «газовый баллон»:

| магазин | ответ посредника |
|---|---|
| **autolong.ru** | **«Баллон газовый „СИБИАР“ 220 г» · фото есть · 83 ₽** |
| **tvoydom.ru** | **название есть · 119 ₽** (фото магазин не отдал) |
| saturn.net | «не пускает робота на эту страницу» (403) |
| etm.ru | «ответил отказом (444)» |

⚠️ Первая выкладка отвечала «сайт не отвечает» **на всё, включая example.com**: в имени
робота была кириллица, а заголовки HTTP — байтовые строки, и `fetch` падал до выхода
в сеть. Урок **У-83**.

### Требование 05.08.2026 — имена прилипают при прокрутке списка

Слово заказчика: «когда я листаю список, нужно, чтобы имена прилипали… иначе сложно
понять, кому это делегируется». Сделано в `flops/DataTable.tsx`: шапка вынута из области
горизонтальной прокрутки, прилипает под верхней панелью и полосой раздела, а вбок её
двигает `transform` вслед за телом (общая для всех блоков раздела, `TableScroll.heads`).

⚠️ Мешали трое, и все — `overflow-hidden` ради скруглённых углов: `flops/Group.tsx`,
`map/RouteBoard.tsx`, `road/RoadCalc.tsx`, `road/Settlement.tsx`. Все переведены
на `overflow-clip`: обрезает так же, но прокручиваемым блок не делает. Урок **У-82**.
Новая переменная `--sec-h` (57 px) в `index.css` — высота липкой полосы раздела.

**Замеры.** 1280: шапка стоит на y = **121** (= `--header-h` 64 + `--sec-h` 57) и
до, и после прокрутки на 400; фон непрозрачный `#FEFAE0`; фотография в колонке «Макс»
отзывается по `elementFromPoint`; ширины колонок шапки и строк совпали
(695 / 112 / 112 / 112 / 80 / 104). 390: y = **113** (56 + 57), прокрутка тела вбок
на 150 → `translateX(-150)` у шапки, колонка шапки и колонка строки на одном x = 27.
Липнут: «Сборы», «Закупка», «Маршрут», «Меню», «Взаиморасчёты». У «Расчёта дороги»
шапки колонок нет вовсе — там подзаголовки внутри строк.

### Требование 05.08.2026 — «чтобы затраты делились с учётом того, кто уже купил»

**Это уже работает, менять ничего не понадобилось** — механика заведена 05.08.2026
в `lib/settle.ts` (режим 3, «куплена одним, делится на всех»). Проверено живьём
на позиции «Репелленты, 3 шт по 550 ₽»: до отметки Костя выложил 17 749 ₽ (итог
+1 952), после отметки «Костя берёт 1 штуку» — выложил **18 115 ₽** (итог **+2 318**),
у Макса и Миши по −183 ₽. Арифметика: Костя оплатил 550 вперёд, остаток 1 100
скинулись поровну по 366,67; доли у всех остались по 550.

⚠️ Позиция со статусом «не берём» (`st: 'skip'`) в зачёт не идёт вовсе — на ней
проверка сначала показала «ничего не изменилось», и это правильное поведение,
а не дефект.

⛔ **Открытый вопрос к заказчику:** блок «Взаиморасчёты» живёт в «Дороге», а думает
он об этом в «Закупке». Переносить или дублировать самовольно нельзя (У-53, дубли),
поэтому спрошено словами.

### Проверки

`check-sums.mjs` — двенадцать строк сходятся (**330 км · 21 385 / 26 005 / 47 390 /
11 848 ₽ · 2 канистры**). `tsc --noEmit` — без вывода. `oxlint` — 10 прежних
предупреждений `only-export-components`, ошибок ноль. `build` — 457,90 КБ,
`build:offline` — 1 273 КБ. Разбор карточки — 22 из 22. Коммит `350e381`.

### Что осталось от брифа

Пункты 5 (права по образцу Телеграма) и 15 (первый заход).

## 🗺️ 05.08.2026 — ВЫЛОЖЕНО: третья порция дизайна и починка немого отката карты

Коммит `4171d98` (и `57be194` с документами прошлой сессии).

### Карта: почему заказчик видел OpenStreetMap

Жалоба дословно: «у тебя по-прежнему open street map вместо Google карты показывается».
**Замером с боевого домена:** ключ жив (загрузчик Google отдаёт 200 с referer
`pine-to-pine.com`), приложение выбирает Google, `google.maps.Map` создаётся, подпись
под картой — «Карта Google». Значит откат срабатывал **у него**, а причина стояла
подписью в самом мелком кегле рядом с номером сборки и не читалась.

Сделано два дела:
* **срок ожидания Google поднят с 12 до 25 с** — шесть-семь запросов подряд на мобильной
  сети в двенадцать секунд не укладываются, и человек получал `timeout` там, где надо
  было подождать;
* **откат перестал быть немым**: над картой полоса со значком, причиной, что делать
  и кнопкой «Попробовать снова». Повтор честный — `retryGoogle()` забывает прошлый отказ,
  а `key={googleTry}` пересоздаёт компонент (без него кнопка ничего бы не делала).

Проверено живьём: `window.gm_authFailure()` → полоса «Карта Google не открылась. Ключ
карты не принят этим адресом… Пока показываем OpenStreetMap», кнопка 183 × 44, откат
на Leaflet состоялся. Нажатие «Попробовать снова» → подпись снова «Карта Google»,
полоса ушла, Leaflet выгружен. Урок **У-76**.

⚠️ Глазами карту из этой среды не проверить вовсе: `computer{screenshot}` отвечает
«the Browser pane is not displayed, so the page is not compositing frames», и плитки
Google по той же причине не рисуются, хотя объект карты создан.

### Задача 1 — «Меню» приведено к общей форме (У-54)

День — группа `Group` (заголовок правится на месте, полоса доли приготовленного, бейдж
«приготовлено: 1 из 9»), блюда — `DataTable`: липкая колонка «Блюдо», своя колонка
«Сколько брать», «Готово», действия. Прокрутка вбок внутри блока, сцеплена между днями.
Прежние карточки плиткой «3 в ряд» убраны — это была пятая форма списка в сервисе.
⛔ Столбцов людей нет: прямой ответ заказчика.

Замеры: **390** — сетка 464 при блоке 357 (прокрутка внутри), колонки 144/144/72/104,
липкая колонка держится на 17…161 при прокрутке 107, название внутри клипа, кнопка
«Готово» 44 × 44 и отзывается по `elementFromPoint`; у страницы горизонтального скролла
нет (390 = 390). **1280** — 1215 = 1215, колонки 520/520/72/104, страница 1265 = 1265.
Живой прогон: отметка «приготовили» → бейдж «приготовлено: 1 из 9», снятие → «0 из 9».

⚠️ Тонкость, которую легко сломать обратно: название и «сколько» лежат теперь в РАЗНЫХ
ячейках, поэтому «правка новой строки закончилась» ищет строку по `data-hit`, а не по
ref первой ячейки (`endFreshWhenLeftRow`). С ref новая пустая строка исчезала бы ровно
в тот миг, когда человек тянется вписать количество.

### Задача 2 — ссылка на товар у позиции (бриф, пункт 16, часть А)

Поля `url`, `img`, `pat` у `Gear` и `Buy`, каждое в **три** места слияния: список в
`pick('gear')` / `pick('buy')`, вызов `fields` (простые значения, не `jsonFields`),
умолчание в `normalizeDoc`. `trip.places[].addr` дописывать в слияние не нужно —
`trip` лежит в `WHOLE_DOC`.

Орган ввода один — поле названия (`lib/producturl.ts`, `saveNameOrUrl`). Проверено
шестью случаями: обычный текст → название; адрес в пустое → `url` + имя сайта названием;
адрес поверх названия → название цело; **`javascript:alert(1)` ссылкой НЕ становится**;
адрес без схемы («alpindustria.ru») — обычный текст; переименование позиции со ссылкой
её не теряет. Показ — `flops/ProductLink.tsx`: имя сайта, `rel="noopener noreferrer"`,
`target="_blank"`, цель 70 × 44, фотография адресом с заглушкой при ошибке загрузки.
Живьём: у «Штопора» строка «Штопор | kant.ru | примечание».

⛔ Часть Б (снять название, фото и цену со страницы) не делалась — это посредник
на сервере Supabase, отдельной выкладкой.

### Задача 3 — даты и адрес заводятся один раз (бриф, пункт 6)

**Даты.** `autoDayTitle` / `isAutoDayTitle` в `format.ts`. Смена дат в обложке
переписывает названия дней раскладки, **выданные автоматом или пустые**, и не трогает
названные человеком («День рыбалки» → `isAutoDayTitle` = false). Новый день сразу
получает свою дату. Живой прогон: 10–14 августа → 17–21 → все пять дней переехали
(«17 августа · день 1» … «21 августа · день 5»), обратно — вернулись.

**Адрес приезда.** У `TripPlace` появилось поле `addr`. Заполняется тем же геокодером,
что подписывает точки маршрута, в момент постановки конечной точки на карте;
показывается на обложке под названием места и в шапке выгрузки («Приезд: …»).
Второго места ввода нет — на обложке он только читается (У-53). Живьём на обложке:
«Ленинградская обл., Приозерский р-н, оз. Вуокса, база «Лосево»».

### Проверки

`check-sums.mjs` — двенадцать строк сходятся. `tsc --noEmit` — без вывода. `oxlint` —
10 предупреждений, все прежние `only-export-components`, ошибок ноль. `build` 454,87 КБ,
`build:offline` 1 272 КБ. Контраст со снятыми переходами: светлая **6,16** (ссылка)
и **7,51** (адрес, шапка колонки), тёмная **6,64** и **4,67** при норме 4,5.

### Грабли сессии

**У-77:** `trip_write` с `p_seen: null` по СУЩЕСТВУЮЩЕЙ строке возвращает HTTP 200
и не делает ничего. Полчаса ушло на поиск несуществующего дефекта слияния. Писать
в существующую строку — только с `p_seen` = её текущий `updated_at`, и сверять
не код ответа, а изменившийся `updated_at`.

### Что из брифа осталось

Пункты 5 (права по образцу Телеграма), 11 (добавление точки на карте очевидно
и на телефоне), 12 (поиск целиком), 15 (первый заход), 16 часть Б (снятие карточки
товара посредником).

## 🎨 05.08.2026 — ВЫЛОЖЕНО: вторая порция дизайна, пункты 7–10 брифа и матрица маршрута

Три коммита `a1b108d`, `0ee2f78`, `2ffa607`, **на боевом**. Вместе с ними уехала
и первая порция (пункты 1–4 и 13), ждавшая слова заказчика.

**Свежесть сверена по содержимому (У-42):** `assets/index-BuqudbR5.js`,
**450 134 байта**, `application/javascript`. В нём есть `w-min min-w-full`,
«едет точкой», «По плановым ценам», «Завести вещь после»; `lg:opacity-0` —
**ни одного совпадения**.

### Что сделано

**Пункт 7 — плюсики и удаление видны всегда.** `RowActions` потерял `lg:opacity-0`:
действия строки видны на обеих ширинах. Полоса вставки между строками (`RowInsert`)
убрана целиком из «Сборов», «Закупки» и «Меню» — она была невидимым органом,
дублирующим кнопку в самой строке (У-58). Компонент удалён вместе с экспортом.
В «Сборах» кнопка вставки открыта и на десктопе (был `lg:hidden`).
⚠️ Замер до правки показывал `opacity: 0` и на 390 — это врал замороженный
переход, урок **У-75**. На телефоне действия были видны и раньше.

**Пункт 8 — пустого поля справа больше нет.** Сетка таблицы: `w-min min-w-full`
вместо `w-max min-w-full`, первая колонка «Сборов» долевая, хвостовая распорка
убрана. Замер: было 936 при блоке 1205 (269 px пустоты) — стало 1205 = 1205.

**Пункт 9 — план, факт и разница.** В итогах «Закупки» появились «По плановым
ценам» и «Дороже/Дешевле плана на», под блоком — «по плану …» рядом с подытогом.
Показываются с первого вписанного факта, иначе это два одинаковых числа и «0 ₽».
Строкой словами: у скольких позиций факта ещё нет. `sumOf` и контрольные цифры
не тронуты — заведена отдельная `planSumOf`.

**Пункт 10 — расчёт не прижат в угол.** Долевая теперь не одна колонка, а все:
«Расчёт дороги» 607 → **324** у названия, числа разошлись по ширине; «Взаиморасчёты»
823 → **387**. Долям понадобилась явная наименьшая ширина (`minW` у `DataTable`) —
почему, разобрано в уроке **У-74**.

**Единая форма (У-54) — маршрут стал матрицей.** Лента `<ol>` с ниткой между
кружками заменена на ту же матрицу, что в «Сборах»: липкая колонка (время,
название, описание, адрес, «на карте»), «Пройдено», столбец на человека,
действия. Связь с картой цела — селектор строки переехал с `data-point`
на `data-hit`.

**Модель: `RoutePoint.o`.** Форма `Record<string, number>`, как `Gear.o` и `Buy.o`.
Слияние — **отдельной строкой** `jsonFields(a, b, ['o'])` в `pick('route')`,
умолчание `{}` в `normalizeDoc`. Копия поездки теперь чистит и распределение:
`gear.o`/`oby`, `buy.o`/`who`, `route.o`.

### Ответы заказчика 05.08.2026 (спрошено прямо, ответы дословно)

| вопрос | ответ |
|---|---|
| столбцы людей в «Меню» | **не нужны** — ни «кто готовит», ни «кто несёт» |
| отметка человека в «Маршруте» | **нужна: кто именно едет этой точкой** (привязки к технике мало) |
| отметки при копировании поездки | **сбрасывать** |
| выкладка | «сначала закрой правки, потом выложи» |

### Замеры живьём (песочница `vuoksa2026-test`, боевая строка не тронута)

| таблица | 1280: сетка / блок | 390: сетка / блок |
|---|---|---|
| Сборы | 1205 / 1205 | 712 / 357 |
| Закупка | 1215 / 1215 | 1028 / 357 |
| Расчёт дороги | 1215 / 1215 | 784 / 357 |
| Взаиморасчёты | 1215 / 1215 | 536 / 357 |
| Вопросы | 1215 / 1215 | 384 / 357 |
| Маршрут (новая) | 1205 / 1205 | 664 / 357 |

Горизонтального скролла у страницы нет ни на одной ширине: 1265 = 1265 и 390 = 390.
Липкая колонка маршрута на 390 — 208 px при видимых 357, все её органы внутри
клипа (У-64). Кнопки действий 44 × 44, `elementFromPoint` отзывается в центре
и на ±21 px. Контраст значков со снятыми переходами: светлая **7,51**, тёмная
**4,67** при норме 4,5.

Живой прогон: вписал факт 700 ₽ при плане 550 у позиции из трёх штук —
счёт 26 005 → 26 455, «Дороже плана на **450 ₽**», подытог блока «по плану
5 660 ₽ · 6 110 ₽». Вернул обратно — 47 390 ₽ и 11 848 ₽ на экране сошлись.
Отметка «Миша едет точкой» доехала на сервер: `route[0].o = {"mis":1}`.
Под ключом участника: в «Сборах» корзины нет, вставка есть; в «Закупке» то же.

### Проверки

`check-sums.mjs` — двенадцать строк сходятся. `tsc --noEmit` — без вывода.
`oxlint` — 10 предупреждений, все прежние `only-export-components`, ошибок ноль.
`build` (450,18 КБ) и `build:offline` (1 267 КБ) прошли.

### ⚠️ Грабли выкладки, купленные этой сессией

Первая проверка свежести пошла **во время** сборки Cloudflare, файла ещё не было,
и край закешировал на его путь SPA-заглушку с `max-age=31536000, immutable`
(`text/html`, 20 005 байт, `cf-cache: HIT`, восемь попыток подряд). Разобрано
уроком **У-73**; лечится следующим коммитом — `__BUILD__` меняет содержимое
бандла, а с ним и хеш имени. Проверять свежесть только через `?cb=<случайное>`.

### Что из брифа осталось

Пункты 5 (права по образцу Телеграма), 6 (даты и адрес заводятся один раз),
11 (добавление точки на карте), 12 (поиск целиком), 15 (первый заход),
16 (ссылка на товар — части А и Б). Единая форма: «Меню» ещё карточками —
привести к общей форме **без столбцов людей** (ответ заказчика). Разбор
и порядок — в `PROMPT-NEXT.md`.

## 🎨 05.08.2026 — ПЕРЕОСМЫСЛЕНИЕ ДИЗАЙНА, первая порция: пункты 1–4 и 13 брифа

Разбор заказчика — `docs/BRIEF-2026-08-05.md`. Закрыто пять пунктов из шестнадцати,
два коммита (`3cc48a5`, `2a2321b`). **На боевой не выложено** — ждёт слова заказчика:
это первая правка, которую он увидит глазами, а не по описанию.

### Что сделано

**Пункт 1 — обложка и карта двумя одинаковыми блоками.** Карта **переехала**
из «Дороги» в «Поездку» (спрошено у заказчика прямо, его ответ — «переезжает»).
Второго экземпляра не заводили: это два места правки одних и тех же точек и вторая
живая карта Google на странице. В «Дороге» остались лента точек со временем
и расчёт дороги. Порядок разделов заказчик уже получил: поездка → команда → сборы.

**Пункт 1 (вторая часть) — «длинная фотография, функциональность под ней».** Обложка
перестроена: широкая фотография сверху (4 : 3), под ней панелью — название,
подзаголовок, даты, места, четыре суммы, погода. Текст больше не лежит поверх снимка,
и снимок не закрыт наполовину тёмной плашкой. На фотографии остались две плашки:
обратный отсчёт слева и смена обложки справа.

**Пункт 2 — цифры на одной линии.** Причина была не в отступах, а в длине подписей
(урок **У-70**): «Транспорт, лодка, парковка» занимает две строки, «Продукты» — одну,
и число уезжает ниже соседнего. Лечится сеткой: у каждой плитки два ряда внешней
сетки (`grid-rows-subgrid`).

**Пункт 3 — погода читается как погода.** Над лентой дней стоит «Погода на дни
поездки» со значком. Ряд «10.08 · 20° / 14°» сам по себе не говорил, что это прогноз.

**Пункт 4 — место поездки показывается на карте, и участнику тоже.** Рядом с названием
места кнопка «Показать на карте» (`askMapLook()` в `lib/mapfocus.ts`), карта наводится
на координаты. ⛔ Кнопка доступна **всем**, включая участника: «участники могут
просмотреть, что это такое. Менять они не смогут» — правка названия по-прежнему
только у владельца и редактора. Место без координат кнопки не получает вовсе.

**Пункт 13 — разнобой шрифтов.** Замер карточки участника: шесть кеглей, из них
16 px и 14 px вне шкалы проекта. Источник оказался не в карточке, а в `ui/drawer.tsx`,
`ui/sheet.tsx`, `ui/dialog.tsx` — заводские `text-base` и `text-sm` из shadcn, то есть
разнобой стоял во **всех** шторках сервиса (урок **У-71**). Стало: видимых кеглей
четыре (13 / 15,5 / 20 / 24), все из шкалы. Заодно выправилась шапка поиска —
15,5 / 13 вместо 16 / 14 (часть пункта 12).

### Замеры живьём (песочница `vuoksa2026-test`, боевая строка не тронута)

| что | 1280 | 390 |
|---|---|---|
| обложка | 24…620 × 874 | 16…374 × 738 |
| карта | 644…1241 × 874 | 16…374 × 460 |
| высоты блоков равны | **да**, до пикселя | блоки друг под другом |
| фотография | 595 × 446 | 356 × 268 |
| подписи плиток | все на линии 848 | по одной линии на ряд |
| числа плиток | все на линии 882 | 665 и 730 |
| скролл страницы | 1265 = 1265 | 390 = 390 |
| кнопка места | — | 44 × 44, `elementFromPoint` отзывается |

Контраст со снятыми переходами (У-55): светлая **14,72 / 7,51**, тёмная
**8,91 / 4,67** при норме 4,5. Под ключом участника: показ места на карте есть,
кнопок смены обложки, дат и правки названия нет вовсе.
Карт в «Дороге» — **ноль**, строка «Карта Google · сборка …» теперь в `sec-trip`.
Названия всех шести разделов целы, полосы не сдвинулись.

⚠️ **Тайлы карты в этой среде не рисуются вовсе** (нет доступа к `googleapis`):
проверена раскладка и поведение, но не картинка карты. Смотреть на боевом.
⚠️ Скриншот в этой среде не снимается («the Browser pane is not displayed»),
вердикт по виду вынесен замерами.

### Проверки

`check-sums.mjs` — двенадцать строк сходятся. `tsc --noEmit` — без вывода.
`oxlint` — 10 предупреждений, все прежние `only-export-components`, ошибок ноль.
`build` (448,54 КБ) и `build:offline` (1 266 КБ) прошли.

### ⚠️ Служебная строка воскресла сама — У-69 подтверждён второй раз

Прошлая сессия удалила `vuoksa2026-test` в 07:40. В начале этой она **нашлась снова**
с меткой **08:00:02** — вкладка, оставшаяся жить после уборки, завела её заново.
Боевая строка при этом цела: метка `07:40:32`, автор «Макс», люди 4 · сборы 99 ·
закупка 53 · маршрут 9 · техника 4 · обложка 428 347 байт.
Порядок уборки из У-69 (сначала вкладка, потом сервер, потом строка) соблюдён и здесь.

### Что из брифа НЕ сделано

Пункты 5 (права по образцу Телеграма), 6 (даты и адрес заводятся один раз),
7 (плюсики и удаление видны только при наведении), 8 (пустое пространство справа),
9 (план и факт рядом), 10 (расчёт прижат в угол), 11 (добавление точки на карте),
12 (поиск целиком), 15 (первый заход), 16 (ссылка на товар — части А и Б).
Разбор и порядок — в `PROMPT-NEXT.md`.

## ✅ 05.08.2026 — ВЫЛОЖЕНО: сид больше не возит поездку, ключи заменены (задача 1)

**Выложено на боевой** — `e57cf86..9db8244`, четыре коммита: закрытие листа
от неопознанных (`3190c6f`, ждал с прошлой сессии), разрез сида и починка слияния
(`98a910c`), задание сессии (`4a045a6`), заголовок вкладки (`9db8244`).

**Свежесть сверена по содержимому (У-42):** `/assets/index-DN8PW3to.js`,
**447 965 байт** (было 700 867). В нём есть «Открываю лист» и «Этот лист закрыт»;
`zdzua343`, `pc2s6bsg`, `9ak33b2w`, `idz7qw3y`, «Суп из бичей», «Юбилейная поездка» —
**ни одного совпадения**. Заголовок отданной страницы — `<title>Pine-to-Pine</title>`.

### ⛔⛔ Личные ключи заменены — старые ссылки погасли

Решение заказчика 05.08.2026: «менять все четыре». Ключи, побывавшие в публичной
сборке, считаются известными посторонним навсегда.

| кто | было | стало | права |
|---|---|---|---|
| Макс | `zdzua343` | **`nk6xafd6`** | владелец |
| Костя | `pc2s6bsg` | **`8sqsfh85`** | редактор |
| Миша | `9ak33b2w` | **`prs90sd7`** | участник |
| Женя | `idz7qw3y` | **`y0usnlqy`** | участник |

Новые ссылки: `https://pine-to-pine.com/?u=Maks&k=nk6xafd6` ·
`?u=Kostya&k=8sqsfh85` · `?u=Misha&k=prs90sd7` · `?u=Zhenya&k=y0usnlqy`.
**Косте, Мише и Жене надо переслать их ссылки** — старые теперь показывают
«Этот лист закрыт».

Записано условной записью через `trip_write` по метке `2026-08-04T22:19:18.111Z`,
новая метка `2026-08-05T07:40:32.360Z`, автор «Макс». Сверено до и после:
люди 4 · сборы 99 · закупка 53 · маршрут 9 · техника 4 · аренда 2 · идеи 10 ·
меню 5 · обложка 428 347 байт · координаты 61.04/30.14 · `secTitles` 5 ·
`tileLabels` 4 · фотографии 286 871 / 206 555 / 157 591 / 208 639 — **всё до байта
как было**, изменились только `key` и `ua` у четверых.

### Живая проверка на БОЕВОМ (не на песочнице)

| заход | что показано |
|---|---|
| голый `https://pine-to-pine.com/` | «Открываю лист», затем **«Этот лист закрыт»**, 887 знаков, ни одного данного поездки |
| `?u=Maks&k=nk6xafd6` (новая) | лист, 6 разделов, 99 сборов, 53 закупки, 9 точек, 4 машины, обложка на месте |
| `?u=Maks&k=zdzua343` (утёкшая) | **«Этот лист закрыт»**, 901 знак, словами: «Ключ, с которым вы пришли, эта поездка не признала» |

После проверки вкладка уведена с боевого домена, её память очищена, служебный
работник снят.

### ⛔ Что ещё НЕ закрыто — задача 2 обязательна

Голый адрес показывает закрытый экран, **но документ он с сервера всё-таки читает**:
в памяти браузера после захода лежит 1 328 517 байт. `SELECT` в базе по-прежнему
открыт всем — это вторая дверь без первой. Прямой запрос
`GET /rest/v1/trips?id=eq.vuoksa2026&select=data` с публичным ключом отдаёт весь
документ кому угодно.

**Теперь `docs/rls-apply-e.sql` даёт настоящую защиту** (ключи заменены, сборка чиста)
и его надо выполнить. Из среды нечем: MCP Supabase требует авторизации, которую
в неинтерактивной сессии не пройти, `service_role`-ключа в проекте нет.
Перед применением — проверить тип колонки `trips.id` (при `varchar`/`uuid` функция
упадёт с 42804) и тем же файлом заполнить
`owner_email = 'starobormaksim@gmail.com'`.

### Что сделано

* **Сид разрезан на два.** Приложение импортирует `app/src/data/seed-base.json`
  (6 291 байт) — только строение листа и справочники: разделы сборов и закупки,
  10 видов транспорта, 13 единиц, единицы расхода, категории аренды, цены топлива,
  валюта, умолчания расчёта. **Ни одного человека, ключа, сбора, закупки, точки
  маршрута, блюда и машины.**
* **Полный образец жив и лежит ВНЕ `src/`** — `app/fixtures/seed-sample.json`.
  Сборка его не видит физически, а не по договорённости. Ключи внутри заменены
  на выдуманные (`obrazec1…4`). Его читает с диска `check-sums.mjs`.
  `migrate.mjs` теперь пишет туда же.
* **Замер сборки:** файл кода **700 867 → 448 010 байт**. Поиск по всему `dist`
  и `dist-offline`: `zdzua343`, `pc2s6bsg`, `9ak33b2w`, `idz7qw3y`, `obrazec`,
  `data:image/jpeg`, «Суп из бичей», «Юбилейная поездка» — **ноль совпадений**.
  Осталось «Вуокса · 2026» в `<title>` и `meta description` — вопрос заказчику.

### ⛔ У-68 — правка чуть не съела лист: из 103 сборов приезжало 3

Найдено живым замером на чистом браузере сразу после разреза сида, до выкладки.
В `merge.ts`, в ветке «этой позиции у меня нет», стояло
`if ((del[k+':'+x.i] || 0) >= (x.ua || 0)) return`. Метки удаления нет — это `0`;
позиция, которую никто не правил руками, имеет `ua: 0`. `0 >= 0` истинно — и позиция
молча не приезжает. Пока сид был полной копией поездки, в эту ветку заводские позиции
не попадали вовсе.

Считано поимённо: на сервере 103 сбора, из них с `ua: 0` — 100 → в браузер приехало **3**.
Закупка 53 − 51 = **2**. Техника 4 − 4 = **0**. Маршрут и люди (у всех `ua > 0`) — целы.
Сходимость до штуки и назвала причину. Ещё минута — и обеднённая копия ушла бы на сервер.

Починено в двух местах (`pick()` и ветка участников): сначала «метка существует»,
потом сравнение — ровно как в проходе по своим позициям ниже. Прогнано четырьмя случаями:
без метки и `ua: 0` → приезжает · метка 1000 > правки 500 → не воскресает · метка = правке
→ не воскресает (прежнее поведение) · правка 900 > метки 500 → приезжает.

### Ещё две мины того же корня, закрытые заодно

* **Пустая копия больше не создаёт строку на сервере.** `Sync.pull` на пустом ответе
  делал «строки нет — создам из своего». С сидом без данных это означало записать
  поверх поездки **пустой документ**. Теперь строку заводит только копия, в которой
  есть люди; иначе человек читает словами «лист не пришёл — откройте свою личную
  ссылку». Родня У-07 и У-67.
* **Экран `components/auth/OpeningList.tsx`** — «Открываю лист» с кружком.
  Без него первое, что читал человек по совершенно правильной ссылке, было
  «Этот лист закрыт»: людей в сиде нет, значит в первую секунду ключ сверять не с чем.
  Ложный отказ выглядит как поломка. Ворота смотрят на новое `opened` — первое чтение
  закончилось чем угодно.

### Живая проверка (песочница `vuoksa2026-test`, боевая строка не тронута)

Память браузера чистилась с адреса `/favicon.svg`: если чистить со страницы приложения,
его отложенная запись (300 мс) возвращает документ обратно, и проверка врёт.

| заход | что показано | утечка данных на экране |
|---|---|---|
| чистая память, `?u=Kostya&k=pc2s6bsg` | лист, 6 разделов | — |
| чистая память, `?u=Maks&k=zdzua343`, чтение задержано | **«Открываю лист»**, 0 разделов, 113 знаков | нет |
| чистая память, `?u=Maks&k=nepravilny` | «Этот лист закрыт», 901 знак | нет |
| чистая память, голый `/?sandbox=1` | «Этот лист закрыт», 887 знаков | нет |

После починки слияния в чистый браузер приехало: люди 4 · сборы **103** · закупка **53** ·
маршрут 8 · техника 4 · меню 5 · аренда 3 · идеи 10 · канистры 2 · виды 10.
Семь меток удаления в документе проверены поимённо — ни одна позиция не воскресла.

Геометрия экрана ожидания: на 1280 заголовок 549…732 × 30, кружок 20 × 20,
скролл 1280 = 1280; на 375 объяснение 16…359 (влезает целиком), скролл 375 = 375.
Контраст со снятыми переходами (У-55): светлая **13,86 / 7,07 / 6,16**,
тёмная **11,04 / 5,79 / 6,64** — при норме 4,5.

### Проверки

`check-sums.mjs` — двенадцать строк сходятся (330 · 21 385 / 26 005 / 47 390 / 11 848 ·
2 канистры · Σ уплачено 47 390 · Σ доля 47 390 · Σ баланс 0 · то же на экране ·
связных строк 4 из 4). `tsc --noEmit` — без вывода. `oxlint` — 10 предупреждений,
все прежние `only-export-components`, ошибок ноль. `build`, `build:offline`,
`publish-v2.mjs` прошли.

Боевая строка `vuoksa2026` на выходе такая же, как на входе: метка
`2026-08-04T22:19:18.111Z`, автор «Макс», обложка 428 347 байт, люди 4, сборы 99,
закупка 53, маршрут 9, техника 4. ⚠️ Строка `vuoksa2026-test` вопреки прошлой записи
удалена НЕ была — она нашлась с меткой `2026-08-05T07:01:13`; на ней и шли проверки,
после работы удалена (в таблице осталась одна строка `vuoksa2026`). Сервер остановлен.

### Что осталось на заказчике (задача 1, шаг 4)

Ключи `zdzua343`, `pc2s6bsg`, `9ak33b2w`, `idz7qw3y` побывали в публичной сборке
и в публичном репозитории — считаются утёкшими навсегда. Заменять их или нет — его
решение: новые ключи означают новые ссылки Косте, Мише и Жене. Меняются одним нажатием
в разделе «Команда», делаю сам. **Пока ключи не заменены, `docs/rls-apply-e.sql`
защиты не даёт** — посторонний возьмёт ключ из старой сборки и позовёт `trip_read`
совершенно законно.

## 🔴🔴 05.08.2026, ночь — НАЙДЕНА НАСТОЯЩАЯ ДЫРА: сид с ключами лежит в файле сайта

**Проверено запросом к боевому домену, а не предположено.** Файл кода
`https://pine-to-pine.com/assets/index-*.js`, 700 867 байт, содержит:
личные ключи всех четверых (`zdzua343`, `pc2s6bsg`, `9ak33b2w`, `idz7qw3y`), имена,
105 позиций сборов, 53 закупки, маршрут, технику. Посторонний читает поездку,
**не сделав ни одного запроса к базе**, и берёт оттуда ключ владельца.

Источник — `app/src/data/seed-v2.json` (260 397 байт), он статически импортируется
приложением и потому попадает в сборку. Репозиторий публичный — те же ключи в нём.

Нашли независимо друг от друга состязательный рецензент и живой тестировщик, оба
вернули работу. Подтверждено `curl`-ом. Урок **У-65**.

⛔ **`docs/rls-apply-e.sql` выполнять НЕЛЬЗЯ, пока это не закрыто.** Он заперт корректно,
но ключ от замка висит рядом на гвозде: посторонний возьмёт ключ владельца из файла
сайта и вызовет `trip_read` совершенно законно. Порядок — в `PROMPT-NEXT.md`, задача 1.

### Что при этом СДЕЛАНО и выложено

**Задача 1, шаги 1 и 2 закрыты.** Приложение больше не показывает лист неопознанному
человеку и умеет читать через функцию базы.

* **`lib/supabase.ts`** — `rpcTripRead()`, `rpcTripOwner()`, `rpcTripList()` и общий
  вход `loadTrip(key, trip)` с откатом на прямое чтение при 404. Все ветки прогнаны
  на подставном сервере (десять проверок): ключ подошёл · чужой ключ · пустой ключ ·
  сервер лёг · функции нет · повторный заход · `RpcMissing` у самой функции.
* **`loadTrip()` на пустом ответе прямого чтения ПЕРЕСПРАШИВАЕТ функцию.** Без этого
  закрытый SELECT (PostgREST отвечает `200 []`, а не отказом) читался бы как «строки
  нет», и приложение создало бы её заново из местной копии — то есть затёрло бы лист
  всей команды ровно в минуту применения SQL. Урок **У-67**, нашёл рецензент.
* **Экран `components/auth/ClosedList.tsx`** вместо листа, когда ключ не сошёлся:
  объяснение словами, поле «вставьте свою личную ссылку», вход по почте для владельца.
  Раскладка скопирована с `FirstStep`, ничего своего не сверстано.
* **Ворота стоят на `perms.authed`, а НЕ на `perms.me`.** Задание требовало `perms.me`,
  и это оказалось дырой: `perms.me` заполняется одним лишь ИМЕНЕМ из пути
  `/vuoksa2026/Maks`, а имя посторонний наберёт руками. Назвался ≠ опознан.
* **Попутно закрыто то, что этот же SQL сломал бы молча:** почта владельца
  (`lib/auth.ts`), список «Мои поездки» и аварийный `restore/restore.mjs` — все трое
  читали таблицу напрямую. Переведены на функции с откатом.
* **`docs/rls-apply-e.sql` разобран и исправлен:** заведена общая проверка права
  `trip_allowed(id, key)` — ключ **или** вошедший по почте владелец. Без второго пути
  владелец в чистом браузере попадал в замкнутый круг: чтобы получить ключ, нужен
  документ, а чтобы получить документ, нужен ключ.

### Живая проверка восьми входов (на 390 и 1280, обе темы)

| адрес | разделов | что показано |
|---|---|---|
| голый `/` без ключа в памяти | **0** | «Этот лист закрыт» |
| `?u=Maks&k=zdzua343` | 8 | лист |
| `?u=Kostya&k=pc2s6bsg` | 8 | лист |
| `?u=Misha&k=9ak33b2w` | 8 | лист |
| `?u=Zhenya&k=idz7qw3y` | 8 | лист |
| `/vuoksa2026/Maks` без памяти | **0** | закрытый экран |
| `/vuoksa2026/Maks` после полной ссылки | 8 | лист |
| `?u=Maks&k=nepravilny` | **0** | «Ключ… эта поездка не признала» |

В теле закрытого экрана данных поездки нет: `innerText` 887 знаков, ни «Вуокса»,
ни имён, ни сумм. Офлайн-копия (`file:`) открывается по-прежнему, 8 разделов —
ворота её не трогают.

Геометрия закрытого экрана: поля 48 px, кнопки 52 px (норма 44), `elementFromPoint`
отзывается в центре и на ±21 px у всех четырёх органов, горизонтального скролла нет
(390 = 390, 1265 = 1265). Соседи листа не поехали: шапка 0…56,7 на 390 и 0…64,7
на 1280, липкая полоса раздела на 80…136,7 и 96,7…153,3, обложка 356,7 и 558,7 px.

### ⛔ Цвет надписи пропадал у ВСЕХ кнопок сервиса — урок У-66

Заказчик 05.08.2026: «хотелось бы, чтобы цвет такой кнопки был светлым, а то плохо
видно его». Причина оказалась не в замысле. `tailwind-merge` знает свой список кеглей
(`text-sm`, `text-lg`, …); наши `text-body` и `text-note` в него не входят, поэтому
библиотека считала их классами ЦВЕТА и оставляла последний:

```
twMerge('text-on-accent', 'text-body')  →  'text-body'     ← цвет потерян
twMerge('text-on-accent', 'text-sm')    →  'text-on-accent text-sm'
```

Теряли цвет все четыре вида кнопки: `text-on-accent`, `text-ink`, `text-muted`,
`text-accent-text`. Живой замер до правки: **2,60 : 1** в светлой теме и **2,13 : 1**
в тёмной при норме 4,5. Починено в одном месте — `app/src/lib/utils.ts`, `cn()` через
`extendTailwindMerge` со списком кеглей проекта (семь штук, сверено с `index.css`).
Стало **5,66 : 1** в светлой и **6,49 : 1** в тёмной.

### Разбор заказчика, записанный целиком

`docs/BRIEF-2026-08-05.md` — пятнадцать пунктов его словами: обложка и карта двумя
одинаковыми блоками, цифры на разных уровнях, погода не читается как погода, точка
места для участников, права по образцу Телеграма, даты и адрес приезда заводятся один
раз, плюсики и удаление видны только при наведении, пустое пространство справа, план
и факт в закупке, расчёт прижат в правый угол, поиск, разнобой шрифтов в карточке
участника, длинная фотография обложки, первый заход. **Работа не начата.**

### Проверки

`check-sums.mjs` — двенадцать строк сходятся (330 · 21 385 / 26 005 / 47 390 / 11 848 ·
2 канистры · Σ уплачено 47 390 · Σ доля 47 390 · Σ баланс 0 · то же на экране ·
связных строк 4 из 4). `tsc --noEmit` — без вывода. `oxlint` — 10 предупреждений,
все прежние `only-export-components`, ошибок ноль. `build` и `build:offline` прошли.

Все замеры — на песочнице `localhost` (строка `vuoksa2026-test`), боевая строка
не тронута. Снимок до работы: 1 343 173 байта, метка `2026-08-04T22:19:18.111Z`,
автор «Макс», обложка 428 347 байт, координаты 61.04027 / 30.13915.
⚠️ Сборов в снимке 99 при 106 в прошлой сессии — это НЕ потеря: в документе лежат
восемь меток удаления `gear:*`, семь из них на прежние позиции. Заказчик чистил сам.

## 🔴 05.08.2026, ночь — ТРЕБОВАНИЕ: ЗАКРЫТЬ ЛИСТ ОТ ПОСТОРОННИХ. Работа не начата

Заказчик дословно: «чтобы не было доступа у человека, который зашёл на
pine-to-pine.com… чтобы в публичном доступе не была информация».

**Проверено запросом, а не предположено:** `GET /rest/v1/trips?id=eq.vuoksa2026&select=data`
с публичным ключом отдаёт весь документ — имена, фотографии, деньги, маршрут.
Голый адрес вдобавок даёт права **владельца** (поведение первой версии, У-01).

⛔ **Клиентской заглушки недостаточно, и делать только её — вредно:** `anon`-ключ лежит
в коде сайта, то есть публичен по своей природе. Спрятать лист на экране, оставив дверь
открытой, — ложное чувство защиты, а оно хуже честно открытой двери.

Готово к работе: **`docs/rls-apply-e.sql`** — снимает все политики `SELECT` и заводит
`trip_read(id, key)`, которая сверяет ключ со списком людей внутри документа (как уже
работающая `trip_write`). ⛔ Применять только ПОСЛЕ того, как код научится читать через
функцию, иначе сайт погаснет у всей команды, включая владельца. Порядок из четырёх шагов —
первой задачей в `PROMPT-NEXT.md`.

Работа не начата осознанно: права доступа — та зона, где наспех проверенная правка
дороже суток ожидания (У-49). Из среды SQL применить нечем — MCP Supabase требует
авторизации, которую в неинтерактивной сессии не пройти.

## ⚡ 05.08.2026, ночь — ВЗАИМОРАСЧЁТЫ, карта во всю ширину, подпись раздела правится

**Выложено на боевой** (коммиты `4542c6d` и `efca23a`). Свежесть сверена по содержимому,
а не по хешу (У-42): `/assets/index-D3wcbVrd.js`, 700 868 байт, в нём «Взаиморасчёты»,
«Скинулись поровну», «Подпись раздела», «Железная дорога», «Делится на всех».
⚠️ Первые ~6 минут край Cloudflare отдавал на запрос кода заглушку `text/html` —
известное поведение сразу после выкладки, кода не касается.

Закрыты задачи 1, 2, 3, 4 и часть 6 задания. **Задача 5 (единая форма «Меню» и «Дороги»)
не начата** — причина и план ниже.

### Подпись раздела снова правится (задача 1)

Значение из `S.secTitles[].sub` было цело в документе, но править его было негде —
подпись убрана с экрана по слову заказчика. Теперь она правится **вторым полем**,
которое появляется при переименовании раздела; на экране её по-прежнему нет.

Сделано общим кирпичом: у `InlineText` появился необязательный проп `second`.
Все два десятка прочих мест вызова работают как работали — проп не задан.
⚠️ Уход фокуса **внутри** правки (Tab с названия на подпись) её не закрывает:
проверка идёт по `relatedTarget` внутри коробки.

Замерено на 390: два поля 302 × 39 и 302 × 44, оба отзываются на `elementFromPoint`,
кнопка «знаки» рядом цела (330…374, 44 × 44), горизонтального скролла нет (390 = 390).
Сохранение проверено живьём: `secTitles.gear` остался объектом `{h, sub}`, соседние
разделы не тронуты.

### ⛔ ВЗАИМОРАСЧЁТЫ — главное этой сессии (задача 2)

Прежняя догадка «весь бюджет делится на всех поровну» отменена. Модель теперь такая,
как её описал заказчик: **у траты есть плательщик и круг делящих**, а зачёт — классический.

* **`app/src/lib/settle.ts`** — новая `shares(S)`. `perPerson()` не тронута ни строкой.
* **Плательщик закупки берётся из уже существующего `Buy.o`** («кто сколько покупает»).
  Новых органов правки под это не понадобилось вовсе: колонки людей в матрице «Закупки»
  и есть плательщики. Поэтому зачёт заработал на боевых данных сразу.
* **Личное** (`BuySection.personal` — алкоголь, сигареты) в зачёт не идёт: признак
  один на весь сервис, `personalSecIds` вынесен из `calc.ts` наружу.
* **Новые поля `payer` и `sp`** у `Transport`, `Rent` и `Buy`. Оба необязательны,
  и **пустое значение = сегодняшнее поведение** — контрольные цифры не сдвинулись.
  Оба заведены в слиянии в трёх местах: `PLAIN.transport`/`PLAIN.rent` (payer),
  `PLAIN_JSON` (sp — массив, через `!==` он сравнивался бы неверно), список полей
  и `jsonFields` в блоке `pick('buy')`.
* **Три новых инварианта** в `check-sums.mjs`, шесть прежних не тронуты:
  `Σ уплачено = 47 390`, `Σ доля = 47 390`, `Σ баланс = 0` (до копейки).
* **Место, где человек читает свою сумму словами** — блок «Взаиморасчёты» в «Дороге»:
  «Вы должны 1 596 ₽» первой строкой, под ней матрица (люди слева, «Выложил / Его доля /
  Итог» справа) и переводы словами: «Макс отдаёт Косте 1 596 ₽».
* **Правка на месте, без шторок:** в строке техники и аренды стоят «Платит»
  (`InlinePick` — список раскрывается ПОД значением) и «Делится» (`ToggleGroup
  type="multiple"` из shadcn). Своей разметки не написано.

Проверено живьём: выбор Кости плательщиком аренды лодки (10 000 ₽) поднял его итог
с 2 484 до 9 984 ₽, общий бюджет остался 47 420 ₽; круг «только Миша» поднял его долю
с 9 355 до 19 355 ₽ и Σ доля осталась равной бюджету. Возврат к «скинулись поровну»
вернул числа один в один.

⚠️ **Дефект языка, найденный живой проверкой:** печаталось «Макс отдаёт **Костя**».
Заведена `dative()` в `app/src/format.ts`; имя, которое под правила не подходит,
возвращается как есть с признаком `sure:false`, и фраза перестраивается без падежа.

### Карта во всю ширину контейнера, лента точек под ней (задача 3)

Дословно: «карта крупная, было бы круто, если бы она была на всю ширину контейнера
контента». Колонка `lg:grid-cols-2` в `RouteBoard` снята. Куда девать ленту, заказчик
оставил за нами — она уехала **под карту**, то есть в тот же порядок, что уже был
на телефоне: сначала видно, куда едем, потом когда.

Замерено на **1280**: карта 24…1241 (1217 px) — ровно ширина контейнера контента,
`#sec-road h2` те же 24…1241. Лента под ней, та же ширина. Полоса раздела 0…1265 × 57 —
не сдвинулась. «Расчёт дороги» 24…1241, «Взаиморасчёты» 24…1241. Скролл страницы
1265 = 1265. На **390**: карта 16…374 (358), лента под ней 358, скролл 390 = 390.

**Железная дорога и автобус** добавлены видами в `S.kinds` сида (`rateU:'fix'`) —
доехали в документ через `mergeSeed`, видов стало 10. ⛔ `RateUnitId: 'ticket'` не
заведён: у `litres()` стоит `default:` на литры от пробега, и незнакомая единица
молча посчитала бы бензин. У обоих видов подсказка словами: топлива нет, деньги
за билет заводятся строкой в «Аренде» — там есть цена, количество и теперь плательщик.

### Пятая ветка маршрута больше не повторяет первую (задача 4, этап Э2)

`toneAt()` выдавал тон и штрих связанной парой из пяти, своих пар четыре — пятая
техника получала пару первой. Развязано: цвет по `index % 4`, штрих по `index / 4`,
латинским квадратом. Проверено прогоном: **16 сочетаний подряд без единого повтора**,
первые четыре байт в байт прежние (главная диагональ квадрата) — ни у кого карта
не перекрасится.

⚠️ Сочетаний 16, а не 20, как прикидывал план: `solid` намеренно оставлен **только**
общей нитке. Отдай его ветке — и ветка отличалась бы от общей одним лишь цветом,
а это ровно то, что запрещает WCAG 1.4.1 и ради чего штрихи вообще заведены.

### Хвост: офлайн-копия непервой поездки (задача 6)

Загрузчик читал `localStorage['flops.doc']` строкой. Теперь ключ едет вместе с данными
(`__PINE_KEY__` в скрипте `pine-doc`), а не с загрузчиком: загрузчик вшивается один раз,
а данные переписываются при каждом сохранении. Старая копия ключа не найдёт и останется
на прежнем поведении. `lastIndexOf` не тронут (У-41).

### Что НЕ сделано и почему

* **Задача 5 — единая форма «Меню» и «Дороги» — не начата.** Это не мелочь: «Меню»
  и «Маршрут» людей не показывают вовсе, значит нужны новые поля модели («кто готовит»
  у блюда, `RoutePoint.who` у точки), а с ними — три места в слиянии на каждое поле
  и снос восьми шторок в «Дороге». Начинать это в конце сессии — ровно тот случай,
  от которого предостерегает урок У-49; половина такой работы хуже, чем её отсутствие,
  потому что оставит два способа править одно и то же.
* **Остальные хвосты задачи 6:** «Попросить отметить», полоса хода дублирования,
  второе перечисление точек в `TripMap` при отсутствии сети.
* **`owner_email` за `starobormaksim@gmail.com`** — из этой среды не поставить:
  прямая запись в `trips` закрыта RLS, а `trip_write` пишет только `data`; нужен
  SQL Editor.

### Состязательный рецензент вернул работу — восемь находок закрыто

Дифф вышел за 200 строк, поэтому кроме тестировщика его смотрел рецензент, который
видит только код и критерии. Он вернул работу, и по делу:

1. **Числа спорили друг с другом на одном экране.** «Выложил 10 251, доля 11 848» —
   разность 1 597, а в столбце «Итог» стояло −1 596. И Женя по переводам отдавал
   888 + 709 = 1 597 ₽ при своём итоге −1 596. Округление каждой величины по
   отдельности рвёт связи между ними. Заведена `wholeSettle()`: округление делается
   **один раз и согласованно** — уплачено и доля раздаются «наибольшим остатком»
   на один итог, баланс считается уже из них, переводы строятся из целых балансов.
   На экране теперь: 10 259 − 11 855 = −1 596 у каждого, Σ итога 0, переводы сходятся
   с балансами. Проверено машиной — три новые строки в `check-sums.mjs` (всего 12).
2. **Офлайн-копия: я обменял гипотетический дефект на настоящую потерю.** Поменял ключ
   ЧТЕНИЯ, а запись осталась прежней: внутри скачанного файла нет ни `?trip=`, ни
   `flops.trip`, поэтому приложение пишет в `flops.doc`. Копия непервой поездки
   перестала бы находить собственные правки. **Откачено**, хвост описан в коде и
   в `PROMPT-NEXT.md`: лечится только парой «читать и писать по одному ключу».
3. **Взаиморасчёты не видели прежнего покупателя.** Таблица «Закупки» читает `Buy.o`,
   а при его отсутствии — старое одиночное `who` (`buy/buylocal.tsx`). Зачёт читал
   только `o`. Человек видел себя отмеченным покупателем, а деньги ему не возвращались.
   Теперь признак один на оба раздела.
4. **Частичная отметка записывала всю сумму.** Отметил «беру 1 штуку из 3» — и в
   «уплачено» шли все 1 650 ₽. Теперь считается в единицах против `q`: прирост 413 ₽
   вместо 1 238 ₽, непокрытый остаток раскладывается «скинулись поровну».
5. **Личные покупки не показывались никому** — по той же причине, что и пункт 3.
   Проверено: пять личных позиций по 2 000 ₽ теперь разносятся по покупателям.
6. **Наведение с карты утаскивало страницу.** Пока лента стояла справа от карты,
   `scrollIntoView` был безобиден; под картой во всю ширину он уводил страницу
   на 351 px, и карта, по которой человек только что попал пальцем, уезжала из вида.
   Теперь двигается только собственная прокрутка ленты — замерено: `scrollY` 5 760
   до и после, верх карты 0 и 0, дальняя точка видна.
7. **Мёртвая ветка предупреждения** о расхождении зачёта убрана: расхождение возможно
   только при нуле людей, а этот случай перехватывается раньше и объяснён словами.
8. **Склонение имён и род в словах.** `dative()` коверкал «Павелу», «Пётру», «Леву»,
   «МИШКе», «Анна-Марии» — добавлены исключения с беглой гласной, а ЗАГЛАВНЫЕ, дефис
   и латиница честно отдаются без падежа. «Его доля» стало «Доля», «он доплачивает» —
   «доплачивает»: в команде бывают и женщины.

Плюс два хвоста той же природы: смена вида техники на поезд или автобус теперь
обнуляет литры и расход (иначе «5 л» от прежнего вида считались бы вопреки подписи
«топлива нет»), а круг делящих у позиции закупки получил свой орган — свёрнутую
строку «Делится на всех», которая раскрывается на месте. Без него `sp` у `Buy`
читался расчётом, но задать его было негде — половина функции.

Что рецензент проверил и признал целым: форма `secTitles` и `tileLabels`, `Transport.owner`,
`fillMissingKeys`, `perPerson`/`routeKm` и все контрольные цифры, `??` против `||`,
`toneAt` (16 сочетаний, `solid` ветке не достаётся), `lastIndexOf`, `isSandbox`,
`trip_write`, потолок шторок, шкала и бренд.

### Приёмка вернула два дефекта — оба исправлены

1. **Столбец «Итог» складывался в 1 ₽.** Точные балансы дают ноль, а округлённые
   по отдельности — нет: −1 596,34 + 2 483,99 + 708,69 − 1 596,34 = 0, а по рублям
   −1 596 + 2 484 + 709 − 1 596 = 1. Заказчик складывает столбец глазами, и лишний
   рубль он прочитает как ошибку счёта — на экране это и есть ошибка. Заведена
   `wholeBalances()` в `settle.ts`: округление «наибольшим остатком», поправка
   достаётся тому, кого она и так почти касалась. Проверено на пяти раскладах —
   Σ показанного везде ровно 0, наивное округление давало ±1.
2. **Значок ⌄ у выбора плательщика уезжал за край на 390 — навсегда.** Липкая колонка
   «Расчёта дороги» шире окна прокрутки (556 px против 357), а липкую колонку боковой
   прокруткой не сдвинуть. Потолок ширины органа опущен ниже видимой части: было
   right = 470 при клипе 373, стало 329. Четыре кнопки круга при этом остались
   в одну строку (25…299).

### Проверки

`check-sums.mjs` — **двенадцать** строк сходятся: 330 · 21 385 · 26 005 · 47 390 ·
11 848 · 2 канистры · Σ уплачено 47 390 · Σ доля 47 390 · Σ баланс 0 · Σ уплачено
**на экране** 47 390 · Σ баланс **на экране** 0 · связных строк 4 из 4.
`tsc` — без вывода. `oxlint` — 10 предупреждений, все прежние `only-export-components`,
ошибок ноль. `build` и `build:offline` прошли. Контраст новых надписей: светлая
7,51 и 14,72 : 1, тёмная 4,67 и 8,91 : 1 — замерено со снятыми переходами (У-55).

Отдельным прогоном проверено слияние: `payer` и `sp` доезжают у всех трёх коллекций,
устаревшая копия без этих полей их не стирает, осознанная очистка (пустая строка,
пустой массив) доезжает, одинаковый `sp` правкой не считается. И крайние случаи
зачёта: людей нет, круг из выбывшего, плательщик не из команды, шестеро в команде —
Σ уплачено везде равно бюджету, Σ баланса ноль, показанные рубли связны.

Все замеры — на песочнице, боевая строка не тронута: метка `2026-08-04T21:48:40`
и автор «Макс» какими были, такими и остались. Строка `vuoksa2026-test` после
работы удалена, сервер разработки остановлен.

## ⚡ 05.08.2026, вечер — ДАННЫЕ ЦЕЛЫ, полосы разделов, обозначения на месте

### ✅ Задача 1 закрыта: правок заказчик НЕ терял

Тревога «на сервере 8 точек, а страница показала 10 · 6 на карте» разобрана до конца
и снята. Снимок боевой строки (метка `2026-08-04T19:19:03.062Z`, автор **«Макс»** —
не пусто, признака беды нет):

| Раздел | Норма | Снимок |
|---|---|---|
| люди | 4 | 4 |
| сборы | 106 | 106 |
| закупка | 53 | 53 |
| «не забыть» | 10 | 10 |
| техника | 4 | 4 |
| меню | 5 | 5 |

Обложка `trip.hero` — 428 347 байт, координаты `trip.lat/lon` — **61.04027 / 30.13915**,
`secTitles` все пять, `tileLabels` все четыре, личные ключи четверых совпадают.

**Откуда взялись 10.** У Кости в 17:24 была вкладка со старой местной копией, ещё не
забравшая удаления 17:10:58. Она честно показывала уже удалённый тестовый мусор —
точку «Новая точка» (Мурино, `rpmsejg8nx`) и строку аренды «Кал» (`rnmsehi2ln`),
оба прямо названы мусором в записи от 04.08. Плюс две точки, прожившие **6 и 7 секунд**
(`rpmsegsd5d`, `rpmseh14xd`) — за такое время геокодер мастера не отвечает, это тестовые
клики.

**Координаты не терялись, и это проверено кодом, а не рассуждением.** `fields()`
в `merge.ts` копирует поле, только если `b[f] !== undefined`; отсутствующие `lat/lon` —
именно отсутствующий ключ (`RoutePointSheet.tsx:188-189` пишет `undefined`, а
`JSON.stringify` такие ключи выбрасывает). Значит копия без координат с более свежей
`ua` чужие координаты **не трогает**. `fillMissingKeys` в массивы не заходит вовсе —
первая же строка `if (Array.isArray(a) || Array.isArray(b)) return 0` (`merge.ts:107`).

**Заодно:** строки `probe-guard` в таблице **нет** — этот хвост можно вычеркнуть.
Забытая `vuoksa2026-test` от позапрошлой сессии удалена. Брошенных серверов не найдено.

### Полоса раздела прилипает, название крупное, подписи нет

Дословно: «полоска раздела остаётся липкой, прилипает к верхней полоске меню; как только
я заканчиваю этот раздел, следующий тоже прилипает». Сделано `position: sticky` внутри
своей `<section>` — поведение получается бесплатно. Замер «Сборы» → «Закупка»:
полоса «Сборов» держится на 64 всю дорогу по разделу, к его концу уезжает на −88, и её
место занимает полоса «Закупки» на 64. Одновременно видна ровно одна.

Высота шапки уехала в **одно число `--header-h`** в `index.css` (56 px + безопасная зона
на мобильном, 64 на десктопе). От него теперь зависят `top` полосы, `scroll-margin-top`
секций и распорка под `fixed`-шапку — раньше каждое место знало это число по-своему.

Подпись под названием убрана с экрана по слову заказчика. **Значение из `S.secTitles[].sub`
в документе цело, форма хранения не тронута** (У-04, У-08). Побочное следствие: править
подпись теперь негде — вопрос вынесен заказчику (урок У-56).

### Условные обозначения — в разделе, а не за нажатием

Шторка `GearLegendSheet` удалена. Обозначения стоят в полосе своего раздела: на 1280
строкой справа с подписями, на 390 раскрываются **на месте под полосой** по кнопке
44 × 44 (полоса растёт с 56,7 до 136,7 px). Никакого попапа. У «Закупки» своя легенда:
что попадает в сумму, что факт важнее плана, что означает цифра в колонке человека.

### Дубли снесены

* **`components/trip/ReadyLeft.tsx` удалён** (У-53). Он перечислял позиции `S.gear`
  чужого раздела и вдобавок печатал названия его групп. Обложка заняла всю ширину.
* **Строка «Мои поездки» над обложкой убрана** — ровно такой же пункт уже был в меню «⋯».
  Один орган — одно место (новый урок У-58).
* **Строка «Вы — Макс» с рядом присутствия убрана из потока** — переехала знаком в шапку.

Полный обход сервиса показал, что других мест, где рисуется перечень позиций чужого
раздела, **нет**. Пограничные случаи (поиск, мастер разметки маршрута, список точек
в `TripMap` при отсутствии сети) перечислены в `PROMPT-NEXT.md`.

### Знак присутствия в шапке, и почему в нём только лица

Заказчик: «под собой должна быть индикация, кто кроме меня находится на сервисе».
Сделано `PresenceStack variant="chip"`: своё лицо первым с янтарным кольцом, остальные
внахлёст за ним.

⛔ **Словами в полосу меню это писать нельзя** — ровно такая строка однажды съела ширину
у названий разделов (У-11). Замерено на 1280: все шесть названий целы, `scrollWidth`
равен `clientWidth` у каждого. На 390 знак ограничен одним лицом и «+N»: в полосе
доступно 358 px, знак сервиса 162 + три кнопки 132 + просветы 8 = 302, на присутствие
остаётся 56. Худший случай (четверо) проверен подстановкой настоящего узла: знак вырос
до 42 px, **соседи не сдвинулись ни на пиксель**, переполнения нет.

### Прочее

* **Карточки участников на телефоне — во всю ширину.** 358 × 358, снимок `object-contain`,
  не кадрируется. Было ≈ 170 («узенькие», говорил трижды). Вторая колонка возвращается
  с 640 px, четыре — с 1024.
* **Плашка под знаком Pine-to-Pine убрана.** «Выглядит убого… какая-то гигантская плашка
  под ним» — прямоугольник `hover:bg-zebra` вырастал под знаком в 55 × 190 px. Наведение
  показывает сам знак.
* **«Цена, план» и «Цена, факт».** Обе колонки были в модели (`Buy.pr` и `Buy.prf`),
  но назывались «Цена» и «По факту» — по этим словам не прочитать, что одна прикидка,
  а вторая магазинная. Модель данных не тронута. Назначение позиции одному или нескольким
  участникам (`Buy.o`) тоже было — оно и есть «общие и индивидуальные закупки».
* **Полосы прокрутки сведены к одному виду** — тонкие, 8 px, цвет от `--line-strong`.
  Убрать внутренние нельзя: на них держится матрица вещь × люди на телефоне.
* **`WhoAmI.tsx` удалён** — осиротел после переезда личности в шапку; объяснения словами
  даёт `PermNotice`.

### Сборка и карта

* **Найдена настоящая дыра в `_headers`.** Правило сверяется с ЗАПРОШЕННЫМ путём, а не
  с тем, что отдал `_redirects`. Замер боевого домена: `/index.html` получал наш
  `no-cache`, а `/` и `/vuoksa2026/Maks` — **нет**, вместо него шла догадка Cloudflare
  по расширению последнего куска пути (для похожего на файл — кеш на 4 часа). Закрыто
  явными правилами. ⚠️ Слить их с `/assets/*` в одно `/*` нельзя: при двойном совпадении
  Cloudflare складывает `Cache-Control` через запятую и годовой кеш кода сломался бы.
  ⚠️ Правило `/vuoksa2026/*` привязано к имени одной поездки — при второй дописывается
  вторая строка.
* **`SKIP_WAITING`** — страница теперь сама толкает ждущего работника, а не полагается
  только на то, что он подстегнёт себя сам. Плюс обработчик `pageshow` с `event.persisted`:
  при возврате из bfcache, когда `visibilitychange` может не выстрелить, обновление
  проверяется принудительно.
* **Под картой печатается ВСЕГДА**, а не только при откате: «Карта Google · сборка …»
  либо «Карта OpenStreetMap · причина · сборка …». Замерено: контраст 7,51 : 1 в светлой
  и 4,67 : 1 в тёмной (запас над порогом 0,17 — при следующей правке палитры перемерить).
* ⚠️ **Устройство, застрявшее на до-04.08 сборке, из кода не вылечить.** Тот код не умел
  перезагружаться по `controllerchange`, и повлиять на уже загруженный древний JS нечем —
  нужна одна полная навигация на устройстве.

### План крупной функции карты — готов, работа не начата

Архитектор разобрал требование заказчика про базу транспорта, цветные ветки по
участникам, километраж по видам транспорта и три способа оплаты. Главная находка:
**бо́льшая часть уже есть в коде** — `S.transport`, справочник видов `S.kinds`, тона
веток `MAP_TONES` (четыре брендовых цвета плюс смеси), привязка точки к технике
`RoutePoint.tr`, OSRM, легенда карты.

Чего нет и что составляет настоящую работу:

1. **Свой пробег у единицы техники** — корень всего. `litres()` (`calc.ts:51`) для ВСЕХ
   авто берёт один общий `routeKm(S)`; две машины с разными маршрутами посчитать
   физически нельзя.
2. **Пятая ветка неразличима от первой.** `toneAt()` (`marks.ts:71-75`) выдаёт тон и
   штрих связанной парой, своих пар четыре. Развязка даёт 20 сочетаний без новых цветов.
3. Железная дорога и автобус как виды · километраж по воде (OSRM зашит на `driving`) ·
   несколько участников на транспорт · поправка на неучтённый километраж · три способа
   оплаты.

Разбивка на пять выкладываемых по отдельности этапов, модель данных, ограничения
слияния и риски — в `PROMPT-NEXT.md`. Ключевое правило всех этапов: **пустое поле =
сегодняшнее поведение**, иначе сдвинется 330 или 21 385.

### Проверки

`check-sums.mjs` — все шесть цифр сходятся (330 · 21 385 · 26 005 · 47 390 · 11 848 · 2).
`tsc` — без вывода. `oxlint` — 10 предупреждений, все прежние `only-export-components`
в чужих файлах, ошибок ноль. `build` и `build:offline` прошли.

Замеры живьём на песочнице `?sandbox=1`, боевая строка не тронута. Типографика проверена
числами против ощущения заказчика: штучных кеглей на экране **ноль**, толщин иконок
**одна**, семейств шрифтов **два** (Manrope везде, Chewy только на названии сервиса),
интервалы между разделами **ровно 40 px** во всех пяти стыках.

## ⚡ 05.08.2026 — ПОСТРОЕНА СИСТЕМА РАЗРАБОТКИ (коммит `248e308`). Кода не тронуто

Заказчик забраковал результат: «мне говорят „готово“, а я открываю сломанное» — и
распорядился построить систему разработки **до** любого кода. Форма взята из его же
рабочего проекта `C:\Projects\SportLocation\app` (там `CLAUDE.md`, `PRINCIPLES.md`,
`LESSONS.md`, 13 профилей агентов, `.claude/rules/`, хуки). Практики — из актуальной
документации Anthropic `code.claude.com/docs`.

Что появилось:

* **`CLAUDE.md`** переписан — 14 постулатов, 136 строк. Anthropic прямо: файл длиннее
  ~200 строк начинает игнорироваться целиком, поэтому подробности уехали в отдельные
  документы, а критерий каждой строки — «уберу, и начнут ошибаться?».
* **`docs/LESSONS.md`** — 54 урока У-01…У-54 в восьми группах (данные · готовность ·
  раскладка · библиотеки · карта · сборка · заказчик · агенты). Уроки **не удаляются
  и не перенумеровываются**: на них ссылаются профили агентов и остальные документы.
* **`docs/DEFINITION-OF-DONE.md`** — четыре ступени. `tsc` и сборка это **допуск
  к проверке**, готовность даёт только живой замер.
* **`docs/WORKING-STANDARD.md`** — источники компонентов (shadcn/ui, Aceternity, Motion,
  lucide), запрет на велосипеды, запрет на попапы, «один список — один раздел», мера и ритм.
* **`docs/AGENTS.md` + `.claude/agents/`** — 16 профилей с маршрутами по размеру задачи
  и правилом приёмки: работа не принята, пока тестировщик не показал измерения.
* **`.claude/rules/environment.md`** — грабли среды, вынесены из `CLAUDE.md`.
* **`.gitignore`** поправлен: `.claude/` игнорировался целиком, и система разработки
  не попала бы в репозиторий вовсе. Теперь `agents/`, `rules/` и `launch.json` под
  контролем версий, личные настройки и рабочие копии — нет.

### Два ответа заказчика, полученные в этой сессии

1. **Фотографии (спрашивал трижды): речь только о карточках участников в «Команде».**
   Шапки колонок в «Сборах» и обложка его устраивают — не трогать. Урок У-44 закрыт.
2. **«Зачем-то внизу сборы» оказалось шире, чем выглядело.** Дословно: «дублируется
   список товаров на всём протяжении… больше нигде не повторяться списками оборудования,
   инвентаря, вещей или продуктов». Плюс требование единой формы: «матрица, слева столбец
   вещей, справа столбцы с участниками… в зависимости от специфики раздела отображение
   локально немного изменено, но чтобы единообразие было». Записано уроками У-53 и У-54.
   **Следствие:** список несобранного справа от обложки (`components/trip/ReadyLeft.tsx`)
   не «решить, нужен ли» — его там быть не должно.

### Проверки

Контрольные суммы прогнаны: 330 км · 21 385 / 26 005 / 47 390 / 11 848 ₽ · 2 канистры —
все шесть сходятся. `oxlint`, `tsc` и сборка не гонялись: код не менялся ни строкой.
Коммит **не отправлен** на боевой — ждёт слова заказчика.

## ⚡ ГЛОБАЛЬНАЯ РЕВИЗИЯ 04.08.2026 — закрыты пункты 1–9 задания заказчика

Заказчик забраковал сервис целиком и распорядился провести глобальную ревизию силами
субагентов. Сделано и **выложено на боевой** (коммит `8090bd1`). Проверено: контрольные
суммы, `tsc`, `oxlint`, сборка, сверка руками на песочнице; строка `vuoksa2026-test`
после проверок удалена, боевая строка не тронута (метка `12:24:27` как была).

### Три находки, которых не было в задании

Каждая объясняла жалобу заказчика лучше, чем сама жалоба.

1. **«У Кости ничего нельзя редактировать» — права были исправны, молчал интерфейс.**
   Ключ в красивую ссылку `/vuoksa2026/Kostya` не входит: он берётся из `?k=` или из
   запомненного в браузере. Открыв её без ключа на телефоне, где запомнен другой человек,
   Костя получает свою карточку, но права участника — и ни слова о причине. `perms.stale`
   считался с самого начала, но **его не показывал ни один компонент** (grep по `.tsx` —
   ноль совпадений). Добавлен `app/src/components/PermNotice.tsx`.
2. **Свои подписи плиток и названия разделов хранились, но не показывались.**
   `S.tileLabels` — позиционный массив первой версии (0 транспорт, 1 продукты, 2 бюджет,
   3 с каждого; порядок задан в `src/app.template.html:1869`), а `MoneyTiles` читал его
   как словарь по ключу — свои подписи не выводились ни разу. ⚠️ Первая попытка правки
   (субагент) переводила массив в словарь: `tileLabels` лежит в `WHOLE_DOC` слияния и
   отдаётся целиком, поэтому подписи пропали бы насовсем. Форма массива сохранена.
   Так же `S.secTitles` (`{h, sub}` по ключам первой версии, где «Дорога» — это `log`):
   хранился, переживал все происшествия, на экран не попадал. Теперь читается и правится —
   `app/src/lib/sectitles.ts` + `flops/SectionHead.tsx` (проп `secId`).
3. **«Шторки работают через жопу» — подложка закрытой шторки оставалась висеть.**
   Найдено вживую: `elementFromPoint` над вкладкой вернул `[data-vaul-overlay]`
   с `data-state="closed"`, `pointer-events: auto` **в inline-стиле**, `z-50`, во весь
   экран. Библиотека снимает подложку по окончании анимации ухода; не доиграла (фон,
   поворот, прерванный жест) — экран цел, но ничего не нажимается. В `index.css` закрытой
   подложке нажатия отобраны. ⚠️ `!important` там обязателен: inline-стиль иначе не перебить.

### Что сделано по пунктам задания

* **1. «Как вижу, так и редактирую».** Название и подзаголовок поездки правятся тапом
  (`TripCover` + шторки в `TripSection`) — их не было вовсе, и именно это заказчик принял
  за «сломано всё». Плюс: подписи денежных плиток, названия и подписи разделов, дни меню
  (`Group onMenu`), места в `PlacesSheet`, живая кнопка «Показать сборы» вместо заглушки
  с тостом в `PersonSheet`.
* **2. Регистрация владельца.** `create_user: true` — вход и регистрация стали одним
  письмом; адрес возврата уехал в query-параметр `redirect_to` (в теле `options.
  email_redirect_to` — форма supabase-js, голый GoTrue её молча игнорирует, письма
  возвращали на Site URL). Ссылки команды — кнопкой на карточке в «Команде».
  ⛔ **`docs/rls-apply-c.sql` заказчик должен выполнить ДО включения регистрации**: в пути Б
  право записи давала РОЛЬ (`auth.role() = 'authenticated'`), то есть при свободной
  регистрации любой вошедший смог бы писать в боевой лист. Теперь сверяется почта
  (`owner_email`, первый вошедший закрепляется). Шаги — `docs/owner-signup-steps.md`.
* **3. Карта.** Причина отката на OSM пишется словами вместе с номером сборки: молчаливых
  путей отката было шесть и различить их было нечем. Добавлен таймаут 12 с — без него
  молчание Google (скрипт отдал 200, callback не пришёл) вешало карту навсегда: ни карты,
  ни отката. Одна повторная попытка на сетевых отказах, на `auth` — сразу OSM.
* **4. Сборы.** Матрица вещь × люди как в листе «Снаряжение», на всех ширинах; вкладки
  по людям убраны; колонка названий липкая, прокрутка вбок внутри блока; итог блока
  «собрано: X из N». Количество теперь правится **долгим нажатием** — было только правой
  кнопкой мыши, то есть с телефона недостижимо.
* **5. «Кто уже собрался».** Проценты и кольца убраны, вместо них список несобранного.
* **6–8. Дорога.** Дубль обложки и карточка-указатель убраны, `RoadCover.tsx` удалён;
  маршрут переехал в «Дорогу» вкладками «Списком / На карте»; расчёт пересобран по листу
  «Логистика» (`RoadInputs.tsx` + `RoadCalc.tsx`); километраж по точкам — `lib/osrm.ts`
  и `road/legs.ts`, по кнопке. `dist.src` молча не переключается.
* **9. Дизайн.** Фотографии участников больше не режутся: `object-contain` плюс размытая
  копия под ней. Метки участников 14 → 16 px, знак в шапке нажимается по 44 px, подсказки-
  инструкции заменены смысловыми строками по образцу таблицы заказчика.

### Что осталось на заказчике

1. **Выполнить `docs/rls-apply-c.sql`, потом включить регистрацию** — порядок в
   `docs/owner-signup-steps.md`, там же удаление строки `probe-guard`.
2. **Мусор в боевых данных от проверок с телефона** — не трогали, ждём его слова:
   точка маршрута «Новая точка» (`i: rpmsejg8nx`, Мурино), у Жени в поле «машина»
   грубость вместо «Chevrolet Aveo · пассажир», позиция аренды «Кал» (`i: rnmsehi2ln`,
   6 ₽ × 5 суток — ровно те лишние 30 ₽, на которые бюджет расходится с контрольным).
3. **Квота Supabase** — «Grace period is over», зайти в Review billing.
4. **Посмотреть глазами на телефоне:** липкая колонка матрицы и долгое нажатие,
   остриё конечной метки, вкладки «Дороги». В этой среде кадры не рисуются — проверить
   было нечем.

⚠️ **Про `secTitles`:** у раздела «Команда» своё название заказчика — крепкое слово.
Оно хранилось в данных и теперь стало видно на экране. Меняется одним тапом по заголовку.

## ⚠️ ПРОИСШЕСТВИЕ 04.08.2026, 10:29–10:46 — боевые данные затёрла чужая вкладка

Читать до того, как что-нибудь открывать. Стоило дорого, повторяться не должно.

**Что было.** Во время переезда на домен боевую строку `vuoksa2026` трижды перезаписал
клиент, открытый по «голому» адресу — без `?u=…`. В записи это видно сразу: `author` пустой,
`data.me` = null. Пропали свои названия разделов (`secTitles`), подписи плиток обложки
(`tileLabels`), координаты места поездки (`trip.lat/lon` — от них берётся погода), живой
прогноз и две отметки «собрано». Люди, фотографии, маршрут, сборы и деньги уцелели.
Восстановлено из снимка 10:00:36 (последняя правка человека), дата окончания поездки взята
из поздней версии — в снимке она была пустая. Снимки лежали в песочнице сессии.

**Механизм — и это слабость сервиса, а не случайность.** Вкладка с УСТАРЕВШЕЙ копией листа
получает свежую версию с сервера, сливает её со своей старой (`mergeInto`) и отправляет
результат обратно. Поля, которых в старой копии нет вовсе, слияние не воскрешает — они
пропадают. Поэтому восстанавливать, пока такая вкладка открыта, бесполезно: она затрёт снова
через несколько минут. Сначала закрыть вкладку, потом восстанавливать.

**Как не повторить.**

* Сайт открывать ТОЛЬКО по личной ссылке: `https://pine-to-pine.com/vuoksa2026/Maks?k=…`.
  Голый адрес без `?u=` даёт права владельца — это поведение v1, портировано намеренно.
* Проверки — только `?sandbox=1`, и локальный адрес открывать сразу с ним.
* **Перед правкой файлов проверять, не работает ли чужой сервер разработки**
  (`netstat -ano | grep :5199`): правка файла вызывает горячую перезагрузку ЧУЖОЙ вкладки,
  и та отправляет своё устаревшее состояние в базу.
* Перед долгой работой снимать страховочный снимок строки:
  `GET /rest/v1/trips?id=eq.vuoksa2026&select=data,updated_at,author`. Он спас этот случай.
* Признак беды в строке: `author` пустой при непустых данных.

Настоящее лечение — задача 3 (Supabase Auth и RLS по `auth.uid()`): пока разграничение
«джентльменское», любая старая вкладка может затереть боевой лист.

## 🔴 ПОВТОР ПРОИСШЕСТВИЯ 04.08.2026, 11:31 UTC — обеднён и ВОССТАНОВЛЕН в 11:55

**Данные на месте.** Но источник записи с этой машины не устранён — читать дальше.

В 11:31:58 UTC боевую строку снова затёрла устаревшая копия. Признак тот же: `author` пуст
при непустых данных. Пропали ровно те же поля, что и утром:

| что | было в снимке 11:00:11 | стало |
|---|---|---|
| обложка поездки `trip.hero` | 428 347 байт | нет |
| координаты места `trip.lat/lon` | 61.04027 / 30.13915 | нет |
| свои названия разделов `secTitles` | есть | нет |
| подписи плиток обложки `tileLabels` | есть | нет |
| живой прогноз `weather` | Open-Meteo, 4 августа | откатился к сидовому тексту |
| отметки «собрано» | 2 | 0 |
| заголовок `trip.title` | «ВУОКСА · 2026» | «Вуокса · 2026» (сид) |

**Списки целы:** люди 4, сборы 106, закупка 53, маршрут 8, «не забыть» 10, техника 4,
аренда 3, меню 5 — всё на месте, фотографии людей на месте.

**Источник — не эта сессия.** На машине заказчика с прошлой сессии Claude остались работать
два брошенных статических сервера (`node`, порты **4173** и **4199**), и в Chrome открыто
окно с приложением по «голому» адресу. Оно держит старую сборку и старую копию листа.
Остановить процессы и закрыть чужое окно из сессии не дали правами.

**Что уже сделано.** Лист восстановлен из снимка 11:00:11 условной записью в 11:55:09,
автор снова «Макс». Проверено: обложка, координаты, названия разделов, подписи плиток, живой
прогноз и обе отметки «собрано» на месте; списки не пострадали и не изменились. Три минуты
наблюдения — метка не сдвинулась, то есть сейчас никто не пишет.

**✅ ИСТОЧНИК УСТРАНЁН 04.08.2026, 12:1x — этапом «защита листа».** Что было сделано:

1. Оба брошенных сервера (`node`, порты **4173** и **4199**) остановлены.
2. Окно Chrome с приложением закрыто — с разрешения заказчика.
3. Из `.claude/launch.json` убрана запись `vuoksa-local`: она указывала на скрипт
   в папке прошлой сессии и порождала тот самый сервер на 4173.
4. **Главное: локальный запуск больше физически не может писать в боевую строку.**
   `isSandbox()` в `app/src/lib/supabase.ts` теперь возвращает `true` не только по
   `?sandbox=1`, но и для любого `localhost` / `127.0.0.1`. Забыть про `?sandbox=1`
   больше нельзя — за пять происшествий подряд забывали именно это.

**Как проверить, что всё цело** (в любой момент): `node restore/restore.mjs` — в выводе
должно быть `автор: "Макс"`, `обложка: есть`, координаты `61.04027 30.13915`,
`свои названия разделов: есть`, `подписи плиток: есть`.

Если когда-нибудь снова увидите `автор: ""` — повторить `node restore/restore.mjs --yes`
и искать источник дальше, в том числе на телефоне: встроенный браузер Телеграма пишет
раз в полторы минуты, и с компьютера его не видно.

Снимок 11:00:11 лежит в `restore/snapshot-vuoksa2026-11-00.json`. Папка `restore/` внесена
в `.gitignore`: внутри фотографии людей, а репозиторий публичный. **Не удалять папку,
пока лист не восстановлен и не проверен.**

Скрипт `restore/restore.mjs` пишет условно (по метке, которую прочитал) и перед записью
сам проверяет, что снимок не беднее сервера: если на сервере появились позиции, которых
в снимке нет, он останавливается и ничего не пишет.

⚠️ Страховка `fillMissingKeys` в `lib/merge.ts` (добавлена этим этапом) этот случай
**не покрыла**: она живёт в коде, а виновная вкладка держит СТАРУЮ сборку, до этой правки.
Для людей, которые откроют сайт после выкладки, такой потери уже не будет. Полное лечение —
всё то же: `docs/rls-migration.sql`.

## ⚡ Этап от 4 августа 2026, день — защита боевого листа (путь Б), хвосты, разрезание сборки

Закрыты задачи 0, 2 (наполовину — код готов, SQL ждёт заказчика) и 4 из `PROMPT-NEXT.md`.
Задачи 1 (проверить глазами) и 3 (свой Map ID) требуют самого заказчика — см. `PROMPT-NEXT.md`.

### Локальный запуск больше не может испортить боевой лист

Корень всех пяти происшествий 4 августа — «голый» адрес на `localhost` даёт права
владельца и пишет в боевую строку. Теперь `isSandbox()` считает песочницей **любой**
локальный запуск, а не только `?sandbox=1`. Проверено вживую: приложение на
`localhost:5199` создало строку `vuoksa2026-test`, боевая метка не сдвинулась.

### Защита листа: путь Б — решение заказчика

Заказчик выбрал путь Б из `docs/rls-migration.sql`. Готовый к применению файл —
**`docs/rls-apply-b.sql`**. Прямая запись в `trips` закрывается всем; правки идут через
функцию `trip_write`, которая сверяет личный ключ человека со списком людей в документе
**на сервере**. Модель прав сохраняется один в один, дыра «голый адрес = владелец»
закрывается насовсем.

В коде уже сделано (работает и до, и после применения SQL):

* `app/src/lib/supabase.ts` — `rpcTripWrite()`, классы ошибок `KeyRejected` и `RpcMissing`.
  Ответ просим короткий (`?select=updated_at`): прежний `PATCH` гонял обратно весь
  документ, то есть лишний мегабайт на каждую запись.
* `app/src/lib/sync.ts` — запись идёт через функцию; если функции в базе ещё нет
  (ответ 404), один раз это выясняется и дальше работает прежний прямой путь.
  Отдельное человеческое сообщение на отказ по ключу: «правки не сохраняются —
  откройте свою личную ссылку».
* `app/src/store.ts` — в синхронизацию отдаётся подтверждённый ключ (`getKey`).
* `restore/restore.mjs` — восстановление тоже переведено на функцию, иначе после
  включения защиты аварийный скрипт перестал бы работать. Ключ владельца берётся
  из самого снимка. Откат на прямую запись, пока функции нет.

⛔ **SQL заказчик применяет сам** — из этой среды к панели Supabase доступа нет.

### ✅ Защита ВКЛЮЧЕНА и проверена — 04.08.2026, 13:33

Заказчик выполнил исправленный файл, `защита включена = true`. Проверено живыми
запросами (все записи — по строке-песочнице, боевую не трогали):

| что пробовали | результат |
|---|---|
| вставка новой строки в обход функции | отказ, HTTP 401 «new row violates row-level security» |
| правка существующей строки в обход функции | 0 строк изменено, автор не поменялся |
| удаление строки, не подпадающей под `%-test` | строка осталась на месте |
| функция, ключ владельца | пишет |
| функция, ключ участника (Миша) | пишет |
| функция, ключа нет вовсе (голый адрес) | отказ «ключ не подходит» |
| функция, подобранный чужой ключ | отказ «ключ не подходит» |
| чтение таблицы | работает у всех |
| удаление строки-песочницы `*-test` | работает |

И то же самое через само приложение: по личной ссылке правка уезжает на сервер
(метка обновилась, автор «Макс»); с голого адреса — не уезжает, и человек видит
полоску «правки не сохраняются — откройте свою личную ссылку».

⚠️ **В таблице осталась строка `probe-guard`** — её завела проверка, и удалить её
теперь нельзя ничем, кроме SQL Editor (политика удаления пускает только `%-test`).
Вреда от неё нет, но убрать стоит:
`delete from public.trips where id = 'probe-guard';`

### Состояние связи наконец видно человеку

`net` из `store.ts` считался с самого начала, но **ни один компонент его не показывал** —
пока писать разрешалось всем, это было незаметно. С включённой защитой стало опасно:
человек без своей ссылки отмечал бы вещи в пустоту. Добавлен
`app/src/components/NetNotice.tsx` — полоска внизу (над нижним меню на мобильном,
в правом нижнем углу на десктопе), появляется только при `err` и `off`. Проверена
вживую: текст, положение и то, что при нормальной работе её нет.

**Прежнее состояние на 04.08.2026, 12:20 — защита включалась НАПОЛОВИНУ.** Заказчик выполнил файл,
функция `trip_write` создалась и работает верно (проверено живыми запросами: с верным
ключом пишет, с неверным отвечает «ключ не подходит»). **Но прямая запись в таблицу
осталась открытой** — проверено: анонимный `POST` создаёт строку, `PATCH` меняет боевую.
Причина — на `trips` висела старая разрешающая политика, а файл сносил только политику
со своим именем. Исправлено: теперь список политик чистится целиком, плюс добавлена
политика на удаление строк `%-test` (без неё песочницу нельзя было бы убрать).
**Заказчику надо выполнить обновлённый `docs/rls-apply-b.sql` ещё раз**, до этого дыра
«голый адрес = владелец» жива на боевом домене и на телефоне.

⚠️ Урок для следующей сессии: проверку «проходит ли запись в обход функции» вести
**только на строке-песочнице**. В этой сессии такая проверка была сделана на боевой
строке, и она прошла — поле `author` переписалось на служебное значение и было сразу
возвращено. Данные (`data`) при этом не менялись, метка `updated_at` не сдвинулась.

### Свой Map ID вместо DEMO_MAP_ID

Заказчик завёл идентификатор в кабинете Google Cloud (проект `pine-to-pine`,
тип JavaScript / Raster), он подставлен в `GOOGLE_MAP_ID` в `app/src/lib/gmaps.ts`
и уже на боевом сайте. Ключ не менялся. Проверено на песочнице: Google Maps
поднимается, `marker.js` подтягивается, в консоли нет ни ошибки о невалидном Map ID,
ни пометки «для разработки».

### Хвосты

* **Сборка разрезана.** Было 1,19 МБ одним куском, стало: свой код 652 КБ плюс
  `react` 190, `leaflet` 149, `motion` 121, `radix` 94, `cmdk` 11 КБ. Смысл в кеше:
  библиотеки остаются в браузере между версиями, и обновление сайта весит не мегабайт.
  Офлайн-копию это не задело — `viteSingleFile` по-прежнему сливает всё в один файл.
* **Цели касания.** `EditNum` в живой фразе: видимая высота осталась 32 px, а зона
  нажатия расширена невидимым слоем до 44 px (проверено `elementFromPoint`: отзывается
  на ±21 px от центра). Базовые размеры shadcn `button.tsx` подняты с 32 до 44 px;
  `scale="sm"` убран из `RoutePointSheet`.
* **Меток участников стало восемь** вместо четырёх. Первые четыре байт в байт прежние —
  команда своих меток не потеряет. ⚠️ Метка рисуется в 14 px, поле под узор ≈ 10 × 10 px:
  восемь узоров на таком размере различимы «на грани», проверять глазами.
* **Промежуточные тосты в мастере «Новая вещь» убраны** — у `TextSheet` и `NumberSheet`
  появился необязательный `quiet`, включён только внутри `GearAddSheet`. Все прочие
  два десятка мест вызова работают как работали.
* **«Кто уже собрался» переставляется плавно** — `layout="position"` у строки, 0,24 с,
  с уважением к «уменьшить движение».

### Что заметили в боевых данных

В 12:24 UTC в лист пришли живые правки от имени Макса: добавлена точка маршрута
в Мурино («Новая точка»), у Жени в поле «машина» лежит грубость, в аренде есть позиция
с названием из одной буквы. Похоже на проверку ввода с телефона. **Это не потеря
данных** — обложка, координаты, названия разделов и подписи плиток на месте.
Восстановление не запускалось и не нужно. Если это был тест — эти три записи стоит
подчистить руками через интерфейс.

## ⚡ Этап от 4 августа 2026, ночь — шапка, самообновление, единый блок карты, вход владельца

Закрыты задачи 1–6 и 8 из `PROMPT-NEXT.md`. Задача 7 (Supabase Auth) сделана наполовину
осознанно — см. ниже.

### Шапка на мобильном: причина найдена, и она была не в цвете

Заказчик просил это третий этап подряд, и два прошлых раза чинили не то. Красили фон,
а фон и так был непрозрачный. **Настоящая причина — `position: sticky`.**

Липкость держится на том, что прокручивается корневой элемент. У нас `body { overflow-x:
hidden }`, и всплытие overflow к области просмотра ломается, стоит любому слою поставить
`overflow` на `<html>` — так делают шторки и так ведёт себя часть встроенных браузеров.
Тогда прокручивается уже `body`, липкость отваливается, шапка уезжает вверх — и наверху
экрана оказывается карта. Ровно то, что заказчик называл «сквозным отверстием».

Проверено опытом прямо в браузере: при `sticky` и прокручиваемом `<html>` верх шапки
уходит на **−600 px**; при `fixed` остаётся на нуле. Поэтому:

* `MobileHeader` теперь `position: fixed` плюс распорка той же высоты в потоке;
* поверх области `env(safe-area-inset-top)` лежит отдельная непрозрачная полоса цвета фона;
* **`<meta name="theme-color">` стал один и меняется вместе с темой** (`src/theme.ts`,
  и первое значение ставит скрипт в `index.html` до первой отрисовки). Раньше тегов было
  два, с `media="(prefers-color-scheme)"` — они слушают СИСТЕМНУЮ тему, а не выбранную
  человеком внутри сайта. Свою полоску (крестик, адрес, «⋯») Телеграм красит именно
  по этому тегу; без него она полупрозрачная и показывает страницу насквозь.

Замерено на 375 px: шапка `fixed`, фон `rgb(249,243,212)` в светлой и `rgb(43,57,26)`
в тёмной, размытия нет, содержимое начинается ровно под шапкой, при прокрутке верхние
57 px закрыты шапкой целиком.

### Сайт обновляется сам (служебный работник переписан)

У заказчика на телефоне держалась старая версия, `Ctrl+Shift+R` не помогал. Причин было три:

1. **Имя кеша было вшито строкой и не менялось.** Значит, `activate` ничего не удалял,
   а браузер не видел повода перекачивать сценарий работника: адрес тот же.
2. **Файлы из `/assets/` отдавались «сначала кеш» без проверки, что именно туда положили.**
   Сразу после выкладки Cloudflare успевает ответить на запрос кода правилом `_redirects`,
   то есть отдать `index.html` вместо файла кода. Такой ответ ложился в кеш **навсегда**.
3. **Странице никто не говорил, что приехала новая версия.**

Теперь: версия сборки живёт в адресе работника (`sw.js?v=…`, значение подставляет Vite —
на Cloudflare это короткий хеш коммита, `__BUILD__` в `vite.config.ts`); имя кеша завязано
на версию, и при переходе старый кеш удаляется целиком; разметка вместо кода в кеш не
попадает; сеть ограничена по времени (6 с), иначе плохая связь вешает страницу; страница
сама перезагружается по `controllerchange`; обновление проверяется при каждом возврате
к вкладке и раз в час. Добавлен `app/public/_headers`: `sw.js` и `index.html` — `no-cache`,
`/assets/*` — `immutable` на год.

Проверено вживую: при смене версии старый кеш удаляется, работник меняется, страница
перезагружается сама. **Заказчику больше ничего нажимать не надо.**

⚠️ Мелочь на будущее: правило `no-cache` для `/sw.js` из `_headers` Cloudflare не применяет —
он отдаёт свой `max-age=14400, must-revalidate` (для остальных путей правила работают,
проверено на манифесте). На самообновление это не влияет: приложение просит
`sw.js?v=<хеш коммита>`, а это каждый раз новый адрес и новая запись в кеше.

⚠️ Заодно это, скорее всего, объясняет и «карта откатывается на OpenStreetMap»: на телефоне
просто держалась сборка, где Google Maps ещё не было. Теория про отсутствующий `Referer`
в webview Телеграма НЕ подтверждена — Maps JS API берёт адрес страницы из JS, а не из
заголовка. Если после выкладки карта на телефоне всё ещё Leaflet — вот тогда смотреть ключ.

### Слияние больше не имеет права терять поля

Прямое следствие происшествия. В `mergeInto` добавлена страховка: всё, что есть на сервере
и отсутствует у нас, добирается независимо от меток времени (`fillMissingKeys`). Действие
только добавляющее — существующие значения не трогаются, чужую свежую правку затереть
не может. Личные поля (`me`, `theme`, `author`, `updatedAt`) исключены.

Проверено отдельным прогоном на воспроизведении происшествия: `secTitles`, `tileLabels`,
прогноз, меню и `trip.lat/lon` воскресают, а своё свежее чужим старым не затирается.
**Это заплатка, а не лечение.** Лечение — в `docs/rls-migration.sql`.

### Карта, тайминг и маршрут — один блок в «Поездке»

Заказчик: «тайминг и маршрут вместе совпадали… вот это вот, которое у тебя есть, оно должно
быть рядом с картой». Новый `app/src/components/map/RouteBoard.tsx`:

* **на десктопе** слева обложка и под ней лента точек, справа карта во всю высоту;
  **на мобильном** сверху обложка, под ней карта, под картой лента (карта раньше списка —
  иначе тапу по строке некуда наводить). Замерено на 1280 px: обложка и лента 24…620,
  карта 645…1240. Горизонтального скролла нет ни на 375, ни на 1280.
* **Блок живёт в «Поездке», а не в «Дороге»** — он начинается с обложки поездки, а она
  здесь; заказчик раньше просил «сначала заглавная фотография, за ней сразу карта».
  В «Дороге» на месте бывшей ленты — карточка-указатель «Маршрут и тайминг наверху»
  с кнопкой перехода. Ленты в двух местах нет.
* **Связь двусторонняя** через посредник `lib/mapfocus.ts` (прямых ссылок между разделами
  по-прежнему нет): тап по строке наводит карту, тап по метке подсвечивает строку в ленте —
  добавлен обратный ход `focusInList()` / `onListFocus()`. Шторка правки точки одна на ленту
  и карту.

**Строка поиска адреса** — `map/MapSearch.tsx`, над картой. Задержка 500 мс, минимум три
буквы, до пяти находок с полным адресом, приблизительные помечены словом «Примерно»,
честное «Ничего не нашлось поблизости». Ничего не подставляется молча: над списком написано,
что именно случится при выборе. Участник без прав правки поиском только наводит карту.

**Место поездки и конечная точка.** `trip.places` с `main` рисуется отдельной меткой —
плашка цвета хвои с названием и значком палатки, не такая, как янтарные кружки маршрута.
Кнопка «Конечная» в полосе под картой включает режим «тапните по карте». Подпись дат
на обложке теперь считается из `trip.start`/`trip.end` (раньше бралась сохранённая строка
`trip.dates`); «10–14 августа 2026» сходится.

**Дублирование пробега.** Чинена подача, расчёт не тронут, 330 км сходятся, `dist.auto`
не тронут. Было: «Едем 150 км в одну сторону, туда и обратно — это 2 конца, плюс 30 км
по месту» — один факт сказан дважды, и при одном конце текст врал. Стало: «Едем 150 км
в одну сторону, плюс 30 км по месту. Дорогу считаем туда и обратно» — слова говорит сам
коэффициент через `kBackWord()` в `roadx.ts`. Тем же вылечена плашка на обложке «Дороги»
(`RoadCover`) и заметка `dist.nt.kBack.c`, которая была третьим пересказом того же факта
(не выброшена — переехала в подпись шторки).

**AdvancedMarkerElement.** `google.maps.Marker` убран целиком, оформление обеих карт живёт
в `map/marks.ts`. Проверено вживую: метки рисуются (это настоящие узлы DOM, их видно
в разметке). Откат на OpenStreetMap живой.

⚠️ **Осталось на заказчике:** карте теперь обязателен `mapId`, и сейчас там стоит служебный
`DEMO_MAP_ID` самого Google. Он работает и без денег, но пишет в консоль пометку
«для разработки» и не предназначен для боевого сайта. Свой заводится в кабинете Google
Cloud (Maps → Map management, тип Raster, тот же проект) и подставляется в `GOOGLE_MAP_ID`
в `lib/gmaps.ts`. Ключ при этом не меняется.

### Шторки: кнопка «Готово» больше не срезается, окно не прилипает к низу

Причин у срезанной кнопки оказалось три, и первые две друг друга прикрывали:

1. **Специфичность.** В `ui/drawer.tsx` стояло `data-[vaul-drawer-direction=bottom]:max-h-[80vh]`.
   Селектор с атрибутом сильнее обычного класса, поэтому `max-h-[88vh]` из `ResponsiveSheet`
   не применялся вовсе. Потолок высоты теперь задан **в одном месте** — в `drawer.tsx`.
2. **`vh` вместо `dvh`.** На iOS `vh` считается по самому большому окну (когда панели
   браузера спрятаны). При видимых панелях низ шторки уезжал за экран. Везде `dvh`.
3. **Сжимался сам подвал.** У шапки и подвала не стоял `shrink-0`, и при длинном содержимом
   флекс отбирал у подвала высоту, а кнопка из него вываливалась. Это и было на снимке.

Окно «про день» на десктопе прилипало к низу по другой причине: у `DialogContent` была
раскладка `grid`, а `ResponsiveSheet` рассчитывает на колонку flex — `min-h-0 flex-1`
у прокручиваемой середины в гриде не значит ничего. Теперь `flex flex-col`.

Замерено на 1280 × 860: окно по центру (60…800 из 860), видно целиком, подвал не сжимается,
крестик закрытия доведён до 44 × 44. В шторке аренды строка «Итого» теперь появляется,
только когда она не повторяет цену.

### Внешний вид

* **«Pine-to-Pine» набрано хвоей.** Токен `--pine`: `#2B391A` в светлой теме и осветлённый
  `#A6C287` в тёмной — иначе в тёмной слово совпало бы с фоном. Контраст 11,0 : 1 и 6,3 : 1.
* **Эмблема крупнее, слово прежнее.** Растить весь локап было нельзя (заказчик сам просил
  уменьшить со 100 px), поэтому соотношение живёт внутри `Logo.tsx`: эмблема 36 px
  на мобильном и 39 px на десктопе, слово осталось 21/23 px. Вызовы не менялись.
* **Метки участников — кружки с подложкой.** Форма (квадрат, ромб) убрана: снаружи у всех
  кружок, различие — тон янтаря на подложке цвета карточки с волосяной каймой. Различие
  не только цветом (WCAG 1.4.1) держит рисунок ВНУТРИ кружка: сплошной, кольцо, половинка,
  точка. Насыщенность метки и насыщенность полосы готовности теперь разные (`alpha`
  и `barAlpha`): метка лежит на светлой подложке и требует контраста, полоса лежит
  на приглушённом фоне и может быть куда бледнее. Одно общее число делало полосы
  разных людей неразличимыми.
* **Кнопка «Добавить участника»** больше не карточка во весь портрет: 208 × 44.
* **Портреты квадратные** (1 : 1 вместо 3 : 4). Уже загруженные снимки не растягиваются:
  везде `object-cover`, старый снимок просто кадрируется по центру. Переснимать не надо.

### Вход владельца через Supabase Auth — сделана половина, и это осознанно

Готово в коде: `app/src/lib/auth.ts` (ссылка на почту, хранение и продление сеанса, выход;
без библиотеки supabase-js — как и весь остальной обмен), экран
`app/src/components/auth/OwnerLogin.tsx` по образцу блока shadcn `login-05`, пункт «Вход
владельца» в меню «⋯», подстановка токена вошедшего в запросы к базе (`setAuthToken`
в `lib/supabase.ts`), запуск из `store.ts`.

**Модель прав не тронута.** Личные ссылки `?u=…&k=…` работают как работали.

⛔ **RLS в базе НЕ включён, и это требует решения заказчика.** Разбор — в
`docs/rls-migration.sql`. Коротко: регистрируется один владелец, но пишут в документ и
редактор, и участники. Значит правило «писать может только `auth.uid()`» ломает модель —
команда перестанет отмечаться. Путей два: (А) читают все, пишет только вошедший — две
команды, но команда становится читателями; (Б) прямая запись закрыта всем, правки идут
через функцию `trip_write`, которая сама сверяет личный ключ человека с карточкой внутри
документа — модель прав сохраняется один в один и заодно закрывается дыра «голый адрес =
владелец», но надо переписать запись в `lib/sync.ts`. **Рекомендую путь Б.**
Ещё четыре шага руками в панели Supabase (magic link, список Redirect URLs, запись
владельца, свой SMTP) перечислены в конце того же файла.

## ⚡ Этап от 4 августа 2026, вечер — переезд на Cloudflare Pages и домен pine-to-pine.com

Закрыта задача 2 из `PROMPT-NEXT.md`. Осталась задача 3 — регистрация владельца.

* **Проект Cloudflare Pages `pine-to-pine`** собирается из репозитория при каждом `git push`.
  Настройки: корневая папка `app`, команда `npm run build`, каталог вывода `dist`,
  ветка `main`. Версию Node задаёт `app/.nvmrc` (22) — без него сборка идёт на версии
  по умолчанию, а Vite 8 и `tsc` требуют свежую.
  ⚠️ Интерфейс Cloudflare переехал: пункта «Workers & Pages» в меню больше нет, всё под
  **Compute**, а создание Pages-проекта спрятано под ссылкой «Looking to deploy Pages?
  Get started» в самом низу карточки «Ship something new» — верхние кнопки заводят Worker.
* **Домены `pine-to-pine.com` и `www.pine-to-pine.com`** привязаны, сертификаты выпущены.
  Cloudflare пишет «до 48 часов», на деле оба адреса ответили через минуту.
* **⚠️ Красивые адреса были сломаны — и это чинилось в коде.** `base: './'` в `vite.config.ts`
  ломает `/<поездка>/<имя>`: страница ищет `./assets/index.js` по адресу
  `/vuoksa2026/assets/index.js`, получает от `_redirects` тот же index.html вместо кода
  и остаётся белой. Теперь `base` зависит от того, где собирают: Cloudflare сам выставляет
  `CF_PAGES=1` и получает `'/'`, GitHub Pages и офлайн-копия остаются на `'./'`.
  Тем же лечением: `%BASE_URL%` у манифеста и значка в `index.html`, путь служебного
  работника через `import.meta.env.BASE_URL` в `main.tsx`.
* **⚠️ Сразу после выкладки браузер может закешировать SPA-заглушку вместо файла кода**
  (запрос успевает прийти раньше, чем файл разъехался по краям Cloudflare). Признак:
  скрипт отдаёт `text/html`. Лечится жёсткой перезагрузкой, кода не касается.
* **GitHub Pages НЕ отключён** — старый адрес работает как запасной, `publish-v2.mjs`
  и собранные файлы в корне репозитория оставлены. Когда домен обживётся, из корня можно
  будет убрать всё, кроме офлайн-файла.
* Проверено на живом домене: красивая ссылка узнаёт человека по пути, права выдаются только
  с ключом (`?k=…` или запомненный), Google Maps поднимается (ключ ограничен по referrer,
  `pine-to-pine.com` в списке), мастер «Разметить маршрут» на месте.

## ⚡ Этап от 4 августа 2026, вечер — маршрут на карте: мастер «Разметить маршрут»

Закрыта задача 1 из `PROMPT-NEXT.md`. Задачи 2 (Cloudflare Pages и домен) и 3 (Supabase Auth)
идут по шагам вместе с заказчиком — в работе.

* **Прямой геокодер появился.** `forwardGeocode` в `app/src/lib/gmaps.ts` (Google) и
  `forwardPlace` в `app/src/lib/geocode.ts` (выбор пути и защита от промаха). Раньше был
  только обратный. Поиск смещается к месту поездки (`mapCenter`), находка дальше **500 км**
  от него отбрасывается: без этого «КАД» находится в другом конце страны.
  Запасной путь — прямой поиск Nominatim, жёстко ограниченный рамкой вокруг поездки.
  ⚠️ Честное «не нашлось» от Google вторым кругом у Nominatim не переспрашивается —
  запасной путь только на случай, когда Google не ответил вовсе.
* **`placeQueries()` — из названия точки в запрос к геокодеру.** Имена точек написаны для
  человека и делятся на два вида: адрес ПОСЛЕ двоеточия («Сбор у Кости: Санкт-Петербург,
  Суздальский пр., 95») и место ДО двоеточия («Приозерск: закупка продуктов»). Имя режется
  по двоеточию, обе половины спрашиваются, первой — та, что больше похожа на адрес.
  Даты («14 августа: …») и слишком короткие куски отбрасываются.
  ⚠️ Слово «город» в признаке «похоже на адрес» держать нельзя: «Выезд из города» — действие,
  а не место, и по нему бесполезная половина уезжала в геокодер первой.
* **Мастер «Разметить маршрут»** — `app/src/components/map/RouteMarkSheet.tsx`. Кнопка
  живёт полосой под картой в «Поездке» и появляется, только пока есть точки без координат
  («Точек без места на карте: 7»). Внутри — список точек, у каждой находка геокодера полным
  адресом и две кнопки: «Поставить сюда» и «Указать на карте». Ничего не подставляется молча.
  Есть «Поставить все найденные: N» — но только после того, как человек увидел список.
  Список замораживается на открытии: размеченная строка не исчезает из-под пальца,
  а отмечается галочкой и координатами.
* **Что реально находится на боевом маршруте** (проверено живым запросом к Google 04.08.2026):
  Суздальский пр., 95 — точно; Приозерск — точно; Вуокса и «А-121, Сортавала» — приблизительно
  (в мастере помечены словом «Примерно»); «Лодочная база», «Высадка, лагерь», «Первый костёр»,
  «14 августа: сборы» — не находятся никогда, ставятся пальцем. Это ожидаемо, а не поломка.
  ⚠️ Находка для точки 2 («Выезд из города: КАД → А-121») уезжает к Сортавале, за Приозерск —
  ниткой маршрута это читается как зигзаг. Её лучше ставить пальцем или сдвинуть маркером.
* **Вид карты пересобирается после разметки** — новый проп `fitAt` у `GoogleRouteMap`
  и `OsmRouteMap`. Подгонка «один раз при первом показе» осталась (дальше человек сам решает,
  куда смотреть), но после мастера точки разъезжаются на 130 км, и вид надо собрать заново.
* **Из ожидания «тапните по карте» теперь есть выход** — кнопка «Отменить» в полосе под
  картой. Без неё человек, передумавший ставить точку пальцем, терял и кнопку мастера:
  полоса под картой одна на все три состояния.

Проверено на песочнице (`?sandbox=1`), строка `vuoksa2026-test` после проверок удалена,
боевая строка не тронута. Живой Google Geocoding отвечает с `localhost:5200`.

## ⚡ Этап от 4 августа 2026, день — единый лендинг, карта Google, фотографии, разбор готовности

Закрыты задачи 1–6 из `PROMPT-NEXT.md` и первая половина задачи 7 (ключ карт).

* **Единый лендинг вместо переключения разделов.** Все шесть разделов рисуются одновременно,
  каждый в своей `<section id="sec-…">` со `scroll-margin-top`. Меню ничего не подменяет —
  прокручивает к разделу (`scrollToSection` в `app/src/sections.ts`) и подсвечивает активный.
  ⚠️ **Активный раздел считается геометрией, а не только `IntersectionObserver`**: наблюдатель
  молчит, пока вкладка не рисует кадры, и подсветка застревала на «Поездке» (проверено).
  Наблюдатель оставлен вторым будильником, источник истины — `getBoundingClientRect()`
  в `useSectionNav` (`App.tsx`). Переход из поиска (`data-hit`) ищет строку с повторами:
  она может быть в свёрнутой группе или в неактивной вкладке.
* **Карта переехала в «Поездку»**, вторым блоком сразу за заглавной фотографией.
  Из «Дороги» вкладки «Тайминг / Маршрут» убраны — осталась одна лента тайминга.
  Карта на всю поездку одна: `app/src/components/map/TripMap.tsx`.
* **Google Maps вместо OSM, с честным откатом.** Ключ заказчика лежит открытым текстом
  в `app/src/lib/gmaps.ts` — так и надо: он ограничен по HTTP-referrer
  (`pine-to-pine.com`, `*.pine-to-pine.com`, `starobormaksim-stack.github.io`, `*.pages.dev`,
  `localhost` с портами 5199/5200). Если Google не поднялся или отказал (`gm_authFailure`),
  приложение молча показывает OpenStreetMap — `app/src/components/map/OsmRouteMap.tsx`
  (бывший `road/RouteMap.tsx`). Серого прямоугольника не бывает.
  ⚠️ **Грабли:** с `loading=async` тег script поднимает только загрузчик, `google.maps.Map`
  появляется лишь после `importLibrary()`. Без этого было «Map is not a constructor»
  и тихий откат на OSM. См. комментарий в `loadGoogleMaps()`.
* **Тап по адресу точки в «Тайминге»** прокручивает страницу к карте и наводит её на точку;
  у точки без координат та же строка предлагает поставить её тапом по карте, после чего
  адрес и предложенное название подставляются обратным геокодированием. Связь разделов —
  через посредник `app/src/lib/mapfocus.ts` (прямых ссылок между разделами нет).
  Геокодер: Google, а если он отказал — бесплатный Nominatim (`app/src/lib/geocode.ts`).
  Geocoding API включён заказчиком 04.08.2026 вечером, Google отвечает — проверено
  на боевом адресе. Запасной путь остаётся на случай лимита или отзыва ключа.
  ⚠️ **Но карта пока пустая:** у всех восьми точек маршрута нет координат, а на карту
  попадают только точки с ними. Заказчик это заметил сразу — первая задача следующего
  этапа, см. `PROMPT-NEXT.md`.
* **Фотографии заработали.** Механика кадрирования перенесена из первой версии
  в `app/src/components/flops/PhotoSheet.tsx` (`usePhotoPick` + `PhotoCropSheet`):
  что видно в рамке — то и сохраняется. Участник — 3 : 4, 800 px, качество 0,82;
  обложка поездки (`trip.hero`) — квадрат, 1400 px, 0,74. Перетаскивание идёт мимо React,
  прямо в стиль картинки, иначе снимок отстаёт от пальца.
* **Команда:** порядок людей от лица читателя (он первый) — `orderedPeople` в
  `app/src/lib/people.ts`; у нового участника карточка сразу зовёт дозаполнить роль,
  описание, права и фото.
* **Сборы:** мастер «Новая вещь» сразу спрашивает, кто повезёт и сколько
  (`gear/GearAddSheet.tsx`); разделы сборов и закупки переименовываются и удаляются
  через шторку раздела (долгий тап по заголовку или кнопка «⋯» — проп `onMenu` у `Group`).
* **Микроаналитика готовности:** `breakdownOf` / `breakdownAll` / `rankedPeople` в
  `lib/gearx.ts`, шторка разбора `trip/ReadySheet.tsx`. Строки «Кто уже собрался» нажимаются,
  отстающие наверху, кольцо открывает разбор по всей команде.
* **Личный цвет участника — не новый цвет.** Бренд остаётся из четырёх, люди различаются
  насыщенностью янтаря и формой метки (круг, квадрат, ромб, кольцо) — `toneOf` в
  `lib/people.ts`, `PersonMark` в `components/flops/`. Поле `person.color` из старой палитры
  не читается. Меток четыре: у пятого участника форма повторится.
* **Красивые адреса заранее.** `readUrlUser` в `lib/perm.ts` понимает и путь
  `/<поездка>/<имя>`, и прежний `?u=…&k=…` — старые ссылки работают как работали.
  Ключ в пути не передаётся: он берётся из `?k=` или из запомненного в браузере.
  Для Cloudflare Pages добавлен `app/public/_redirects` (любой путь → index.html).

Что по задаче 7 ещё НЕ сделано: переезд на Cloudflare Pages, домен `pine-to-pine.com`
и регистрация владельца через Supabase Auth. Ключ Google получен и ограничен.

## ⚡ Название сервиса: **Pine-to-Pine** (решение заказчика от 04.08.2026)

Прежнее имя FLOPS отменено. Новое название набирается **только шрифтом Chewy** —
заказчик прислал файл сам, лицензия Apache 2.0, шрифт вшит base64 в `app/src/fonts-chewy.css`
и подключён токеном `--font-brand`. Ни системным шрифтом, ни капслоком название не пишется.

* Знак в шапке = **эмблема из брендбука + слово Chewy**. Прежний локап `logo-*.svg`
  со словом FLOPS больше не используется (файлы оставлены в репозитории, не удалять).
* Единственное место, где живёт строка названия, — `app/src/components/Logo.tsx`,
  константа `BRAND`. Компонент `<Wordmark/>` пишет одно слово, `<Logo/>` — эмблему со словом.
* **Высота знака в шапке прежняя: 26 px на мобильном, 28 px на десктопе.**
* Внутренние имена НЕ переименованы намеренно: папка `app/src/components/flops/`
  и ключи браузера `flops.doc` / `flops.theme` / `flops.auth`. Переименование ключей
  стёрло бы у людей тему и подтверждённую личность. Это внутренние идентификаторы,
  на экране их не видно.

## ⚡ Этап от 4 августа 2026, ночь — React-версия доделана и ОПУБЛИКОВАНА

**Вторая версия заменила первую на GitHub Pages.** Первая никуда не делась: её собранные
файлы лежат в `v1/` и открываются по адресу `…/vuoksa-2026/v1/`, исходники (`src/`, `build.js`)
не тронуты. Раскладка по корню — скриптом `node publish-v2.mjs` (сборку он не запускает,
это делает npm; см. комментарий в файле).

Что сделано на этапе:

* **Подключён shadcn/ui** (`npx shadcn@latest init -b radix -p nova`) — 30 компонентов из
  списка раздела 5 UX-проекта. Ловушка из 2.5 обезврежена: **токен `--card` переименован
  в `--surface`**, а переменные shadcn объявлены алиасами к токенам Pine-to-Pine
  (`--primary: var(--accent-fill)` и т. д.). Источник истины — только наши токены,
  ни одного самостоятельного значения у shadcn нет. Заодно выброшены навязанные пресетом
  шрифт Geist и `next-themes` (тема живёт в `src/theme.ts`).
* **Библиотека паттернов** — `app/src/components/flops/`: `ItemRow`, `Group`, `StatusDial`,
  `ResponsiveSheet`, `SheetRow`, `NumberSheet`, `PickSheet`, `TextSheet`, `EmptyState`,
  `SentenceCard`/`EditNum`/`ResultNum`, `SectionHead`/`AddRow`, `Btn`. Из них собраны все разделы.
* **Все шесть разделов сделаны по UX-проекту**: Поездка, Экипаж, Сборы, Закупка, Дорога, Меню.
  Заглушек больше нет.
* **Правило «список показывает — шторка редактирует» соблюдено буквально**: в разметке
  разделов ноль `<input>`, `<select>` и `contenteditable`; на экране ни одного знака `×`, `÷`, `=`.
* **Поиск** по всему листу (`command`/cmdk, ⌘/Ctrl+K) с переходом к строке по `data-hit`.
* **Меню «⋯»**: ссылки экипажа и скачивание офлайн-копии (только владелец), «О сервисе».
* **PWA**: манифест, служебный работник (`app/public/sw.js`), значок вшит строкой в разметку.
* **Офлайн-копия одним файлом**: `Вуокса-2026.html` в корне, 1,3 МБ, **ни одной внешней
  загрузки**. Владелец жмёт «Скачать офлайн-копию» — текущий документ вшивается в файл
  строкой base64 (`app/src/lib/offline.ts`), при открытии его подхватывает `store.ts`
  через `window.__PINE_DOC__`.
* Раздел «Поездка» доведён: права на даты (менять могут только владелец и редактор —
  раньше мог кто угодно), кольцо готовности считается из реальных статусов «Сборов»
  (`readyAll` в `lib/gearx.ts`), погода раскрывается **гармошкой прямо в строке**,
  добавлен блок «Кто уже собрался», у денег появился разбор «Как это считается».

Технические решения, о которых стоит знать:

* **`AnimatePresence mode="wait"` убран из `App.tsx`.** В нём смена раздела ждала окончания
  анимации ухода, а если кадры не рисуются (фоновая вкладка, экономия батареи), переключение
  просто зависало. Въезд нового раздела остался, ожидания ухода — нет.
* **`ResponsiveSheet` на десктопе — окно по центру, а не `Popover` у строки** (в макете 4.2
  был поповер). Строка списка сама кнопка с вложенными кнопками, и якорить к ней поповер
  в шести разделах пришлось бы шестью разными способами. Единый API даёт одну механику
  на обеих ширинах.
* **Матрица «Сборов»** рисуется только при ширине ≥ 1024 и только в режиме «Все».
* **`dist.auto` по-прежнему 0**: переключатель «считать расстояние по карте» не сделан
  намеренно — он молча обнулил бы пробег до 30 км. Живая фраза правит активное поле.
* Расстояния между точками маршрута (`leg`, OSRM) по-прежнему не считаются автоматически.

## ⚡ Этап от 4 августа 2026, вечер — логотип, палитра, UX-проект, синхронизация

* **Новый логотип** (заказчик прислал макет-пару 1024×1024). Из него собраны:
  горизонтальный локап `src/logo-light.svg` / `src/logo-dark.svg` (эмблема + начертание рядом)
  и отдельная эмблема `src/emblem-*.svg`. Вертикальный вариант из макета для шапки не годится —
  на высоте 26–30 px слово нечитаемо. **Размер знака в шапке: 26 px мобильный, 28 px десктоп**
  (было 44 и 100 — заказчик сказал «слишком крупно, подумай о премиальном размере»).
  Сборка локапов — скриптом, исходный макет в `Downloads`, индексы путей измерены getBBox.
* **Палитра сведена к четырём цветам** и приведена к логотипу: графит `#262513`, хвоя `#2B391A`,
  крем `#F9F3D4`, янтарь `#BC6C25`. Вторичные тона тоже из логотипа: `#565237`, `#B1B499`.
  Песочный `#E4D0A5` и водный синий `#3E6B7A` выведены. Прежние зашитые оттенки в v1
  (мятный, пергаментные, серая заглушка аватарки) переведены на производные четырёх.
* **UX-проект переработки интерфейса** — `docs/v2-ux-redesign.md` (1400 строк). Заказчик
  жаловался: «в закупке какие-то формулы, всё криво и косо, пользоваться неудобно».
  Главное правило проекта: **список показывает, шторка редактирует** — в строке нет ни одного
  поля ввода. Там же: список компонентов shadcn, макеты всех разделов, тексты пустых состояний,
  порядок внедрения и чек-лист приёмки. **Это главный документ следующего этапа.**
* **Синхронизация портирована в React-версию**: `app/src/lib/supabase.ts`, `perm.ts`, `merge.ts`,
  `sync.ts`, хук `useTrip()` в `store.ts`. Проверено на песочнице: права владельца/участника,
  неверный ключ, приём чужой правки, одновременная запись, присутствие. Боевая строка не тронута.
  ⚠️ Голый адрес без `?u=` даёт права владельца — это поведение v1, портировано намеренно.

## ⚡ Этап от 4 августа 2026 — что сделано

* **Дизайн-ревизия v1 закрыта** (проверено вычисленным контрастом WCAG по каждому видимому
  элементу в обеих темах на 390 и 1280 — скриншоты в среде недоступны). Убраны все чужие цвета:
  фиолетовый статус `#8A6FA8` (остаток пятистатусной модели), мятный `#9CC3AF`, старые тёмные
  `#0E1F1A`/`#16222A`, «не тот» янтарь `#E8A44A`; красные унифицированы на брендовый `#A74612`
  (в тёмной — светлый `#E9A288`, токен `--dang`). Легенда «Сборов» теперь совпадает с реальными
  кнопками статусов. Главные кнопки (Готово/сохранить/добавить) в светлой — белым по `--amberDeep`,
  в тёмной — `#161C10` по янтарному. Инверсные плашки — `#FEFAE0`/`#161C10`.
  Брендбук подтверждён заказчиком файлом «FLOPS ЛОГО и ЦВЕТА.svg»: основные `#E4D0A5`/`#2B391B`,
  акценты `#FEFAE0`, `#BC6C25`, `#A74612`, `#161C10` (+градиенты между основными).
* **Даты и место на обложке стали интерактивными** (задача от заказчика голосом):
  тап по датам → всплывающий календарь (`tripDatesSheet`, выбор дня выезда и возвращения,
  строка дат собирается сама, `S.trip.start` сохраняет время, появилось `S.trip.end`);
  тап по месту → шторка с картой OSM (`tripPlaceSheet`, iframe-embed, точка `S.trip.lat/lon`,
  поиск по названию через геокодер open-meteo). Менять могут владелец и редактор, остальные
  смотрят. В офлайн-файле карта честно пишет «Карта показывается в онлайне» + координаты,
  календарь работает полностью. Координаты применяются только по «Готово».
  В сид добавлены `trip.end`, `trip.lat`, `trip.lon` (Приозерск) — `mergeSeed` доливает их в боевой документ.
* Опубликовано на Pages (коммиты `7b6f4c0`, `4a9b449`), сборка `built`.
* ⚠️ Строка песочницы `vuoksa2026-test` НЕ удалена (среда заблокировала DELETE-запрос) — удалить
  вручную: `DELETE https://oagonfdnlgqkoosvgaly.supabase.co/rest/v1/trips?id=eq.vuoksa2026-test`
  с anon-ключом из `src/online.js`.
* Фото участников: папка `фото/` пуста — задача ждёт файлы от заказчика.

## ⚡ Этап от 3 августа 2026 — что изменилось (главное)

* **Сервис называется FLOPS** (по логотипу; название набирается ТОЛЬКО логотипом, не шрифтом).
  Логотип: `src/logo-light.svg` / `src/logo-dark.svg` (+ исходник-постер `src/packflow-logo-full.svg`);
  песочные элементы эмблемы заливаются цветом фона через CSS `[fill="#E4D0A5"]{fill:var(--bg)}`.
* **Заказчик снял ограничение «один файл, без фреймворков»: решён переход на React** (Vite + Tailwind +
  shadcn/ui и Aceternity как источники компонентов + Motion + Lucide + Leaflet/OSM, маршруты OSRM без ключа).
  Каркас лежит в `app/`, офлайн-сборка одним файлом проверена (`npx vite build --mode offline`).
  Старая версия (`src/` + `build.js`) остаётся боевой до паритета. Ничего из v1 не удалять.
* **Схема v2 утверждена заказчиком**: `docs/v2-architecture.md` (разделы, Техника/Топливо/Аренда/точки
  маршрута, единицы и валюта списками, право количества у назначившего, миграция seed.json без потерь).
* **Дизайн-система v2**: `docs/v2-design-system.md` (Manrope везде, светлая тема — пергамент `#F5EFD3`,
  тёмная — хвоя `#243016`, акценты `#BC6C25/#A74612`; верхнее прилипающее меню на десктопе,
  ЧИСЛО РАЗДЕЛОВ ДИНАМИЧЕСКОЕ; тема в v2 — личная настройка браузера, не поле документа).
* В v1 уже сделано на этом этапе: бренд FLOPS (логотип в шапке на мобильном 44px по краю контента,
  в левом меню 60px, без дублей), Manrope вшит base64 (`src/fonts-manrope.css`), песочная/хвойная темы,
  контейнер 1120, экипаж 4 в ряд на десктопе, «сейчас здесь» — точка в текстовой части карточки,
  бейджи «роль»/«это я» без налезаний, тема в один тап из шапки, шапка онлайна без дублей
  (`mountHdrTrim` в `src/online.js` прячет #bSave и #bMe — в офлайн-файле они остаются),
  квадратная обложка на десктопе (`homeTop`/`homeSide`), спокойная прокрутка меню (goLock),
  анимация въезда разделов один раз (`body.settled`).
* Многоагентная работа разрешена заказчиком на весь этап (архитектор/дизайнер/тестировщик).

## Где что лежит

```
C:\Projects\Вуокса-2026        рабочая папка, она же репозиторий
├── index.html                 собранная онлайн-версия (публикуется)
├── Вуокса-2026.html           собранная офлайн-версия
├── build.js                   сборка: node build.js
├── sw.js, manifest.webmanifest, icon-192.png, icon-512.png
└── src/
    ├── app.template.html      весь интерфейс и логика (плейсхолдеры __SEED__, __HERO__)
    ├── online.js              синхронизация, права по ссылке, Realtime, присутствие
    ├── seed.json              данные поездки, фотографии в base64
    └── hero-b64.txt           обложка
```

Публикация: **https://starobormaksim-stack.github.io/vuoksa-2026/**
Репозиторий: `starobormaksim-stack/vuoksa-2026`, Pages из `main`, папка `/ (root)`.

Обновление публикации (вторая версия, боевая):

```bash
npm --prefix app run build && npm --prefix app run build:offline && node publish-v2.mjs
git add -A && git commit -m "что поменялось" && git push
```

Первая версия (архив в `v1/`) пересобирается по-старому: `node build.js` — но её файлы
теперь надо класть в `v1/` вручную, корень занят второй версией.

Pages пересобирает за минуту. Проверить, что новая версия доехала:
`gh api repos/starobormaksim-stack/vuoksa-2026/pages/builds/latest --jq .status` → `built`.

## Ссылки экипажа (в них ключ прав)

| Кому | Роль | Ссылка |
|---|---|---|
| Макс | владелец | `…/vuoksa-2026/?u=Maks&k=zdzua343` |
| Костя | редактор | `…/vuoksa-2026/?u=Kostya&k=pc2s6bsg` |
| Миша | участник | `…/vuoksa-2026/?u=Misha&k=9ak33b2w` |
| Женя | участник | `…/vuoksa-2026/?u=Zhenya&k=idz7qw3y` |

Ключи лежат в `S.people[].key` и в `src/seed.json`. Меняются при смене прав — старая ссылка гаснет.

## Как проверять, ничего не сломав

* **Боевые данные — одна строка `trips` с `id='vuoksa2026'`.** Перед экспериментами снимай копию:
  `GET /rest/v1/trips?id=eq.vuoksa2026&select=data,updated_at,author` с anon-ключом из `src/online.js`.
* Для проверок добавляй к адресу **`?sandbox=1`** — это отдельный документ `vuoksa2026-test`,
  боевой не трогается. После проверок строку песочницы удаляй.
* Локальный просмотр: простой статический сервер на `node` из папки проекта, порт 4173,
  затем `?u=Maks&k=zdzua343&sandbox=1`. Проверять на ширине 390 px, смотреть консоль на ошибки.
* Realtime уже включён на сервере (таблица `trip_pings` в публикации `supabase_realtime`).
  Присутствие требует в `phx_join` конфига `presence:{key:…, enabled:true}` — без `enabled`
  сервер не присылает `presence_state`.

## Модель прав (согласована с заказчиком, менять нельзя без него)

* **Владелец** — всё. Только он скачивает офлайн-копию, возвращает её в онлайн и видит ссылки экипажа.
* **Редактор** — всё, кроме того, что касается владельца: его карточку, его права и его отметки
  не трогает. Файл не скачивает. Остальных участников может править, удалять и отмечать за них.
* **Участник** — свой список: свои отметки, свои позиции, своё описание и фотография.
  Может добавлять позиции и **ставить задачи другим**, но не отмечать за них и не трогать общие
  параметры (логистика, маршрут, меню, экипаж).

Точки входа в коде: `permOf` / `myPerm` / `isEditor` / `isChief` / `canEditItem` / `canMark` /
`canEditPerson` / `canSetPerm` / `canSaveFile` / `permRights` в `src/app.template.html`.
В онлайне `permOf` и `canSaveFile` подменяются в `src/online.js` — права даёт только ключ из ссылки.

## Сделано (не переделывать)

Публикация на Pages · личные ссылки с ключом и ревокацией · слияние правок по позициям
(условная запись по `updated_at`, метки удалений `S.del`) · Realtime по таблице-сигналу `trip_pings` ·
присутствие «кто сейчас здесь» · офлайн как приложение на экране «Домой» (`sw.js`, манифест, значок
с островом) · тёмная тема на глубокой хвое · безопасные зоны iPhone · нижнее меню не прячется и
поднимается над панелью Телеграма · квадратные фотографии с кадрированием (и у обложки) ·
четыре состояния сборов с подписью-кнопкой, «есть дома» убрано, «не могу взять» без обязательной
причины · индикатор несохранённой копии на кнопке файла · выбор темы шторкой · единое название.

Закрыты все пять задач прошлой передачи:

1. **Поиск** — `searchIndex` / `searchFind` / `paintSugg` / `jumpTo` в `src/app.template.html`.
   Под полем список находок с указанием раздела, тап прокручивает к строке, раскрывает её группу
   и подсвечивает (`.hit`). Ищет по снаряжению, закупке, маршруту, вопросам и блюдам меню.
   Строки помечены атрибутом `data-hit` — по нему поиск их и находит.
   Прежнее поведение (фильтрация списков по `searchQ`) убрано.
2. **Погода** — отдельная карточка «Сегодня» (`S.weather.today`) и отдельный блок «Дни поездки».
   Тап по дню открывает шторку с осадками, ветром и заметкой «что это значит для нас».
   Автообновление: `weatherAuto()` в `src/online.js`, не чаще раза в 30 минут (`S.weather.ts`),
   в фоне не ходит в сеть. Текущая погода берётся отдельным запросом `current=` — диапазон дат
   основного запроса относится к дням поездки.
3. **Назначенные задачи** — плашка «Костя добавил тебе 3 задачи» на «Поездке» и шторка со списком
   (`myTasks` / `newTasks` / `tasksSheet` / `plashTasks`). Кто поручил — новое поле `as` у позиции;
   если его нет, автором считается `by`. Отметка «что уже видел» лежит **в браузере**
   (`localStorage['vuoksa2026.seen.<id>']`), а не в документе: иначе один прочитал — у всех погасло.
4. **Контекст раздела и аватарка** — полоска `#ctxBar` под шапкой показывает «Раздел · Группа»,
   как только страница отрывается от начала (`paintCtx`). Аватарка выбранного участника —
   кнопка `#bMe` в шапке, тап открывает «Кто заполняет» (`paintMeBtn`).
5. **Мобильная вёрстка 390 px** — строка «Закупки» собрана из трёх полос, каждая переносится
   целиком: `.buyNums` (количество, единица, план, факт), `.buyMeta` (статус и корзина),
   `.buyWho` (кто покупает). Ряд инициалов «М К М Ж» заменён одной строкой с фотографией и
   именем — тап открывает выбор (`buyWhoSheet`). В «Сборах» нижняя полоса разделена на
   `.gearLine` / `.gearOwn`, в общей базе у участников теперь полные имена, а не инициалы.
   Проверено: горизонтального скролла нет, ничего не выходит за 390 px.

История: `git log --oneline`.

## Дизайн-ревизия v1 — закрыта 04.08.2026

Чек-лист заказчика пройден, см. раздел «Этап от 4 августа» выше. Метод проверки: вычисленный
контраст WCAG по каждому видимому элементу в обеих темах на 390 и 1280 (скриншоты в этой среде
недоступны — вкладка не рисует кадры). Осталось на будущее: конфликт классов `lgD` (логотип)
↔ `.lgD` (кружки легенды) уже разведён — логотип `logoL`/`logoD`, легендовые `.lgD` НЕ трогать.

Ещё от заказчика: **реальные фотографии участников** — в данных у всех четверых заглушки-текстуры;
папка `фото/` пока пуста, ждём файлы. Встроить с единым кадрированием, аватарки везде одинаковые.

## ⚡ Замечания заказчика по опубликованной версии (04.08.2026)

Смотрел через встроенный браузер Телеграма. **Сделано сразу:**

* **Полоска Телеграма.** Шапка и нижняя панель были полупрозрачными с размытием, содержимое
  просвечивало сквозь них. Фон сделан непрозрачным: содержимое уезжает под шапку и там
  пропадает, сверху остаётся ровный цвет фона. Добавлен `env(safe-area-inset-top)`.
* **Шапка на десктопе.** Аватарки присутствия налезали на пункт «Меню» (видно на снимке
  заказчика). Правый блок кнопок больше не сжимается, присутствие показывается от 1280 px,
  разделы встали посередине, ширина контейнера поднята с 1120 до 1280 px.
* **Клик по знаку** возвращает к началу страницы — в обеих раскладках.
* **Раздел «Экипаж» переименован в «Команда»** — заказчик выбрал из трёх вариантов.
  Название раздела живёт в `app/src/sections.ts`, видимые тексты переписаны
  («Ссылки команды», «Убрать из команды», подписи прав). В `docs/v2-ux-redesign.md`
  раздел 7 остался под старым именем — документ переписывать не стали.

**Остальное перенесено в `PROMPT-NEXT.md` как задачи следующей сессии:** единый лендинг
вместо переключения разделов; карта сразу после обложки и переход с OSM на Google Maps
с обратным геокодированием точек; загрузка и кадрирование фотографий участников и обложки
(сейчас не работает вовсе); заполнение данных при добавлении участника; переименование
раздела «Экипаж» в «Команда»; порядок людей от лица открывшего ссылку; назначение «кто
повезёт» сразу при добавлении вещи; переименование разделов сборов и закупки; микроаналитика
готовности с цветом на человека и разбором «что осталось и у кого»; домен `pine-to-pine.com`
(зарегистрирован заказчиком в Cloudflare) и настоящая регистрация владельца через Supabase Auth
с готовыми экранами из shadcn-блоков.

**Заказчик согласился на переезд с GitHub Pages на Cloudflare Pages** и попросил вести его
по шагам через кабинеты Cloudflare и Google (ключ карт): одно действие — одно сообщение,
порядок шагов определяет тот, кто ведёт. В его аккаунты самому не заходить.

## Что делать дальше (после публикации 04.08.2026, ночь)

1. **Фотографии участников** — заказчик добавляет сам. Сейчас у всех подложка с инициалом.
2. **Проверить вживую на айфоне**: приложение на экране «Домой», офлайн-копия, перетаскивание
   точки на карте. Перетаскивание маркера в среде разработки проверить было нечем —
   Leaflet не реагирует на синтетические события ввода.
3. ~~Удалить строку песочницы~~ — **сделано 04.08.2026**: строка `vuoksa2026-test`
   и её сигналы `trip_pings` удалены, в таблице `trips` осталась одна боевая строка.

   ⚠️ **Урок этого этапа.** Проверки в браузере шли по адресу `http://localhost:5199/`
   без `?sandbox=1`, а голый адрес без `?u=` даёт права владельца и пишет в БОЕВУЮ строку.
   В документ попали две тестовые записи — точка маршрута «Новая точка» и вопрос
   «Отметить „Носки“ в сборах». Обе убраны по правилам слияния (вычеркнуты из массивов
   и получили метки в `S.del`), документ сверен с сидом: 105 позиций сборов, 53 закупки,
   8 точек маршрута, 10 вопросов, 4 человека с прежними ключами, фотографии на месте,
   заголовок «ВУОКСА · 2026» как был.

   **Правило на будущее: локальный адрес для проверок открывать ТОЛЬКО с `?sandbox=1`,
   включая превью-страницы, которые поднимают агенты.** Хорошо бы вообще запретить запись
   в боевую строку с `localhost` — этого пока нет.
4. Бандл 1,15 МБ (455 КБ в сжатии) — при желании делить на чанки: Leaflet и cmdk просятся
   в отдельные.
5. Хвосты v1 (не потерять): настоящие права через Supabase Auth + RLS; чистка `S.del`;
   «ВУОКСА · 2026» заглавными против «Вуокса · 2026» — заказчик так и не решил.

## Состояние React-версии v2 (`app/`)

**Сделано (коммит `c216843`):**
* **Расчётное ядро** — `app/src/lib/types.ts` (типы модели v2) и `app/src/lib/calc.ts`
  (чистые функции: `routeKm`, `litres`, `fuelCost/fuelTotal`, `rentSum/rentTotal`,
  `buyTotal`, `personalTotal`, `cans`, `money`, агрегатор `calcAll`).
  Сверка — `node app/scripts/check-sums.mjs`: 330 км, транспорт 21 385 ₽, закупка 26 005 ₽,
  итого 47 390 ₽, с каждого 11 848 ₽, канистр АИ-92 — 2. **Все шесть сходятся.**
  Гонять этот скрипт после КАЖДОЙ правки расчётов.
* **Каркас** — токены обеих тем (`app/src/index.css`), Manrope, логотип с заливкой эмблемы
  цветом фона, тема — личная настройка браузера (`flops.theme`, НЕ поле документа).
  Десктоп: прилипающее верхнее меню с индикатором на Motion layoutId, контейнер 1120.
  Мобайл: нижняя панель, все цели ≥ 44 px. Разделы — динамический массив (`app/src/sections.ts`),
  логика «Ещё» при > 6 пунктов готова, шторка — нет.
* **«Поездка»** — обложка (квадрат слева на десктопе, даты, места, обратный отсчёт),
  свой календарь диапазона дат, шторка точек назначения, четыре денежные плитки из живого расчёта.
* **Контрастные токены**: `--accent-text` (мелкий акцентный текст: `#A74612` на песочном даёт
  лишь 3.9:1) и `--accent-fill` (заливка кнопок: кремовый на светлом янтаре — 3.8:1).
  Использовать их, а не `--accent`/`--accent-deep`, для текста и кнопок.

**Дальше по порядку:**
1. Разделы Экипаж, Сборы, Закупка, Меню — сейчас заглушки.
2. «Дорога»: карта Leaflet/OSM + OSRM, Техника, Топливо, Аренда, Расчёт, Канистры —
   вся арифметика уже есть в `lib/calc.ts`, нужен только интерфейс.
3. Погода по дням — гармошка в строке (не шторка), кольцо готовности из статусов «Сборов».
4. Право-модель (сейчас в v2 даты меняют все) и синхронизация: портировать `src/online.js`
   (слияние по позициям, права по ссылке, Realtime, presence) — модель прав НЕ менять.
5. PWA + офлайн-сборка одним файлом. Публикация вместо v1 — только после паритета и согласия.

**Риски v2:** документ целиком лежит в localStorage (~270 КБ с фото) — при росте фотографий
упрёмся в квоту; бандл 655 КБ (фото в сиде) — при подключении Leaflet стоит разделить на чанки;
`dist.auto` пока не считается из точек маршрута (нужен OSRM).

Запуск v2 локально: конфиг `flops-app-dev` в `.claude/launch.json`, порт 5199.

Хвосты v1 (не потерять):
1. **Настоящие права** — Supabase Auth (magic link) и RLS по `auth.uid()`, если «джентльменских» окажется мало.
2. **Чистка `S.del`** — метки удалённых позиций копятся вечно.
3. **Мелкие цели касания** — исторические кнопки строк 38 px, кружок 30 px; закрыть при v2.
4. Обложка: «ВУОКСА · 2026» заглавными против «Вуокса · 2026» в остальных местах — заказчик так и не решил.

## Ограничения, которые нельзя нарушать

Один HTML-файл на версию, весь CSS и JS внутри, ванильный ES5, без npm, фреймворков и внешних
загрузок (модуль синхронизации и всё, что связано с сетью, вырезается при сохранении файла —
список селекторов в `SAVE_STRIP`). Мобильный приоритет, проектирование под 390 × 844,
интерактивные элементы не меньше 44 × 44 px, горизонтального скролла нет. Всё на русском,
с русской типографикой, без `alert()` и `confirm()`, эмодзи как иконки не используются.
Имена участников: **Миша**, не «Мишка». Ничего из данных поездки не выбрасывать.

## Как работает заказчик

Не программист. Объяснять словами, что сделано и что нажать. Правки — сразу в файлы, после каждой
значимой проверять в браузере: нет ошибок в консоли, ничего не разъехалось на 390 px. Работать
автономно, вопросы задавать только там, где без ответа получится не то. В конце задачи давать:
список изменённых файлов, какие проверки выполнены, какие риски остались, что рекомендуется дальше.
