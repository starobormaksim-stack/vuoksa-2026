# Задание сессии · двадцать восьмая порция

Pine-to-Pine, двадцать восьмая порция.
Читать: CLAUDE.md → docs/DEFINITION-OF-DONE.md → docs/LESSONS.md
(У-01…У-152, особенно У-148…У-152) → .claude/rules/environment.md →
HANDOFF.md, верхняя секция.

⛔ ПЕРВОЕ ДЕЙСТВИЕ — проверить, что боевой ЖИВ (У-140): имя файла из свежего
index.html → спросить его С «?bust=<время>» → и только потом голый адрес.
Вердикт «работает» — только загрузкой страницы Playwright'ом, root.children = 1.

⚠️ Ключ в правилах среды НЕ хранится (У-133). Доставать из снимка:
node -e "const d=require('./backups/prod-2026-08-06T2246.json')['0'].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"

СОСТОЯНИЕ: всё свёрнуто по умолчанию и помнится браузером — `foldpref.ts`,
ключ `flops.fold.<поездка>.<раздел>`, карта РАСКРЫТОГО (У-152). Прыжки
в свёрнутое идут через `requestUnfold` (поиск в App.tsx, плитки сумм
в MoneyTiles, «плюс» в RoadSection). Свёрнутая статья «Закупки» показывает
`secSum` в шапке; инвариант «Σ подытогов статей = закупка» — в check-sums.
Поиск: окно `top-4 sm:top-1/3`, CheckIcon `hidden`, строки 44 px (У-151).
Ветки карты: шеврон у каждой, «Убрать ветку» в свойствах. «Добавить раздел»
в «Сборах» и «Закупке».

ГЛАВНАЯ РАБОТА — по убыванию боли:
1. Единая таблица расходов, шаг 2: `BuySection.kind?: 'goods' | 'fuel' |
   'rent' | 'transport' | 'thing'` — аддитивно, и ОБЯЗАТЕЛЬНО добавить
   'kind' в PLAIN.buySections в lib/merge.ts, иначе тип не доедет с чужого
   телефона. ⚠️ Заказчик на четыре вопроса 26-й порции прямо не ответил;
   слияние «Дороги» и «Закупки» НЕ подтверждено — шаг 3+ не начинать без
   его «да». ⚠️ Это деньги: check-sums после каждого шага.
2. Подразделы («создать раздел, подраздел, строку» — подразделов нет нигде).
   Сначала спросить, где они ему нужны: в «Закупке» внутри статей или везде.
3. Премиальный аудит: типографика, выравнивания, лишние пробелы и пустые
   строки по всему листу («всё должно быть просто шикарно… лаконично»).
   a11y-auditor + design-system-keeper, отчёт числами.
4. Офлайн-карта картинкой: последний кадр, листается чуть шире точек.
5. Водитель у ветки (аддитивно + PLAIN.transport в lib/merge.ts).
6. Время у точки (RoutePoint.time уже есть) — без «менюшки» на карте (У-128).

⛔ НЕ ТРОГАТЬ: `foldpref.ts` — карта хранит РАСКРЫТОЕ, чистое хранилище =
«всё свёрнуто»; в документ состояние вида не писать (У-152); requestUnfold
при любом новом прыжке в свёрнутое; RING в lib/gearx.ts (У-146); CARD_W
в CrewSection (У-147); BuyBox — один орган на три места (У-150); обёртку
<Command> в ui/command.tsx и её top-4 (У-151); S.weather.daylight/concl/src
из документа не выбрасывать (У-144); becomeInCopy только в офлайн-копии
(У-141); «Взаиморасчёты» — один блок в конце листа (У-143); ord (У-136);
commonKmUsed (У-138); правила /assets/* (У-140); погоду в документ
не писать (У-135); applySpend; ключи в репозиторий не возвращать (У-133);
pickHits и shouldFilter={false}; visibleBlockId (У-131); плитки сумм — две
колонки; прежние запреты (У-128…У-130, --bottom-nav-h, toneOf, dg(value),
MAP_TONES только для карты, lastIndexOf, S.tileLabels позиционный).

СРЕДА: netstat -ano | findstr ":5199 :5200 :4173 :4199" (LISTENING).
preview_start открывает голый адрес — сразу location.replace('about:blank').
Проба — Playwright (глобальный,
C:/Users/staro/AppData/Roaming/npm/node_modules/playwright/index.mjs,
подключать как file:///C:/...). Свой статический сервер на app/dist +
route-заглушки. ⚠️ Общую заглушку **/rest/v1/** регистрировать ПЕРВОЙ,
точные rpc/trip_read (отдаёт МАССИВ строк [{data,…}]), trip_write,
trip_touch — ПОСЛЕ неё (У-149). ⚠️ Меряя круг состояний одного органа,
брать узел один раз через evaluateHandle (У-148). Снимок боевого —
backups/prod-2026-08-06T2246.json (ключ ['0'].data).

⚠️ Ключи заказчику НАПОМНИТЬ ПЯТЫЙ раз: полежали в публичном репозитории,
история гита их хранит. Лечение — сменить в разделе «Команда» и раздать
новые ссылки (говорилось в 23, 25, 26, 27-й порциях).
