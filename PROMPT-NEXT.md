# Задание двадцать шестой сессии

Pine-to-Pine, двадцать шестая порция.
Читать: CLAUDE.md → docs/DEFINITION-OF-DONE.md → docs/LESSONS.md
(У-01…У-143, особенно У-141…У-143) → .claude/rules/environment.md →
HANDOFF.md, верхняя секция.

⛔ ПЕРВОЕ ДЕЙСТВИЕ — проверить, что боевой ЖИВ. ⚠️ Порядок такой (У-140):
взять имя файла из свежего index.html, спросить его С «?bust=<время>» —
это идёт к источнику и кеш не портит; и ТОЛЬКО когда источник отдал код,
спрашивать голый адрес. Голый адрес файла сборки в цикле после выкладки
не дёргать вовсе. Вердикт «работает» — только загрузкой страницы
Playwright'ом с root.children = 1.

⚠️ Ключ в правилах среды НЕ хранится (У-133, репозиторий публичный).
Действующий доставать из снимка:
node -e "const d=require('./backups/prod-2026-08-06T2246.json')['0'].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"

СОСТОЯНИЕ: офлайн-копия спрашивает «Кто вы в этой копии?» и помнит выбор
внутри файла (У-141, `OfflineWho.tsx` + `becomeInCopy` в store.ts).
Из-под карты убраны кнопка «Точка» и строка «Карта Google · сборка …» —
часть У-30…У-32 про вечную строку ОТМЕНЕНА словом заказчика (У-142);
полоса под картой рисуется только когда ей есть что сказать. Блок
«Взаиморасчёты» — один и в САМОМ КОНЦЕ листа, после «Меню» (У-143,
App.tsx); расчёт не менялся.

ГЛАВНАЯ РАБОТА — единая таблица расходов. Слова заказчика 08.08.2026
дословно: «У нас же должны добавляться строки разных видов сущностей:
товар, топливо, аренда, просто вещь. Они по-разному фиксируются. У тебя
должна быть одна таблица… бензин должен вписываться в строку, в которой
идёт речь про топливо… Общая промежуточная информация не нужна. Общее
считается в конце… Сворачивай — свернуть и держать все строки, особенно
в части логистики». Это модельная работа: СНАЧАЛА план через
chief-architect и product-architect (что считается «одной таблицей», что
происходит с формой хранения — постулат 4, форму хранения не менять),
ПОТОМ код. ⚠️ Это деньги: check-sums после каждого шага.

ДАЛЬШЕ по убыванию боли:
2. Офлайн-карта картинкой: последний кадр, листается чуть шире точек.
3. Водитель у ветки (аддитивно + PLAIN.transport в lib/merge.ts).
4. Время у точки (RoutePoint.time уже есть) — без «менюшки» на карте (У-128).
5. Транспорт без расчёта по карте (электричка).
6. «Тянуть от выбранной точки» (Г-2), раздел «Транспорт».

⛔ НЕ ТРОГАТЬ: `becomeInCopy` работает только в офлайн-копии — в онлайн эту
дверь не открывать (У-141); кнопку «Точка» и строку сборки под карту
не возвращать (У-142); «Взаиморасчёты» — один блок в конце листа, второй
не заводить (У-143, У-53); порядок точек берётся из ord (У-136); величина
и подпись из одного вызова (У-137); commonKmUsed — единственный предикат
общего пробега (У-138); правила /assets/* в _redirects и _headers (У-140);
погоду в документ не писать (У-135); колонку «Стоимость» (У-134);
applySpend — единственная арифметика зачёта; ключи в репозиторий
не возвращать (У-133); поиск отбирает сам (pickHits, shouldFilter={false});
visibleBlockId по середине видимой части (У-131); свёрнутая группа расчёта
обязана показывать итог; плитки сумм — две колонки; прежние запреты в силе
(У-128…У-130, --bottom-nav-h, toneOf, dg(value), MAP_TONES только для
карты, обёртка <Command>, lastIndexOf, S.tileLabels позиционный).

СРЕДА: netstat -ano | findstr ":5199 :5200 :4173 :4199" (LISTENING).
preview_start открывает голый адрес — сразу location.replace('about:blank').
Проба — Playwright (глобальный,
C:/Users/staro/AppData/Roaming/npm/node_modules/playwright/index.mjs,
подключать как file:///C:/...). Свой статический сервер на app/dist +
route-заглушки — образец в скретчпаде прошлой сессии не ищи, собери по
HANDOFF: rpc/trip_read ОТДАЁТ строку снимка (пустой массив уводит на
«Заведите свою поездку»), trip_write и trip_touch — обрывать; заглушить
maps.googleapis.com, tile.openstreetmap.org, nominatim.openstreetmap.org,
api.open-meteo.com. Снимок боевого — backups/prod-2026-08-06T2246.json
(ключ ['0'].data).

⚠️ Ключи заказчику НАПОМНИТЬ: полежали в публичном репозитории, история
гита их хранит. Полное лечение — сменить в разделе «Команда» и раздать
новые ссылки (говорилось в 23-й и 25-й порциях — пока не сделано).
