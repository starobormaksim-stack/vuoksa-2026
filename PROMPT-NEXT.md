# Задание следующей сессии · Pine-to-Pine, тридцатая порция

Читать: `CLAUDE.md` → `docs/DEFINITION-OF-DONE.md` → `docs/LESSONS.md`
(У-01…У-155, особенно У-148…У-155) → `.claude/rules/environment.md` →
`HANDOFF.md`: сначала секцию **«АРХИТЕКТУРА РАЗДЕЛОВ ЦЕЛИКОМ, СЛОВАМИ
ЗАКАЗЧИКА»**, потом «ДВАДЦАТЬ ДЕВЯТАЯ ПОРЦИЯ».

## ⛔ Первое действие — боевой жив (У-140)

Имя файла сборки из свежего `index.html` (запрашивать с `?bust=`), вердикт —
только живой загрузкой Playwright, `root.children = 1`.

## ⚠️ Ключи: действуют СТАРЫЕ, от 06.08 (У-155)

Ротация 08.08 откатилась сама. Рабочие ключи — в `backups/prod-2026-08-06T2246.json`.
В репозиторий их не возвращать (У-133), брать так:

```bash
node -e "const d=require('./backups/prod-2026-08-06T2246.json')['0'].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"
```

## ГЛАВНАЯ РАБОТА: перестройка разделов по словам заказчика

Полностью записана в `HANDOFF.md`. Коротко: **«Взять с собой» → «Расходы»
(внутри: купить · арендовать · логистика) → «Проживание» → «Меню» →
«Вопросы» → взаиморасчёты В САМОМ КОНЦЕ**. Это 3–4 сессии.

⛔ Начинать не с кода: сначала chief-architect (план шагов, сохранность
`secTitles`, `canRows`, коллекций) и ux-designer (раскладка «Расходов»
с подразделами). Каждый шаг — самостоятельно выкладываемый, с контрольными
цифрами после него.

**Порядок, малое сначала (можно сделать быстро и выложить):**

1. **Вид топлива под картой Google** — сегодня не показан вовсе, заказчик
   заметил. Мелкий дефект, закрывается до перестройки.
2. **«Общий счёт» — в конец списка «Закупки»**, а не перед ним. Слово
   с нажимом: «по десять раз я тебе сказал: сначала список, потом в конце
   уже разблюдовка». Взаиморасчёты уже в конце (У-143), этот блок — нет.
3. **Новый раздел «Проживание»** — пустой, но существующий.
4. **Канистры из «Логистики» в «Взять с собой»** («инвентарь для техники»).
   ⚠️ Это `S.canRows` и контрольная цифра «2 канистры» — переносить показ,
   не трогая расчёт.
5. **Аренда отдельным разделом, парковка внутри неё** (сейчас один блок
   «Аренда и парковка»).
6. **Переименования разделов** — ⛔ помнить про `S.secTitles`
   (`lib/sectitles.ts`, «Дорога» = ключ `log`): там лежат СВОИ названия
   заказчика, затирать нельзя.
7. **Техника:** топливо внутрь строки техники (сейчас `S.fuelPrices`
   и `S.transport` — два блока на экране), вид топлива выпадающим списком
   со своей ценой, «Посчитать по карте» у каждой техники, добавленная техника
   сразу видна на карте, плюс «Добавить технику» у шапки.
8. **Меню по числу дней поездки** — день без меню существует пустым.

## Спросить у заказчика (без ответов выйдет не то)

1. **Чей чек Пятёрочки на 5 333,41 ₽** — 34 позиции не записаны ни на кого
   (`o` и `payer` пусты), и потому не идут во взаиморасчёты. Лечится
   проставлением `o = {max: q}`, если платил Макс.
2. **Сводить ли 16 пар «плановая строка ↔ строка из чека»** — это удаление
   строк, без подтверждения не трогать (постулат 4).
3. **Повторная ротация ключей** — скрипт починен, откат не повторится,
   но у команды отвалятся ссылки.
4. **Удаление техники со спросом** — он просит «вы уверены?», а постулат 9
   запрещает `confirm()`, и сегодня это тост с «Отменить». Прямое
   противоречие, решить словами.
5. **Короткий бейдж «0 из 14»** в шапке «Сборов» на 390 — иначе плюс строки
   в шапку не помещается.

## Готовый план, ждёт вёрстки: плюс добавления СТРОКИ у блока

В `HANDOFF.md`, секция двадцать девятой порции: плюс 44 × 44 в шапку каждого
блока, `AddRow` убрать, «Добавить раздел» увести в «⋯».

## ⛔ Не трогать

`foldpref.ts` (У-152) · `requestUnfold` · `RING` (У-146) · `CARD_W` (У-147) ·
`BuyBox` (У-150) · `<Command>` и top-4 (У-151) · отложенный фокус `TextSheet`
(У-154) · слот `marks` в `StripRow` — он внутри кнопки, интерактив туда класть
нельзя · `ord` · `applySpend` · ключи в репозиторий не возвращать (У-133).

## ⚠️ Среда

`vuoksa2026-test` и `vuoksa2026-rotate-test` — надгробия (У-153), для репетиций
заводить свежее имя на «-test». Общую заглушку `**/rest/v1/**` в Playwright
регистрировать ПЕРВОЙ, точные — после (У-149). Уборка в конце: сначала увести
вкладку на `about:blank`, потом гасить сервер (У-69).
