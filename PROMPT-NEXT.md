# Промпт новой сессии · Pine-to-Pine · РЕВИЗИЯ ПО ПОСТРОЕННОЙ СИСТЕМЕ

Рабочая папка — `C:\Projects\Вуокса-2026`, репозиторий `starobormaksim-stack/vuoksa-2026`.
Боевой адрес: **https://pine-to-pine.com** (Cloudflare Pages, собирается сам при `git push`).
Заказчик — Максим, **не программист**.

## 0. Система разработки построена. Работать по ней

Сессия 05.08.2026 (коммит `248e308`) сделала то, ради чего останавливали код.
**Читать в этом порядке, до первой правки:**

| Файл | Что там |
|---|---|
| `CLAUDE.md` | 14 постулатов и запреты. 136 строк при потолке 200 |
| `docs/DEFINITION-OF-DONE.md` | четыре ступени готовности. **Читать до слова «готово»** |
| `docs/LESSONS.md` | 54 урока У-01…У-54 в восьми группах |
| `docs/WORKING-STANDARD.md` | откуда берутся компоненты, мера и ритм |
| `docs/AGENTS.md` + `.claude/agents/` | 16 профилей субагентов, маршруты, приёмка |
| `.claude/rules/environment.md` | грабли среды: чем мерить, что врёт |

Два урока, ради которых всё затевалось:
**У-10** — прошедшие `tsc` и сборка это **допуск к проверке**, а не готовность.
**У-11** — любая правка раскладки меряется на **обеих** ширинах и на **соседях**.

Кода в той сессии не было тронуто ни строки. Контрольные суммы прогнаны, сходятся.

---

## ⛔ Задача 1 — возможная потеря правок заказчика. Первым делом

**Найдено 04.08.2026 и не разобрано.** На сервере в строке `vuoksa2026` **8 точек
маршрута**, а открытая по личной ссылке страница показала **«10 точек · 6 на карте»**.
Либо часть правок не доехала до сервера, либо страница показывает устаревшую местную копию.

Это важнее любого оформления: заказчик мог ставить точки, а они не сохранились.

Порядок (зови `data-guard` первым):
* снять снимок `?id=eq.vuoksa2026&select=data,updated_at,author`, сверить `route`;
* понять, откуда взялись 10: местная копия `flops.doc`, слияние, или запись отклонена;
* если запись отклоняется — найти причину (ключ, `trip_write`, RLS после `rls-apply-d.sql`)
  и починить.

Заказчик заходит по ссылке Кости, автор последней записи — «Костя», метка
`2026-08-04T17:24:17.913+00:00`. Признак беды: `author` пуст при непустых данных.

## Задача 2 — карта: не спор о Google, а обновление сборки

**Замерено и закрыто:** боевой домен отдаёт **Google-карту**, Leaflet отсутствует.
Прежний вывод «Google не пускает» снят. Старую карту показывает **устройство заказчика** —
на нём держится прошлая сборка.

Значит задача одна: **вылечить обновление окончательно**. Зови `release-engineer`.
Смотреть `app/public/sw.js`, `sw.js?v=…`, `__BUILD__`, `_headers`, кеш Cloudflare
(уроки У-38, У-39).

И отдельно, независимо от лечения: **под картой всегда печатать, какая карта нарисована
и какая сборка**. Тогда следующий такой спор закрывается одним снимком экрана заказчика,
а не сессией переписки (уроки У-30, У-31, У-32).

## Задача 3 — фотографии. ✅ Вопрос закрыт ответом заказчика

**Ответ 05.08.2026: речь только о карточках участников в «Команде».**
Шапки колонок в «Сборах» (квадрат 40 px) и обложка поездки его устраивают — **не трогать**.

Сделать: на телефоне карточка участника **во всю ширину или близко к ней** (сейчас
сетка 2 колонки, фотография ≈ 170 px — он называет это «узенькими», говорил трижды).
Снимок по-прежнему **не кадрируется**: `object-contain` плюс размытая копия под ним.

## ⛔ Задача 3.5 — дубли списков. ✅ Вопрос закрыт, и ответ шире, чем ждали

Заказчик, дословно 05.08.2026:

> «У тебя дублируется список товаров на всём протяжении. Не нужно повторять: у тебя
> должны быть чёткие разделы, в них чётко находится. Больше нигде не повторяться списками
> оборудования, инвентаря, вещей или продуктов. Больше нигде это не дублируется, это
> не нужная информация.
>
> У тебя есть матрица, в которой у тебя есть участники, которые собирают те или иные вещи.
> С левой стороны столбец „вещей“, справа столбцы с исполнителями, участниками. Как у тебя
> было, так это идёт. Дальше, уже в зависимости от специфики того или иного раздела,
> отображение должно быть локально немного изменено, но чтобы единообразие было.»

Это ответ и на пункт 4 прошлого задания (правый столбец с несобранным), и на «зачем-то
внизу сборы». Решение больше не «решить, нужен ли он рядом с обложкой» — **его там быть
не должно**.

Сделать:
1. **Снести `components/trip/ReadyLeft.tsx`** и его место в раскладке
   `components/trip/TripSection.tsx`. Сводка остаётся, но **числом**, а не списком.
2. **Пройти весь сервис и найти остальные дубли** — любое место, где перечисляются
   позиции чужого раздела. Это отдельный обход, а не одна правка.
3. **Свести форму списочных разделов к одной** (урок У-54): липкая колонка названий
   слева, столбцы людей справа, прокрутка вбок внутри блока. Свои колонки раздел
   добавляет по специфике, структуру не меняет. Проверка: открыть «Сборы», «Закупку»,
   «Дорогу», «Меню» подряд — человек не переучивается.

**Уже починено, переделывать не надо** (коммит `00dfdc6`): названия разделов резались;
одно лицо рисовалось дважды; панель сумм накрывала название поездки.

Осталось от прошлого разбора: **посмотреть обложку целиком живьём** — на снимке под
суммами просвечивали подзаголовок «ЮБИЛЕЙНАЯ · 10 лет» и строка дат. После правки это
должно было уйти, но проверено только расчётом на 1280, глазами — нет.

## Задача 4 — живая ревизия всего сервиса

Не по коду. На **390 × 844** и на десктопе, в **обеих темах**, с измерениями.
Зови `live-tester` и `a11y-auditor`. Для взгляда глазами — Playwright.

Обойти: обложка · таблица сборов (строки ~80 px, кружок и счётчик столбиком — попадает
ли палец) · закупка (таблица ≈ 1030 px, листается ли внутри блока) · дорога · меню ·
команда · карта · поиск · меню «⋯» · состояния наведения и нажатия во всём сервисе.

## Задача 5 — хвосты

* Четыре шторки выбора в «Дороге» → на готовый `InlinePick` (вид техники, топливо,
  категория аренды, метка точки). Это же и есть постулат «попапов нет».
* Вернуть «Попросить отметить» и «Попросить изменить количество» — поля `Gear.q`
  и `Buy.qask` целы, но написать просьбу негде.
* Полоса хода при дублировании поездки (тянет под мегабайт через браузер).
* Офлайн-копия непервой поездки: ключ другой (`flops.doc.<поездка>`) — проверить.
* Строка `probe-guard` в `trips` — убирается только SQL, делать самому.
* `owner_email` строки `vuoksa2026` закрепить за `starobormaksim@gmail.com` — самому.
* Квота Supabase: «Grace period is over».
* `S.del` копится вечно; `dvh` не понимает iOS < 15.4; колонка «V» в Excel односторонняя.

---

## Чего ломать нельзя

Полный список — `CLAUDE.md`, раздел «Чего ломать нельзя». Коротко: боевые данные ·
обёртка `<Command>` в `ui/command.tsx` · `lastIndexOf` в `lib/offline.ts` · строку «Вы — …»
обратно в полосу меню · `trip_write` с откатом при 404 · `isSandbox()` с `localhost` ·
модель прав и `fillMissingKeys` · формулы `calc.ts` и суммы **330 км · 21 385 / 26 005 /
47 390 / 11 848 ₽ · 2 канистры** · `base` и `__BUILD__` · бренд из четырёх цветов и Chewy ·
шкала размеров без штучных значений · одна толщина иконок 1,75 · интерактив ≥ 44 × 44 ·
потолок высоты шторок только в `ui/drawer.tsx` · правило в `index.css` про закрытую
подложку · `S.tileLabels` — массив, `S.secTitles` — объекты `{h, sub}`, «Дорога» = `log` ·
русский язык, без `alert`/`confirm`, «Миша» не «Мишка».

## Данные

Снимок строки `vuoksa2026` до работы · проверки только на `vuoksa2026-test` · тесты
записи в обход НИКОГДА не на боевой · служебные строки называть только `*-test` ·
песочницу после работы удалять.

`preview_start` открывает голый адрес — уводить `location.replace` на
`http://localhost:5199/?u=Maks&k=zdzua343&sandbox=1`.

Ссылки: Макс `/?u=Maks&k=zdzua343` · Костя `/?u=Kostya&k=pc2s6bsg` ·
Миша `/?u=Misha&k=9ak33b2w` · Женя `/?u=Zhenya&k=idz7qw3y`.

## Проверки

```bash
node app/scripts/check-sums.mjs      # 330 км · 21 385 / 26 005 / 47 390 / 11 848 ₽ · 2 канистры
npx oxlint app/src
cd app && ./node_modules/.bin/tsc --noEmit -p tsconfig.app.json   # npx tsc в корне НЕ работает
npm --prefix app run build
npm --prefix app run build:offline
```

Публикация: сборка + офлайн + `node publish-v2.mjs` + `git push`.
Свежесть боевого сверять **по содержимому**, не по хешу.

⚠️ В этой среде **вкладка не рисует кадров**: скриншоты и измерения на узком экране
молча врут. Геометрия — `getBoundingClientRect`, перекрытия — `elementFromPoint`,
Radix кликать `pointerdown` + `pointerup` + `click`. Подробности —
`.claude/rules/environment.md`.

**Потолок сессии — 250–300 тысяч токенов.** Дойдя до него: закоммитить, дописать
`HANDOFF.md` и этот файл, сказать «Рекомендую новую сессию».
