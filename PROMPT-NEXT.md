# Задание следующей сессии · Pine-to-Pine, тридцатая порция

Читать: `CLAUDE.md` → `docs/DEFINITION-OF-DONE.md` → `docs/LESSONS.md`
(У-01…У-155, особенно У-148…У-155) → `.claude/rules/environment.md` →
`HANDOFF.md`, верхняя секция «ДВАДЦАТЬ ДЕВЯТАЯ ПОРЦИЯ».

## ⛔ Первое действие — боевой жив (У-140)

Имя файла сборки из свежего `index.html` (запрашивать с `?bust=`), вердикт —
только живой загрузкой Playwright, `root.children = 1`.

## ⚠️ Ключи: действуют СТАРЫЕ, от 06.08 (У-155)

Ротация 08.08 откатилась сама. Рабочие ключи — в `backups/prod-2026-08-06T2246.json`.
В репозиторий их не возвращать (У-133):

```bash
node -e "const d=require('./backups/prod-2026-08-06T2246.json')['0'].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"
```

## Работа по порядку

1. **Ответы заказчика по данным** (спрошено в конце 29-й порции, работать
   только по его слову):
   * чей чек Пятёрочки на 5 333,41 ₽ — 34 позиции не записаны ни на кого,
     и потому не идут во взаиморасчёты. Лечится `o = {max: q}` (или чей он);
   * сводить ли 16 пар «плановая строка ↔ строка из чека» — это удаление
     строк, без подтверждения не трогать (постулат 4);
   * делать ли повторную ротацию ключей (у команды отвалятся ссылки).
     Скрипт уже починен, откат не повторится.
2. **Плюс добавления СТРОКИ у блока** — план готов и лежит в `HANDOFF.md`
   (плюс 44 × 44 в шапку блока, `AddRow` убрать, «Добавить раздел» — в «⋯»).
   Перед вёрсткой спросить про короткий бейдж «0 из 14» в шапке «Сборов»
   на 390. Мерить обе ширины и соседей поимённо (У-11).
3. **Единая таблица денег, шаг 3** — экран. Шаги 1 и 2 сделаны
   (сумма в свёрнутой шапке; `BuySection.kind` + провоз через слияние).
   Разбор архитектора — в `HANDOFF.md`, секция двадцать шестой порции.
   ⛔ Коллекции `S.buy` / `S.transport` / `S.rent` не сливать: сотрёт `nt`,
   `blocks`, `calcT`, `warn`, `rateU`, `carry`, `kmSrc`, `tone`, `color`,
   `owner`, а метки удаления привязаны к имени коллекции.
4. **«Ссылка на почту»** — спроектировать. Слово заказчика: «если прошу
   ссылку на почту — должна приходить ссылка, которую можно скопировать
   и перейти». Вход по почте уже есть (`lib/auth.ts`, magic link);
   чего нет — отправки ЛИЧНОЙ ссылки участника письмом.
5. **Хвост:** подразделы · аудит типографики · офлайн-карта картинкой ·
   водитель у ветки · время у точки.

## ⛔ Не трогать

`foldpref.ts` (У-152) · `requestUnfold` · `RING` (У-146) · `CARD_W` (У-147) ·
`BuyBox` (У-150) · `<Command>` и top-4 (У-151) · отложенный фокус `TextSheet`
(У-154) · слот `marks` в `StripRow` — внутри кнопки, интерактив туда класть
нельзя · `ord` · `applySpend` · ключи в репозиторий не возвращать (У-133).

## ⚠️ Среда

`vuoksa2026-test` и `vuoksa2026-rotate-test` — надгробия (У-153), для репетиций
заводить свежее имя на «-test». Общую заглушку `**/rest/v1/**` в Playwright
регистрировать ПЕРВОЙ, точные — после (У-149). Уборка в конце: сначала увести
вкладку на `about:blank`, потом гасить сервер (У-69).
