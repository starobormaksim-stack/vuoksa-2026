# Задание двадцать девятой порции

Pine-to-Pine, двадцать девятая порция.
Читать: CLAUDE.md → docs/DEFINITION-OF-DONE.md → docs/LESSONS.md
(У-01…У-154, особенно У-148…У-154) → .claude/rules/environment.md →
HANDOFF.md, верхняя секция.

⛔ ПЕРВОЕ ДЕЙСТВИЕ — проверить, что боевой ЖИВ (У-140): имя файла из свежего
index.html → спросить его С «?bust=<время>» → и только потом голый адрес.
Вердикт «работает» — только загрузкой страницы Playwright'ом, root.children = 1.

⚠️ Ключ в правилах среды НЕ хранится (У-133). Ключи СМЕНЕНЫ 08.08 (утёкшие
в историю гита больше не работают). Доставать из СВЕЖЕГО снимка:
node -e "const d=require('./backups/prod-2026-08-08T1333-receipt.json')[0].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"

СОСТОЯНИЕ: ключи ротированы (rotate-keys.mjs), чек Пятёрочки вписан
в «Закупку» (85 позиций, +5 333,40 ₽ факта, все «куплено»), TextSheet
фокусирует поле через 550 мс на телефоне (У-154), плюс команды по центру.

ГЛАВНАЯ РАБОТА — по убыванию боли (слова заказчика 08.08, вторая половина):
1. ЕДИНАЯ ТАБЛИЦА ДЕНЕГ — ПОДТВЕРЖДЕНА заказчиком («как договаривались»).
   Шаг 2: BuySection.kind (аддитивно) + 'kind' в PLAIN.buySections
   в lib/merge.ts. Дальше экран по плану архитектора (HANDOFF 26).
   Деньги: check-sums после КАЖДОГО шага.
2. Плюс добавления СТРОКИ товара у каждого блока — как плюс у команды
   («сущность какая-то не нужна»). Сначала ux-designer, потом вёрстка.
3. «Ссылка на почту» — спроектировать: заказчик хочет получать личную
   ссылку письмом, чтобы копировать/переходить. Из среды письма не шлются —
   думать про mailto из «Ссылок команды» или функцию базы.
4. Хвост: подразделы · аудит типографики · офлайн-карта · водитель у ветки ·
   время у точки.

⛔ НЕ ТРОГАТЬ: foldpref.ts (карта РАСКРЫТОГО, У-152); requestUnfold; RING
(У-146); CARD_W (У-147); BuyBox (У-150); <Command> и top-4 (У-151);
S.weather.daylight/concl/src (У-144); becomeInCopy (У-141); «Взаиморасчёты»
одним блоком (У-143); ord (У-136); commonKmUsed (У-138); /assets/* (У-140);
погоду в документ не писать (У-135); applySpend; ключи в репозиторий
не возвращать (У-133!); pickHits и shouldFilter={false}; visibleBlockId;
отложенный фокус TextSheet не снимать (У-154); прежние запреты (У-128…У-130).

СРЕДА: netstat -ano | findstr ":5199 :5200 :4173 :4199" (LISTENING).
preview_start открывает голый адрес — сразу location.replace('about:blank').
Проба — Playwright (file:///C:/Users/staro/AppData/Roaming/npm/node_modules/
playwright/index.mjs), свой статический сервер на app/dist + route-заглушки.
⚠️ Общую **/rest/v1/** регистрировать ПЕРВОЙ, точные rpc/* — ПОСЛЕ (У-149).
⚠️ Узел одного органа мерить через evaluateHandle (У-148).
⚠️ vuoksa2026-test и vuoksa2026-rotate-test — НАДГРОБИЯ (У-153): не открываются
никому; для репетиций заводить свежую строку на «-test».
Снимок — backups/prod-2026-08-08T1333-receipt.json (массив, [0].data).
