# Задание следующей сессии · Pine-to-Pine, тридцатая порция

Читать: `CLAUDE.md` → `docs/DEFINITION-OF-DONE.md` → `docs/LESSONS.md`
(У-01…У-155, особенно У-148…У-155) → `.claude/rules/environment.md` →
`HANDOFF.md`: сначала **«АРХИТЕКТУРА РАЗДЕЛОВ ЦЕЛИКОМ, СЛОВАМИ ЗАКАЗЧИКА»**,
потом «ПАРТНЁРЫ НА КАРТЕ», потом «ДВАДЦАТЬ ДЕВЯТАЯ ПОРЦИЯ».

## ⛔ Первое действие — боевой жив (У-140)

Имя файла сборки из свежего `index.html` (запрашивать с `?bust=`), вердикт —
только живой загрузкой Playwright, `root.children = 1`.

## ⚠️ Ключи и состояние боевого

Ротация 08.08 откатилась сама (У-155) — действуют СТАРЫЕ ключи от 06.08.
**Менять их заказчик пока НЕ просит** — не трогать. Брать так (в репозиторий
не возвращать, У-133):

```bash
node -e "const d=require('./backups/prod-2026-08-06T2246.json')['0'].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"
```

⚠️ **Заказчик правит лист живьём прямо сейчас** — сам убирает дубли чека
(позиций было 85, стало 81). Любая правка данных в обход приложения обязана
двигать `ua` (У-155) и читать документ непосредственно перед записью.

## Что заказчик уже ответил (не переспрашивать)

* **Чек Пятёрочки оплачивал он** — покупатель проставлен 08.08 в 19:16
  (`backups/set-receipt-payer.mjs`, 33 позиции на 5 073,42 ₽; всего позиций
  с покупателем стало 37). ✅ закрыто.
* **Дубли план↔чек сводить НЕ надо** — «я уберу лишнее» сам. ✅ снято.
* **Ключи менять сейчас НЕ надо.** ✅ снято.
* **Удаление техники — со спросом:** «это серьёзно повлияет на процесс».
  ⚠️ `confirm()` и попапы запрещены (постулаты 2 и 9) — спрашивать
  НА МЕСТЕ, в самой строке: действие превращается в «Удалить? · Отмена».
  Образец подтверждения в строке в проекте искать через component-scout;
  тост с «Отменить» остаётся дополнительно, а не вместо.

## ГЛАВНАЯ РАБОТА: перестройка разделов

Полностью записана в `HANDOFF.md`. Коротко: **«Взять с собой» → «Расходы»
(внутри: купить · арендовать · логистика) → «Проживание» → «Меню» →
«Вопросы» → взаиморасчёты В САМОМ КОНЦЕ**. Это 3–4 сессии.

⛔ Начинать не с кода: сначала chief-architect (план шагов, сохранность
`secTitles`, `canRows`, коллекций) и ux-designer (раскладка «Расходов»
с подразделами). Каждый шаг — самостоятельно выкладываемый, контрольные
цифры после каждого.

**Малое сначала, всё это можно сделать и выложить быстро:**

1. **Вид топлива под картой Google** — сегодня не показан вовсе.
2. **«Общий счёт» — в КОНЕЦ списка «Закупки»**, а не перед ним. Слово
   с нажимом: «по десять раз я тебе сказал: сначала список, потом в конце
   уже разблюдовка».
3. **Удаление техники со спросом** (см. выше).
4. **Новый раздел «Проживание»** — пустой, но существующий.
5. **Канистры из «Логистики» в «Взять с собой»** («инвентарь для техники»).
   ⚠️ `S.canRows` — контрольная цифра «2 канистры»: переносить показ,
   не расчёт.
6. **Аренда отдельным разделом, парковка внутри неё.**
7. **Переименования разделов** — ⛔ `S.secTitles` (`lib/sectitles.ts`,
   «Дорога» = ключ `log`) хранит СВОИ названия заказчика, затирать нельзя.
8. **Техника:** топливо внутрь строки техники (сейчас `S.fuelPrices`
   и `S.transport` — два блока), вид топлива выпадающим списком со своей
   ценой, «Посчитать по карте» у каждой техники, добавленная техника сразу
   видна на карте, плюс «Добавить технику» у шапки.
9. **Меню по числу дней поездки** — день без меню существует пустым.

## Новое направление: партнёры на карте (лодочные станции)

Замысел записан в `HANDOFF.md` целиком. ⛔ **Кода не начинать**, сначала
разговор с заказчиком. Суть: партнёры регистрируются, бесплатно ставят
метку на карте в обмен на обратную ссылку на сервис, размещают услуги
с ценой и лимитом, привязывают счёт и получают оплату напрямую — комиссия
удерживается в пользу Pine-to-Pine.

⚠️ **Сказать заказчику прямо и одним абзацем:** расщепление платежа между
партнёром и площадкой делается только через провайдера со «сплит-платежами»
(ЮKassa, Точка, Т-Бизнес), договор заключается на ИП или юрлицо, плюс нужны
оферта, проверка партнёров, чеки по 54-ФЗ и согласие на обработку данных.
Пока этого нет — приём денег сделать нельзя, а **всё остальное можно**:
метка партнёра на карте, карточка, услуги списком с ценой и остатком,
заявка на размещение, подтверждение обратной ссылки. Оплата — последней.

## ⛔ Не трогать

`foldpref.ts` (У-152) · `requestUnfold` · `RING` (У-146) · `CARD_W` (У-147) ·
`BuyBox` (У-150) · `<Command>` и top-4 (У-151) · отложенный фокус `TextSheet`
(У-154) · слот `marks` в `StripRow` — он внутри кнопки, интерактив туда
класть нельзя · `noAdd` в `SectionHead` (общий плюс убран из «Команды»
по слову заказчика) · `ord` · `applySpend` · коллекции `buy` / `transport` /
`fuelPrices` / `rent` / `canRows` не сливать в одну.

## ⚠️ Среда

`vuoksa2026-test` и `vuoksa2026-rotate-test` — надгробия (У-153), для
репетиций заводить свежее имя на «-test». Общую заглушку `**/rest/v1/**`
в Playwright регистрировать ПЕРВОЙ, точные — после (У-149). Уборка в конце:
сначала увести вкладку на `about:blank`, потом гасить сервер (У-69).

## Незакрытое

Три коммита двадцать девятой порции лежат **локально, на боевой не выложены** —
заказчик слова «выкладывай» не давал. Спросить в начале сессии.
