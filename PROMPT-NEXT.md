# Задание следующей сессии · Pine-to-Pine, тридцать вторая порция

Читать: `CLAUDE.md` → `docs/DEFINITION-OF-DONE.md` → `docs/LESSONS.md`
(У-01…У-163, особенно **У-160…У-163**) → `.claude/rules/environment.md` →
`HANDOFF.md`: сначала **«ТРИДЦАТЬ ПЕРВАЯ ПОРЦИЯ»**, потом «АРХИТЕКТУРА
РАЗДЕЛОВ ЦЕЛИКОМ, СЛОВАМИ ЗАКАЗЧИКА», потом «ПАРТНЁРЫ НА КАРТЕ».

## ⛔ Первое действие — боевой жив (У-140 + У-159)

Порты (`netstat`, смотреть LISTENING), потом живая загрузка Playwright:
`root.children = 1`. **Имя файла сборки брать СО СТРАНИЦЫ**
(`document.querySelector('script[src]')`), а не из `curl`-копии `index.html`.

Playwright ставится глобально, в проекте его нет:
`import { chromium } from 'file:///C:/Users/staro/AppData/Roaming/npm/node_modules/playwright/index.mjs'`

## ⚠️ Слово заказчика 09.08.2026, действует всегда

**«Боевой сервер никогда не должен падать.»** Выкладывать только после живых
замеров; после КАЖДОЙ выкладки — живая загрузка боевого; увидел поломку —
откатывать коммит немедленно, разбираться потом.

## Ключи, данные, песочница

Ключи прежние (сверено 09.08 — боевая отвечает 200):

```bash
node -e "const d=require('./backups/prod-2026-08-09-p31-start.json')[0].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"
```

⚠️ Локальный запуск без `?trip=` читает **`vuoksa2026-test`, а это надгробие**
(У-153): `rpc/trip_read` ответит `401`, и будет выглядеть как «ключ протух».
Заводить свежую песочницу под НОВЫМ именем на `-test` (занятые: `vuoksa2026-test`,
`vuoksa2026-rotate-test`, `vuoksa2026-p31-test`) копией боевого снимка и открывать
`http://localhost:5199/?trip=<имя>&u=Maks&k=<ключ>&sandbox=1` через
`location.replace(...)`. Уборка в конце: сначала вкладка на `about:blank`,
потом сервер, потом обнуление строки (У-69).

## Что уже решено — НЕ переспрашивать

* «Расходы» — блоки одной формы, не сплошная лента; общий итог внизу.
* Топливо живёт внутри техники; в «Купить» видно числом, своих строк не заводит.
* Цена литра — своя у каждой техники (**сделано**, `Transport.fuelPr`).
* У аренды план и факт (**сделано**, `Rent.priceF`).
* «Проживание» — отдельный раздел (**сделано**, пустой).
* «Меню» по числу дней: дни из дат поездки, заведённый руками день вне
  диапазона **показывается** и помечается «вне поездки».
* Партнёры на карте — **пока не начинать**.

## Работа сессии, по порядку

1. **C2 · Канистры показом во «Взять с собой».** Заказчик: «эти канистры
   должны идти вверх, в раздел „взять с собой“ — просто отдельно, будет
   прописано как „для техники“ или „инвентарь для техники“. Но люди-то могут
   менять эти названия». ⛔ Сначала раскладка (ux-designer): «Взять с собой» —
   матрица вещь × люди, а канистры считаются сами и по людям не делятся.
   Переносится **показ**, не расчёт; `S.canRows` — контрольная цифра
   «2 канистры». Вместе с показом переезжает и правка `S.doc.canVol`,
   иначе поле видно и не правится (постулат 1).
2. **B3…B5 · Техника.** «Посчитать по карте» у каждой единицы (сегодня одна
   на весь расчёт) · добавленная техника сразу видна на карте, даже без
   маршрута · плюс «Добавить технику» напротив шапки раздела.
3. **«Меню» по числу дней** — ответ получен, можно делать.
4. **D · «Расходы» тремя подразделами** через слой проекции `lib/spend.ts`
   (`SpendRow.src` = имя коллекции, суммы только из `calc.ts`, записи ноль)
   · «Вопросы» отдельным разделом (`S.ideas` из `RoadSection.tsx`).
   ⚠️ В том же коммите, где «Дорога» перестаёт быть разделом, сменить
   `ADD_KINDS[].section` (`addnew.ts`) с `'road'` на `'buy'` и перенести
   заявки поиска: `goSection('road')` без якоря молча ничего не делает
   (`jump.ts`) — молчаливый отказ, постулат 5.
5. **«Кто заходил и когда»** — сначала план архитектора. Хранить отдельной
   картой `S.seen = { <slug>: <время> }`, слияние «побеждает большее число»,
   без `ua`. ⛔ В карточку человека не дописывать: поля человека переносятся
   только при `ip.ua > have.ua`, и метка начнёт драться с правами и ключами
   (так 08.08 откатились ключи, У-155).

## Мелкое, но записанное

* **Верхняя панель при восьмом разделе.** Сейчас семь подписей влезают
  на 1280 впритык (805 px при 832) и не влезают на 1024 — там ряд едет
  вбок. Восьмой раздел («Вопросы») сломает и 1280: переводить панель
  на «Ещё», как нижнюю (`splitForBottomNav`, У-156).
* **`InlineNum` в покое игнорирует `className`** (У-163) — приглушить
  пустое число нельзя ни у аренды, ни у покупок. Чинить разом в
  `Inline.tsx`, не подпирая по одному месту.

## ⛔ Не трогать

`foldpref.ts` (У-152) · `requestUnfold` · `RING` (У-146) · `CARD_W` (У-147) ·
`BuyBox` (У-150) · `<Command>` и `top-4` (У-151) · отложенный фокус `TextSheet`
(У-154) · слот `marks` в `StripRow` · `noAdd` в `SectionHead` · `ord` ·
`applySpend` · `ConfirmAction` (У-158) · `LEGACY_V1` в `sectitles.ts` — там
ровно два раздела, добавишь `log` и «Логистика» станет «Дорогой» (У-160).

⛔ Коллекции `S.buy`, `S.transport`, `S.fuelPrices`, `S.rent`, `S.canRows`
НЕ сливать: метки удаления привязаны к имени коллекции (`del['rent:'+i]`).
Меняется ЭКРАН, а не форма хранения. `node app/scripts/check-sums.mjs` —
после каждого шага. Каждый шаг — самостоятельно выкладываемый.
