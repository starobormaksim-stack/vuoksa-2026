# Задание следующей сессии · Pine-to-Pine, тридцать первая порция

Читать: `CLAUDE.md` → `docs/DEFINITION-OF-DONE.md` → `docs/LESSONS.md`
(У-01…У-159, особенно У-156…У-159) → `.claude/rules/environment.md` →
`HANDOFF.md`: сначала **«ТРИДЦАТАЯ ПОРЦИЯ»**, потом «АРХИТЕКТУРА РАЗДЕЛОВ
ЦЕЛИКОМ, СЛОВАМИ ЗАКАЗЧИКА», потом «ПАРТНЁРЫ НА КАРТЕ».

## ⛔ Первое действие — боевой жив (У-140 + У-159)

Порты (`netstat`, смотреть LISTENING), потом живая загрузка Playwright:
`root.children = 1`. **Имя файла сборки брать СО СТРАНИЦЫ**
(`document.querySelector('script[src]')`), а не из `curl`-копии `index.html`:
край Cloudflare отдаёт её из кеша со старым именем ещё несколько минут,
и по `curl` боевой выглядит сломанным, когда он жив (У-159).

Playwright ставится глобально, в проекте его нет:
`import { chromium } from 'file:///C:/Users/staro/AppData/Roaming/npm/node_modules/playwright/index.mjs'`

## ⚠️ Слово заказчика 09.08.2026, действует всегда

**«Боевой сервер никогда не должен падать.»** Выкладывать только после
живых замеров; после КАЖДОЙ выкладки — живая загрузка боевого; увидел
поломку — откатывать коммит немедленно, разбираться потом.

## Ключи и данные

Действуют СТАРЫЕ ключи от 06.08 (ротация 08.08 откатилась, У-155),
менять их заказчик не просит. Брать (в репозиторий не возвращать, У-133):

```bash
node -e "const d=require('./backups/prod-2026-08-06T2246.json')['0'].data; for(const p of d.people) console.log(p.slug, p.key, p.perm)"
```

Снимок 09.08 до работы — `backups/prod-2026-08-09-p30-start.json`
(63 позиции закупки, 91 сбор, 20 точек, 3 человека). Заказчик правит лист
живьём каждый день: любая правка в обход приложения обязана двигать `ua`
и читать документ прямо перед записью (У-155).

## Что уже сделано в тридцатой порции (выложено на боевой, коммит 4ea00c7)

Вид топлива под картой · «Общий счёт» под список «Закупки» · удаление
техники со спросом в строке (`ConfirmAction` в `flops/Inline.tsx`) ·
пустой раздел «Проживание» (`components/stay/StaySection.tsx`) ·
короткая подпись «0 из 14» на телефоне · кнопка «Ещё» в нижней панели
перестала быть молчаливым отказом (У-156).

## Что заказчик уже ответил — НЕ переспрашивать

* **«Расходы» — блоки одной формы**, не сплошная лента: статьи закупки,
  потом «Аренда», потом «Логистика»; у каждого вида свои колонки, общий
  итог внизу.
* **Топливо живёт внутри техники.** Строка техники в «Логистике» и есть
  строка топлива («Бензин АИ-95 — Honda Accord»). В «Купить» топливо
  видно числом, своих строк не заводит — иначе двойной счёт.
* **Цена литра — своя у каждой техники:** «каждая машина может заправиться
  на разной заправке, цена может быть разная; по умолчанию сейчас можно
  указать одинаковую». То есть `Transport.fuelPr?: number`, пусто — цена
  вида из `S.fuelPrices`.
* **«Проживание» — отдельный раздел** (не часть «Аренды»).
* Чек оплачивал он, покупатель проставлен · дубли план↔чек уберёт сам ·
  ключи не менять · удаление со спросом, но без попапа.

## ГЛАВНАЯ РАБОТА: продолжение перестройки разделов

План chief-architect (сессии A…D) целиком — в `HANDOFF.md`. Из сессии A
сделаны A1, A2, A4. Дальше по порядку:

1. **A3 · Переименования.** `app/src/sections.ts:21-22`: «Сборы» →
   «Взять с собой», «Закупка» → «Расходы». ⛔ Менять ТОЛЬКО `title`,
   `id` заморожены: они держат якоря, `S.secTitles` (ключи первой версии,
   «Дорога» = `log`, карта в `lib/sectitles.ts`), `addnew.ts` и память
   свёрнутого (`foldpref.ts`). Проверка: `secTitles` в снимке до и после
   побайтово равны.
2. **A5 · Порядок разделов** в `SECTIONS` + вместимость панелей. Целевой
   порядок: Поездка · Команда · Взять с собой · Расходы · Проживание ·
   Меню · Вопросы, взаиморасчёты в самом конце (они уже вне `SECTIONS.map`,
   `App.tsx:232`, У-143). Мерить: `TopNav` на 1280 при 8 пунктах,
   `BottomNav` на 390 (`elementFromPoint` в центре каждой кнопки),
   горизонтальной прокрутки нет.
3. **C1 · Аренда отдельным блоком, парковка внутри неё** (через
   `S.rentCats`, коллекция `S.rent` не трогается).
4. **C2 · Канистры показом в «Взять с собой»** — вместе с правкой
   `S.doc.canVol`, иначе поле видно и не правится. ⚠️ `S.canRows` —
   контрольная цифра «2 канистры»: переносится ПОКАЗ, не расчёт.
5. **B1+B2 · Топливо внутрь строки техники.** `Transport.fuelPr?: number`
   → `'fuelPr'` в `PLAIN.transport` **в `fields`, не в `PLAIN_JSON`**
   (`merge.ts:73-77`); `fuelPriceFor(t,S) = t.fuelPr > 0 ? t.fuelPr :
   fuelPriceOf(S,t.fuel)`, зовёт только `fuelCost` (`calc.ts:125-127`).
   Пусто = сегодняшнее поведение, цифры не двигаются. Вид топлива —
   `InlinePick` из `road/RoadSetup.tsx:124-144`. ⛔ Не заводить строку
   в `S.fuelPrices` на каждую технику: `canRows` привязаны к `fuel`,
   а `cans()` группирует по `f.i` (`calc.ts:207`) — разъедет контрольную
   цифру. Цена ВИДА обязана остаться правимой (полка «у АИ-95» рядом
   с «у этой техники»).
6. **B3…B5** · «Посчитать по карте» у каждой техники · добавленная техника
   сразу на карте · плюс «Добавить технику» у шапки.
7. **D** · три подраздела «Расходов» через слой проекции `lib/spend.ts`
   (`SpendRow.src` = имя коллекции, суммы только из `calc.ts`, записи ноль)
   · «Вопросы» отдельным разделом (`S.ideas` из `RoadSection.tsx:358-485`)
   · «Меню» по числу дней.

⛔ Через всё это: коллекции `S.buy`, `S.transport`, `S.fuelPrices`,
`S.rent`, `S.canRows` НЕ сливать — метки удаления привязаны к имени
коллекции (`del['rent:'+i]`). Меняется ЭКРАН, а не форма хранения.
Контрольные цифры (`node app/scripts/check-sums.mjs`) после каждого шага.
Каждый шаг — самостоятельно выкладываемый.

⚠️ В том же коммите, где «Дорога» перестаёт быть разделом, сменить
`ADD_KINDS[].section` (`addnew.ts:70,78`) с `'road'` на `'buy'`:
`goSection('road')` при отсутствующем якоре молча ничего не делает
(`jump.ts:28-31`) — молчаливый отказ, постулат 5. И перенести заявки
поиска (`requestUnfold`/`jumpToItem`) для техники и аренды.

## Открытые вопросы заказчику (задать в начале, не угадывать)

1. **«Меню» по числу дней** — дни считаются от `trip.start`/`trip.end`?
   Что делать с днями, заведёнными руками вне диапазона: показывать сверх
   счёта или прятать? Выбрасывать нельзя (постулат 4).
2. **План и факт у аренды** — у покупок две цены, у аренды сегодня одна.
   Заводить обе?
3. **Партнёры на карте** — ответа ещё не было. Сказано было так:
   приём денег с расщеплением требует ИП/юрлица и договора с провайдером
   (ЮKassa · Точка · Т-Бизнес), плюс оферта, проверка партнёров, 54-ФЗ,
   согласие на обработку данных; без этого оплату сделать нельзя, а метку
   на карте, карточку, услуги с ценой и остатком, заявку и подтверждение
   обратной ссылки — можно. ⛔ Кода не начинать без его слова.

## Новое, ещё не начатое: «кто заходил и когда»

Слово заказчика 09.08.2026: хочет видеть **последний визит каждого
участника**. Сегодня сервис знает только, кто на листе прямо сейчас
(`presence`), памяти о прошлых заходах нет.

⛔ **Не дописывать время визита в карточку человека:** поля человека
слияние переносит только при `ip.ua > have.ua` (`merge.ts`), и метка,
которая двигается сама по себе, начнёт драться с правками прав и ключей —
это ровно тот механизм, которым 08.08 откатились ключи (У-155). Хранить
отдельной картой `S.seen = { <slug>: <время> }` с правилом слияния
«побеждает большее число», без `ua`. Сначала план архитектора, потом код.

## ⛔ Не трогать

`foldpref.ts` (У-152) · `requestUnfold` · `RING` (У-146) · `CARD_W` (У-147) ·
`BuyBox` (У-150) · `<Command>` и `top-4` (У-151) · отложенный фокус
`TextSheet` (У-154) · слот `marks` в `StripRow` (внутри кнопки, интерактив
нельзя) · `noAdd` в `SectionHead` · `ord` · `applySpend` · `ConfirmAction`
в `flops/Inline.tsx` (У-158).

## ⚠️ Среда

`vuoksa2026-test` и `vuoksa2026-rotate-test` — надгробия (У-153), для
репетиций брать свежее имя на «-test». В Playwright общую заглушку
`**/rest/v1/**` регистрировать ПЕРВОЙ (У-149). Узел для серии нажатий
брать один раз через `evaluateHandle` (У-148) и прокручивать к нему
(`scrollIntoViewIfNeeded`) перед нажатием — иначе клик уходит мимо.
Уборка в конце: сначала увести вкладку на `about:blank`, потом гасить
сервер (У-69).
