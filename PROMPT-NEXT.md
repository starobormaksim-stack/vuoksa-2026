# Задание следующей сессии · Pine-to-Pine, тридцать третья порция

## ⛔ Первое действие — выяснить, жива ли база

Квота Supabase кончилась 09.08.2026, и боевой лист перестал открываться:
`rpc/trip_read` отвечает `402 exceed_egress_quota`. Сайт при этом свежий
и рабочий — заказчик видит экран «Заведите свою поездку».

```bash
netstat -ano | findstr ":5199 :5200 :4173 :4199"
```

Смотреть строки со словом **LISTENING**. Потом — жива ли база:

```
POST https://oagonfdnlgqkoosvgaly.supabase.co/rest/v1/rpc/trip_read
     {"p_id":"vuoksa2026","p_key":"<ключ Макса из backups/prod-2026-08-09-p31-start.json>"}
```

* **200** — квота снята. Первым делом СНЯТЬ СНИМОК (`backups/prod-2026-08-09-…`):
  его не снимали с 31-й порции, и это единственная страховка.
* **402** — сказать заказчику одним предложением: лист не откроется, пока
  в кабинете Supabase не снято ограничение расходов или не поднят план.
  Это его деньги и его решение, кодом не лечится. Работать дальше можно:
  замеры идут локально, боевые данные подставляются Playwright-заглушкой
  из снимка (`scratchpad/harness.mjs` прошлой сессии — общая заглушка
  `**://*.supabase.co/**` ПЕРВОЙ, точные `rpc/*` после неё, У-149).

⛔ Песочницу заводить только под НОВЫМ именем на `-test` (заняты: `vuoksa2026-test`,
`vuoksa2026-rotate-test`, `vuoksa2026-p31-test`, `vuoksa2026-p31b-test`).
Ключи — из снимка, в репозиторий не писать.

Читать: `CLAUDE.md` → `docs/DEFINITION-OF-DONE.md` → `docs/LESSONS.md`
(особенно **У-166…У-168** этой сессии, плюс У-53, У-54, У-143, У-159, У-161)
→ `.claude/rules/environment.md` → `HANDOFF.md`, верхний блок про квоту
и раздел «ТРИДЦАТЬ ВТОРАЯ ПОРЦИЯ».

## Что уже сделано и переспрашивать не надо

Главная претензия заказчика закрыта (коммит `3bc9b6e`, выложен и проверен):
**«Расходы» — раздел с подразделами**. Статьи закупки · Аренда · Логистика ·
Проживание · итог по закупке · итоги поездки, и сразу за разделом
взаиморасчёты. Разделов на листе шесть: Поездка · Команда · Взять с собой ·
Расходы · Меню · Вопросы. «Дороги» и «Проживания» отдельными разделами нет.

Показ собирает проекция `lib/spend.ts`; коллекции не слиты. «Аренда»
и «Проживание» — один `S.rent`, поделённый по категории `place`.

## Что делать, когда база жива

1. **Показать заказчику** и спросить про «Проживание»: подраздел есть, строка
   заводится («Добавить место»), но полей ночёвки — адрес, кто бронирует,
   ссылка на бронь — у аренды нет. Нужны ли они, или хватает того, что есть.
2. **Канистры показом во «Взять с собой»** — «инвентарь для техники».
   Переносится ПОКАЗ, не расчёт (`S.canRows` — контрольная цифра «2 канистры»),
   вместе с правкой `S.doc.canVol`. Раскладку сначала спроектировать:
   «Взять с собой» — матрица вещь × люди, а канистры считаются сами
   и по людям не делятся.
3. **«Посчитать по карте» у каждой техники** · добавленная техника сразу
   на карте · плюс «Добавить технику» у шапки.
4. **«Кто заходил и когда»** — сначала план архитектора: `S.seen` отдельной
   картой, слияние «побеждает большее число», без `ua`. ⛔ В карточку человека
   не дописывать (так 08.08 откатились ключи, У-155).
5. **`InlineNum` в покое игнорирует `className`** (У-163) — чинить разом
   в `Inline.tsx`.
6. **Верхняя панель при седьмом разделе** — переводить на «Ещё», как нижнюю
   (`splitForBottomNav`, У-161). На 1024 ряд разделов едет вбок уже сейчас:
   676 px в полосе 592.

Партнёров на карте **не начинать**.

## ⛔ Не трогать

`foldpref.ts` (У-152) · `requestUnfold` · `RING` (У-146) · `CARD_W` (У-147) ·
`BuyBox` (У-150) · `<Command>` и `top-4` (У-151) · отложенный фокус `TextSheet`
(У-154) · слот `marks` в `StripRow` · `noAdd` в `SectionHead` · `ord` ·
`applySpend` · `ConfirmAction` и `ConfirmButton` (У-158, У-164) · `LEGACY_V1`
в `sectitles.ts` — впишешь туда `log`, и «Логистика» станет заводской
«Дорогой» (У-160) · `LAST_MONEY_SECTION = 'buy'` — вернёшь `stay`, и зачёт
снова покажет половину трат (У-143) · `scope` у `RoadCalc` — общий ключ
затирает раскрытое у соседнего подраздела · стартовый ноль у `ufRef`
в `Matrix` (У-166).
