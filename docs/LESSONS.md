# LESSONS.md · Pine-to-Pine — уроки, купленные ошибками

> Каждая запись здесь стоила заказчику данных, денег или доверия.
> Правило пополнения (Boris Cherny, создатель Claude Code): *«Anytime we see Claude do
> something incorrectly, we add it to CLAUDE.md so it doesn't repeat next time.
> This loop is everything.»* У нас роль такого файла играет этот, а `CLAUDE.md`
> держит только постулаты и запреты.
>
> **Формат записи:** что произошло · почему · как больше не делать · где исправлено.
> **Уроки не удаляются и не перенумеровываются.** Устаревший помечается `〜 неактуален с …`.
> Новый урок дописывается в конец своей группы со следующим свободным номером.

Группы: [A. Данные](#a-данные) · [B. Готовность](#b-готовность-и-проверка) ·
[C. Раскладка](#c-раскладка-и-темы) · [D. Библиотеки](#d-чужие-библиотеки-и-токены) ·
[E. Карта и сеть](#e-карта-сеть-внешние-службы) · [F. Сборка](#f-сборка-и-доставка) ·
[G. Заказчик](#g-заказчик-и-процесс) · [H. Агенты](#h-субагенты-и-оркестрация)

---

## A. Данные

### У-01. Голый адрес на `localhost` = права владельца, и он писал в боевую строку

**Что:** 4 августа 2026 боевую строку `vuoksa2026` затёрли **пять раз**. Пропадали
обложка (428 КБ), координаты места, `secTitles`, `tileLabels`, живой прогноз, отметки
«собрано».
**Почему:** поведение первой версии портировано намеренно — адрес без `?u=` даёт права
владельца. Проверки шли по `http://localhost:5199/` без `?sandbox=1`. Забывали именно
`?sandbox=1` — пять раз подряд.
**Правило:** `isSandbox()` считает песочницей **любой** `localhost`/`127.0.0.1`, а не
только `?sandbox=1`. Это единственная надёжная защита — на память полагаться нельзя.
Снимать её нельзя ни под каким предлогом.
**Где:** `app/src/lib/supabase.ts`.

### У-02. Признак беды в строке — пустой `author` при непустых данных

**Что:** каждое из пяти происшествий опознавалось по одному и тому же следу.
**Правило:** первый запрос любой сессии, работающей с данными:
`GET /rest/v1/trips?id=eq.vuoksa2026&select=updated_at,author`. Пустой `author` при
непустых данных — тревога, немедленно `node restore/restore.mjs` (без `--yes` — только
показывает).
⚠️ Сдвиг `updated_at` — **не** происшествие: заказчик правит лист живьём.
**Где:** `restore/restore.mjs`.

### У-03. Слияние не воскрешало поля, которых нет в старой копии

**Что:** вкладка с устаревшей копией получала свежую версию, сливала со своей и
отправляла обратно. Поля, отсутствующие у неё, исчезали насовсем. Восстанавливать,
пока такая вкладка открыта, бесполезно — затрёт снова через минуты.
**Почему:** `mergeInto` сравнивал по меткам времени, а отсутствие поля меткой не является.
**Правило:** `fillMissingKeys` добирает всё, что есть на сервере и отсутствует у нас,
независимо от меток. Действие только добавляющее. Личные поля (`me`, `theme`, `author`,
`updatedAt`) исключены. Порядок восстановления: **сначала закрыть виновную вкладку,
потом восстанавливать.**
**Где:** `app/src/lib/merge.ts`.

### У-04. Форму хранения поля менять нельзя — слияние отдаёт его целиком

**Что:** `S.tileLabels` — позиционный массив первой версии (0 транспорт, 1 продукты,
2 бюджет, 3 с каждого), а `MoneyTiles` читал его как словарь по ключу. Свои подписи
заказчика не показывались ни разу. Субагент «починил» переводом массива в словарь.
**Почему:** `tileLabels` лежит в `WHOLE_DOC` слияния и отдаётся целиком. Подмена формы
стёрла бы подписи насовсем — у всех и навсегда.
**Правило:** починка чтения — это починка **чтения**. Форму хранения не трогать.
То же у `S.secTitles` — объекты `{h, sub}` по ключам первой версии, где «Дорога» это `log`.
**Где:** `app/src/components/trip/MoneyTiles.tsx`, `app/src/lib/sectitles.ts`.

### У-05. Проверка «проходит ли запись в обход» была сделана на боевой строке

**Что:** проверяли, закрыта ли прямая запись в `trips`. Проверка прошла — то есть
поле `author` боевой строки переписалось на служебное значение.
**Правило:** любые проверки записи — **только** на `*-test`. Служебные строки называть
только с суффиксом `-test` (политика удаления пускает только их). Песочницу удалять
после работы.
**Где:** урок процесса.

### У-06. Строку, заведённую проверкой, потом нельзя удалить

**Что:** проверка защиты завела строку `probe-guard`. Политика удаления пускает только
`%-test` — убрать её теперь можно только через SQL Editor, куда заказчик не ходит.
**Правило:** временная строка получает суффикс `-test` **в момент создания**, а не потом.
Прежде чем что-то записать, ответить: чем я это удалю.

### У-07. Брошенные серверы прошлых сессий пишут в боевую строку

**Что:** на машине заказчика с прошлой сессии остались `node` на портах 4173 и 4199 и
открытое окно Chrome по голому адресу. Правка файла вызывала горячую перезагрузку **чужой**
вкладки, и та отправляла своё устаревшее состояние в базу.
**Правило:** до первой правки файлов — `netstat -ano | findstr :5199` (и 4173/4199/5200).
Найденное остановить. Записи в `.claude/launch.json`, указывающие в чужие папки, удалять.
**Где:** `.claude/launch.json`.

### У-08. Данные поездки — единственная ценность

**Что:** трижды терялись поля, которые «никто не смотрел»: `tileLabels`, `secTitles`,
координаты, прогноз.
**Правило:** если поле есть в документе, но не показано на экране — это **дефект**, а не
экономия. Ничего из данных не выбрасывать: ни позиций, ни полей, ни чужих названий.

### У-09. `restore.mjs` — не резервная копия, а последний рубеж

**Что:** дважды спасал снимок, снятый заранее.
**Правило:** перед долгой работой снимать `GET …&select=data,updated_at,author` в
`restore/`. Папка в `.gitignore` — внутри фотографии людей, а репозиторий публичный.
Не удалять, пока лист не восстановлен и не проверен.

### У-77. `trip_write` с `p_seen = null` по существующей строке молча ничего не делает

**Что:** 05.08.2026 песочницу заводили копией боевой через `rpc/trip_write`. Первый вызов
(строки ещё не было) прошёл и создал её. Второй, с новыми полями и тем же
`p_seen: null`, вернул **HTTP 200** — и не изменил ничего: `updated_at` остался прежним.
Полчаса ушло на поиск несуществующего дефекта слияния: правка «не доезжала» до экрана,
хотя её просто не было в базе.

**Почему:** `p_seen` — это «версия, которую я видел». `null` означает «строки не было»,
и функция вставляет её; по существующей строке такое условие не выполняется, и `UPDATE`
не задевает ни одной строки. Ошибки при этом нет: ноль строк — законный результат.
Это ровно тот молчаливый отказ, что уже записан за `putNew` в задаче 5 `PROMPT-NEXT.md`,
но здесь он проявился в служебном скрипте, а не в приложении.

**Правило:** пишешь в существующую строку — сначала прочитай `updated_at` и передай его
в `p_seen`. И **никогда не считай HTTP 200 подтверждением записи**: подтверждение — это
изменившийся `updated_at` в ответном `select`, а `[]` в теле означает «не записано».

---

## B. Готовность и проверка

### У-10. ⛔ Прошедшие `tsc` и сборка — это ДОПУСК К ПРОВЕРКЕ, а не готовность

**Что:** за одну сессию несколько раз работа объявлялась готовой по типам и сборке,
а живьём была сломана: поиск сносил всё приложение, обложка уезжала за экран, панель
съедала фотографию целиком. Всё нашлось замерами, которых не было в плане.
**Почему:** типы и сборка проверяют, что код **компилируется**, а не что человек видит
на экране то, что задумано.
**Правило:** `tsc` + `oxlint` + `build` — **вход** на проверку. Готовность даёт только
живой замер: `getBoundingClientRect` (геометрия), `elementFromPoint` (перекрытия и
попадание нажатия), вычисленный контраст (читаемость). Без вывода замера слово «готово»
не произносится. Полный гейт — `docs/DEFINITION-OF-DONE.md`.

### У-11. ⛔ Правка раскладки проверяется на ОБЕИХ ширинах и на СОСЕДНИХ элементах

**Что, случай первый:** строка «Вы — Макс · владелец», добавленная в полосу верхнего меню
ради полезного требования, съела ширину у разделов — на экране осталось «Поез…», «Кома…»,
«Сб…», «Заку…», «Дор…», «М…».
**Что, случай второй:** блок сумм, вынесенный ради видимости фотографии на телефоне,
лёг абсолютным слоем поверх нижней панели обложки — и на десктопе накрыл название поездки.
**Почему:** оба раза правка была **верной по замыслу** и проверена ровно на той ширине,
ради которой делалась. Соседей никто не смотрел.
**Правило:** любая правка раскладки меряется на **390 и на 1280**, и не только у того
элемента, что чинили, а у **соседей в том же контейнере** — до и после. Если правка
меняет ширину или высоту чего-либо, соседи перечисляются в отчёте поимённо с числами.
**Где:** `app/src/App.tsx`, `app/src/components/trip/TripCover.tsx`.

### У-12. Симптом «орган пропал» проверяется попаданием нажатия, а не наличием узла

**Что:** подложка закрытой шторки оставалась висеть во весь экран — `data-state="closed"`,
`pointer-events: auto` **в inline-стиле**, `z-50`. Экран цел, ничего не нажимается.
Заказчик прочитал это как «шторки работают через жопу».
**Почему:** узел в разметке есть и размер у него ненулевой — по DOM всё «в порядке».
**Правило:** жалоба «не работает / пропало» проверяется `elementFromPoint` в центре
органа: доходит ли до него нажатие. Наличие узла ничего не доказывает.
**Где:** `app/src/index.css` — правило, отбирающее нажатия у закрытой подложки.
⚠️ `!important` там обязателен: библиотека пишет `pointer-events` в inline-стиль.

### У-13. Вычисленное состояние, которое никто не показывает, — это отсутствующая функция

**Что:** трижды подряд. `perms.stale` считался с самого начала — по нему не было ни одного
совпадения в `.tsx`. `net` (состояние связи) считался — никто не показывал. Причина отката
карты на OSM вычислялась — на экран не попадала.
**Почему:** «оно уже есть в сторе» ощущается как «сделано».
**Правило:** новая вычисленная величина не считается сделанной, пока не найдено место,
где человек её **читает словами**. Молчаливых отказов не бывает (постулат 4).
**Где:** `components/PermNotice.tsx`, `components/NetNotice.tsx`, `map/TripMap.tsx`.

### У-14. Скриншот в этой среде — не доказательство

**Что:** вкладка не рисует кадров. Скриншоты, `read_page`, `.gm-style`, анимации и
измерения на узком экране молча врут: эмуляция 390 px частично считает раскладку по
реальному окну.
**Правило:** геометрия — `getBoundingClientRect`; перекрытия — `elementFromPoint`;
Radix кликать тройкой `pointerdown` + `pointerup` + `click`; `IntersectionObserver`,
`scroll`-события и ленивые картинки в такой вкладке спят. Для взгляда глазами —
поднимать Playwright. Пустой или подозрительно гладкий результат пробы — признак
деградации канала, а не успеха: деградировавший канал возвращает правдоподобную выдумку.
**Где:** `.claude/rules/environment.md`.

### У-15. Активный раздел считался наблюдателем, который спит

**Что:** подсветка раздела в меню застревала на «Поездке».
**Почему:** `IntersectionObserver` молчит, пока вкладка не рисует кадры.
**Правило:** источник истины — `getBoundingClientRect()` в `useSectionNav`, наблюдатель
оставлен вторым будильником.
**Где:** `app/src/App.tsx`.

### У-16. Замер может быть верным и отвечать не на тот вопрос

**Что (соседний проект, урок общий):** четыре круга подряд четыре разных прибора отвечали
«поля симметричны», а глаз заказчика видел перекос. Все четыре замера были верны и все
четыре печатали **вывод**, а не исходные данные.
**Правило:** когда замер расходится с глазом заказчика **больше одного раза** — печатать
сырые данные (координаты, цвета пикселей, вычисленные стили), а не посчитанный по ним
итог. И проверять подозреваемого **снятием** прямо в браузере (`el.style.border = 0` →
перемерить), а не рассуждением о нём.

### У-17. Две жалобы в разных углах экрана могут иметь один корень

**Что:** «названия разделов режутся» и «одно лицо дважды» оказались следствием одной
строки «Вы — Макс · владелец», добавленной в полосу меню.
**Правило:** прежде чем чинить две жалобы по отдельности, поискать общий корень.

### У-55. Замороженный CSS-переход даёт ЛОЖНЫЙ контраст, и `getAnimations()` его не выдаёт

**Что (05.08.2026):** замер контраста кнопки «знаки» в липкой полосе раздела дал
**1,56 : 1** — провал. Соседний элемент с тем же классом `text-muted` в том же
контейнере дал 5,79 : 1. Сырые данные: у кнопки `color: rgb(86,82,55)` — это значение
токена `--muted` из **светлой** темы, хотя тема уже тёмная.
**Почему:** у кнопки стоит `transition-colors`. Вкладка среды не рисует кадров, поэтому
CSS-переход не доигрывает **никогда** и элемент навсегда остаётся на стартовом цвете.
`getAnimations()` при этом возвращает **0** — переход через WAAPI не виден, как и у Motion
(урок У-29). То есть прибор молчит, а картинка врёт.
**Правило:** перед замером контраста снимать переходы у всего дерева —
`document.querySelectorAll('*').forEach(e => e.style.transition = 'none')` плюс синхронный
рефлоу `void document.body.offsetWidth`. Проверка подозреваемого — **снятием**
(`el.style.transition='none'` → перемерить), а не рассуждением о нём (урок У-16).
После снятия контраст оказался 5,79 : 1 в тёмной и 7,07 : 1 в светлой — норма.
**Где:** `.claude/rules/environment.md`, раздел «Вкладка не рисует кадров».

---

## C. Раскладка и темы

### У-18. `position: sticky` у шапки ломается, стоит любому слою поставить `overflow` на `<html>`

**Что:** шапку на мобильном чинили два этапа подряд — красили фон, а фон и так был
непрозрачный. Заказчик называл это «сквозным отверстием»: наверху экрана оказывалась карта.
**Почему:** липкость держится на прокрутке корневого элемента. У нас `body { overflow-x:
hidden }`, всплытие overflow ломают шторки и часть встроенных браузеров. Тогда
прокручивается `body`, липкость отваливается. Замер: при `sticky` верх шапки уходит на
**−600 px**, при `fixed` остаётся на нуле.
**Правило:** шапка — `position: fixed` + распорка той же высоты в потоке.
**Где:** `app/src/components/MobileHeader.tsx`.

### У-19. `<meta name="theme-color">` с `media` слушает СИСТЕМНУЮ тему, а не выбранную

**Что:** полоска Телеграма (крестик, адрес, «⋯») была полупрозрачной и показывала
страницу насквозь.
**Почему:** тегов было два, с `media="(prefers-color-scheme)"`.
**Правило:** тег ровно один, значение меняется вместе с темой; первое ставит скрипт в
`index.html` до первой отрисовки.
**Где:** `app/src/theme.ts`, `app/index.html`.

### У-20. Прозрачный узел, поднятый над содержимым, обязан получить подложку

**Что:** панель сумм лежала абсолютным слоем поверх нижней панели обложки — «ВУОКСА · 2026»
читалось сквозь цифры.
**Правило:** после любой правки слоёв (`z-index`, `sticky`, `fixed`, абсолютный слой)
смотреть сцену **в двух состояниях** — исходном и прокрученном — и мерить не только
«доходит ли нажатие», но и «что видно сквозь».

### У-21. `vh` на iOS считается по самому большому окну

**Что:** кнопка «Готово» в шторке срезалась. Причин оказалось три, и первые две
прикрывали друг друга.
**Почему:** (1) `data-[vaul-drawer-direction=bottom]:max-h-[80vh]` — селектор с атрибутом
сильнее обычного класса, поэтому `max-h-[88vh]` из `ResponsiveSheet` не применялся вовсе;
(2) `vh` при видимых панелях браузера уводит низ шторки за экран; (3) у шапки и подвала
не стоял `shrink-0`, и флекс отбирал у подвала высоту.
**Правило:** потолок высоты шторок задан **в одном месте** — `app/src/components/ui/drawer.tsx`.
В `ResponsiveSheet` его возвращать нельзя. Везде `dvh`, не `vh`. У шапки и подвала —
`shrink-0`.
⚠️ `dvh` не понимает iOS < 15.4 — открытый хвост.

### У-22. `DialogContent` с раскладкой `grid` ломает `min-h-0 flex-1`

**Что:** окно «про день» на десктопе прилипало к низу.
**Правило:** `ResponsiveSheet` рассчитывает на колонку flex — у `DialogContent`
должно быть `flex flex-col`.

### У-23. Правая кнопка мыши недостижима с телефона

**Что:** количество в матрице «Сборов» правилось только правой кнопкой.
**Правило:** любой жест проверяется вопросом «а пальцем?». Долгое нажатие — да, правая
кнопка — только как дополнение к нему. Интерактив ≥ 44 × 44, зона нажатия может быть
шире видимой (невидимый слой), это проверяется `elementFromPoint` на ±21 px от центра.

### У-24. Восемь узоров меток на 10 × 10 px различимы «на грани»

**Что:** меток участников стало восемь вместо четырёх, метка рисуется в 14 px.
**Правило:** различие не только цветом (WCAG 1.4.1) держит рисунок внутри кружка.
Насыщенность метки и насыщенность полосы готовности — **разные числа** (`alpha` и
`barAlpha`): одно общее делало полосы разных людей неразличимыми. Различимость
восьми узоров — открытый хвост, судит заказчик глазами.

### У-74. Доля `1fr` внутри ширины «по содержимому» превращается в max-content

**Что (05.08.2026):** чтобы таблицы занимали блок целиком (пункт 8 брифа), числовым
колонкам «Расчёта дороги» дали доли — `minmax(9rem,1fr)` вместо `9rem`. На 1280
таблица вместо 1215 px разъехалась до **1869** и завела прокрутку там, где всё
помещалось. Замер: `gridTemplateColumns` строки — `498 311 311 218 249 280`,
то есть каждый трек получил не долю блока, а max-content своего содержимого.
**Почему:** у сетки стояло `w-max min-w-full`. `max-content` — это **неопределённая**
ширина, а по спецификации сетки при неопределённой ширине доля размеряется так,
чтобы вместить max-content каждого элемента, пересекающего долевой трек. То есть
`1fr` при `w-max` — это не «доля», а «сколько попросит содержимое». Пока долевой
была одна колонка, ловушка молчала: остальные треки были жёсткими и держали сумму.
**Правило:** доля работает только при **определённой** ширине сетки. Если у таблицы
больше одной долевой колонки — задать ей `minW` (сумма минимумов, `DataTable`,
проп `minW`) и `w-full`; вычисление «по содержимому» тогда не участвует вовсе.
`w-max`/`w-min` в такой таблице возвращать нельзя.
**Проверка:** `getComputedStyle(row).gridTemplateColumns` против ширины блока.
Сходится — доли честные; сумма больше — сетка размерялась по содержимому.
**Где:** `app/src/components/flops/DataTable.tsx` (проп `minW`),
`road/RoadCalc.tsx` (`COLS_MIN`), `road/RoadSection.tsx` (`IDEA_COLS_MIN`),
`road/RouteTiming.tsx`.

### У-108. Орган, переехавший в другой контейнер, — это НОВЫЙ орган: цель нажатия мерить заново

**Что (06.08.2026, тринадцатая порция):** `InlinePick` — выбор из списка — жил
в шторках `TransportSheet` и `RentSheet` строками ≥ 44 px и нареканий не вызывал
ни разу. Шторки убрали, их содержимое переехало в раскрытие строки
(`road/RoadSetup.tsx`), и там у органа остался только `py-0.5`: видимая высота
**26 px** при норме 44. Поймал тестировщик до выкладки и вернул работу.
**Почему:** высоту задавала не сама кнопка, а её окружение — полка шторки.
Перенос окружение сменил, а память говорила «этот орган давно проверен».
Так же однажды поехала строка «Вы — Макс» (У-11): правка верна по замыслу,
проверена не там, где живёт.
**Правило:** при переносе органа в новый контейнер геометрия и попадание нажатия
меряются **заново** — `getBoundingClientRect().height` и `elementFromPoint`
на ±21 px от центра, — а не наследуются по памяти о старом месте. В список правок
переноса это входит наравне с самим переносом.
⚠️ Родня У-95, но не противоречие: там добор высоты был вреден, потому что цель
инлайновая внутри текста и расширитель накрыл бы соседа. Здесь орган —
самостоятельная строка выбора, соседей у неё по вертикали нет, и `min-h-11` ей
положен.
**Где:** `app/src/components/flops/Inline.tsx:543` — `min-h-11` у кнопки
`InlinePick`, рядом комментарий с числом 26.

### У-109. `offsetTop` двух узлов с разными `offsetParent` сравнивать нельзя

**Что (06.08.2026):** подводка ленты маршрута к точке (`useRouteFocus`) считала
`el.offsetTop − pane.offsetTop`. У строки `offsetTop` отмерялся от `.relative`-обёртки
полоски, у области прокрутки — от страницы. Разность всегда выходила отрицательной,
`scrollTop` зажимался в 0, и лента вместо подводки нужной точки прыгала к своему
началу. Для человека это «тапнул метку на карте — всё уехало вверх».
**Почему:** `offsetTop` — расстояние до `offsetParent`, а `offsetParent` у двух узлов
разный, стоит любому предку получить `position: relative`. Оба числа при этом
верны сами по себе, и разность выглядит правдоподобно — прибор не врёт, врёт
сопоставление.
**Правило:** взаимное положение двух узлов считать только рект-разностью:
`el.getBoundingClientRect().top − pane.getBoundingClientRect().top + pane.scrollTop`.
`offsetTop` годится ровно тогда, когда `offsetParent` у обоих узлов **один и тот же**,
и это проверяется, а не предполагается.
**Где:** `app/src/components/road/RouteTiming.tsx:105`, хук `useRouteFocus` —
запрет записан комментарием прямо над расчётом.

---

## D. Чужие библиотеки и токены

### У-25. `--muted` у нас — цвет ТЕКСТА, а shadcn использует `bg-muted` как подложку

**Что:** при наведении контраст падал до 1,98 : 1.
**Правило:** у shadcn нет ни одного самостоятельного значения — все его переменные
объявлены алиасами к нашим токенам. Токен `--card` переименован в `--surface` намеренно.
Для подложки наведения заведён отдельный `--accent-fill-hover` (стало 12,7 : 1).
Прежде чем взять класс shadcn — посмотреть, чем он у нас является.

### У-26. Обёртку `<Command>` у `CommandDialog` потеряли — поиск сносил всё приложение

**Что:** белый экран при открытии поиска.
**Правило:** в `app/src/components/ui/command.tsx` обёртка `<Command>` обязательна, там
стоит комментарий. Не удалять.

### У-27. Компоненты не выдумывать — брать из shadcn/ui и Aceternity

**Что:** самописные заготовки повторяли то, что уже есть в подключённых источниках,
и расходились с ними по поведению и по размерам.
**Правило (решение заказчика 03.08.2026, подтверждено 05.08.2026):** источники интерфейса —
**shadcn/ui** (структура и поведение), **Aceternity UI** (выразительные блоки),
**Motion** (движение), **lucide** (иконки). Перед тем как верстать что-либо — проверить
инвентарь `app/src/components/ui/` и реестры источников. Своя разметка допустима только
там, где в источниках нет ничего близкого, и это обосновывается в отчёте.
**Где:** `docs/WORKING-STANDARD.md`.

### У-28. `AnimatePresence mode="wait"` вешает переключение в фоновой вкладке

**Что:** смена раздела ждала окончания анимации ухода, а кадры не рисовались.
**Правило:** ожидание кадра (`requestAnimationFrame`, окончание анимации) в продуктовом
коде — только через гонку с таймером. Въезд оставлен, ожидание ухода снято.
**Где:** `app/src/App.tsx`.

### У-29. Элемент с недоигранным входом Motion навсегда стоит на смещении

**Что (соседний проект, механика та же):** `getAnimations()` при этом **пуст** — Motion
гонит инлайн-стиль по кадрам, а не через WAAPI.
**Правило:** геометрию покоя читать как `rect − new DOMMatrixReadOnly(cs.transform).f`.
`a.finish()` не помогает и создаёт вид, будто анимации нет.

---

## E. Карта, сеть, внешние службы

### У-30. ⛔ Ответ загрузчика `maps/api/js` — ВСЕГДА 200, даже когда ключ отклонён

**Что:** дважды подряд было заявлено «на боевом Google работает». Оба раза проверка была
негодной: смотрели код ответа загрузчика.
**Почему:** отказ приходит позже, из `AuthenticationService`, уже в работающей странице —
через `gm_authFailure`.
**Правило:** по коду ответа загрузчика о карте судить **нельзя**. Единственное
доказательство — что напечатано под картой на устройстве заказчика.
**Где:** `app/src/components/map/TripMap.tsx`.

### У-86. ⛔ Причину отказа карты нашёл Playwright за минуту — и она была не в коде

**Что:** заказчик трижды за три сессии просил «почини карту». Правились сроки ожидания,
писались объяснения отката, поднимался Map ID. Настоящая причина: домен
`https://pine-to-pine.com/` **не внесён в ограничения ключа** в кабинете Google Cloud.
**Как нашлось:** боевой домен открыт настоящим браузером (Playwright), прочитана консоль.
Google пишет прямым текстом: `RefererNotAllowedMapError` и даже подсказывает строку,
которую надо разрешить, — «Your site URL to be authorized: https://pine-to-pine.com/».
**Замер, показывающий, почему без браузера это не ловится:** запрос загрузчика
`maps/api/js` с трёх разных `Referer` — боевого, `pages.dev` и заведомо чужого
`evil.example.com` — во всех трёх случаях **HTTP 200 и одинаковые 13 261 байт**
(развитие У-30). Все 20 запросов к `googleapis` на живой странице тоже 200,
и плитки при этом не рисуются.
**Правило:** карта не работает — первым делом **открыть боевой домен Playwright'ом
и прочитать консоль**, а не править код. Диагноз стоит одной минуты, а гипотезы
о таймаутах стоили трёх сессий.
**Где:** ключ и Map ID — `app/src/lib/gmaps.ts`; чинится не кодом, а строкой
в Google Cloud → APIs & Services → Credentials → ключ → Website restrictions.

### У-89. Смена ИМЕНИ файла ещё не значит, что доехало содержимое

**Что (05.08.2026):** ожидание выкладки было написано как «жди, пока имя
`/assets/index-*.js` перестанет быть прежним». Оно сменилось, проверка пошла — и живой
браузер показал **старую** сборку: подпись «сборка 8b99948d» и прежние 83 × 147 там,
где ожидались 332 × 44. Полминуты ушло на версию «виноват служебный работник».
**Почему:** Cloudflare докатывает файлы по краям не мгновенно. `index.html` уже
ссылался на новое имя, а сам файл в этот момент отдавался ещё не везде.
**Как отличить за один запрос:** искать в теле файла **хеш нового коммита**
(`__BUILD__`, он же подпись под картой) и строку самой правки — а не сверять имя.
Здесь: `basis-64` — есть, `a55494b3` — есть, `8b99948d` — нет. После этого повтор
живой проверки сразу дал «сборка a55494b3» и 332 × 44.
**Правило:** ждать выкладки по имени файла можно, **выносить вердикт — только по
содержимому** (развитие У-42 и У-73). Разошлись прибор и глаз — сперва спросить
«а тот ли файл я смотрю», и лишь потом строить версии.

### У-31. Живой `google.maps.Map` в браузере среды ничего не говорит об устройстве заказчика

**Что:** у него другой браузер, другая сеть, возможно другой профиль и другой платёжный
статус проекта.
**Правило:** вердикт о карте выносится по снимку экрана заказчика или по строке, которую
сервис сам печатает под картой. Поэтому под картой **всегда** пишется, какая карта
нарисована и какая сборка.

### У-32. Молчаливых путей отката было шесть, и различить их было нечем

**Правило:** причина отката пишется словами вместе с номером сборки. Точная ошибка
Google (`RefererNotAllowedMapError`, `ApiNotActivatedMapError`, `BillingNotEnabledMapError`,
`ExpiredKeyMapError`) перехватывается и показывается человеку — тогда следующий спор
закрывается одним снимком экрана, а не сессией переписки.

### У-33. Молчание Google вешало карту навсегда

**Что:** скрипт отдал 200, callback не пришёл — ни карты, ни отката.
**Правило:** таймаут 12 с; одна повторная попытка на сетевых отказах; на `auth` — сразу OSM.

### У-34. С `loading=async` тег script поднимает только загрузчик

**Что:** «Map is not a constructor» и тихий откат на OSM.
**Правило:** `google.maps.Map` появляется только после `importLibrary()`. См. комментарий
в `loadGoogleMaps()`.
**Где:** `app/src/lib/gmaps.ts`.

### У-35. Имена точек написаны для человека, а геокодер этого не понимает

**Что:** «КАД» находился в другом конце страны; «Выезд из города» уезжало в геокодер как
адрес.
**Правило:** имя режется по двоеточию, обе половины спрашиваются, первой — та, что больше
похожа на адрес. Слово «город» в признаке «похоже на адрес» держать нельзя. Находка
дальше 500 км от места поездки отбрасывается. Честное «не нашлось» от Google вторым
кругом у Nominatim не переспрашивается.
**Где:** `app/src/lib/geocode.ts`, `app/src/lib/gmaps.ts`.

### У-36. `PATCH` возвращал обратно весь документ

**Что:** лишний мегабайт на каждую запись.
**Правило:** просить короткий ответ `?select=updated_at`.

### У-37. Ключ в красивую ссылку не входит — и человек молча становился участником

**Что:** «у Кости ничего нельзя редактировать». Права были исправны, молчал интерфейс.
**Почему:** `/vuoksa2026/Kostya` не несёт ключа, он берётся из `?k=` или из запомненного
в браузере. Открыв ссылку на телефоне, где запомнен другой человек, Костя получал свою
карточку и права участника — без единого слова о причине.
**Правило:** права, выданные не так, как ожидает человек, объясняются словами на экране.

### У-76. Откат на запасную карту, о котором сказано шёпотом, читается как «сделал не то»

**Что:** заказчик 05.08.2026, дословно: «у тебя по-прежнему open street map вместо Google
карты показывается… ты заебал, меня уже конкретно этой хуйнёй». Замером с боевого
домена: ключ жив (загрузчик отдаёт 200 с referer `pine-to-pine.com`), приложение
выбирает Google, `google.maps.Map` создаётся, подпись под картой говорит «Карта Google».
То есть спор шёл не о выборе карты, а о том, что на СТОРОНЕ ЗАКАЗЧИКА срабатывал откат,
и узнать об этом было неоткуда.

**Почему.** Две причины сложились.
1. **Срок ожидания 12 с был мал.** Загрузчик Google плюс четыре набора классов —
   шесть-семь запросов подряд. На мобильной сети они в двенадцать секунд укладываются
   далеко не всегда, и человек получал `timeout` → OpenStreetMap там, где надо было
   просто подождать. Поднято до 25 с.
2. **Причина отката стояла подписью в самом мелком кегле** (`text-micro text-muted`)
   в одной строке с номером сборки. Формально постулат 5 соблюдён — практически
   человек эту строку не читает и видит только чужую карту.

**Правило:** отказ, который подменяет одну функцию другой, объясняется **в полный
голос и в том месте, где произошёл**: полоса над самим блоком, причина словами, что
делать — и кнопка «попробовать снова», потому что причина может быть временной.
Подпись мелким кеглем — это журнал, а не сообщение. Исправлено: `lib/gmaps.ts`
(`LOAD_TIMEOUT_MS`, `retryGoogle()`), `components/map/TripMap.tsx` (`failFix`, полоса,
`googleTry` ключом компонента).

⚠️ **Отдельно про среду:** проверить карту глазами из браузерной панели нельзя вовсе —
`computer{screenshot}` отвечает «the Browser pane is not displayed, so the page is not
compositing frames», и плитки Google по той же причине не рисуются, хотя объект карты
создан. Судить только по DOM и подписи, а не по «карты не видно».

---

## F. Сборка и доставка

### У-38. Имя кеша служебного работника было вшито строкой

**Что:** у заказчика на телефоне держалась старая версия, `Ctrl+Shift+R` не помогал.
**Почему:** три причины сразу. (1) Имя кеша не менялось → `activate` ничего не удалял,
браузер не видел повода перекачивать работника: адрес тот же. (2) Файлы из `/assets/`
отдавались «сначала кеш» без проверки — сразу после выкладки Cloudflare успевает ответить
на запрос кода правилом `_redirects`, то есть отдать `index.html` вместо файла кода, и
такой ответ ложился в кеш **навсегда**. (3) Странице никто не говорил, что приехала новая
версия.
**Правило:** версия сборки живёт в адресе работника (`sw.js?v=…`, значение подставляет
Vite — `__BUILD__`); имя кеша завязано на версию; при переходе старый кеш удаляется
целиком; разметка вместо кода в кеш не попадает; сеть ограничена по времени (6 с);
страница перезагружается по `controllerchange`; обновление проверяется при возврате
к вкладке и раз в час.
**Где:** `app/public/sw.js`, `app/vite.config.ts`, `app/public/_headers`.

### У-39. Cloudflare не применяет `no-cache` для `/sw.js` из `_headers`

**Что:** отдаёт свой `max-age=14400, must-revalidate`. Для остальных путей правила
работают (проверено на манифесте).
**Правило:** на самообновление это не влияет, потому что приложение просит
`sw.js?v=<хеш коммита>` — каждый раз новый адрес. Но полагаться на `_headers` для `sw.js`
нельзя.

### У-40. `base: './'` ломает красивые адреса

**Что:** страница `/vuoksa2026/Maks` искала `./assets/index.js` по адресу
`/vuoksa2026/assets/index.js`, получала от `_redirects` тот же `index.html` вместо кода
и оставалась белой.
**Правило:** `base` зависит от того, где собирают: Cloudflare выставляет `CF_PAGES=1` и
получает `'/'`; GitHub Pages и офлайн-копия остаются на `'./'`. Пути манифеста и значка —
через `%BASE_URL%`, путь работника — через `import.meta.env.BASE_URL`.
**Где:** `app/vite.config.ts`, `app/index.html`, `app/src/main.tsx`.

### У-41. Офлайн-копия была сломана молча

**Что:** данные вшивались по первому `</head>`, который попадался **внутри кода**.
**Правило:** в `app/src/lib/offline.ts` только `lastIndexOf`. На `indexOf` не переписывать.
Хвост: копия читает документ из `flops.doc`, а для непервой поездки ключ другой
(`flops.doc.<поездка>`).

### У-42. Свежесть боевого сверять по содержимому, а не по хешу

**Правило:** искать в отданных файлах конкретную строку, появившуюся в этой правке.
Хеш коммита совпадает и тогда, когда край Cloudflare ещё отдаёт старое.

### У-73. ⛔ Проверка свежести ВО ВРЕМЯ сборки кеширует заглушку на год

**Что (05.08.2026):** сразу после `git push` проверял свежесть боевого — запрашивал
`/assets/index-*.js` по имени из разметки. Файла в этот момент ещё не было: сборка
Cloudflare не закончилась. `_redirects` отдал на несуществующий путь `index.html`,
а `_headers` приклеил к ответу `public, max-age=31536000, immutable`. Край запомнил
эту связку **на год**: восемь запросов подряд возвращали `text/html`, 20 005 байт,
`cf-cache: HIT`. Для человека это белый экран — разметка просит скрипт, а получает
разметку.
**Почему:** «немедленно проверить» и «выложить» — разные моменты времени, а
`immutable` в `_headers` не различает настоящий файл и заглушку вместо него.
**Правило:** после `git push` **не запрашивать** `/assets/*` без обхода кеша.
Проверять свежесть только с `?cb=<случайное>` — такой ответ край не кеширует
и заглушку не закрепляет:

```powershell
$cb = Get-Random
$html = (iwr -UseBasicParsing "https://pine-to-pine.com/?cb=$cb").Content
$m = ([regex]::Matches($html,'assets/index-[^"]*\.js')[0]).Value
$r = iwr -UseBasicParsing "https://pine-to-pine.com/$m`?cb=$cb"
[Text.Encoding]::UTF8.GetString($r.RawContentStream.ToArray()).Contains('<строка правки>')
```

⚠️ Читать тело **только** через `RawContentStream` + UTF-8: `.Content` в Windows
PowerShell приходит в системной кодировке, и поиск русской строки врёт «нет».
**Чем лечится, если уже закешировано:** любой следующий коммит. `__BUILD__`
в сборке — хеш коммита, поэтому содержимое бандла меняется, имя файла получает
новый хеш, и разметка начинает просить путь, на котором заглушки нет.
Панель Cloudflare для этого не нужна.

---

## G. Заказчик и процесс

### У-43. ⛔ Шторки-попапы забракованы. Правка — на месте

**Что, дословно (04.08.2026):** «мне не нужен поп-ап, в котором всё написано; это прямо
вот здесь, в этой таблице уже должно быть». Подтверждено 05.08.2026: «мне не нужны
поп-апы, особенно в мобильной версии и на десктопе».
**Почему это отменяет прежнее:** правило первой версии «список показывает — шторка
редактирует» соблюдалось буквально (в разметке разделов было ноль `<input>`) — и именно
это заказчик принял за «сломано всё».
**Правило:** тап по названию — правка названия прямо в строке. Тап по количеству —
счётчик прямо в ячейке. Статус — кружок в ячейке. Удаление — действие в самой строке.
Шторка допустима только там, где иначе физически некуда деться (выбор фотографии,
календарь), и это обосновывается в отчёте.

### У-44. Заказчик повторяет требование третий раз — значит его ни разу не поняли

**Что, дословно:** «я тебе говорил, что те квадратные должны быть фотографии на всю
ширину, либо, условно, не узенькие фотографии».
**Правило:** требование, прозвучавшее второй раз, — сигнал не «сделать усерднее», а
**переспросить одним вопросом, о чём именно речь**, и не угадывать. Цена вопроса —
одна строка в чате; цена угадывания — раунд.
Отдельно: снимок **не кадрируется** (`object-contain` + размытая копия под ним) — за это
он ругался дважды.

**✅ Ответ получен 05.08.2026.** Речь идёт **только о карточках участников в «Команде»**.
Шапки колонок в «Сборах» и обложка поездки его устраивают — их не трогать. Один вопрос
закрыл требование, которое до этого не понимали три раза.

### У-45. Заказчик больше не ходит в чужие кабинеты

**Что, дословно:** «всякое остальное я делать не буду. Твоя задача — делать всё
самостоятельно».
**Правило:** Supabase, Google Cloud, Cloudflare — делать самому доступными средствами
(Playwright, MCP, коннекторы). Что физически невозможно из среды — сказать **одним
предложением**, а не списком домашних заданий. Ни один пункт отчёта не начинается со
слов «заказчику надо».
⚠️ `docs/rls-apply-d.sql` **применён**, переспрашивать не надо.

### У-46. Подсказка объясняет правило, а не жест

**Правило:** «„Купить“ попадает в сумму» — можно. «Тап по строке открывает карточку» —
нельзя. Эталон списков — таблицы Excel заказчика.

### У-47. Русский язык и «Миша»

**Правило:** ёлочки, длинное тире, неразрывный пробел перед единицами. Ни одного
английского слова в интерфейсе. Без `alert()` и `confirm()`. Эмодзи как иконки не
используются. **Миша, не «Мишка»** — даже если в таблице заказчика «МИШКА».
У раздела «Команда» своё название заказчика — крепкое слово; оно хранится в данных и
показывается, редактировать его самовольно нельзя.

### У-48. Отчёт без вывода команд — не отчёт

**Правило:** в конце каждой задачи — изменённые файлы · выполненные проверки **с выводом** ·
оставшиеся риски · что рекомендуется дальше · промпт следующей сессии **текстом в чат**,
а не только файлом.

### У-49. Потолок сессии — 250–300 тысяч токенов

**Что:** длинные сессии деградируют — начинается выдумывание и потеря контекста;
именно в конце длинной сессии случились самые дорогие ошибки.
**Правило:** дойдя до потолка, работу закоммитить, дописать `HANDOFF.md` и `PROMPT-NEXT.md`
и сказать: «Рекомендую новую сессию: <причина>. Текущее состояние закоммичено».
Не начинать крупную фичу за 30 тысяч до потолка.

### У-53. ⛔ Один список — один раздел. Дублей списков нет нигде

**Что, дословно (05.08.2026):** «У тебя дублируется список товаров на всём протяжении.
Не нужно повторять: у тебя должны быть чёткие разделы, в них чётко находится. Больше
нигде не повторяться списками оборудования, инвентаря, вещей или продуктов. Больше нигде
это не дублируется, это не нужная информация».
**Почему мы так пришли:** каждый дубль заводился из добрых побуждений — показать
несобранное поближе, вынести итог на обложку, напомнить о задачах. По отдельности каждый
казался полезным. Вместе они превратили сервис в повторяющийся список.
**Правило:** список вещей, инвентаря или продуктов существует **ровно в одном месте** —
в своём разделе. Ни на обложке, ни рядом с ней, ни в чужом разделе его копии нет.
Сводка вместо списка допустима только числом («собрано 42 из 106»), но не перечислением
позиций.
**Что это сносит:** список несобранного справа от обложки (`components/trip/ReadyLeft.tsx`)
и любые его родственники.

### У-54. Единый паттерн таблицы: вещи слева, участники столбцами справа

**Что, дословно (05.08.2026):** «У тебя есть матрица, в которой у тебя есть участники,
которые собирают те или иные вещи. С левой стороны столбец „вещей“, справа столбцы
с исполнителями, участниками. Как у тебя было, так это идёт. Дальше, уже в зависимости
от специфики того или иного раздела, отображение должно быть локально немного изменено,
но чтобы единообразие было».
**Правило:** у всех списочных разделов **одна и та же форма**: липкая колонка названий
слева, столбцы людей справа, прокрутка вбок внутри блока. Раздел может добавить свои
колонки по своей специфике (цена и факт в «Закупке», единицы, статусы) — но структуру
и способ правки не меняет.
**Как проверять:** открыть «Сборы», «Закупку», «Дорогу» и «Меню» подряд и убедиться,
что человек не переучивается при переходе между ними.

### У-56. Полоса раздела липкая, название крупное, подписи под названием нет

**Что, дословно (05.08.2026):** «полоска раздела остаётся липкой, она прилипает
к верхней полоске меню; как только я заканчиваю этот раздел, следующий тоже прилипает
на время, пока я иду по разделу… должно быть написано крупно „Сборы“, а вот это
„общая база вещей, личные списки“ — вообще убирай, такой информации не нужно, лишнее…
нет никакой лишней информации, пустого скроллинга нету».
**Правило:** полоса раздела — `position: sticky` внутри своей `<section>`, `top`
берётся из одного числа `--header-h` в `index.css`. Фон непрозрачный (урок У-20).
Под названием подписи нет.
⚠️ **Открытый хвост.** Значение подписи из `S.secTitles[].sub` с экрана убрано, но
из документа НЕ выброшено и форма хранения не тронута (уроки У-04, У-08). Побочное
следствие: править подпись теперь негде. Спросить заказчика одним вопросом —
нужна ли она вообще, или поле можно считать отмершим (урок У-44: переспросить дешевле,
чем угадать).
**Где:** `app/src/components/flops/SectionHead.tsx`, `app/src/index.css`.

### У-57. Условные обозначения — в разделе, а не за нажатием

**Что, дословно (05.08.2026):** «я нажимаю — галочка возникает, какие-то часики, потом
машина, потом какой-то треугольник; человек должен понимать, что это такое, а для этого
нужны условные обозначения… справа по краю контента».
**Почему так вышло:** легенда была шторкой `GearLegendSheet` за кнопкой «что означают
значки» — то есть попапом, который заказчик забраковал дважды (урок У-43), да ещё и
спрятанным от того, кому он нужен.
**Правило:** обозначения стоят в полосе своего раздела: на десктопе строкой справа
с подписями, на телефоне раскрываются на месте под полосой (в строку они физически
не влезают — пять значков со словами это ≈ 470 px при 358 доступных). Никакой шторки.
Подпись объясняет правило, а не жест (урок У-46).
**Где:** `components/flops/SectionHead.tsx` (проп `legend`), `components/gear/legend.tsx`,
`components/buy/legend.tsx`.

### У-58. Второй орган того же действия — это тоже дубль

**Что:** над обложкой стояла кнопка «Мои поездки» отдельной строкой в 44 px, и ровно
такой же пункт «Мои поездки» уже был в меню «⋯». Заказчик читал это как лишнюю
информацию и пустой скроллинг.
**Правило:** правило «один список — один раздел» (У-53) распространяется и на **органы**:
одно действие живёт в одном месте. Прежде чем добавить кнопку «поближе», проверить,
нет ли её уже в меню.
**Где:** `app/src/App.tsx`, `app/src/components/MoreMenu.tsx`.

### У-59. «Делится на всех» — это была догадка, а не модель. Настоящая — БАЛАНС

**Что, дословно (05.08.2026):** «Если человек ведёт транспорт и оплачивает бензин,
он оплатит его вначале, стоимость зафиксирована, и в конечном итоге она ему
компенсируется… Оплатили 3000, каждый по полторы — остальные должны им ровно столько,
сколько не хватает. То, что покупается индивидуально — например алкоголь, сигареты, —
никогда не делится на всех».
**Почему мы ошибались:** `perPerson()` делит общий бюджет на людей, и это выглядело
как исчерпывающий ответ. Он отвечает на другой вопрос — «сколько стоит поездка на
человека», а не «кто кому доплачивает». Второй вопрос без плательщика не решается вовсе.
**Правило:** у траты ДВА независимых свойства — кто выложил деньги и между кем делится.
`баланс = уплачено − доля`. Пустое значение обоих = сегодняшнее поведение, иначе
сдвинулись бы контрольные цифры. Инварианты сверяет машина: `Σ уплачено === Σ доля ===
grandTotal` и `Σ баланс === 0` (строки 7–9 в `check-sums.mjs`).
⚠️ `perPerson()` не трогать — на ней 11 848 ₽, её читают `MoneyTiles` и `lib/export.ts`.
**Где:** `app/src/lib/settle.ts`, `components/road/Settlement.tsx`, `road/SpendShare.tsx`.

### У-60. Пустая строка и отсутствующий ключ — РАЗНЫЕ вещи, и `||` их слепляет

**Что:** у техники плательщик по умолчанию — её владелец. Написано было
`t.payer || t.owner`. Тогда человек, выбравший «скинулись поровну» (то есть пустую
строку), молча получал обратно владельца, и его выбор не сохранялся никогда.
**Почему:** `||` не отличает «поле пустое, потому что так решили» от «поля нет вовсе,
документ про него ещё не знает».
**Правило:** умолчание берётся через `??`, а не через `||`, везде, где пустое значение —
это осознанный выбор человека. Слияние такие поля различает так же: `fields()` копирует
поле, только если оно `!== undefined`, поэтому пустая строка доезжает, а отсутствие — нет.
**Где:** `app/src/lib/settle.ts`, функция `spends`.

### У-61. Имена людей склоняются. «Макс отдаёт Костя» — это дефект, а не мелочь

**Что:** первая живая проверка взаиморасчётов напечатала «Макс отдаёт Костя 1 596 ₽».
Имена в документе лежат в именительном, а фраза требует дательного.
**Правило:** там, где имя попадает в падежную позицию, оно склоняется — `dative()`
в `app/src/format.ts`. Имя, которое под правила не подошло (латиница, прозвище,
мягкий знак на конце), возвращается КАК ЕСТЬ вместе с признаком `sure:false`,
и вызывающий обязан перестроить фразу без падежа. Коверкать чужое имя нельзя:
выдуманное окончание хуже неуклюжей формулировки.
⚠️ Беглые гласные не покрыты: «Пётр» даёт «Пётру» вместо «Петру».
**Где:** `app/src/format.ts`, `components/road/Settlement.tsx`.

### У-62. Ключ хранилища, вшитый строкой, читает документ ЧУЖОЙ поездки

**Что:** загрузчик офлайн-копии читал `localStorage['flops.doc']` — это ключ поездки
по умолчанию. У всякой другой поездки ключ `flops.doc.<поездка>` (`lib/trips.ts:56`),
и копия второй поездки, открытая на адресе сайта, показала бы документ первой как свой.
**Правило:** имя ключа едет вместе с ДАННЫМИ (`__PINE_KEY__` в скрипте `pine-doc`),
а не с загрузчиком: загрузчик вшивается один раз, а данные переписываются при каждом
сохранении. Старая копия с прежним загрузчиком ключа не найдёт и останется на
`flops.doc` — то есть будет вести себя ровно как до правки.
**Где:** `app/src/lib/offline.ts`.
**✅ Закрыто 05.08.2026 ПАРОЙ, а не одной правкой:** загрузчик `BOOT` читает
`W.__PINE_KEY__||'flops.doc'`, а `docKey()` в `lib/trips.ts` отдаёт приложению то же
имя. Поменять что-то одно означало бы копию, которая не находит собственных правок
и молча откатывается на вшитый снимок. Замер во вкладке: на сайте `flops.doc`,
при вшитом `__PINE_KEY__` — `flops.doc.vuoksa2027`.

### У-63. Столбец, который человек складывает глазами, обязан складываться

**Что:** точные балансы дают в сумме ноль, а показанные рубли давали единицу:
−1 596,34 + 2 483,99 + 708,69 − 1 596,34 = 0, но −1 596 + 2 484 + 709 − 1 596 = 1.
Машинная проверка при этом сходилась: она считает по точным числам.
**Почему:** округление каждой строки по отдельности не сохраняет сумму. Инвариант
проверялся там, где его нельзя увидеть, а не там, где на него смотрит человек.
**Правило:** если столбец на экране складывают глазами, округление делается
«наибольшим остатком» — округляем вниз, недостающие единицы раздаём тем, у кого
дробная часть больше. Проверять надо ПОКАЗАННОЕ, а не только посчитанное.
**Где:** `app/src/lib/settle.ts`, функция `wholeBalances`.

### У-64. В липкой колонке правый край органа не спасёт никакая прокрутка

**Что:** значок ⌄ у выбора плательщика стоял на 470 px при клипе прокрутки 373 —
то есть был не виден НИКОГДА.
**Почему:** орган занимал всю ширину ячейки грида (556 px), а окно прокрутки на 390
всего 357. Обычную ячейку можно домотать вбок, липкую — нельзя: она по определению
стоит на левом краю.
**Правило:** всё, что живёт в липкой колонке, обязано умещаться в ВИДИМУЮ ширину
блока, а не в ширину ячейки. Проверка — `getBoundingClientRect().right` органа
против `right` его `.overflow-x-auto`.
**Где:** `app/src/components/road/SpendShare.tsx`.

### У-65. ⛔ Сид уезжает в файл сайта целиком — вместе с личными ключами

**Что:** требование «закрыть лист от посторонних» решалось закрытием базы. Обе
проверки — состязательная и живая — вернули работу с одним и тем же доводом,
и он подтвердился запросом к боевому домену: `https://pine-to-pine.com/assets/index-*.js`,
700 867 байт, содержит `zdzua343`, `pc2s6bsg`, `9ak33b2w`, `idz7qw3y`, имена, 105 позиций
сборов, 53 закупки, маршрут и технику. Посторонний читает поездку, **не сделав ни одного
запроса к базе**, и вдобавок берёт оттуда ключ владельца.
**Почему:** `app/src/data/seed-v2.json` (260 397 байт) статически импортируется
приложением, а значит попадает в сборку, которую Cloudflare раздаёт всем. Репозиторий
к тому же публичный — те же ключи лежат и в нём.
**Правило:** прежде чем запирать серверную дверь, посмотри, **что сайт раздаёт сам**.
Проверка одна и занимает минуту:
`curl -s https://pine-to-pine.com/ | grep -o 'assets/index-[^"]*\.js'`, затем поиск
личного ключа в отданном файле. В сборку имеет право попасть только строение листа
и справочники — ни людей, ни ключей, ни содержимого поездки.
**Следствие:** ключ, побывавший в сборке, считается утёкшим навсегда. Закрытие базы
без замены таких ключей защиты не даёт, а лишь создаёт её ощущение — и это хуже
честно открытой двери.
**Где:** `app/src/data/seed-v2.json`, импорт в `app/src/lib/trips.ts`; предупреждение
в шапке `docs/rls-apply-e.sql`.

### У-66. `tailwind-merge` принимает свой кегль за цвет и стирает цвет

**Что:** подпись главной кнопки давала контраст **2,60 : 1** в светлой теме и **2,13 : 1**
в тёмной при норме 4,5. Прибор показал, что класса `text-on-accent` на кнопке нет вовсе,
хотя `flops/Btn.tsx` его задаёт. Пострадали не новые экраны, а ВСЕ кнопки сервиса —
и `text-ink`, и `text-muted`, и `text-accent-text`.
**Почему:** библиотека знает свой список кеглей (`text-sm`, `text-lg`, …). Наши
`text-body`, `text-note` в него не входят, поэтому она считает их классами цвета —
и как два цвета подряд оставляет последний:
`twMerge('text-on-accent','text-body')` → `'text-body'`.
**Правило:** свой кегль в дизайн-системе обязан быть объявлен библиотеке склейки.
Появился новый — дописать в список `КЕГЛИ`. Перестановка классов местами эту ловушку
прячет, а не убирает.
**Где:** `app/src/lib/utils.ts`, `cn()` через `extendTailwindMerge`.

### У-67. Пустой ответ закрытого SELECT приложение читает как «строки нет» — и пишет поверх

**Что:** найдено разбором до применения SQL, поэтому обошлось. Вкладка, поймавшая
404 на функцию чтения ДО того, как её завели, запоминает «функции нет» навсегда
и переходит на прямое чтение. Через минуту SELECT закрывают — и PostgREST отвечает
не отказом, а **200 и пустым массивом**. Приложение читает это как «строки поездки
ещё нет» и создаёт её заново из своей местной копии, то есть затирает лист всей
команды. Это У-07 в новой одежде, только уже без «голого адреса».
**Правило:** пустой ответ и отказ — разные вещи, и разница ценой в боевой документ.
Если пусто пришло по пути, который мог быть закрыт, переспроси другим путём, прежде
чем делать вывод «этого нет». Право создать строку даёт только подтверждённое чтение.
**Где:** `app/src/lib/supabase.ts`, `loadTrip()` — повторный запрос к `trip_read`
на пустом ответе прямого чтения.

### У-68. ⛔ `0 >= 0` в защите от воскрешения удалённого съел сто позиций из ста трёх

**Что:** сразу после того, как сид перестал возить данные поездки (У-65), чистый
браузер по правильной личной ссылке получил лист, в котором из **103 сборов приехало 3**,
из **53 закупок — 2**, из **4 машин — ни одной**. На экране при этом всё выглядело
прилично: заголовок, разделы, обложка. Через минуту эта обеднённая копия ушла бы
на сервер и стала общей.
**Почему:** в `pick()` вход новой позиции сторожит строка
`if ((del[k + ':' + x.i] || 0) >= (x.ua || 0)) return`. Метки удаления нет — это `0`.
Позиция, которую никто не правил руками, имеет `ua: 0` — заводское значение. Значит
`0 >= 0` истинно, и позиция молча не приезжает. Пока сид был полной копией поездки,
ловушка не стреляла: заводские позиции уже лежали в списке, и ветка «этой позиции
у меня нет» для них не выполнялась ни разу. Убрали данные из сида — и в неё пошло всё.
Ровно та же строка стояла в ветке участников. Ниже, в проходе по своим позициям,
существование метки проверялось правильно (`t && t > …`) — расхождение прожило
незамеченным несколько месяцев.
**Правило:** сравнение с меткой имеет смысл только тогда, когда метка **есть**.
Сначала `if (t && …)`, потом сравнение. И шире: убрав данные оттуда, где они были
всегда, проверь не «собралось ли», а **пересчитай позиции поимённо** — прибор
«страница нарисовалась» здесь не показывает ничего.
**Как поймано:** счётом, а не глазами. `103 − 100 = 3`, `53 − 51 = 2`, `4 − 4 = 0`,
где вычитаемое — число позиций с `ua: 0` на сервере. Сходимость до штуки и назвала
причину.
**Где:** `app/src/lib/merge.ts`, `mergeInto()` — ветка участников и `pick()`.

### У-69. Остановленный сервер — не остановленная вкладка

**Что:** сервер разработки погашен `preview_stop`, служебная строка `vuoksa2026-test`
удалена, порты чисты. Через пять минут строка **нашлась снова** — с полным документом.
**Почему:** вкладка, открытая до остановки, живёт дальше: страница уже загружена,
сервер ей больше не нужен, а `Sync` продолжает опрашивать базу каждые восемь секунд.
Прочитала «строки нет» — и завела её заново из своей копии, совершенно законно.
Это У-07 в третьей одежде, и на этот раз он выстрелил по служебной строке, а не
по боевой, только потому, что `isSandbox()` держит `localhost` песочницей.
**Правило:** порядок уборки — **сначала увести вкладку** (`location.replace`),
потом гасить сервер, потом удалять строку. Проверка «серверов не осталось» ничего
не говорит о вкладках. Заодно: `netstat` смотреть на `LISTENING`, а не на весь вывод —
`TIME_WAIT` остаётся от закрытых соединений и читается как «сервер жив».
**Где:** порядок работ в `.claude/rules/environment.md`.

### У-70. «Цифры на разных уровнях» — это не отступы, это длина подписей

**Что:** заказчик: «неграмотно расположены вот эти вот цифры… всё на разных уровнях,
я не знаю, зачем ты это так сделал». Подгонять хотелось отступы, а причина была
в другом: подписи плиток разной длины («Транспорт, лодка, парковка» против
«Продукты») занимают то одну строку, то две, и число под длинной подписью уезжает
ниже соседнего ровно на высоту строки.
**Правило:** выравнивание разноимённых величин делает **сетка**, а не подгонка.
Каждая плитка занимает два ряда внешней сетки (`row-span-2` + `grid-rows-subgrid`) —
тогда все подписи стоят на одной линии и все числа на одной, сколько бы строк ни занял
текст. ⛔ Обрезать подпись в одну строку нельзя: своя подпись из документа
потерялась бы на глазах у владельца.
**Замер:** до — числа на разных линиях; после, 1280: подписи все на 848, числа все
на 882; 390: по две линии, по одной на каждый ряд сетки.
**Где:** `app/src/components/trip/MoneyTiles.tsx`.

### У-71. Чужие кегли приезжают не из своего файла, а из органа, который его рисует

**Что:** заказчик: «в карточке участника столько разновидностей шрифтов, что с ума
сойти можно». Замер: шесть кеглей, из них 16 px и 14 px вне шкалы проекта. Искать
их в самой карточке было бесполезно — заголовок и подпись приезжали из `ui/drawer.tsx`,
`ui/sheet.tsx` и `ui/dialog.tsx` с заводскими `text-base` и `text-sm` shadcn. То есть
разнобой был во **всех** шторках сервиса, а карточка просто попалась на глаза.
**Правило:** увидел кегль вне шкалы — ищи его в компоненте-органе, а не в экране.
Компоненты shadcn приносят свою шкалу; при переносе в проект их кегли переводятся
на наши (`text-body`, `text-note`) вместе с цветами. Родня У-66.
**Замер:** после — видимых кеглей четыре (13 / 15,5 / 20 / 24), все из шкалы;
оставшиеся 14 px у `sr-only`, на экране его нет.
**Где:** `ui/drawer.tsx`, `ui/sheet.tsx`, `ui/dialog.tsx`, `crew/PersonSheet.tsx`.

### У-72. Правка файла скриптом на Windows переписывает его целиком

**Что:** четыре замены классов сделаны однострочником на Python — `git diff` показал
**436 изменённых строк из 436**. Файл лежал с CRLF, скрипт записал LF: содержательных
правок четыре, а рецензенту приезжает весь файл, и настоящая правка в нём не видна.
**Правило:** правки исходников — только построчным инструментом правки. Скрипты
годятся для чтения и замеров, но не для записи в файлы репозитория. Лечится
`git checkout --` и повтором правок руками (постулат 13, минимальный дифф).

### У-75. Замороженный переход врёт не только про цвет, но и про видимость

**Что (05.08.2026):** проверял пункт 7 брифа — видны ли на телефоне плюсик и корзина
в строке. Прибор показал `opacity: 0` при ширине 390, хотя в коде `opacity-100`
и только `lg:opacity-0`. Вывод «на телефоне действия недостижимы» был бы ложным:
после снятия переходов та же ячейка дала `opacity: 1`.
**Почему:** у ячейки стоит `transition-opacity`, а вкладка среды не рисует кадров —
переход от смены ширины окна не доигрывает и застревает на старте. Это ровно У-55,
только там был цвет, а здесь видимость.
**Правило:** снимать переходы у всего дерева перед ЛЮБЫМ замером вычисленного
стиля, а не только перед замером контраста:
`document.querySelectorAll('*').forEach(e => e.style.transition = 'none')`
плюс `void document.body.offsetWidth`. Особенно после `resize_window`: смена
ширины меняет значения, которые переход обязан доиграть.

### У-78. Действие, доступное только жестом, на телефоне не существует

**Что (05.08.2026):** заказчик про карту — «я вот забил туда условную информацию
по разным точкам, но толку я не увидел… если я хочу добавить новую точку, я её даже
должен…». Точка на карте заводилась **тапом по пустому месту**, и работал этот тап
безупречно. Узнать о нём было неоткуда: на карте не было ни кнопки, ни строки, а
на телефоне нет ни наведения, ни курсора, который мог бы смениться на крестик.
**Почему:** мы судили по своей осведомлённости. У жеста нет ни имени, ни следа
в интерфейсе — значит его не существует ни для кого, кроме того, кто его написал.
Ровно как У-58 про невидимую полосу вставки, только там орган был прозрачный,
а здесь его не было вовсе.
**Правило:** у **каждого** действия есть видимый орган. Жест допустим только как
второй, быстрый путь к тому, что уже сделано кнопкой. Проверка простая: закрыть
глаза на всё, что знаешь о коде, и найти действие на экране за пять секунд.
**Где исправлено:** `app/src/components/map/TripMap.tsx` — кнопка «Точка» рядом
с «Конечной», режим ожидания со строкой словами и «Отменить», тап оставлен как был.

### У-79. Список, показанный до первой буквы запроса, — это не список, а свалка

**Что (05.08.2026):** заказчик про поиск — «я нажимаю на поиск, и там какая-то
херобора… наверху всё перекошено, дизайн не единообразен, и размеры у тебя все везде
по-разному». Замер показал две разные беды в одном окне: поле ввода **30 px** высоты
при цели касания 44 и кегле 14 px (чужом шкале проекта), строки находок **53 px**, —
и **217 позиций листа, показанных сразу**, без запроса и без единого заголовка.
**Почему:** орган приехал из образца shadcn со своими `text-sm` / `text-xs` / `h-8`
(это У-71), а «показать всё» казалось щедростью. На деле две сотни строк подряд
не читаются вовсе: человек ищет глазами то, за чем шёл, и не находит.
**Правило:** до запроса поиск показывает, ЧТО и ГДЕ ищется, а не всё подряд.
Находки — группами по разделам, с заголовками. Кегли органа — из шкалы проекта,
и чинить их надо в самом органе (`ui/`), а не в экране, который его зовёт.
**Где исправлено:** `app/src/components/ui/command.tsx` (поле 52 px, кегли body/note/
micro, строки ≥ 44) и `app/src/components/SearchCommand.tsx` (группы, пустой запрос).

### У-80. Замороженная анимация окна врёт и про РАЗМЕР — все замеры меньше на 5 %

**Что (05.08.2026):** окно поиска замерялось `getBoundingClientRect` — 532 × 199
и поле ввода 49 px. Вычисленный стиль того же поля говорил `height: 52px`, ровно как
задано в классе. Расхождение объяснилось не раскладкой: у окна стоит вход
`zoom-in-95`, вкладка среды кадров не рисует, анимация застревает на старте —
и весь узел стоит с `scale(0.95)`. 52 × 0,95 = 49,4.
**Почему:** это третье лицо одной и той же беды. У-55 — врёт цвет, У-75 — врёт
видимость, здесь — врёт геометрия. Опаснее прочих: 49 вместо 52 выглядит как
правдоподобное число, и его легко принять за настоящее.
**Правило:** любой замер внутри окна или шторки сверять с `getComputedStyle().height`.
Разошлись ровно на 5 % — это недоигранный `zoom-in-95`, а не ваша раскладка.
Вердикт «меньше 44 px» по одному лишь `rect` внутри диалога выносить нельзя.

### У-81. Новый орган в узкой липкой колонке съедает соседа целиком

**Что (05.08.2026):** к ссылке на товар добавилась кнопка «Проверить цену». На 1280
всё было хорошо, а на 390 замер показал: имя сайта сжалось до **8 px** — от
«alpindustria.ru» не осталось ничего, хотя текст в разметке был на месте.
**Почему:** липкая колонка «Закупки» на телефоне — 184 px. Значок 16 + две кнопки
по 44 = 104, и ссылке физически нечего занять; `min-w-0` без `flex-1` разрешает
сжаться до нуля молча, без переноса и без многоточия.
**Правило:** добавляя орган в липкую колонку, мерить **соседей** на 390, а не только
новый орган (это У-11 в приложении к строке таблицы). Строке органов — `flex-wrap`,
тексту — `flex-1` и наименьшая ширина (`basis-24`): пусть кнопки уедут вниз,
но имя останется читаемым.
**Где исправлено:** `app/src/components/flops/ProductLink.tsx`.

### У-82. `overflow-hidden` — это скрытая прокрутка, и она убивает липкую шапку

**Что (05.08.2026):** заказчик — «когда я листаю список, нужно, чтобы имена прилипали…
иначе сложно понять, кому это делегируется». Шапка получила `position: sticky`
и не прилипла: замер показал y = −394, потом −492, потом −237 — по разу на каждого
виноватого предка.
**Почему:** `position: sticky` цепляется за **ближайший прокручиваемый бокс**,
а прокручиваемым его делает любой `overflow`, кроме `visible` и `clip`. Виноватых
оказалось трое, и все — ради скруглённых углов или прокрутки вбок:
* сама область таблицы `overflow-x: auto` — сосед `visible` по правилам CSS молча
  становится `auto`;
* `overflow-hidden` у группы (`flops/Group.tsx`) и у блоков «Дороги»;
* попытка вылечить это через `overflow-y: clip` **не работает** рядом с `auto`:
  по спецификации такой `clip` вычисляется в `hidden`. Замер: `[auto, hidden]`.
**Правило:** обрезаешь углы — пиши `overflow-clip`, а не `overflow-hidden`: обрезают
одинаково, но `clip` прокручиваемым бокс не делает. А шапку, которая обязана прилипать
поверх прокрутки ВБОК, из этой области надо вынимать вовсе и двигать `transform` вслед
за телом — что и сделано в `flops/DataTable.tsx`.
**И второе:** липких полос у страницы уже две — верхняя панель (`--header-h`) и полоса
раздела (`--sec-h`). Третья обязана вставать под обеими: `top: calc(var(--header-h) +
var(--sec-h))`. Без второго слагаемого имена встали ровно ПОД полосой «Сборы» — обе
на y = 64, и та, что с бо́льшим `z`, перекрыла имена.

### У-83. Кириллица в заголовке HTTP роняет `fetch` до выхода в сеть

**Что (05.08.2026):** посредник, читающий карточку товара, был выложен и на всё отвечал
«сайт не отвечает» — включая `example.com`. Полчаса выглядело как «Deno Deploy не пускают
в русский интернет».
**Почему:** в `User-Agent` было написано «…читает карточку товара по просьбе человека».
Заголовки HTTP — байтовые строки, кириллица в них недопустима, и `fetch` бросает
исключение ещё до соединения. Наш `catch` честно называл это «сайт не отвечает» —
формулировка была верна по форме и совершенно ложна по сути.
**Правило:** в заголовки запроса — только латиница. И шире: когда отказ приходит
**одинаковый на все входы**, включая заведомо живой, виноват не мир, а наш код;
первую пробу ставить на заведомо рабочем адресе, а не на спорном.

### У-84. «Первый заход» оказался просьбой УБРАТЬ текст, а не добавить подсказки

**Что (05.08.2026):** пункт 15 разбора звучал так: «всё должно быть логично, понятно
человеку, который зашёл в первый раз и сразу же хочет что-то сделать». Готовился
онбординг — полоса «С чего начать» с делами и числами. Спросили одним вопросом.
Ответ дословно: «Я имел в виду логику взаимодействия… **без огромного количества
инструкций. У тебя просто гигантское количество текста**, везде которого можно было бы
в теории просто избегать вообще. Какие-то критические детали можно фиксировать,
но писать целые статьи о том, как пользоваться каким-то конкретным элементом, это слишком».
**Почему это важно:** сделанное «как понял» было бы точно противоположно просьбе —
к тексту, который мешает, добавился бы ещё один блок текста. Это У-44 в чистом виде:
цена вопроса — одна строка в чате, цена угадывания — целая функция и раунд правок.
**Правило:** объяснение живёт там, где без него человек ошибётся (правило расчёта,
причина отказа, что попадает в сумму), и нигде больше. Инструкция «как пользоваться
органом» — не пишется вовсе: у действия есть видимый орган (У-78), и этого довольно.
**Мера, которой это проверяется:** собрать со страницы весь видимый текст, которого
нет в документе поездки, — это и есть текст интерфейса. Было 1 439 знаков в 32 узлах
(четыре абзаца длиннее 60 знаков), стало 905 в 31 (длиннее 60 — ни одного).
Отдельно: примечание «Выложил деньги вперёд, ему возвращают» стояло у КАЖДОГО имени
в КАЖДОЙ строке расчёта — 21 повторение одной фразы, ещё 7 у «Скинулись поровну».
**Где исправлено:** `road/SpendShare.tsx`, `buy/Settlement.tsx`, `buy/BuyTotals.tsx`,
`road/RoadSection.tsx`, `gear/GearSection.tsx`, `MoreMenu.tsx`, `auth/OwnerLogin.tsx`,
`auth/ClosedList.tsx`, `trips/TripsScreen.tsx`, `trips/FirstStep.tsx`,
`crew/PersonSheet.tsx`, `road/RouteTiming.tsx`, `flops/SectionHead.tsx`.

### У-85. `import()` отдаёт модуль, загруженный ДО правки, — и это выглядит как «не работает»

**Что (05.08.2026):** три свежие правки проверялись прогоном модуля прямо во вкладке
(`await import('/src/lib/trips.ts')` — Vite это умеет). Прибор показал старое поведение
у всех трёх сразу: ссылка без ключа, `docKey()` мимо вшитого имени, «Санёку» вместо
«Саньку». В Node тот же код давал правильный ответ.
**Почему:** страница была загружена ДО правок. `import()` не идёт в сеть за модулем,
который уже есть в графе вкладки, а горячая перезагрузка Vite обновляет то, что рисует
экран, — не то, что лежит в графе под старым запросом. После `location.reload()`
все три ответа стали верными.
**Правило:** прогон модуля через `import()` во вкладке — прибор точный, но только
**после перезагрузки страницы**. Родня У-55/У-75/У-80: среда отвечает правдоподобно
и неверно, и отличить можно лишь вторым, независимым замером (здесь им был Node).

### У-87. ⛔ Открыл новую дверь — посмотри, что из неё видно постороннему

**Что (05.08.2026):** на закрытый экран добавлена кнопка «Мои поездки» — чтобы человек,
вошедший по почте, не упирался в тупик. Дверь заработала сразу, и вместе с ней
посторонний **без всякого ключа** прочитал в списке «ВУОКСА · 2026 · правки 5 августа,
Макс». Причина не в новой кнопке: `listTrips()` всегда показывала «открытую сейчас»
поездку, и это было безобидно ровно до тех пор, пока в список нельзя было попасть,
не пройдя ворота.
**Правило:** новая дверь в закрытый раздел — это не только «стало удобнее», это ещё
и **новый путь для того, от кого закрывались**. Пройти этим путём самому: чистая память
браузера, голый адрес, посмотреть глазами, что видно. Починено условием
`savedKey() ? [here, …] : […]`; замер после: «Поездок пока нет», название и автор
не показаны, а своему человеку с ключом поездка видна по-прежнему.
**Где:** `app/src/lib/trips.ts` (`listTrips`), `app/src/components/auth/ClosedList.tsx`.

### У-88. Замер контраста «по порядку узлов» врёт на выбранном органе Radix

**Что:** контраст трёх строк выбора роли мерился селектором `span > span`. У двух строк
вышло 14,72, у выбранной — **1,0**, то есть «текст цвета фона». Дефекта не было:
Radix рисует у выбранного органа ещё один `<span>` — индикатор кружка, и он встаёт
первым, сдвигая всю нумерацию на один.
**Правило:** цвета мерить по **классу или роли** узла (`.font-semibold`, `.text-note`),
никогда по позиции среди соседей: у выбранного, открытого и наведённого состояния
разметка на узел длиннее. Перемер по классам дал 14,72 / 7,51 у всех трёх строк.

### У-90. Цель касания 42 px оказалась настоящими 44 — виновата замороженная анимация

**Что:** шестая порция записала дефект «цель касания в меню „⋯“ — 42 px при норме 44»
и поставила его в задание. Перемер 06.08.2026: у `[role="menuitem"]` вычисленный
`min-height` — **44px**, а `getBoundingClientRect().height` — **41,8**. Ровно 44 × 0,95.
Radix открывает меню с `zoom-in-95`, а анимации в этой среде не доигрывают (У-80),
и меню навсегда остаётся на стартовом масштабе 0,95. Снятие `animation` и `transform`
у контейнера вернуло **44,0** тем же замером.
**Правило:** прежде чем чинить размер, снять анимацию у **родителя** и перемерить:
`el.style.animation='none'; el.style.transform='none'; void document.body.offsetWidth`.
Число, кратное 0,95 от круглого (41,8 · 34,2 · 20,9), — это почти всегда У-80, а не
дефект. Правка «добрать 2 px» сделала бы орган на 46 px, и никто бы не заметил.

### У-91. Липкая шапка внутри блока со своей прокруткой не имеет права знать про шапку страницы

**Что:** заказчик: «пункт меню с шапкой, с изображением, и точки, и участников — он
по какой-то причине перелистывается… он не прилипший». Шапка ленты маршрута стояла
на `top: var(--header-h)` = 64 px. На десктопе её блок имеет свою прокрутку
(`lg:max-h-[600px] lg:overflow-y-auto`, содержимое 1123 при высоте 599), и внутри него
`top: 64px` означает «висеть на 64 px ниже КРАЯ БЛОКА»: над шапкой оставалась полоса,
по которой ползли строки. Замер после правки: шапка стоит на y = 239 при верхе блока
239 и не двигается при `scrollTop = 300`.
**Правило:** поправка на высоту верхней панели нужна только тогда, когда прокручивается
**страница**. Внутри своего прокручиваемого блока липкий узел ставится на `top: 0`.
В `flops/DataTable.tsx` для этого заведён проп `inScroller`, и он обязан включаться
там же, где включена прокрутка блока (у нас — только на `lg:`).

### У-92. `Math.max(0, …)` в ограничителе сдвига намертво центрирует то, что меньше рамки

**Что:** редактор кадра не давал двигать снимок, уменьшенный меньше рамки. Предел
считался как `Math.max(0, (dw - BW) / 2)`: пока снимок был крупнее рамки, это читалось
как «дальше края не уедешь», но при `dw < BW` разность отрицательная, предел
становился нулём, и снимок прилипал к центру. Заказчик просил ровно обратного:
«по ширине расставить фотографию квадратную, а у меня не получается».
**Правило:** предел сдвига — это **модуль** разности: `Math.abs(dw - BW) / 2`. Он верен
в обе стороны: крупный снимок не уходит за край рамки, мелкий не уходит за её пределы
изнутри. И отдельно: холст под JPEG закрашивается ДО отрисовки — прозрачные поля
сохранились бы чёрными полосами.

### У-93. Автопроба контраста врёт и на `oklab`, и поверх фотографии

**Что:** финальный прогон напечатал «контраст 1,0» у надписи «До выезда» и «1,1»
у имени на карточке человека. Оба — ложные. Первое: цвет пришёл строкой
`oklab(0.96 -0.005 0.04 / 0.85)`, а разбор брал первые три числа как RGB. Второе:
текст лежит **на фотографии**, у которой `background-color` прозрачный, и подъём
по предкам нашёл кремовый фон карточки — крем на креме.
**Правило:** автопроба контраста — это фильтр подозрений, а не вердикт. Печатать сырые
цвета, а подозрение проверять снимком экрана. Узел, у которого между ним и предком
с фоном лежит `<img>`, из пробы исключать вовсе.

### У-94. `block` рядом с `line-clamp-2` отменяет обрезку молча

**Что:** в `flops/StripRow.tsx` подпись строки была объявлена
`line-clamp-2 block …`. Обрезка держится на `display:-webkit-box`, а утилита
`block` ставит `display:block` и перебивает её. Порядок классов в строке ничего
не решает — побеждает то, что объявлено позже в слое `utilities`.
Прибор при этом молчал: `-webkit-line-clamp` в вычисленных стилях честно равен `2`,
`overflow: hidden` на месте, и только высота выдавала правду. Замер 06.08.2026:
у 5 подписей из 157 текст занимал **4 строки** (71,5 px при `line-height` 17,875)
вместо двух. Полтора месяца это никого не задевало, потому что в «Сборах»
и «Закупке» подписи короткие; вылезло на «Дороге», где во второй строке стоит
примечание точки.
**Правило:** `line-clamp-*` живёт **без** соседней утилиты `display`. Проверять
обрезку не наличием класса и не `webkitLineClamp`, а делением высоты узла
на `line-height`.

### У-95. Цель касания у правки текста меряется вместе с соседом, иначе «добор» делает хуже

**Что:** кнопка покоя `InlineText` — это сам текст с отступом 2 px: замер на 390
даёт **19…34 px** по высоте, и таких целей 91 из 170. Просилось «добрать высоту
невидимым слоем», как у стрелок счётчика. Замер соседей показал, почему нельзя:
у «Названия поездки» (34 px) точка **+21 px по вертикали уже попадает
в «Подзаголовок поездки»** — другую правку, стоящую под ним. Невидимый
расширитель накрыл бы соседа целиком, и тап по подписи правил бы название.
Ровно от этого в самом файле стоит предупреждение: «строка целиком под кнопку
не отдана, иначе тап по описанию правил бы название».
**Правило:** у **инлайновой** цели внутри текста добор высоты — это не улучшение,
а перенос промаха на соседа. Решение 06.08.2026: оставить как есть, а высоту
до 44 px давать окружением (полка `StripField`, строка таблицы). Если менять —
то не паддингом, а расстановкой: разнести соседние правки так, чтобы между ними
было чему расти. Числа: 19 px — подпись плитки, 22 px — подзаголовок и место,
34 px — название поездки, 48 px — название точки в раскрытой ленте.

### У-96. Общая кнопка не нарушает «один список — один раздел», если заводит строку у неё дома

**Что:** заказчик 06.08.2026 просил один липкий плюс с четырьмя видами позиции
(«расчёт с топливом, расчёт с арендой, товар купить, либо просто вещь»), а
постулат 3.5 запрещает списку жить в двух местах. Противоречие оказалось мнимым:
запрещено **перечислять** чужие позиции, а не **заводить** их. Общий плюс ничего
не показывает — он создаёт строку в её родном разделе и уводит туда человека
(`lib/jump.ts`, `jumpToItem`). Замер 06.08.2026: «Аренда», нажатая в «Сборах»,
кладёт строку в «Дорогу» и подводит к ней — y 6288 → 23186 на 390, строка видна
на top 241.
**Правило:** прежде чем объявлять требование заказчика противоречащим постулату,
проверить, что именно постулат запрещает. Здесь запрет был на показ, а
требование — про создание.

### У-97. Общее действие просит раздел сделать своё, а не копирует его правила

**Что:** соблазн был написать в общем месте четыре функции создания — вещи,
покупки, техники, аренды. Но правила создания у каждого свои и живут в разделе:
у «Сборов» это перенумерация блока перед вставкой, у «Закупки» своя `ord`,
у «Дороги» вид техники и цена топлива из справочников. Копия этих правил
разошлась бы с оригиналом на первой же правке.
**Правило:** общее действие шлёт **заявку** (`lib/addnew.ts`: `requestAdd` →
`useAddRequest`), а раздел выполняет её теми же руками, что и по своей кнопке.
⚠️ «Здесь ли человек» меряется ДО прокрутки (`sectionInView`): после неё раздел
назначения на экране всегда, и признак стал бы бесполезным. Позиция ложится
в блок перед глазами, только если человек читал этот раздел; иначе — в первый,
потому что `visibleBlockId` мерил бы ещё не доехавшую прокрутку.

### У-98. Повтор числа лечится связью, а не удалением

**Что:** четыре суммы стояли и плитками на обложке, и строками «Итоги поездки»
в «Дороге». Просилось убрать одно из мест — но оба заказчик просил сам, в разное
время и осознанно: плитки он расставлял 05.08.2026, а расчёты «внизу, в разделе
другом» требовал 04.08.2026. Удаление любого из них откатило бы его же решение.
**Правило:** повтор ЧИСЛА (в отличие от повтора списка) постулат 3.5 разрешает —
«сводка допустима числом». Лечится он связью: тап по числу на обложке уводит
к строке расчёта, где написано, из чего оно сложилось (`data-hit="sum-<slot>"`).
Замер: плитка «С каждого» подводит к строке «С каждого · Делим на 3 человек ·
15 716 ₽», top 331,8. ⚠️ Тап по ПОДПИСИ плитки остаётся правкой подписи: два
действия на одном месте — та самая беда, из-за которой «тап просто не работает».

### У-99. Мёртвой ветка бывает не везде: перед удалением проверить офлайн-копию

**Что:** ветка `perms.stale` в `PermNotice` считалась недостижимой — при `stale`
ворота в `App.tsx` уводят на «Этот лист закрыт» раньше. Так и есть, но **только
в онлайне**: офлайн-копия воротами не закрывается намеренно, иначе владелец
остался бы без собственного файла. То есть ветка жива, и текст в ней был неверен
для единственного случая, где она показывается: «попросите новую ссылку» —
у копии сети нет вовсе.
**Правило:** прежде чем сносить недостижимую ветку, пройти по ней все три входа:
онлайн, офлайн-копия, первый заход до ответа сервера.

### У-100. ⛔ Обработчик «конца правки» читал проп своего рендера — и съедал новую строку

**Что (06.08.2026, финальный прогон):** на телефоне новая покупка **исчезала**.
Замер: нажали «Добавить покупку» — в базе 53 → 54, поле открылось на правку;
вписали название, нажали Enter — в базе снова **53**, на экране строки нет, и
тоста об удалении человек не видит, потому что тост говорит «Пустая строка
не сохранилась» и выглядит как правда. На 1280 та же правка работала: там
матрица (`BuyRow`), и она хранит признак в `useRef`.
**Почему:** в ленте стояло `onEditEnd={() => onFreshEnd(p.i, !!p.n)}`. `p.n` —
проп **того рендера**, в котором строка ещё пустая. `InlineText.save()` сначала
зовёт `onSave` (документ меняется), потом `close()` → `onEditEnd`, но React
к этому мигу не перерисовал строку, и замыкание отдаёт старое `''`. Условие
«ничего не ввели» срабатывало всегда, и свежая строка удалялась вместе
с только что вписанным названием.
**Правило:** признак «успели ввести» живёт в `useRef` и ставится **в момент
сохранения**, а не вычисляется из пропа в обработчике. Любое `!!prop` внутри
колбэка, который сам же этот проп и меняет, — это чтение прошлого.
**Как проверять:** сценарий кончается не нажатием, а **сверкой с базой**:
завели → вписали → прочитали строку из `trip_read`. Девятая порция проверила
создание («53 → 54») и остановилась на нём — дефект жил в следующем шаге.
**Где исправлено:** `app/src/components/buy/BuyStrip.tsx` (ссылка `saved`,
сбрасывается после чтения — она одна на список, свежая строка бывает одна).

### У-101. ⛔ Край отдаёт SPA-заглушку с кодом 200 — и офлайн-копия скачивается битой

**Что (06.08.2026):** «Забрать офлайн-копию» на боевом отдавала файл 1,53 МБ,
внутри которого ссылки на `/assets/index-*.js`. Открытый двойным щелчком, он
показывал **пустую страницу** с одной полоской объяснения: файлов рядом нет.
Проверка `res.ok` проходила, тост говорил «Копия у вас. Откроется без интернета».
**Почему:** заготовку (`vite build --mode offline`) клал в **корень репозитория**
скрипт `publish-v2.mjs`, а боевой адрес собирает Cloudflare Pages из `app` и
раздаёт **`app/dist`**. В `dist` заготовки не было никогда — с самого перехода
на Cloudflare. Запрос к `/Вуокса-2026.html` попадал в правило SPA, край отвечал
**200** и отдавал `index.html`, и он же уезжал человеку под видом копии.
Проверено запросом: `/build.js` (есть только в корне) тоже отдаёт 19 172 байта
`text/html` — то есть корень репозитория боевой не раздаёт вовсе.
**Правило:** на SPA-хостинге **код 200 ничего не доказывает**. Файл, который
приложение забирает у собственного сайта, проверяется по содержимому — здесь
самодостаточная сборка не имеет ни одной ссылки `src="/assets/…"`. И второе:
всё, что должно попасть на боевой, кладётся в **публикуемую папку**, а не рядом
с ней; путь публикации у Cloudflare один — `npm run build`.
**Где исправлено:** `app/scripts/offline-into-dist.mjs` (кладёт заготовку
в `dist` и падает, если она не самодостаточна), `app/package.json`
(`build` собирает и офлайн-версию), `app/src/lib/offline.ts` (имя латиницей
`pine-offline.html` и проверка содержимого перед отдачей), `publish-v2.mjs`.

### У-102. Чтение листа обгоняло вход по письму — и человек читал «лист закрыт» по дороге к себе

**Что (06.08.2026):** заказчик — «по ссылке с почты я перехожу сначала
на центральную страницу, и она уже через какое-то время перезагружается
и входит в кабинет». Причина в порядке запросов: `startSync()` звал `initAuth()`
**без ожидания** и тут же поднимал `Sync`. Первое `trip_read` уходило от общего
ключа, сервер честно отвечал «не ваш лист», ворота показывали `ClosedList`,
и только со вторым чтением — уже с токеном — приезжал лист.
**Правило:** если в адресе лежит код входа (`token_hash` или `access_token`
в решётке), первое чтение обязано **дождаться** обмена. Отказ, вынесенный
раньше, чем человек успел войти, — это не защита, а поломка на глазах.
**Замер после правки:** порядок запросов стал `verify` 424 мс → `trip_read`
829 мс; на экране только «Открываю лист», «Этот лист закрыт» не мелькает.
**Где:** `lib/auth.ts` (`hasAuthCodeInUrl`), `store.ts` (`entering`,
отложенный `sync.start()`), `App.tsx` (ворота).

### У-103. Ярлык на домашнем экране открывается в СВОЁМ хранилище — общий адрес там пуст

**Что (06.08.2026):** заказчик добавил сервис на «Домой» на iPhone и при запуске
снова упёрся во вход: «я опять должен заходить заново. Такого не должно
происходить. Я зашёл — значит я зашёл». В манифесте стояло `start_url: "./"` —
общий адрес без личной ссылки, а у приложения с домашнего экрана **своё
хранилище**, отдельное от браузера: запомненный ключ туда не переезжает.
**Правило:** ярлык обязан нести личную ссылку. Манифест подменяется в браузере
на личный (`Blob` + `URL.createObjectURL`), `start_url` — ссылка этого человека,
`id` — общий адрес, чтобы смена ключа не заводила второй значок.
⛔ Ключ при этом **не попадает в файл сайта**: статический
`manifest.webmanifest` остаётся общим (У-65).
**Замер:** на боевом под ключами Макса и Кости `link[rel=manifest]` — `blob:`,
внутри `start_url: https://pine-to-pine.com/?u=…&k=…`, иконок 4, ошибок нет.
**Заодно иконка:** «рандомная ёлка на острове» заменена эмблемой брендбука
(`app/scripts/make-icons.mjs` рисует 192, 512 и maskable-512 настоящим
браузером из `emblem-light.svg`).
**Где:** `lib/homescreen.ts`, `App.tsx`, `public/manifest.webmanifest`.

### У-104. Число в свёрнутой строке считалось по ВСЕМ, включая удалённого человека

**Что (06.08.2026):** заказчик — «сверху написано „сапоги резиновые, 4 штуки“,
открываешь внутрь: три человека, три штуки». Так и было: `totalQty()` складывала
`Object.values(g.o)` — все ключи подряд, — а раскрытая подробность рисует строки
только по нынешним людям. Женю убрали из поездки в августе, его единицы остались
в **31 позиции из 96**, и каждая такая строка врала на его долю.
**Правило:** любая сводка по людям считается по **списку людей поездки**,
а не по ключам объекта. Ключи переживают человека — это правильно (постулат 4,
вернётся — вернётся и его число), но складывать их нельзя.
**Мера:** число в свёрнутой строке обязано совпадать с суммой того, что видно
под ней. Проверяется сверкой всех строк с документом, а не одной позицией.
**Замер:** 96 позиций, расхождений 0 на 390 и на 1280 (в матрице колонка «Всего»
даёт 1+1+1 = 3 шт.); было 31 позиция с завышенным счётом (Носки 8 → 6,
Сапоги 4 → 3).
**Где:** `lib/gearx.ts` (`totalQty(g, people)`), `gear/GearStrip.tsx`,
`gear/GearMatrix.tsx`, `holdersLine`.

### У-105. Safari кладёт в ярлык АДРЕС СТРОКИ — значит в ней обязана быть личная ссылка

**Что (06.08.2026, вторая жалоба подряд):** личный манифест (У-103) владельцу
не помог. Он вошёл по почте в Safari, добавил значок на домашний экран — и при
запуске снова «впишите». Дословно: «я уже должен быть зарегистрирован, то есть
мне не нужно будет повторять эту процедуру, и никому не надо».
**Почему:** Safari берёт в ярлык **текущий адрес строки** (личный манифест —
это `blob:`, и он его может не принять). А у вошедшего по письму в адресе нет
ни имени, ни ключа: код из письма стирается сразу после обмена — и правильно
делает. Ярлык запоминался голым, в новом хранилище приложения ни ключа,
ни сеанса почты, и человек упирался во вход. Дальше он просил письмо ещё раз
и получал от почтового сервиса «слишком часто, попробуйте через несколько минут».
**Правило:** если вход держится на ссылке, то у опознанного человека личная
ссылка обязана **стоять в адресной строке** — это единственное, что переживает
и ярлык, и обновление страницы. Ставится `replaceState` (страница не грузится
заново), прочие метки адреса сохраняются — потерять `sandbox=1` значит увести
проверку в боевую строку (У-01), а `trips=1` трогать нельзя вовсе.
**Замер:** голый адрес с запомненным ключом → строка стала
`?sandbox=1&u=Maks&k=nk6xafd6`, лист открыт; список поездок → адрес не тронут;
«К поездке» и обновление страницы работают как работали.
**Где:** `lib/homescreen.ts` (`keepPersonalUrl`).

**И про иконку, той же просьбой:** поле вокруг круглого знака сделано хвойным
(`#2B391A`), а не кремовым: телефон обрезает иконку своей формой, и кремовые
углы читались как рамка. Внутри круга ничего не менялось.

### У-106. ⛔ Значок на iPhone запускал ОТДЕЛЬНОЕ приложение — вход из браузера туда не переезжает

**Что (06.08.2026, третья жалоба подряд по одному и тому же):** заказчик добавил
сервис на домашний экран, открыл — «Этот лист закрыт», и экран вдобавок
не нажимался. Два признака одной беды. Первый: `apple-mobile-web-app-capable`
плюс манифест с `display: standalone` заставляют iOS завести **веб-приложение
со своим хранилищем**; вход, сделанный в Safari, туда не переезжает, а личная
ссылка в `start_url` помогает только если Safari её принял. Второй: замерший
экран — это не поломка, это **заставка iOS**: система показывает снимок прошлого
запуска, пока приложение грузится, и снимок, конечно, не нажимается. Человек
читает «зависло».
**Правило:** если вход держится на ссылке и на хранилище браузера, значок
на iPhone обязан оставаться **закладкой Safari**, а не приложением: снимаем
`apple-mobile-web-app-capable` из разметки и `<link rel="manifest">` на iOS —
тогда система берёт адрес строки, а он уже личный (У-105). Иконка и имя значка
при этом на месте: их дают `apple-touch-icon` и `apple-mobile-web-app-title`.
Android не трогаем: там значок ставит Chrome, хранилище у него общее
с браузером, и личный манифест работает как задумано.
**Замер (движок Safari, iPhone 14 Pro):** манифест снят, `apple-mobile-web-app-capable`
нет, голый адрес после входа сам становится `?u=Maks&k=nk6xafd6`, лист открыт;
на Android (Chromium, Pixel 7) манифест по-прежнему личный, `display: standalone`.
**Общий вывод по трём заходам У-103 → У-105 → У-106:** правку, которую нельзя
проверить в своей среде, нельзя и объявлять сделанной. Движок Safari
(`playwright install webkit`) стоило поднять сразу — он показал правду за минуту.

### У-107. ⛔ Тег, снятый из кода, Safari успевает прочитать раньше — снимать надо из РАЗМЕТКИ

**Что (06.08.2026, четвёртый заход по одному и тому же):** заказчик — «ничего
у тебя не получилось. Мало того что ещё и иконка полностью пропала… и всё равно
просит войти». Прошлый заход (У-106) снимал `<link rel="manifest">` вызовом
`link.remove()` из `lib/homescreen.ts`. Но код этот бежит, когда человек уже
опознан, то есть **после** того, как лист приехал с сервера, а Safari читает
голову страницы при загрузке. Гонку выигрывал Safari: манифест с
`display: standalone` он уже прочитал, значок опять становился отдельным
приложением с пустым `localStorage`, а `start_url` в общем файле — голый `./`.

**Правило:** если тег меняет поведение системы, а не приложения, снимать его
из кода нельзя — гонку выигрывает система. Тега не должно быть **в разметке**,
а вешать его надо из кода тем, кому он нужен. Теперь `app/index.html` ссылки
на манифест не содержит вовсе, а `lib/homescreen.ts` её создаёт — только не
на iOS, и сразу личную.

**Вторая половина — то, чего не понимали три захода.** С iOS 14 значок-приложение
делит с Safari служебного работника и **`CacheStorage`**, но НЕ делит `localStorage`
и куки ([firt.dev/ios-14](https://firt.dev/ios-14/)). Вход лежит именно
в `localStorage` (`flops.auth`) — вот почему он и пропадал. Отсюда:
* развилка «офлайн ИЛИ вход» оказалась ложной: закладка Safari даёт и то, и другое,
  потому что работник и его кеш у неё общие с браузером и уже прогреты;
* появилась страховка: личная ссылка кладётся в кеш `pine-me`, и приложение
  с пустым хранилищем себя вспоминает (`вспомнитьСебя()` в `main.tsx`).
  ⛔ `sw.js` в `activate` сносит все чужие кеши — для `pine-me` там оговорка,
  без неё страховка стиралась бы при каждой выкладке.

**Про иконку.** Единственной ссылкой был `apple-touch-icon` на `icon-192.png`
без атрибута `sizes`, а **192 в списке Apple нет вовсе** — она ждёт 152, 167
и 180 (Apple, Configuring Web Applications). Теперь три ссылки с точными
размерами, `-precomposed` для старых iOS и, главное, файлы
`apple-touch-icon.png` и `apple-touch-icon-precomposed.png` **в корне сайта**:
их Safari ищет сама, даже если разметку прочитать не смогла.

**Замер (собранная версия, песочница):** WebKit с UA iPhone 18.5 — манифестов
**0**, значков Apple **4** (180/167/152/precomposed), `apple-mobile-web-app-capable`
нет; Chromium с UA Pixel 7 — манифестов **1**, `blob:`, `start_url`
`?u=Maks&k=nk6xafd6`, `display: standalone`, иконок 4. Три роли × 390 и 1280 ×
обе темы: горизонтальной прокрутки нет (390/390, 1280/1280), ошибок консоли 0.
Без сети лист рисуется во всех четырёх сочетаниях (14 040–17 475 знаков текста).
**Решающая проба:** в кеш положена ссылка КОСТИ, `localStorage` очищен, открыт
голый адрес — адрес сам стал `?u=Kostya&k=8sqsfh85`, на экране «Костя, редактор».
Так может только страховка: голый адрес на localhost дал бы владельца. Кеш пуст
и хранилище пусто — переход ровно 1, круга нет.

**Чего среда проверить не даёт:** что именно iOS кладёт в значок при
«На экран „Домой“». WebKit — движок Safari, но не его оболочка. Это
подтверждает только заказчик на своём телефоне.

**✅ ПОДТВЕРЖДЕНО ЗАКАЗЧИКОМ 06.08.2026.** Он удалил старый значок и добавил
заново, и на вопрос о трёх вещах отметил **все три**: эмблема на домашнем
экране есть · лист открывается сразу, без «впишите» · открывается и в самолётном
режиме. Четыре захода (У-103 → У-105 → У-106 → У-107) закрыты. Замер WebKit'ом
и живой iPhone на этот раз сошлись — но узнать это можно было только спросив.

**Где:** `app/index.html`, `app/src/lib/homescreen.ts`, `app/src/main.tsx`,
`app/src/lib/perm.ts` (`AUTH_KEY` стал экспортируемым), `app/public/sw.js`,
`app/scripts/make-icons.mjs`, `app/vite.config.ts`.

### У-110. Отметка, которая кладёт число в расчёт денег, принимается по ДОСЛОВНОМУ примеру заказчика

**Что (06.08.2026):** заказчик просил галочки вместо чёрточек в «Закупке» и привёл
свой пример: «туалетную бумагу 8 рулонов купил Макс — галочку поставил, значит это
идёт в перерасчёт». Галочку сделали, и она ставила покупателю **1**. При количестве 8
это значит: в перерасчёт идёт восьмая часть цены, а семь рулонов висят ничьими.
Орган при этом работал безупречно — нажимался, сохранялся, показывался. Поймано
приёмкой `product-architect` до выкладки, потому что она сверялась со словами,
а не с поведением.
**Почему:** орган делали правильным «по себе»: галочка — булево, булево — единица.
Но он пишет число в деньги, а какое именно, решает не форма органа, а жизнь:
человек берёт позицию **целиком**, а по штуке — редкое исключение, для которого
есть счётчик.
**Правило:** если орган подставляет число в расчёт денег, приёмка идёт по
дословному примеру заказчика с его числами, а не по слову «работает». Числа
примера подставляются в проверку: 8 рулонов, один покупатель → у него 8, остаток 0;
второй ставит галочку → ему достаётся остаток, а не ещё восемь. Родня У-63: там
проверялось посчитанное вместо показанного, здесь — поведение органа вместо смысла
числа.
**Где:** `app/src/components/buy/buylocal.tsx` — `restQty()` (остаток количества
позиции, меньше единицы не бывает), `buy/BuyRow.tsx`, `buy/BuyStrip.tsx`.
⚠️ `lib/settle.ts` и `lib/calc.ts` не тронуты: меняется только то, какое число
подставляется при первом нажатии.

### У-111. Картинка, расходящаяся с числом, — это дефект, даже когда число верное

**Что:** километры между точками считались по дорогам (`roadLegs`, OSRM), а нитка
на карте шла ПРЯМОЙ из точки в точку — через лес и залив. Заказчик 06.08.2026:
«Google-карты простраивали реальный маршрут. Этот маршрут шёл в основу
километража». Он смотрел на прямую и не верил числу — и был прав: прямая
через залив выглядит ровно как ошибка расчёта.
**Правило:** если величина показана дважды — числом и картинкой, — обе обязаны
приехать из одного источника. Разошлись — верят той, что видна, а не той, что
верна (родня У-98: повтор числа лечится связью, а не согласованием).
**Где:** `lib/osrm.ts` — `roadShape()` (`overview=full&geometries=geojson`);
`map/shapes.ts` — хук `useRoadShapes`, линии живут В ПАМЯТИ вкладки;
`map/OsmRouteMap.tsx` и `map/GoogleRouteMap.tsx` берут ломаную из `shapes`.
⛔ **Геометрию в документ не класть**: одна нитка — две тысячи координат
(замер 06.08.2026: 2086 точек на три метки), а форму хранения потом не поменять
(постулат 4). Отказ маршрутизатора — прямая и СЛОВА под картой, почему она
прямая (постулат 5). По воде и пешком линия прямая всегда: у публичного OSRM
живёт только профиль `driving`, и это свойство, а не поломка.

### У-112. Карточка метки на 390 не помещается в карту и обрезается

**Что:** найдено замером 06.08.2026 по ходу У-111, **не чинено** — пишу, чтобы
не искать заново. На 390 карта высотой 280 px (`min-h-[280px]`), а карточка
метки — 366 px. Куда её ни поставь (`under` решает, вверх или вниз), она
вылезает за край блока, а у блока `overflow-hidden`. Замер: карточка `84, 0,
256 × 366`, `elementFromPoint` в середине кнопки ряда техники отдаёт не кнопку,
а `DIV.flex flex-col gap-10` — то есть колонку страницы ПОЗАДИ карты. На 1280
всё в порядке: карточка `861, 365`, все пять кнопок отвечают за себя.
**Правило:** блок с `overflow-hidden` не может показать содержимое выше себя.
Плавающее над картой окно меряется на самой узкой ширине И на самой низкой карте.
**Починено 06.08.2026 первым из двух путей:** на телефоне (< 1024) карточка стоит
**полосой под картой**, в потоке, и блок под неё растёт (`h-auto` вместо числовой
высоты). Второй путь — подрастить карту — был бы напрасен: карточку сажают
у метки, а вид подводится так, что метка стоит на 68 % высоты (`PAN_DOWN`), —
чтобы окно в 366 px поместилось НАД ней, карте нужно было бы за 550 px, то есть
две трети экрана телефона под карту.
**Замер после правки (390 × 844, обе темы):** карточка `17, 451, 356 × 309`,
запас внутри блока сверху 576 px, снизу 231 px; все **10** органов карточки
по `elementFromPoint` в центре отдают себя; блок 460 → 1116 → 460 при открытии
и закрытии; горизонтальной прокрутки у страницы нет (390 / 390); контраст
названия 8,91 (тёмная) и 14,72 (светлая), адреса 4,67 и 7,51. На 1280 **ничего
не изменилось**: карточка `861, 450, 256 × 366`, слой прежний
(`absolute z-10 -translate-x-1/2 -translate-y-full`), соседи сдвинулись на 0 px.
⚠️ Это НЕ регрессия правки У-111: ряд техники в карточке был и до неё, высота
карточки от кнопки «＋» не изменилась.
**Где:** `map/MapPointCard.tsx` — проп `flat`; `map/TripMap.tsx` — `useIsDesktop()`,
полоса под картой и `h-auto` у блока; `map/GoogleRouteMap.tsx`,
`map/OsmRouteMap.tsx` — слой карточки рисуется только при непустом `card.node`.

### У-113. Поиск «своей области прокрутки» подъёмом по предкам всегда доходит до страницы

**Что (06.08.2026):** тап по метке на карте уносил страницу на 17 483 px — с карты
к ленте «Дороги» на другом конце листа (замер на 390: `scrollY` 734 → 18 217).
Карта, по которой человек только что попал пальцем, уезжала из вида вместе
с карточкой метки. Виноват был `useRouteFocus`: он ищет область прокрутки подъёмом
по предкам, `while (pane.scrollHeight <= pane.clientHeight + 1) pane = pane.parentElement`.
На десктопе подъём кончался быстро — у блока ленты стоит `lg:overflow-y-auto`.
На телефоне блок `overflow-clip`, прокручиваемых предков у ленты нет ВОВСЕ, и подъём
доходил до `<html>` — единственного узла, где `scrollHeight` больше `clientHeight`.
Замер цепочки: девять предков подряд «не прокручиваемый», десятый — `HTML`,
24 032 против 844.
**Почему:** условие «содержимое выше видимой части» истинно у страницы всегда.
Цикл без потолка не может не найти её — он ищет не «свою область», а «хоть какую».
Это тот же корень, что и в У-109 (`scrollIntoView` двигает ВСЕХ предков): разница
лишь в том, что там предков перебирал браузер, а здесь мы сами.
**Правило:** подъём за областью прокрутки обязан иметь потолок. Страница и `body`
областью прокрутки компонента не считаются: не нашлось своей — не прокручиваем
ничего. Молчаливый уход страницы хуже, чем ненаведённая строка.
**Где:** `road/RouteTiming.tsx`, `useRouteFocus` — цикл останавливается
на `document.scrollingElement` и `document.body`. Замер после правки: `scrollY`
в момент нажатия и через полторы секунды **734 и 734**, сдвиг 0.

### У-114. Блок с числовой высотой обрезает своё содержимое молча (найдено, НЕ чинено)

**Что (06.08.2026):** блоку карты высота на телефоне задана числом —
`h-[460px]` в `trip/TripSection.tsx`. Содержимого в нём при боевых данных больше:
строка поиска 61 + карта 280 (её `min-h-[280px]` сжаться не даёт) + нижняя полоса
230 = **572 px** (замер: плашку отказа Google сняли из разметки и дали блоку
переверстаться — видно 118 px нижней полосы из 230). Разница в **112 px**
срезается `overflow-hidden`, и нижняя полоса
блока — легенда ниток, кнопки «Точка · Я здесь · Конечная» и строка «какая карта
нарисована и какая сборка» — уходит за край. Полоса выросла до 230 px потому,
что в боевом документе шесть точек без координат и над кнопками встаёт ещё
«Разметить маршрут». Если Google не поднялся, сверху добавляется плашка отказа
на 233 px, и срезается уже 344 px — вся нижняя полоса целиком (замер: её верх
стоит на 574 px от края блока высотой 460).
**Почему это важно:** строка «Карта OpenStreetMap · сборка …» стоит под картой
по прямой просьбе заказчика (У-30, У-31, У-32) — по ней разбирают спор о том,
какая карта нарисована. Невидимая строка равна отсутствующей.
**Правило:** числовая высота у блока, содержимое которого зависит от данных
(сколько точек, поднялась ли карта, сколько ниток в легенде), — это обрезка,
о которой никто не узнает. Мерить надо на боевом документе, а не на пустом сиде:
на сиде маршрута нет вовсе и полоса вдвое ниже.
**Где:** `trip/TripSection.tsx:124` (`h-[460px]`), `map/TripMap.tsx` — нижняя
полоса блока. ⚠️ **Не чинено сознательно** (постулат 13): правка этой сессии
про карточку метки, а высота обложки-близнеца рядом мерится по постулату 12
отдельно. С открытой карточкой блок теперь `h-auto` и не режет ничего —
обрезка остаётся только в закрытом состоянии.
**Починено 06.08.2026 (шестнадцатая порция):** числовой высоты у блока на
телефоне больше нет вовсе — `className` у `TripMap` стал
`border border-line shadow-md ring-0`. Высоту задаёт содержимое: карта берёт
свои 280 px минимума, полоса — сколько ей нужно. Замер на 390 при живой плашке
отказа Google (худший случай): блок 358 × **874**, строка «Карта OpenStreetMap ·
… · сборка …» стоит на 840 и целиком внутри блока, все три кнопки «Точка ·
Я здесь · Конечная» по `elementFromPoint` отдают себя. На 1280 не изменилось
ничего: блок 604 × 890, обложка-близнец 604 × 890 — симметрия та же, высоту
там по-прежнему даёт строка сетки.

### У-115. Список нельзя удалить раньше, чем всё, что на нём висело, поедет на новое место

**Что (06.08.2026):** заказчик дважды и дословно отменил ленту точек маршрута —
«разметка маршрута в виде списка не нужна вообще» и потом «Да, она не нужна
вообще. Просто список точек на карте». Соблазн — удалить `RouteBoard`,
`RouteStrip`, `RouteTiming` и сказать «сделано». Инвентарь полей перед правкой
показал, что на ленте висело **девять** вещей, которых на карте не было:
правка адреса, «пройдено» (`p.done`), «кто едет» (`p.o`), метка этапа
(`lab`/`labT`), чем добираемся (`mode`), расстояние от прошлой точки
(`leg`/`legSrc`), «Посчитать по карте», координаты и «убрать точку с карты»,
а сверх того — словесное описание маршрута из документа (`trip.route`,
в боевой строке это живая строка на 88 знаков) и счёт точек. Плюс шесть точек
боевого маршрута **вообще без координат**: метки у них нет, и после удаления
ленты они стали бы недоступны целиком.
**Правило:** порядок обязателен — **переселить → замерить → удалить**.
Инвентарь полей выписывается ДО первой правки и сверяется ПОСЛЕ, один в один
(постулат 4). Точки, у которых нового места нет по своей природе (нет
координат — нет метки), получают вход в том единственном органе, что остался:
мастер «Разметить маршрут» научился править название и убирать точку, а время
и описание он теперь показывает, и словами объясняет, где правится остальное.
**Где:** `map/MapPointCard.tsx` (адрес правится, «пройдено», ряд «кто едет»,
подробности блоком `road/RoutePointSetup`), `map/TripMap.tsx` (`trip.route`
и счёт точек под картой), `map/RouteMarkSheet.tsx` (правка и удаление точки
без координат), `road/RoadSection.tsx` (лента убрана), удалены
`map/RouteBoard.tsx`, `road/RouteStrip.tsx`, `road/RouteTiming.tsx`.

### У-116. Удаляя список, проверь ВСЕХ, кто на него прыгал

**Что (06.08.2026):** после удаления ленты точек поиск по листу продолжал
предлагать точки маршрута и вести их в «Дорогу» — к `data-hit`, которого больше
нет. Прыжок молча не делал ничего: ровно тот молчаливый отказ, ради которого
писан постулат 5. Перевёл точки в раздел «Поездка» (карта) и завёл просьбу
`askMapPoint` — и **тем же замером поймал собственную регрессию**: в
`SearchCommand` есть свой список групп `GROUPS`, и раздела `trip` в нём не было,
так что находка попадала в индекс, но на экран не выходила вовсе.
**Правило:** удаление места показа — это правка не одного файла, а всех входов
в него. Проверять поимённо: поиск по листу, общий «плюс» (`lib/addnew.ts`),
переходы `jumpToItem`/`goSection`, тексты офлайн-копии, посредники в `lib/*`.
И проверять **живьём нажатием**, а не чтением кода: список групп поиска —
третье место, где раздел надо назвать, и по коду одного файла этого не видно.
**Где:** `components/SearchCommand.tsx` (`GROUPS` + раздел точек), `App.tsx`
(`onJump` разводит точку маршрута и обычную строку), `lib/mapfocus.ts`
(`askMapPoint` вместо ушедших `askMap` / `focusInList`), `map/TripMap.tsx`
(текст скачанной копии больше не отсылает в «Дорогу»).

---

## H. Субагенты и оркестрация

### У-50. Отчёту субагента верить нельзя — сверять с диффом

**Что:** субагент «починил» чтение `tileLabels`, поменяв форму хранения (см. У-04).
Отчёт был бодрый.
**Правило:** отчёт субагента — это утверждение, а не факт. Принимается только вместе с
диффом и выводом замера. Тестировщик имеет право вернуть работу.

### У-51. Субагент — не средство самопроверки

**Что:** Anthropic дословно: *«do not use subagents to verify or double-check your own
work»* (docs «Prompting Claude Opus 5»).
**Правило:** обычную правку проверяю сам командой (`tsc`, `oxlint`, замер) — звать за
этим агента значит платить вдвое за тот же ответ. Субагент зовётся туда, где свежий
контекст даёт то, чего у меня нет: широкий аудит, крупный дифф, живой прогон
интерфейса, спорный вердикт. При этом генератор и оценщик разделяются: тестировщик не
видит хода моих рассуждений, только результат и критерии.

### У-52. Цепочка агентов на мелкой правке — трата денег и времени

**Правило:** ростер профилей в `.claude/agents/` — **справочник, а не маршрут**.
Маршрут выбирается по размеру и риску задачи, таблица — в `docs/AGENTS.md`.
Кап: один субагент на подзадачу.

### У-117. Одна вереница точек на несколько маршрутов — это молча неверные деньги

**Что:** «Посчитать по карте» отдавала маршрутизатору ВСЕ точки с координатами
одним списком, в порядке документа. Пока маршрут был один, это было верно.
Как только у техники появились свои нитки (`p.tr`, `threads()`), число поехало:
в боевом документе `trip.dist.auto = 141` оказалось склейкой из точек Honda,
Aveo и лодки вперемешку — с прыжками «парковка → вода → парковка». Это число
шло множителем в бензин ОБЕИМ машинам, то есть прямо в деньги, и ни одна
проверка его не ловила: контрольные суммы считаются на сиде, где нитка одна.

**Почему не заметили раньше:** картинка и число разошлись беззвучно. Нитки
на карте уже рисовались по технике (`map/marks.ts`), а километры считались
по общему списку — глазами видно правильные линии, в расчёте неправильная сумма.

**Правило:** как только у сущности появилась своя ветка, ВСЁ, что считалось
по общему списку, обязано быть пересчитано по веткам в тот же заход. Проверять
не на сиде, а на боевом документе: сид — простейший случай, и он молчит именно
там, где ломается жизнь.

**Где исправлено:** `road/legs.ts` — по нитке на запрос, `p.leg` от прошлой точки
своей нитки, `t.kmAuto` своей суммой; по воде и пешком — прямая (`legSrc:'line'`).
`trip.dist.auto` трогается, только если общая нитка правда посчиталась: иначе
прежние 141 км обнулились бы вместе с деньгами всей техники без своей цифры.

---

### У-118. Одно и то же число, показанное трижды, читается как беспорядок

**Что случилось.** Заказчик 06.08.2026 вечером: «С левой стороны посмотри,
у тебя какой-то бардак… миллиард разновидностей типографики, бардак, нигде нету
порядка. Вот так в логистике просто всё через жопу сделано». Под картой к тому
моменту стояли подряд: легенда ниток (цвет · значок · название), строка «пробег
каждой техники» (тот же цвет · то же название · километры) и плитка «Разметить
маршрут». Каждая вещь по отдельности была заказана им же и сделана правильно.

**Почему.** Постулат 3.5 запрещает второй СПИСОК, и его соблюдали буквально:
списка не было. Но легенда и строка пробега — это и есть перечисление одного
и того же набора по одному и тому же признаку, только разными наборами шрифтов.
Три показа одного маршрута дают ровно то ощущение, которое человек называет
«бардак», даже когда каждый показ по отдельности красив.

**Как больше не делать.** Мерить не «список это или нет», а сколько раз на одном
экране назван ОДИН И ТОТ ЖЕ набор сущностей. Больше одного — резать, оставляя
тот показ, по которому ещё и нажимают.

**Где исправлено:** `map/RouteBranches.tsx` — один ряд веток над картой: цвет,
значок, название, километры и выбор активной ветки. Легенда и строка пробега
из `map/TripMap.tsx` удалены.

---

### У-119. Служебный код геокодера — это не адрес

**Что случилось.** Заказчик 06.08.2026: «Какие-то непонятные значения: X5, WW,
WF, город Ленинградской области. Я не совсем понимаю, зачем эта информация
и как она вообще здесь помогает». В боевом документе лежало
`"XVWW+WF Горы, Ленинградская область, Россия"` — Plus Code (Open Location Code),
который Google отдаёт вместо улицы там, где улицы нет. Такие адреса стояли
у конечной точки поездки и у точек маршрута.

**Почему.** Ответ геокодера клали в документ и показывали как есть — вместе
с кодом, страной и почтовым индексом. Машинная строка попала человеку на глаза
и была прочитана как поломка.

**Как больше не делать.** Всё, что пришло из чужой службы, показывать через
свою функцию показа, а не напрямую. Само поле в документе при этом не трогать
(постулат 4) — режем при показе и при записи новых значений.

**Где исправлено:** `lib/geocode.ts` — `humanAddr()`: срезает Plus Code, «Россия»
и индекс, приводит «обл.» к «область». Применено на обложке (`TripCover`),
в карточке метки, в находках поиска и при записи новых адресов (`TripMap`).

---

### У-120. Номер, который считает не то, что человек считает глазами

**Что случилось.** Заказчик 06.08.2026, поздний вечер: «Если я условно завожу
новую точку для нового человека, то она должна начинаться с единицей. А у тебя
сейчас как новая точка — и пятнадцати, число выявляется. Не совсем правильный
подход. Для каждого вида транспорта будет своя начальная и финальная точка».

Номер в кружке метки был местом точки в общем массиве `S.route`. Пока маршрут
был один, это совпадало со здравым смыслом. Как только у техники появились свои
нитки, первая же точка лодки получила номер 15 — и читалась как «пятнадцатый
этап поездки», хотя на воде она первая и единственная.

**Почему не заметили.** Числа были верные — по документу. Ошибка была не в
арифметике, а в том, ЧТО этим числом названо: порядок хранения выдали за порядок
движения. Такую подмену не ловит ни одна проверка, потому что оба числа
существуют и оба «правильные».

**Правило.** Как только сущность разошлась по веткам, всё, что нумеруется,
нумеруется ВНУТРИ своей ветки. Тот же вопрос задавать каждому видимому числу:
это порядок в документе или порядок в жизни? Показывать нужно второе.

**Где исправлено:** `map/marks.ts` — `MarkStyle.no` считается в `markStyles()`
по месту точки в своей нитке; обе карты и карточка метки (`MapPointCard`) берут
номер оттуда, а не из `points.findIndex`.

---

### У-121. Полоса свойств над картой отжимает саму карту за экран

**Что случилось.** Заказчик 06.08.2026, поздний вечер, дословно: «карта идиотски
сделана с этими снизу какими-то конечными точками… С правой стороны у тебя должна
быть просто карта, аккуратненько, наверху найти. А снизу автотранспорт».

Полоса веток стояла НАД картой — её туда поставили по прошлой просьбе («над
картой или прям на карте ты выбираешь сначала вид транспорта»). Вместе с поиском
и плашками она заняла 240 px сверху, и карте на телефоне оставалось 280 px —
её минимум. Кнопка «Конечная» при этом стояла снизу третьим входом в действие,
у которого уже было два.

**Почему.** Каждое требование выполнялось буквально и по одному, а высота экрана
общая. Просьба «над картой» относилась к выбору транспорта ДО расстановки точек,
а не к тому, чтобы отдать полосе верх экрана.

**Правило.** У блока с картой считать высоту как бюджет: что не карта — то отнято
у карты. Каждой строке над и под картой отвечать на вопрос «сколько она стоит
в пикселях и что человек делает без неё». Замер обязателен и до, и после:
здесь он показал 194 px нижнего блока и 179 px плашки об отказе Google.

**Где исправлено:** `map/TripMap.tsx` — полоса веток переехала под карту;
кнопка «Конечная» убрана (место без координат ставится значком на обложке,
поставленное переносится перетаскиванием метки); в полном экране скрыты описание
маршрута, число точек и объяснение прямой линии, а плашки об отказе сокращены
до одной строки. Замер после правки на 390 × 844: карта 547 px вместо 347.

---

### У-122. Число, посчитанное по карте, но не попавшее в расчёт, — это не функция

**Что случилось.** Заказчик 06.08.2026, поздний вечер: «при добавлении маршрута
для определённого вида транспорта — авто или водного, неважно — он сразу же
берёт расчёты по точкам, ведёт авторасчёты по точкам в логистике».

Километры считались только по нажатию «Посчитать по карте» в другом разделе.
Человек расставлял точки на карте, видел маршрут — и не понимал, почему в деньгах
ничего не изменилось. Осторожность («молча ничего не переключаем») была принята
как правило раньше и в своё время была верной, но она же сделала расстановку
точек занятием без последствий.

**Правило.** Осторожность «не трогаем расчёт без явного действия» действует ровно
до тех пор, пока человек не сказал обратного. Сказал — расчёт идёт сразу, а
защищается тогда только то, что он вписал РУКАМИ: `kmSrc === 'manual'` карта
не отменяет.

**Где исправлено:** `road/legs.ts` — `calcLegsByMap({ adopt: true })` ставит
`t.kmSrc = 'auto'` всем веткам, кроме тех, у кого цифра ручная; `map/TripMap.tsx`
зовёт расчёт сам через 1,5 с после того, как набор точек изменился. Пустая запись
в документ при этом невозможна: если все числа совпали с прежними, `update`
не зовётся вовсе — иначе каждое открытие листа помечалось бы новым автором.

---

### У-123. Цвет, выбранный человеком, обязан дойти до КАЖДОГО места, где вещь названа цветом

**Что случилось.** Заказчик 06.08.2026: «почему у тебя автотранспорт
и автомобили разного цвета? Получается, я завёл Chevrolet синий цвет, синяя
ветка, а у тебя его машина отмечена как зелёная».

Свой цвет ветки (`t.tone`) появился в восемнадцатой порции, и его научились
понимать те два места, ради которых он заводился: нитка на карте и полоса веток
под ней (обе через `toneOf`). Третье место — кружок техники в ряду карточки
точки — осталось на прежнем `toneAt`, который знает только МЕСТО в списке.
Chevrolet Aveo стояла третьей, `toneAt(2)` даёт зелёный — и человек видел
на одном экране синюю линию и зелёный кружок одной и той же машины.

Ошибка не в расчёте цвета, а в том, что новое поле подключили к местам показа
поимённо, а не ко всем сразу. Тот же корень, что у постулата 12: правку меряли
там, ради чего делали.

**Правило.** Завёл поле, которое человек правит руками, — найди `grep`-ом ВСЕ
места, где эта величина показывается, и подключи их разом. Отдельно проверь ряды
выбора и кружки: они обычно рисуются другой функцией, чем главный показ.
Замер после правки — цвет ОДНОГО и того же объекта в разных местах экрана,
числом, а не глазом.

**Где исправлено:** `map/MapPointCard.tsx` — `toneAt(idx)` → `toneOf(t, idx)`.
Живой замер на боевом документе с выбранным синим: линия на карте `#1D4ED8`,
кружок в полосе веток `#1D4ED8`, кружок в карточке точки `#1D4ED8` — три числа
совпали. До правки третье было `#166534`.

---

### У-124. Панель, которую нельзя убрать, — это отнятая часть экрана

**Что случилось.** Заказчик 06.08.2026: «дай возможность сворачивать вот это
меню, которое снизу. Она мешает, у тебя должна быть такая возможность».

Нижняя панель разделов занимала 81 px из 844 и накрывала ровно то, что человек
в эту секунду правит: полосу веток под картой, последнюю строку списка. На
телефоне это десятая часть экрана, отданная навигации навсегда.

**Правило.** Постоянный орган, занимающий больше строки, обязан уметь
сворачиваться, а свёрнутый — оставлять видимый след, которым его вернут:
исчезнувший без следа орган это молчаливый отказ (постулат 5). Настройка вида
пишется в браузер, а не в документ, — иначе один сворачивает панель четверым
(`flops.nav`, ровно как `flops.theme`).

⚠️ Высоту такого органа обязана знать вся страница. Отдана переменной
`--bottom-nav-h` на `<html>`: от неё считают и отступ `main`, и плашки «нет
связи» и «нет прав». Без общей переменной свёрнутая панель оставила бы под собой
пустоту, а плашки повисли бы в воздухе — постулат 12, соседи в том же контейнере.

**Где исправлено:** `navpref.ts` (новый), `components/BottomNav.tsx`, `App.tsx`,
`components/NetNotice.tsx`, `components/PermNotice.tsx`. Замер 390 × 844:
панель 85 → 28 px, отступ `main` 108 → 52 px, ряд разделов СНЯТ с разметки
(скрытый прозрачностью продолжал бы ловить нажатия), язычок 56 × 28 с целью
касания 70 × 47, `elementFromPoint` в центре отдаёт сам язычок, настройка
переживает перезагрузку. На 1280 панели нет вовсе (`lg:hidden`), отступ 48 px.

---

### У-125. Одно и то же число в двух местах — два разных `digits`, и одно из них врёт

**Что случилось.** Заказчик 06.08.2026: «Вот я прописываю там 2,5 л/ч, а у тебя
почему-то округляется до 3. Кто тебя просил это делать? Никто».

Расход правится в трёх местах: `road/RoadStrip.tsx`, `road/RoadCalc.tsx` и
полоса веток под картой. В первых двух стоит `digits={dg(t.rate)}`, в полосе
веток — восемнадцатая порция завела поле без `digits`, а по умолчанию у
`InlineNum` `digits = 0`.

⚠️ Данные при этом были ЦЕЛЫ: `commit()` не округляет, в документе лежало 2,5,
и деньги считались верно. Врал только показ — и это ничуть не лучше. Человек
читает с экрана и планирует бензин по трёшке.

**Правило.** Одна и та же величина, показанная в разных местах, обязана иметь
одинаковую точность. Заводя ещё одно место показа существующего поля, копируй
не только `value` и `onSave`, а ВЕСЬ набор свойств показа — `digits`, `unit`,
`min`, `max`. `grep` по имени поля, а не по памяти.

**Где исправлено:** `map/RouteBranches.tsx` — добавлен `digits={dg(activeT.rate)}`.
Живой замер на боевом документе: «Расход 2,5 л/ч» вместо «Расход 3 л/ч».

---

### У-126. Прокрутка вбок прячет ровно то, ради чего блок сделан

**Что случилось.** Заказчик 06.08.2026: «ни скролла, ни горизонтального…
Сделай просто: первая строка — один транспорт, вторая — второй, в третьей
строке — третий транспорт».

Полоса веток ехала вбок. На 390 в неё влезали две ветки из четырёх, остальные
надо было искать пальцем — а ветка это и есть главный орган карты: от неё
зависит, куда упадёт следующая точка.

Прокрутка вбок была принята как способ не давать горизонтального скролла
СТРАНИЦЕ (постулат 8) — и это правило верное. Но оно про страницу, а не про
разрешение прятать содержимое: «внутри блока можно» ≠ «внутри блока нужно».

**Правило.** Прокрутка вбок допустима там, где содержимое однородно и его много
(матрица людей в списках). Там, где элементов единицы и каждый — выбор режима
работы, они встают строками: строке край экрана не мешает.

**Где исправлено:** `map/RouteBranches.tsx` — `flex overflow-x-auto` →
`flex flex-col`, чипы `shrink-0` → `w-full`, потолок `max-w-40` у названия снят.
Заодно убрана строка-подсказка «Новые точки на карте попадают в „…“» («эта вся
описательная часть глупая, ненужная»). Замер: пять веток строками, x у всех
один, каждая 44 px, у страницы скролла нет (390 = 390, 1280 = 1280).

---

### У-127. `curl` без сжатия отвечает за здоровый вариант адреса, а браузер идёт в отравленный

**Что случилось.** Заказчик 07.08.2026: «pine-to-pine по-прежнему не работает.
Мне важно, чтобы он работал. Очень важен: чтобы он сейчас работал». Сайт стоял
белым экраном со вчерашнего вечера — то есть с самой выкладки двадцатой порции.

Проверка свежести накануне прошла и сказала «выложено»: `curl` по адресу
`assets/index-Q6qbLPJn.js` отдавал `200 · application/javascript · 506 033 байта`,
и внутри находились строки именно этой правки. Прибор показывал здоровый сайт.

Живой браузер при этом получал по ТОМУ ЖЕ адресу:

```
content-type: text/html; charset=utf-8
content-encoding: br
cache-control: public, max-age=31536000, immutable
cf-cache-status: HIT · age: 850 · vary: accept-encoding
тело: <!doctype html><html lang="ru">…
```

То есть на краю Cloudflare под адресом файла кода лежала заглушка `index.html`,
закешированная как «неизменяемая» на год. Модуль не грузился, `root` оставался
пустым, ошибка в консоли одна: «Expected a JavaScript-or-Wasm module script but
the server responded with a MIME type of "text/html"».

**Почему приборы разошлись.** В ответе стоит `vary: accept-encoding` — у одного
адреса на краю живут РАЗНЫЕ записи под разные сжатия. `curl` по умолчанию
brotli не просит и попадает в вариант `identity`, где лежит настоящий код.
Браузер просит `br` и попадает в вариант, отравленный заглушкой. Отравился он
гонкой выкладки: пока новая сборка расходилась по краю, запрос к ещё
не появившемуся файлу поймало правило `/* /index.html 200` из `_redirects`,
а `_headers` навесило на `/assets/*` годовой `immutable` — и HTML остался
жить под адресом кода.

**Правило.** Свежесть боевого сверяется запросом, который делает БРАУЗЕР,
а не запросом, который удобно набрать. Минимум — `curl --compressed`
(или `-H 'Accept-Encoding: br, gzip'`) и сверка `content-type`. А вердикт
«работает» имеет право выносить только загрузка страницы целиком с чтением
консоли: `rootChildren > 0`. Файл, отданный с кодом 200 и нужным содержимым,
доказывает лишь то, что этот вариант этого адреса цел.

⚠️ Признак именно этой беды: `content-type: text/html` у `.js`, `cf-cache-status:
HIT` и `age` больше нуля. Лечится сменой имени файла сборки — имя считается
от `BUILD_ID` (`app/vite.config.ts`), то есть от хеша коммита, поэтому ЛЮБОЙ
новый коммит уводит браузер на чистый адрес. Чистить кеш Cloudflare руками
не требуется.

**Где исправлено:** этот урок и есть починка — коммит меняет `BUILD_ID`,
`BUILD_ID` меняет содержимое главного куска, содержимое меняет его хеш и имя.
Проверка после выкладки — `curl --compressed` по новому имени и загрузка
страницы Playwright'ом с подсчётом узлов в `root`.

---

### У-128. Карточка, всплывающая на каждое действие, отменяет само действие

**Что случилось.** Заказчик 07.08.2026: «при фиксации каждой новой точки
не должно возникать вот эта менюшка, внутренняя, которая мешает на карте».

Карточка точки открывалась сразу после постановки — по замыслу «поставил
и тут же назвал». Замысел верен для ОДНОЙ точки. Маршрут расставляют десятком
подряд, и карточка вставала ровно поверх того места, куда ставят следующую.

Цена видна в самом боевом документе: из 25 точек **одиннадцать назывались
«Новая точка»**. То есть карточка не только мешала — она и своей задачи
не решала: человек закрывал её, не читая, и шёл ставить дальше.

**Правило.** Орган, который открывается сам после действия, обязан быть дешевле
самого действия. Действие повторяют подряд — орган не открывается вовсе.
Что он давал, отдаётся другим путём: либо тем же жестом по готовому объекту
(тап по метке), либо автоматически (имя от геокодера).

⚠️ Отвечать «чем тогда назвать» догадкой нельзя — спросили. Ответ 07.08.2026:
«Остаётся просто. Если я захочу, я на неё нажму и назову, как хочу, по умолчанию
просто адрес с карты».

**Где исправлено:** `map/TripMap.tsx` — `openCard(id, true)` убран из `onAdd`,
`onLine`, `onPick` и ветки `placing`; у `guessAddr` появился признак `nameIt`,
подставляющий имя из `shortPlaceName(humanAddr(addr))` — но ТОЛЬКО вместо
заводского `NEW_POINT` и пустого, чужое имя не трогается никогда (постулат 4).
Живой замер: тап по чистому месту карты — точек 25 → 26, имя «Приозерское
шоссе» вместо «Новая точка», карточка закрыта; тап по метке карточку открывает.

---

### У-129. Слепок перерисовки, в котором нет величины, — это величина, которой не видно

**Что случилось.** Свой цвет ветки лёг в документ (`#ff00aa` прочитан из
`localStorage`), кружок в полосе веток его показал, а линия на карте осталась
`#C2410C`. Проверялось живым замером `stroke` у пути Leaflet.

Обе карты пересобирают слой не на каждой перерисовке, а по строке-слепку —
иначе метка выпрыгивает из-под пальца при перетаскивании. В слепке стояло
`${t.i}:${t.leg}` — id и способ передвижения. Ни `tone`, ни `color` в него
не входили, поэтому смена цвета не была для карты поводом перерисоваться.

⚠️ Беда старше правки: выбор ГОТОВОГО тона (`t.tone`) не перерисовывал карту
точно так же — с восемнадцатой порции. Не замечали, потому что цвет меняют
редко и следом обычно двигают точку, а движение точки слепок меняет.

**Правило.** Слепок перерисовки — это список всего, что видно на экране.
Завёл поле, которое человек правит и которое ВИДНО, — впиши его в слепок
тем же движением, что и в показ. Проверка одна: поменять величину и прочитать
не документ, а нарисованное (`stroke`, `getComputedStyle`).

**Где исправлено:** `map/OsmRouteMap.tsx` и `map/GoogleRouteMap.tsx` — в `sig`
добавлены `t.tone` и `t.color`, правка парная, карты обязаны вести себя
одинаково. Замер после: `stroke` линии `#ff00aa`, после «Сбросить» — `#1D4ED8`.

---

### У-130. Заливка `accent-fill` живёт только в паре с `text-on-accent`

**Что случилось.** Замер контраста на выбранной ветке дал **2,60 : 1**
в светлой теме и **2,13 : 1** в тёмной при норме 4,5 : 1. Читалось хуже,
чем у невыбранных соседей, — то есть подсветка выбора работала наоборот.

`--accent-fill` это плотный янтарь (`#A74612` в светлой, `#DD9A4E` в тёмной),
и во всём проекте он идёт с `text-on-accent`: `Btn` primary, `SectionHead`,
номер точки в `MapPointCard`, галочка в `MenuSection`. Ровно два места
взяли его с `text-ink` — оба в `map/RouteBranches.tsx`, оба с восемнадцатой
порции: строка ветки и кнопка человека в ряду «Едут».

**Правило.** Подсветка выбора фоном при ПРЕЖНЕМ цвете текста — это
`bg-accent-soft` (`#F2E3C0` / `#3E3A20`), и так уже помечены активный раздел
в `BottomNav` и своя колонка в `BuyTable`. `accent-fill` — заливка органа,
у которого свой цвет текста. Перепутать их легко: имена соседние, а разница
в том, кто отвечает за текст.

⚠️ Мерить обязательно СО СКЛАДЫВАНИЕМ прозрачности: полупрозрачный фон,
прочитанный как плотный, даёт выдуманное число. И перед чтением цвета снимать
переходы (У-55).

**Где исправлено:** `map/RouteBranches.tsx`, обе строки → `bg-accent-soft`.
Замер после: 12,18 : 1 в светлой, 10,27 : 1 в тёмной.

---

### У-131. «Что перед глазами» меряется по СЕРЕДИНЕ экрана, а не по его верху

**Что случилось.** Заказчик 08.08.2026: «если я добавляю какой-то пункт,
допустим, в снеки… вот там, где я добавил, там оно и должно появляться…
чтобы я никуда не прыгал, не скакал, потом не искал».

Замер воспроизвёл это с первого раза. Человек читает блок «Алкоголь и сигареты»
посреди экрана, жмёт «плюс» в шапке «Закупки» — строка ложится в «Снеки
и сладкое». `visibleBlockId` брал ПЕРВЫЙ блок, чей низ ниже полосы шапки,
то есть блок, доживающий в верхних пикселях: низ «Снеков» проходил на 420 px,
и это считалось «человек его читает».

**Правило.** Читаемым считается блок, накрывающий середину видимой части;
не накрыл никто — ближайший к ней. Верхний край экрана — место, откуда текст
уходит, а не то, куда смотрят.

**Где исправлено:** `lib/visible.ts`. Замер после, тот же сценарий на 390:
позиция легла в «Алкоголь и сигареты».

---

### У-132. Нечёткий поиск ищет не то, что человек написал

**Что случилось.** Заказчик 08.08.2026: «я написал, допустим, слово „сов“…
и мне выдаёт кучу слов… он всё равно ищет огромное количество ненужного
материала».

Отбор делал cmdk (`command-score`): находкой считается любая строка, где буквы
запроса встречаются ПО ПОРЯДКУ вразбивку. «сов» ловил «Спальник новый»
и «Закрытая обувь (крос**сов**ки…)», а искали «Тушёнку „Совок“». Вдобавок
в ключ отбора шли примечание и название раздела — слово находилось там,
где человек его не видит.

**Правило.** Ищем целый кусок слова и только по названию; примечание — запасной,
самый дальний уровень. Показывается ровно ОДИН, лучший встретившийся уровень
точности: нашлось по началу слова — совпадения внутри слова и в примечаниях
не показываются вовсе. Срезанный хвост не замалчивается, но говорится одной
строкой.

**Где исправлено:** `SearchCommand.tsx` (`pickHits`), `ui/command.tsx`
(проброшен `shouldFilter`). Замер на боевом документе: «сов» → 1 находка
(«Тушёнка „Совок“ 338 г»), «сгущ» → 0 (такого слова в документе нет).

---

### У-133. Ключ, который «перестал работать», — это старый ключ, а не поломка

**Что случилось.** Задание двадцать третьей сессии предупреждало: «ссылка
`?u=Maks&k=…` боевым листом больше не признаётся — разобраться, менялись ли
ключи». Живая проба ответила за одну минуту: `rpc/trip_read` отдавал
`401 {"code":"42501","message":"ключ не подходит"}`, а сам сайт при этом был
жив — `root.children` = 1, страница рисовалась и честно писала «Этот лист
закрыт. Ключ, с которым вы пришли, поездка не признала».

Ключи действительно менялись, и об этом была запись в `HANDOFF.md`. Не
обновили только `.claude/rules/environment.md` и `CLAUDE.md` — то есть ровно
те два файла, которые читаются в начале КАЖДОЙ сессии. Двое суток каждая
сессия начинала работу с недействующей ссылки.

**Правило.** Действующий ключ живёт в одном месте — в последнем снимке
`backups/prod-*.json`, поле `people[].key`. Правила среды на него ссылаются,
но его не хранят.

⛔ **И не хранят по второй причине: репозиторий публичный.** Ключ — это
пропуск в боевой лист, а `.claude/rules/environment.md`, `CLAUDE.md`
и `HANDOFF.md` читает кто угодно. Так они однажды уже утекли — потому
их и меняли.

**Где исправлено:** ключи вынуты из `.claude/rules/environment.md`,
`CLAUDE.md`, `.claude/agents/live-tester.md` и `HANDOFF.md`; вместо них —
команда, достающая ключ из снимка. Замер после: `?u=Maks&k=<ключ из снимка>`
открывает боевой лист, `rpc/trip_read` → 200.

---

### У-134. «Я не понимаю, что это» и «покажи мне это» — про одно число

**Что случилось.** 06.08.2026 заказчик забраковал колонку «Сумма»: «я не
понимаю, что это такое. Бред же: у тебя есть цена плановая и цена факт —
а сумма-то тут на кой?». Колонку убрали. 08.08.2026 он же попросил: «справа
должна быть указана финальная стоимость, а не за единицу, и там фактическая».

Это одна и та же величина — количество × цена. Противоречия нет, и читать
второе слово как каприз было бы ошибкой. Разница в РОЛИ числа: тогда оно
стояло третьим лишним рядом с двумя ценами и ничего не объясняло; теперь
оно главное — то самое, что складывается в «Сумма, факт» под блоком.
Три пачки по 90 ₽ показывали «90 ₽», а внизу набегало 270, и глаз не сводил
одно с другим.

**Правило.** Когда новое слово заказчика повторяет отменённое, спрашивать
надо не «что он забраковал», а «какую работу число делает на экране».
Отменённая величина возвращается — но в другой роли, под своим именем
(«Стоимость», не «Сумма») и с другим весом: главная набрана жирным,
вспомогательные приглушены.

**Где исправлено:** `buy/buylocal.tsx` (`colsFor`, `costOf`, `setCost`),
`buy/BuyTable.tsx`, `buy/BuyRow.tsx`, `buy/BuyStrip.tsx`. Замер на боевом
снимке: «Вода питьевая 5 л», 8 × 130 ₽ — справа `1 040 ₽` и подпись
«стоимость, план» (было `130 ₽` / «цена план»).

---

### У-135. Прогноз погоды — сведения снаружи, а не данные поездки

**Что случилось.** Заказчик 08.08.2026: «было бы неплохо, чтобы прогноз погоды
постоянно актуализировался, а то он у тебя не актуализируется». В боевом листе
стояло «обновлён 4 августа, 10:40» — за четыре дня до выезда человек читал
позавчерашние цифры, потому что `S.weather` заполнялся руками.

Напрашивалось «пусть само сходит и запишет в документ». Так делать нельзя:
документ ходит через слияние и синхронизацию, и фоновая запись погоды дёргала
бы боевую строку каждые полчаса — у четверых сразу, без спроса и без автора.
Это ровно тот случай, что и `trip.dist.auto`, который молча включать запрещено.

**Правило.** Что меняется само и приходит снаружи — не хранится в документе.
Живой прогноз лежит в памяти вкладки и в `localStorage`, а на экран кладётся
ПОВЕРХ сохранённого: обновляются четыре величины дня (день, ночь, осадки,
ветер), а авторский текст «что это значит для нас», световой день и выводы
остаются документными — их писал человек.

**Где исправлено:** новый `lib/weather.ts` (Open-Meteo, без ключа), хук
`useLiveWeather` в `trip/WeatherStrip.tsx`, вызов один на обложку —
`trip/TripCover.tsx`. Замер: лента показала `10.08 · 22° / 15°` вместо
документных `21° / 14°`, подпись «Прогноз обновлён 8 августа, 01:33 — сам,
каждые полчаса», текст дня «День заезда и переправы…» на месте.
Отказ сети говорится словами: «Свежий прогноз получить не удалось».

---

### У-136. Число, посчитанное по порядку, обязано и храниться с этим порядком

**Что случилось.** Заказчик 08.08.2026: «какой-то странный расчёт километража
получился у Миши: 742 км, в то время как в одну сторону это в два раза меньше
должно быть. Там от точки к точке получается заметно меньше. Почему получилось
у тебя больше, я не знаю».

Разбор по цифрам. У ветки «Chevrolet Aveo» пять точек: юг Петербурга →
Кронштадт → Сестрорецк → Приозерское шоссе → Приозерск. Живой OSRM на этот
порядок отвечает 49 + 24 + 39 + 116 = **228 км**. В документе лежало
49 + 155 + 51 + 116 = **371 км**, и `kmOf` умножал на `kBack = 2` → **742**.

Откуда взялись 155 и 51, показала матрица расстояний между теми же точками:
155 — это Приозерск → Сестрорецк, 51 — Кронштадт → Приозерское шоссе. То есть
считали ветку в порядке Кронштадт → Приозерское → Приозерск → **Сестрорецк**:
сестрорецкая точка стояла ПОСЛЕДНЕЙ, и расчёт честно намерил обратный конец.

**Почему.** `threads()` в `map/marks.ts` собирал нитку в порядке МАССИВА
`S.route`, а слияние (`pick` в `lib/merge.ts`) кладёт чужую новую точку в КОНЕЦ
массива — сравнивать её не с чем. Её `ord` при этом приезжает свой, правильный.
Значит на телефоне, куда точка приехала, нитка шла одним порядком, а в документе
она стояла другим. Расстояния участков (`route[].leg`) записываются в документ
БЕЗ порядка, в котором их считали, — и разъехались с ним навсегда.

**Правило.** Порядок задаёт поле документа (`ord`), а не место в массиве.
Всё, что от порядка зависит — нитка на карте, номера меток, расчёт участков,
выгрузка, — берёт его из одного места. Новая точка получает `ord` = максимум
плюс шаг (а не «длина массива × 10»: после удаления шести точек длина стала 20,
а последний `ord` был 260, и следующая точка влезла бы в середину чужой ветки).
Точка, вставленная нажатием на линию, получает число МЕЖДУ соседями — без
перенумерации всего маршрута, которая меняла `ord` двум десяткам чужих точек
без свежего `ua` и до других телефонов всё равно не доезжала.

**Где исправлено:** `threads()` в `app/src/components/map/marks.ts` (устойчивая
сортировка по `ord`), `addPoint` и `onLine` в `app/src/components/map/TripMap.tsx`.
Замер живьём на боевом снимке: «Расстояние в одну сторону 228 км», «Пробег этой
техники 456 км», ветка на карте — «Chevrolet Aveo · 456 км — туда и обратно».

---

### У-137. Множитель, который применяется, и множитель, о котором пишут, — одно поле

**Что случилось.** Под числом «742 км» стояла подпись «371 км — только туда».
Заказчик: «Я не знаю, зачем это 742».

`kmOf` умножает на `kBackOf(t, S)` — СВОЙ множитель ветки (`t.kBack`), а подпись
в `TransportKm` печатала общий `trip.dist.kBack`. У «Aveo» стояло ×2 у ветки при
общем ×1 — и подпись говорила «только туда» под удвоенным числом. Ошибка вида
«вычислено одним, рассказано другим»: прибор и табличка спорят, верить нечему.

**Правило.** Величину и подпись к ней берут из одного вызова. Если для показа
понадобилось второе выражение — это уже второй источник правды, и он разойдётся.

**Где исправлено:** `TransportKm` в `app/src/components/road/RoadSetup.tsx`
(`kBackWord(kBackOf(item, S))`), чип ветки `branchNote` в
`app/src/components/map/RouteBranches.tsx` («456 км — туда и обратно»:
итог с «×2» обязан называть себя итогом).

---

### У-138. Запасное число перестаёт быть запасным — и становится дублем

**Что случилось.** Заказчик 08.08.2026: «убрать дубли — расстояние
Дворцовая–Приозерск, коэффициент туда-обратно отдельной строкой, местные
разъезды не по транспорту. У ветки уже есть свои kBack, kmLocal, kmSrc».

Три общих числа поездки (`trip.dist.manual`, `kBack`, `local`) появились раньше
своего пробега у техники и остались как запасные: `kmOf` откатывается на них,
пока у единицы нет `kmSrc`. В боевом листе `kmSrc` стоит у ВСЕЙ техники — значит
эти три строки не влияют ни на рубль, но занимают четыре строки расчёта, полосу
«Вернуть своё число» и подпись раздела «Пробег 165 км». Человек их правит и
не понимает, почему деньги не двигаются.

**Правило.** Запасное значение показывается ровно тогда, когда по нему кто-то
считает. Один предикат на весь сервис — `commonKmUsed(S)` в `lib/calc.ts`, —
иначе матрица и лента разойдутся. Из документа `trip.dist` при этом никуда
не девается (постулат 4): заведут технику без своего пробега — всё вернётся.

**Где исправлено:** `commonKmUsed` в `app/src/lib/calc.ts`; группа «Пробег»
в `road/RoadCalc.tsx`, полоска в `road/RoadStrip.tsx`, кнопки и подпись полосы
карты в `road/RoadSection.tsx`. Замер на боевом снимке: подпись раздела —
«80,6 л топлива · 20 604 ₽», под полосой — «У каждой техники свой пробег —
по её точкам на карте. Он в её строке».

---

### У-139. Погода считается от точки — значит точку надо уметь выбрать и увидеть

**Что случилось.** Заказчик 08.08.2026: «Что, погода должна отталкиваться
от чего-то? Я не знаю, чего сейчас она отталкивается… она должна отталкиваться
от точки, которую отметит человек… я выбрал конкретную точку, конкретный адрес —
и он пишет погоду». И тут же: «Там, где прописывается „впишите локацию“, должен
быть выпадающий список… Когда ты ищешь город, допустим, Приозерск».

Прогноз и правда брался от координат главного места поездки (`useLiveWeather`),
но об этом нигде не было сказано, а само место правилось простой строкой текста:
напиши «оз. Вуокса» — координат не появится, и цеплять прогноз не за что.
Вычисленное, о котором молчат, читается как выдумка (постулат 5).

**Правило.** Если величина считается от чего-то в документе — это «что-то»
называется на экране и выбирается человеком, а не угадывается. Поле, за которым
стоит место на карте, даёт подсказки: список находок ПОД полем (попапов нет),
выбор кладёт имя, координаты и адрес разом.

**Где исправлено:** `InlineSuggest` в `app/src/components/flops/Inline.tsx`
(состояния «ищем» · «вот что нашлось» · «не нашлось» · «спросить было некого»
взяты у `map/MapSearch.tsx`, велосипеда нет), место поездки в
`trip/TripCover.tsx`, подпись `weatherWhere` в `trip/WeatherStrip.tsx`.
Замер: «Погода на дни поездки — оз. Вуокса»; набор «Приозер» даёт список
с «Приозерск» и правилом «Выберите место — по нему считается погода».

---

### У-140. Отравленный адрес и пропавший файл различаются одним запросом с «?bust=»

**Что случилось.** Через минуту после выкладки двадцать четвёртой порции боевой
встал белым экраном: `index.html` ссылался на `assets/index-COvxgSWS.js`,
а по этому адресу приходил `text/html` — и curl'у, и браузеру одинаково.
Playwright показал `root.children = 0`. Повтор У-127, но БЕЗ расхождения
приборов: на этот раз отравлены были оба варианта сжатия.

Соблазн прочитать это как «сборка сломалась, файла нет» велик, а вывод был бы
ложный: правка уехала целиком.

**Как отличить одно от другого.** Тот же адрес с дописанным `?bust=<время>`:

```bash
curl -sL --compressed -H 'Origin: https://pine-to-pine.com' \
  "https://pine-to-pine.com/assets/index-XXXX.js?bust=$(date +%s)" -D -
```

Запрос с новой строкой не попадает в кеш края (`cf-cache-status: MISS`) и идёт
к источнику. Пришёл `application/javascript` и внутри строки правки — файл цел,
беда в кеше. Пришёл `text/html` — файла действительно нет, и надо смотреть сборку.
В этот раз: `MISS · application/javascript · 492 331 знак`, все три новые строки
на месте.

**Правило.** Диагноз «выложилось / не выложилось» ставится по ИСТОЧНИКУ,
а вердикт «работает» — по загрузке страницы. Это два разных вопроса, и мерить
их одним запросом нельзя.

**Лечение сиюминутное:** новый коммит меняет `BUILD_ID` (хеш коммита
в `app/vite.config.ts`), тот меняет содержимое главного куска, содержимое —
его хеш и имя файла. Браузер идёт на чистый адрес.

⛔ **Но одного этого мало, и 08.08.2026 это выяснилось дорого:** новое имя
`index-C3kT9YC2.js` отравилось ровно так же, через три минуты после выкладки.
Отравляет ЛЮБОЙ запрос к файлу сборки, пришедший до того, как боевой адрес
переключился на новую выкладку, — в том числе моя же проверка свежести
в цикле. Источник при этом цел: `?bust=` отдавал `application/javascript`,
492 331 знак, а голый адрес — `text/html`, `cf-cache-status: HIT`, `Age: 261`,
`Cache-Control: public, max-age=31536000, immutable`.

**Настоящая починка — в двух файлах Cloudflare:**

1. `app/public/_redirects`: первым правилом `/assets/*  /index.html  404`.
   Файла нет — честный 404, а не 200 с телом HTML. Правила разбираются
   сверху вниз, первое совпадение выигрывает, поэтому эта строка стоит
   ВЫШЕ `/*  /index.html  200`.
2. `app/public/_headers`: у `/assets/*` вместо года с `immutable` — сутки.
   Выгода при повторном заходе та же (имена хешированные), но отравленный
   адрес лечится сам за сутки, а не живёт год.

⚠️ И правило поведения: после выкладки НЕ дёргать голый адрес файла сборки
в цикле. Сначала `?bust=` — он идёт к источнику и кеш не портит; и только
когда источник отдал код, можно спрашивать голый адрес.

---

### У-141. Копию скачивает один, а открывают все — файлу нужен вопрос «кто вы»

**Что случилось.** Заказчик 08.08.2026: «Я им скидываю свою офлайн-версию,
а у них она не работает, потому что они не зашли через себя».

Замер подтвердил: получатель копии Макса в чистом браузере — «Вы сейчас:
Макс · участник». У файла адрес `file:`, в нём нет ни `?u=` с ключом,
ни запомненной личности сайта (другой origin — хранилище другое). Открывший
оставался тем, кем копию сохранили, но БЕЗ подтверждённого ключа: чужое имя
на отметках и права участника даже у владельца в его собственном файле.
Вдобавок плашка прав советовала «откройте личную ссылку целиком» — совет,
невыполнимый в файле без сети.

**Правило.** Личность даёт ссылка, а у скачанного файла ссылки нет — значит
файл обязан спрашивать сам. Карточка «Кто вы в этой копии?» стоит над
разделами, пока личность не подтверждена; выбор кладёт `doc.me` и ключ
из карточки человека в самом документе (`becomeInCopy` в store.ts),
запоминается хранилищем файла и наружу не ведёт: сеть копии закрыта
загрузчиком `pine-boot`, хранилище `file:` до сайта не достаёт.

**Где исправлено:** `components/OfflineWho.tsx` (карточка), `becomeInCopy`
в `store.ts`, подавленный совет в `PermNotice.tsx`. Замер: 390/1280, обе темы,
чипы 44 px, контрасты 4,67–14,72 : 1; после выбора «Вы сейчас: Костя ·
редактор», после перезагрузки файла выбор помнится, карточка не возвращается.

---

### У-142. Служебная строка и второй вход в жест живут до первого снимка экрана заказчика

**Что случилось.** Заказчик 08.08.2026 прислал снимок полосы под картой:
«точка у тебя на карте отвечается. На хер ты её ещё раз здесь фиксируешь?
Информация о сборке, она тоже на хер не нужна. Я не понимаю, ты дизайн сайта
делаешь или для какого-то программиста?»

Кнопка «Точка» была заведена 05.08.2026, когда жест «тап по карте ставит
точку» был человеку неоткуда узнать. Жест он выучил — и кнопка стала вторым
входом в то же действие. Строка «Карта Google · сборка …» не пропадала
никогда по У-30…У-32 — но её читатель всегда был программист, а не команда.

**Правило.** Обучающий орган и служебный показ — временные жители экрана:
как только заказчик увидел в них шум, они снимаются. Новое слово отменяет
старое (У-30…У-32 в части строки под картой ОТМЕНЕНЫ). Отказы при этом
не замолкают: «сети нет» и «Google не открылся — показываем OpenStreetMap»
говорят плашки над картой с кнопкой «Попробовать снова». Функции живы:
точка — тапом по карте и по линии, полоса под картой рисуется только когда
ей есть что сказать (постановка точки, нет прав) — пустых 52 px нет.

**Где исправлено:** `TripMap.tsx` — вырезаны `placingNew`, кнопка «Точка»
и строка сборки. Замер: строки «сборка» нет, кнопки нет, пустой полосы нет,
тап по карте ставит точку (19 → 20), обе ширины и темы, ошибок 0.

---

### У-143. Зачёт, стоящий раньше половины трат, читается как второй расчёт

**Что случилось.** Заказчик 08.08.2026: «Почему у тебя взаиморасчёты после
еды, после закупки идут, а отдельно расчёты по логистике? У тебя всё в конце
должно считаться… Общая промежуточная информация не нужна. Общее считается
в конце, грамотно там прописано: кто кому что должен».

Блок «Взаиморасчёты» стоял последним в «Закупке» — ПЕРЕД «Дорогой», хотя
топливо и аренду из неё уже учитывал. Для читателя это выглядело как два
независимых расчёта: один после еды, второй по логистике, — и не верилось
ни одному.

**Правило.** Итог, который считается по всему листу, стоит в конце всего
листа — после последнего раздела с тратами. Блок один (У-53), арифметика
одна (`applySpend`/`wholeSettle` в lib/settle.ts, У-58) — переносится показ,
а не расчёт.

**Где исправлено:** `App.tsx` (блок после всех разделов), `BuySection.tsx`
(вырезан вызов). Замер: экземпляр один, в «Закупке» отсутствует, последний
элемент листа, ниже низа «Меню» на обеих ширинах и в обеих темах; переводы
сходятся со строкой «Вы должны» копейка в копейку.

### У-144. Авторский текст в блоке технических сведений — не данные, а окаменелость

**Что случилось.** Заказчик 08.08.2026 про блок «Световой день и выводы»:
«оставишь там только техническую информацию, которая автоматически должна
подтягиваться с Open-Meteo… не задействуя тебя как разработчика, который там
пишет мне: „нужна информация термобельё и нормальный спальник“. Написать
„купаться можно“ не нужно — это не та информация, которая техническая».

Всё, что стояло в блоке, было написано человеком один раз и с тех пор
не менялось: пять советов про термобельё, сапоги и гермомешок, температура
воды «по типичному августу, не замер» и ОДНА пара «восход/закат» на всю
поездку — хотя за пять дней рассвет уезжает на десять минут, а закат
на двенадцать.

**Правило.** Если величину умеет считать источник — её считает источник,
а не автор. Написанное руками в блоке, который называется техническим,
устаревает в тот же день и врёт молча.

**Где исправлено:** `lib/weather.ts` (`sunrise`, `sunset`, `daylight_duration`
по каждому дню), `trip/WeatherStrip.tsx` (блок называется «Световой день»,
внутри строка на день). Поля `S.weather.daylight`, `.concl`, `.src` убраны
С ЭКРАНА, но не из документа: слияние их переносит, выгрузка берёт
(постулат 4). Замер: пять строк по дням, отметка «Open-Meteo, обновлено …»,
контраст 7,51 / 14,72 в светлой теме и 4,67 / 8,91 в тёмной.

### У-145. Температуру воды для озера взять неоткуда

**Что случилось.** В сводку просились «технические» величины, и температура
воды выглядела одной из них. Морская модель Open-Meteo
(`marine-api.open-meteo.com`, `sea_surface_temperature_max`) на координатах
Вуоксы (61.04, 30.14) отдаёт `null` на все пять дней: это озеро, а не море.
Стоявшие в документе «+17…+19 °C» были оценкой человека, а не замером.

**Правило.** Прежде чем обещать величину «автоматически», спросить у источника
ровно те координаты и ровно те даты, о которых идёт речь. Модель, покрывающая
мир, не обязана покрывать конкретное озеро.

**Где исправлено:** строка убрана, в шапке `lib/weather.ts` записано, почему
её нельзя вернуть.

### У-146. Порядок обхода круга — не то же самое, что порядок хранения

**Что случилось.** Заказчик 08.08.2026: «по умолчанию всегда при нажатии
на тот или иной пункт в вещах должен показываться в первую очередь
„упакован“, а потом идёт „в процессе“, „в машине“. Последним будет
„не могу взять“. То есть их нужно поменять местами». Резон из жизни: тапают
по вещи, когда её ПОЛОЖИЛИ, а не когда начали искать, — и чтобы отметить
сложенное, приходилось жать дважды.

Соблазн был переставить `ST_NAME`. Это стёрло бы смысл уже проставленных
отметок у всех четверых разом: в `gear.s` лежат числа.

**Правило.** Числа — форма хранения, порядок нажатия — поведение. Меняется
маршрут обхода, а не значения. Легенда при этом переставляется вслед
за обходом: легенда, читаемая не в том порядке, в каком нажимается,
объясняет неверно.

**Где исправлено:** `lib/gearx.ts` — отдельный `RING = [0, 2, 1, 3]`,
`ST_NAME` не тронут; `gear/legend.tsx` — порядок значков по `RING`.
Замер: семь нажатий по ОДНОЙ ячейке дают упаковано → в процессе → в машине →
не могу взять → не взято → упаковано → в процессе.

### У-147. Ширину карточки в ленте считают вместе с тем, что стоит в той же строке

**Что случилось.** «Плюс» 64 px поставили в одну строку с людьми, а ширину
карточки оставили как в сетке — `25% − 0.75rem`. Замер на 1280 при ЧЕТЫРЁХ
участниках, то есть на боевых данных: `scrollWidth − clientWidth = 80`, плюс
стоит на x = 1272 при экране 1280 (срезан), `elementFromPoint` в его середину
до него не доходит, и рядом зажигается стрелка «Показать следующих»,
которой некуда вести.

**Правило.** Добавили в строку орган — вычтите его из доли соседей
и перемерьте на РЕАЛЬНОМ числе элементов, а не только на переполнении.
Раскладка, верная «при многих», может быть сломана «при четырёх».

**Где исправлено:** `crew/CrewSection.tsx`, `CARD_W` = `(100% − 8rem) / 4`
(64 px квадрата плюс четыре зазора по 16). Замер после правки: при 4 людях
переполнение 0 и стрелок нет, при 9 — переполнение 1460 и стрелка есть;
нажатие доходит до плюса на 390 и 1280.

### У-148. Локатор Playwright по меняющейся подписи уезжает на другой узел

**Что случилось.** Круг статусов мерили
`page.locator('button[aria-label*="упаковано"]').first()` и жали шесть раз.
После «в машине» подпись стала «не могу взять», локатор перестал совпадать,
`.first()` пере-разрешился на ДРУГУЮ ячейку — и в отчёт попал круг
«упаковано → в процессе → в машине → не взято», где «не могу взять»
не было вовсе. Прибор показал правдоподобную неправду.

**Правило.** Меряя последовательность состояний ОДНОГО органа, брать узел
один раз (`evaluateHandle`) и дальше только жать по нему. Локатор,
отобранный по признаку, который сам меняется, — не прибор.

**Где исправлено:** проба переписана; правило дописано
в `.claude/rules/environment.md`.

### У-149. В Playwright побеждает route, зарегистрированный ПОЗЖЕ

**Что случилось.** Общая заглушка `**/rest/v1/**` → `[]` была поставлена
ПОСЛЕ точной `rpc/trip_read`, перехватила её, и проба два прогона подряд
показывала экран «Заведите свою поездку» вместо листа. Выглядело это как
«лист не открывается», хотя ломалась только заглушка.

**Правило.** Общие заглушки регистрировать первыми, точные — после них.

**Где исправлено:** пробы сессии; правило дописано
в `.claude/rules/environment.md`.

### У-150. Третья копия одного органа — это уже велосипед

**Что случилось.** Галочка «куплено» жила двумя копиями, слово в слово:
`buy/BuyRow.tsx` (десктоп) и `buy/BuyStrip.tsx` (телефон). В обеих стоял
комментарий «два экрана одного раздела обязаны показывать её одинаково».
08.08.2026 понадобилось третье место — строка находки в поиске.

**Правило.** Две одинаковые копии — совпадение, три — велосипед (постулат 3).
Орган выносится в общий, а в местах использования сохраняется прежнее имя,
чтобы разметка не менялась ни в одном файле.

**Где исправлено:** `flops/BuyBox.tsx`; обе копии заменены на
`import { BuyBox as Box }`, разметка мест использования не тронута.
