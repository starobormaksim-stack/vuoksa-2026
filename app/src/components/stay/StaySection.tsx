import { SectionHead } from '@/components/flops'

/**
 * Раздел «Проживание».
 *
 * ─── Откуда взялся ───
 * Заказчик 08.08.2026, диктуя порядок разделов целиком: «Отельные варианты
 * или какие-то camping места; туда будет фиксироваться информация
 * по проживанию». И тут же про сегодняшнее состояние — «он просто есть,
 * существует».
 *
 * ─── Почему здесь нет заглушки ───
 * Стояла плашка «Пока не решено · здесь будут места ночёвок, адрес, кто
 * бронирует». Заказчик 09.08.2026: «Не нужно проживанию писать вот это.
 * „Пока не решено“, „по центру здесь будут места ночёвок, адрес, кто
 * бронирует“ — не надо это». Обещание будущего — не данные, а мусор
 * на экране: перегруженный экран заказчик читает как брак.
 *
 * ⛔ Тем же разговором он определил и место: «проживание фиксируешь тоже
 * как подраздел внутри расходов, потому что это расходы в том числе».
 * То есть отдельным разделом ему стоять недолго — он переезжает в «Расходы»
 * вместе с «Логистикой». До переезда раздел остаётся на месте: убирать его
 * из меню раньше, чем появилось новое место, значило бы отнять у человека
 * дверь, ничего не дав взамен.
 *
 * Своей коллекции нет: форму хранения (адрес, даты, цена, кто бронирует)
 * заводит переезд в «Расходы», а не эта заготовка.
 *
 * Название правится на месте, как у всех разделов, — своё имя ляжет
 * в `S.secTitles` по ключу `stay` (`lib/sectitles.ts`).
 */
export function StaySection() {
  return (
    <div className="flex flex-col gap-4">
      <SectionHead
        title="Проживание"
        secId="stay"
        hint="Где ночуем: отель, база, палатки"
      />
    </div>
  )
}
