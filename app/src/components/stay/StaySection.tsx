import { BedDouble } from 'lucide-react'
import { EmptyState, SectionHead } from '@/components/flops'

/**
 * Раздел «Проживание».
 *
 * ─── Откуда взялся ───
 * Заказчик 08.08.2026, диктуя порядок разделов целиком: «Отельные варианты
 * или какие-то camping места; туда будет фиксироваться информация
 * по проживанию». И тут же про сегодняшнее состояние — «он просто есть,
 * существует».
 *
 * ⛔ Поэтому здесь ПУСТО, и это не заготовка «на потом»: раздел обязан
 * существовать в списке, в меню и в оглавлении с первого дня, чтобы человек
 * видел, куда это ляжет. Своей коллекции у него пока нет — заводить её до
 * разговора о полях (адрес, даты, цена, кто бронирует) значило бы выдумать
 * форму хранения за заказчика, а форму менять потом нельзя (постулат 4).
 *
 * ⚠️ Деньги за жильё живут в «Расходах» строкой аренды, а не здесь: один
 * список в одном месте (постулат 3.5). Здесь будут адреса, даты и заметки.
 *
 * Название правится на месте, как у всех разделов, — своё имя ляжет
 * в `S.secTitles` по ключу `stay` (`lib/sectitles.ts`).
 */
export function StaySection() {
  return (
    <div className="flex flex-col gap-4">
      <SectionHead
        title="Проживание"
        secId="stay"
        hint="Где ночуем: отель, база, палатки"
      />

      <EmptyState
        icon={BedDouble}
        title="Пока не решено"
        text="Здесь будут места ночёвок: адрес, даты и кто бронирует. Деньги за жильё считаются в «Расходах»"
      />
    </div>
  )
}
