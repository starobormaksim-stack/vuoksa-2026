import {
  Backpack,
  CircleHelp,
  House,
  ShoppingCart,
  Users,
  Utensils,
  type LucideIcon,
} from 'lucide-react'

/** Раздел приложения. Список динамический: в будущем сюда добавятся пользовательские разделы. */
export interface SectionDef {
  id: string
  title: string
  icon: LucideIcon
}

export const SECTIONS: SectionDef[] = [
  { id: 'trip', title: 'Поездка', icon: House },
  { id: 'crew', title: 'Команда', icon: Users },
  /* Имена продиктованы заказчиком 08.08.2026. `id` заморожены: на них держатся
     якоря разделов, ключи `S.secTitles` (первая версия, карта в `lib/sectitles.ts`),
     заявки общего «плюса» (`lib/addnew.ts`) и память свёрнутого (`lib/foldpref.ts`). */
  { id: 'gear', title: 'Взять с собой', icon: Backpack },
  /* ⛔ «Расходы» — раздел с подразделами: статьи закупки, «Аренда»,
     «Логистика», «Проживание». Отдельными разделами «Дорога» и «Проживание»
     БОЛЬШЕ НЕТ, и вернуть их нельзя. Заказчик 09.08.2026, дословно: «Логистика
     должна быть одним из подразделов внутри расходов, но ты этого не сделал…
     я тебе говорил, вот, и не доделал до сих пор», «ты проживание фиксируешь
     тоже как подраздел внутри расходов, потому что это расходы в том числе».
     Подразделы собирает проекция `lib/spend.ts`; коллекции при этом остались
     каждая своей (постулат 4). */
  { id: 'buy', title: 'Расходы', icon: ShoppingCart },
  /* «А вот меню идёт отдельно» — тот же разговор 09.08.2026. */
  { id: 'menu', title: 'Меню', icon: Utensils },
  /* «Вопросы» стояли внутри «Дороги» и вместе с ней потеряли бы дверь.
     В порядке разделов, продиктованном 08.08.2026, они идут последними. */
  { id: 'ideas', title: 'Вопросы', icon: CircleHelp },
]

/**
 * Последний раздел, где тратятся деньги. Сразу за ним встаёт общий зачёт.
 *
 * Заказчик 09.08.2026: «уже по итогу всех этих подразделов, по итогу всего
 * раздела расходов там будет условно как раз этот расчёт и фиксироваться»,
 * и тут же — «а вот меню идёт отдельно». То есть зачёт замыкает деньги,
 * а не весь лист: за «Меню» он висел ни к чему не относящимся хвостом.
 *
 * ⚠️ Раньше зачёт стоял в конце «Закупки» и показывался РАНЬШЕ трат
 * «Дороги» — за это заказчик ругал 08.08.2026 (урок У-143). Пока «Логистика»
 * и «Проживание» стояли отдельными разделами ПОСЛЕ «Расходов», зачёт обязан
 * был стоять за ними, и здесь лежало `stay`.
 *
 * ⛔ Теперь они — подразделы «Расходов» (`lib/spend.ts`), и все деньги листа
 * кончаются вместе с этим разделом. Менять это значение обратно нельзя,
 * не вернув подразделы наружу: зачёт снова показал бы половину трат.
 */
export const LAST_MONEY_SECTION = 'buy'

/** Сколько пунктов помещается в нижнюю панель. При переполнении последним встаёт «Ещё». */
export const BOTTOM_NAV_LIMIT = 6

/**
 * Раскладка нижней панели: если разделов больше лимита, показываем первые (лимит − 1)
 * и пункт «Ещё» со шторкой остальных (шторка — TODO, появится вместе с пользовательскими разделами).
 */
export function splitForBottomNav(sections: SectionDef[]): {
  visible: SectionDef[]
  overflow: SectionDef[]
} {
  if (sections.length <= BOTTOM_NAV_LIMIT) return { visible: sections, overflow: [] }
  return {
    visible: sections.slice(0, BOTTOM_NAV_LIMIT - 1),
    overflow: sections.slice(BOTTOM_NAV_LIMIT - 1),
  }
}

/* ─────────── единый лендинг ─────────── */

/**
 * Якорь раздела на странице. Разделы больше не подменяют друг друга: они идут один
 * за другим сверху вниз, а меню к ним прокручивает (заказчик, 04.08.2026).
 */
export function anchorOf(id: string): string {
  return 'sec-' + id
}

/**
 * Прокрутить к разделу. Отступ под прилипающую шапку задан в разметке через
 * `scroll-margin-top`, поэтому здесь достаточно обычного scrollIntoView.
 * Плавность отключается, если человек попросил систему не анимировать.
 */
export function scrollToSection(id: string): void {
  const el = document.getElementById(anchorOf(id))
  if (!el) return
  const calm =
    typeof window !== 'undefined' &&
    window.matchMedia('(prefers-reduced-motion: reduce)').matches
  el.scrollIntoView({ behavior: calm ? 'auto' : 'smooth', block: 'start' })
}
